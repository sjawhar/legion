# Written by the Go `legion` (packages/daemon-go/internal/modelroute) when Oh My Pi starts in a Legion
# pod: the worker shim and `legion probe-image` write it beside models.yml, from which every model
# named here is served, through the model gateway. They also name this file as Oh My Pi's last
# settings overlay (PI_CONFIG_FILES), which Oh My Pi ranks above the settings of the repository the
# agent works in. For a single value or a list, the overlay's replaces the repository's: without it,
# a repository's .omp/config.yml that emptied disabledProviders brought Amazon Bedrock back
# (omp_test.go reproduces it). A record (retry.fallbackChains, modelRoles) merges key by key, so a
# repository can add keys this file does not set; that is why fallback is off below.
#
# The gateway is the one model route, and Oh My Pi falls back from a failing provider without a word:
# with the gateway key failing and nothing pinned, a turn answers from Amazon Bedrock on the host's
# instance role (amazon-bedrock/us.anthropic.claude-opus-4-8, exit 0; omp_test.go reproduces it).
# enabledModels holds every session's own model to the gateway's aliases, so a process without the
# key exits naming the provider it has no key for (No API key found for anthropic), having sent
# nothing. Every other provider Oh My Pi knows is disabled, so no key a pod could come by (ambient
# cloud credentials, a repository's .env, a local server) puts one in reach: the set is closed. The
# pinned Oh My Pi resolves no model of a disabled provider and gives it no key however a setting
# names it: a subagent override, an agent's model frontmatter, a custom role, a fallback chain
# (omp_test.go runs each).
#
# Two ids name more than a model provider, and disabling them is intended. `cursor` is also Oh My
# Pi's Cursor capability source, so a pod loads no repository's .cursor/ rules or MCP servers (a
# repository's .env can carry CURSOR_API_KEY; its AGENTS.md still loads). `web` holds the web
# search backends, several of them models of other providers (Perplexity, Z.ai, Kimi), so a pod's
# web_search tool has no backend.
#
# disabledProviders is every provider id in Oh My Pi's bundled catalog and provider auth rules at
# the pinned fork, less anthropic, derived in the fork's checkout with
#   { git show <tag>:packages/catalog/src/models.json | jq -r 'keys[]'
#     git grep -h -o '^auth "[^"]*"' <tag> -- 'packages/catalog/src/compat/rules/auth/*.kdl' |
#       sed 's/^auth "\(.*\)"$/\1/'; } | LC_ALL=C sort -u | grep -vx anthropic
# derived at: github:sjawhar/oh-my-pi@18.2.9-sami.20260924-172934
# modelroute_test.go reads only that line: it fails when packages/daemon/src/daemon/omp-pin.ts names
# another fork release, so a pin bump re-derives this list and testdata/provider-keys.txt, then
# moves the line. omp_test.go checks the list against the pinned binary.
enabledModels:
  - anthropic/*-legion
disabledProviders:
  - abliteration
  - aiand
  - aimlapi
  - alibaba-coding-plan
  - alibaba-token-plan
  - amazon-bedrock
  - azure
  - baseten
  - bedrock-mantle
  - cerebras
  - charm-hyper
  - cline-pass
  - cloudflare-ai-gateway
  - commandcode
  - coreweave
  - cursor
  - deepinfra
  - deepseek
  - devin
  - exa
  - firepass
  - fireworks
  - github-copilot
  - gitlab-duo
  - gitlab-duo-agent
  - gmi-cloud
  - google
  - google-antigravity
  - google-gemini-cli
  - google-vertex
  - groq
  - huggingface
  - kagi
  - kilo
  - kimi-code
  - litellm
  - llama.cpp
  - lm-studio
  - local
  - meta
  - minimax
  - minimax-cn
  - minimax-code
  - minimax-code-cn
  - mistral
  - moonshot
  - muse-code
  - nanogpt
  - novita
  - nvidia
  - ollama
  - ollama-cloud
  - openai
  - openai-codex
  - openai-codex-device
  - opencode-go
  - opencode-zen
  - openrouter
  - parallel
  - perplexity
  - qianfan
  - qwen-portal
  - sakana
  - siliconflow
  - siliconflow-cn
  - singularityapi-dev
  - singularityapi-tech
  - stencil
  - synthetic
  - tavily
  - together
  - typesafe
  - umans
  - venice
  - vercel-ai-gateway
  - vllm
  - wafer-serverless
  - web
  - xai
  - xai-oauth
  - xiaomi
  - xiaomi-token-plan-ams
  - xiaomi-token-plan-cn
  - xiaomi-token-plan-sgp
  - yolo-auto
  - zai
  - zai-coding-plan
  - zenmux
  - zhipu-coding-plan
# The roles of the devbox's `legion` Oh My Pi profile, the one the TypeScript daemon's panes run, each
# on its gateway alias; the two it gives to models the gateway does not serve Legion (slow, vision)
# run the nearest alias. DefaultModel (modelroute.go) names the default role's model.
modelRoles:
  default: anthropic/claude-fable-5-1-legion:xhigh
  plan: anthropic/claude-fable-5-1-legion:max
  slow: anthropic/claude-opus-5-legion:xhigh
  smol: anthropic/claude-sonnet-5-legion
  task: anthropic/claude-sonnet-5-legion
  vision: anthropic/claude-sonnet-5-legion
  commit: anthropic/claude-sonnet-5-legion
  tiny: anthropic/claude-sonnet-5-legion
  advisor: anthropic/claude-sonnet-5-legion:medium
retry:
  # A failed turn does not fall back to another model: a repository's .omp/config.yml can add
  # retry.fallbackChains keys (a record, merged key by key across settings layers, unlike these
  # lists). The pinned Oh My Pi resolves a candidate only among enabled providers, so a chain could
  # at most move a turn to another alias; with fallback off a failure is the default alias's own.
  # modelFallback is one value, so this overlay's wins.
  modelFallback: false
# The settings a repository could name that send a pod's conversation, or the gateway token with
# it, somewhere Oh My Pi posts to on its own, each held off here (one value, so this overlay's
# wins). Remote compaction, first in Oh My Pi's default method order, posts the conversation to
# remoteEndpoint with the session model's key (omp_test.go reproduces it); compaction falls through
# to the methods that run on the gateway. A memory backend other than off sends the conversation
# to its own servers (hindsight.apiUrl, mnemopi's embedding and LLM URLs); SQL session storage
# writes it to the database session.sql.dsnFile names (a pod's own store is chosen by
# OMP_SESSION_STORAGE, which outranks this); images.urls publishes images to a backend; dev.autoqa
# pushes tool-issue reports to dev.autoqaPush.endpoint. The endpoint-shaped settings left unpinned
# reach nothing on their own: share, stream and collab run only on their commands, searxng and the
# antigravity endpoint belong to disabled providers, browser.cdpUrl and relayUrl serve the browser
# tool the agent drives, and auth.broker.url is read from the profile alone.
compaction:
  remoteEndpoint: ""
memory:
  backend: "off"
session:
  storage: file
images:
  urls:
    enabled: false
dev:
  autoqa: false
