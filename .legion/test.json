{
  "verdict": "pass",
  "passed": 7,
  "failed": 0,
  "failures": [],
  "headSha": "3ff0bbf46315fd7d4e762430d3bd001ce478b1a3",
  "pr": "https://github.com/sjawhar/legion/pull/981",
  "ci": "pr-checks-result at 3ff0bbf4: pr-title/lint/typecheck/test all SUCCESS (runs 34720564124, 34720630218), read via `legion gh -- pr view 981 --json statusCheckRollup`",
  "acceptance": [
    {
      "line": 1,
      "criterion": "A locator carries a process identity beyond the pane id; a probe against a pane whose identity does not match reports dead, never alive.",
      "tests": [
        "processes.test.ts: it.each(reissuedPaneCases) 'probe reports a root dead when its recorded pane now runs another process (%s), and resurrecting it resumes the session without killing that pane' -- 3 cases: 'another process took the pane id' (pid 777), 'the same pid came back with a different start time' (pid 12345, ticks 999999), 'its /proc stat vanished after list-panes reported it' (ENOENT)",
        "processes.test.ts: 'treats a locator recorded before identity tracking as dead on its first probe -- even though its pane is live and running OMP -- logging that once and resuming the root onto a fresh, fully-recorded pane'",
        "legion-state.test.ts: 'persists and reloads panePid/paneStartTicks on tree, worker, and controller locators'; 'migrates v23 state to v24 unchanged, leaving a pre-identity locator without panePid/paneStartTicks and backing up the v23 bytes'"
      ],
      "command": "cd -- \"$LEGION_WORKSPACE\" && bun test packages/daemon/src/daemon/__tests__/processes.test.ts packages/daemon/src/daemon/__tests__/worker-boot-watchdog.test.ts packages/daemon/src/daemon/__tests__/proc-stat.test.ts --reporter=junit --reporter-outfile=/tmp/legion27-test/junit.xml",
      "output": "216 pass / 3 skip / 0 fail at 7d13d3bf; re-run at 3ff0bbf4: all three it.each cases PASS, legacy-locator test PASS (processes+watchdog 213 pass / 3 skip / 0 fail).",
      "result": "pass",
      "notes": "Each case discriminates on behaviour and, since 3ff0bbf4, on the observed identity in the log as well (`observed` regex per case)."
    },
    {
      "line": 2,
      "criterion": "reconnectWorkers/reconnectRoots at boot treat a mismatched identity as dead and clear the locator, so the claim stops counting toward worker_cap; two claims sharing a pane id, one live and one stale; only the stale one is cleared.",
      "tests": [
        "processes.test.ts: 'reconnectWorkers clears a stale unconfirmed claim whose pane id was reissued to a live sibling -- without killing that pane -- and the live claim's re-armed watchdog verifies its pane at the next interval' (stale locator cleared, launchFailures 1, queued; live locator intact with panePid 2001; zero kill-pane; log carries pid 2001 and 'recorded pid 1001 start 3000')",
        "processes.test.ts (reconnectRoots half): 'reconnectRoots' re-armed deadline treats a restart-surviving locator whose pane id was reissued as dead: resumes the root once, never killing that pane' (generation 2, launchFailures 1, no kill-pane -t %1, new-window carries --resume=<session file>, fresh locator %1 pid 10001)",
        "processes.test.ts: 'resurrects a root once, without killing the pane, when its registration deadline elapses on a pane id since reissued to another OMP process'"
      ],
      "command": "same JUnit run as line 1; isolation: bun test processes.test.ts -t \"reconnectRoots' re-armed deadline treats a restart-surviving\" (x72)",
      "output": "All three PASS in the file run. reconnectRoots half: 30/30 isolated at 3ff0bbf4.",
      "result": "pass",
      "notes": "The stale claim is retired by the pre-existing socket-first reconnect probe; what this change adds is that the retire's kill gate refuses to kill the live sibling's pane. The isolated flake seen at 7d13d3bf is gone at 3ff0bbf4: both reissued-pane deadline tests await the resurrection's own persist (30/30 isolated each)."
    },
    {
      "line": 3,
      "criterion": "A daemon whose tmux server was recreated under it resurrects every root with --resume and never adopts another role's pane.",
      "tests": [
        "processes.test.ts: 'resurrects every located root with --resume, killing nothing, when the private tmux server was recreated under the daemon and reissued its pane ids' (two roots, ids restart at @1/%1, onProbe composition, zero kill-pane, both new-windows carry --resume, second root's stale %1 reissued to the first root's fresh pane still reads dead)",
        "LEGION_TMUX_LIVE=1 gated: 'probes its live root through its window id when a duplicate cosmetic name exists', 'creates the first live root without a default bash window', 'probes a real tmux pane as alive, detects its death, and resurrects it once' (real /proc read; recorded paneStartTicks equals parseProcStatStartTicks(/proc/<panePid>/stat))"
      ],
      "command": "LEGION_TMUX_LIVE=1 bun test packages/daemon/src/daemon/__tests__/processes.test.ts -t 'probes its live root through its window id|creates the first live root without a default bash window|probes a real tmux pane as alive'; LEGION_TMUX_LIVE=1 bun test packages/daemon/src/daemon/__tests__/processes.test.ts; bun /tmp/legion27-test/resync-recreate-driver.ts; bun /tmp/legion27-test/identity-driver.ts",
      "output": "Gated: 3 pass / 204 filtered / 0 fail; full file under LEGION_TMUX_LIVE=1: 207 pass / 0 fail / 0 skip. Driver A (REAL ProcessManager, REAL tmux -L legion-smoke29629, OMP swapped for `sleep 999`, real /proc reads): spawnRoot recorded @1/%1 pid 51109 start 161400424 == real /proc/51109/stat field 22; probe alive; `tmux -L legion-smoke29629 kill-server`; tmux.openWindow for another role -> @1/%1 pid 52183 (tmux.panePid('%1') = 52183: the pane-id-only probe would have said alive); onProbe composition -> probe dead; log `treating LEGION-42's root as dead: pane %1 now runs pid 52183 (recorded pid 51109 start 161400424)` and `not killing pane %1 for legion-smoke29629-legion-42-architect, treating it as already gone: ...`; kill-pane commands: []; new-window last arg contains --resume=<session file>; fresh locator @2/%2 pid 52285 start 161400540; generation 1 -> 2; list-panes -a afterwards `@1 %1 52183 sleep 999` + `@2 %2 52285 sleep 999`; kill -0 52183 alive; probe alive on fresh pane and on a repeat probe. RESULT: PASS. Driver B (tmux.panePid + parseProcStatStartTicks + real /proc on tmux -L legion-smoke-96553): server A @1/%1 pid 4171537 start 161387250 VERIFIED; kill-server; recreate -> @1/%1 pid 4172223; old pane-id-only check: ALIVE (pane %1 live, pid 4172223 running); identity check: not verified: pid-mismatch (pane now runs pid 4172223; recorded pid 4171537 start 161387250); fresh locator VERIFIED. RESULT: PASS. Both throwaway servers killed afterwards (`ls` -> no server running); deployment server legion-sjawharlegion never targeted.",
      "result": "pass",
      "notes": "Same-pid-different-start could not be produced on real tmux (no pid wraparound); it is covered by the it.each case 2 unit test and by the real-shutdown-e2e negative control (start ticks off by one on a real pane)."
    },
    {
      "line": 4,
      "criterion": "Daemon AGENTS.md operational invariant gains 'and is the process this locator recorded'.",
      "tests": [],
      "command": "jj -R \"$LEGION_WORKSPACE\" diff --git -r 'main..@-' packages/daemon/src/daemon/AGENTS.md; read against processes.ts verifyPaneProcess (l.2887-2914 of the branch), stopProcess (l.2939-2990), worker-boot-watchdog.ts probeAlive",
      "output": "Sentence 1 (l.48): 'A root is trusted only when its recorded window's pane is live, running OMP, and is the process this locator recorded (the pane's root pid plus its `/proc/<pid>/stat` start ticks, captured at launch and re-checked by `verifyPaneProcess`)' -- TRUE: verifyPaneProcess requires tmux.panePid(paneId) defined (live), pid === panePid, parseProcStatStartTicks(/proc/<pid>/stat) === paneStartTicks, then isOmpPane; identity captured by recordedPaneIdentity inside launchShimmedProcess (spawnTree l.2133 and launchWorker l.2561 both call it) and spawnController. Sentence 2 (l.50): '... or immediately if its shim is unreachable or already confirmed dead, and never at all when the pane no longer runs the process the locator recorded (a reissued pane id is treated as already gone, so a stale locator can never kill another role's pane)' -- TRUE: stopProcess runs verifyPaneProcess after the graceful branch and returns normally (no tmux.killPane) on any non-verified verdict, logging non-pane-gone reasons. Sentence 3 (l.59): 'probed (its tmux pane still running the process its locator recorded -- pid and `/proc` start ticks -- or a reachable/answering shim socket)' -- TRUE: WorkerBootWatchdog.probeAlive is `if (await this.deps.paneProcessVerified(locator)) return true;` then the socket probe; ProcessManager wires paneProcessVerified to verifyPaneProcess(...).verified.",
      "result": "pass",
      "notes": "Wording nit only: the unchanged clause later in l.59 'Only a boot whose pane is gone *and* whose socket refuses a connection is retired' now also covers a pane that is present but fails identity; the new parenthetical redefines the probe so it reads correctly, but 'gone' is doing double duty."
    },
    {
      "line": 5,
      "criterion": "No daemon-issued kill-pane ever lands on a pane whose process is not the one the locator recorded; a stop against a locator whose pane id now reports a different pid issues no kill-pane, treats the pane as gone, clears the locator.",
      "tests": [
        "processes.test.ts: 'never kills a pane whose process is not the one the locator recorded on tree close: treats it as already gone, clears the claim, and logs both identities' (kill-pane commands toEqual([]); claim undefined; tree closed; log has %1, pid 777, 'recorded pid 12345 start 4242')",
        "processes.test.ts: 'never kills a controller pane whose id was reissued to another OMP process when its recorded process is unreachable: the identity gate treats it as already gone, logs both identities, and a fresh controller spawns' (refusing socket -> the gate alone blocks kill-pane -t %1) and its graceful sibling 'asks the recorded controller process to shut down over its own socket before replacing a locator whose pane id was reissued, spawning a fresh controller once it closes'",
        "real-shutdown-e2e.test.ts (LEGION_E2E=1): 'never kills a real pane whose recorded identity is not the process running in it: the stuck worker's claim clears, the tree closes, and the pane survives'",
        "Driver A above: `not killing pane %1 ... treating it as already gone` with zero kill-pane on real tmux during root resurrection (removeTreeWindow -> stopProcessSerialized)"
      ],
      "command": "same JUnit run as line 1; LEGION_E2E=1 LEGION_TMUX_LIVE=1 bun test packages/daemon/src/daemon/__tests__/real-shutdown-e2e.test.ts",
      "output": "Both processes.test.ts tests PASS; real-shutdown-e2e.test.ts 4 pass / 0 fail.",
      "result": "pass",
      "notes": "At 3ff0bbf4 the controller path is covered on both branches: the identity gate (refusing socket) and the graceful shutdown. The gate is also exercised with refusing sockets by the tree-close test, the it.each resurrection tests, Driver A on real tmux, and the real-tmux e2e negative control."
    },
    {
      "line": 6,
      "criterion": "A recorded window id is reused for a new worker pane only when a recorded pane in that window verifies as its recorded process; otherwise a fresh window opens.",
      "tests": [
        "processes.test.ts: 'opens a fresh window for a new worker when the issue's recorded window is live but none of its recorded panes is still the process its locator recorded' (new-window issued; no split-window; no select-layout; tester locator @99/%201 pid 12345)",
        "processes.test.ts (control): 'splits a new worker into the issue's recorded window when a recorded pane in it still verifies as its recorded process' (split-window -t @42; no new-window; tester locator @42/%202 pid 67890)"
      ],
      "command": "same JUnit run as line 1",
      "output": "Both PASS.",
      "result": "pass",
      "notes": "Errors row 6's 'the stale id is dropped from the in-memory issue-to-window map' is implemented (probedWindowId deletes the issueWindows entry) but has no direct test; its only observable consequence needs a launch failure between the failed verify and the rewrite, so this is a low-value gap, not a criterion miss."
    },
    {
      "line": 7,
      "criterion": "The worker boot watchdog never re-arms on a reissued pane: an unconfirmed boot whose pane id now belongs to another OMP process is retired at the interval (socket refused), not re-armed.",
      "tests": [
        "worker-boot-watchdog.test.ts: 'decides the pane branch by paneProcessVerified(locator) -- never the pane id alone -- consulting it before the socket, and retires when both fail' (asserts the armed locator's panePid/paneStartTicks reach the dep; order pane -> socket -> retire)",
        "worker-boot-watchdog.test.ts: 'retires an unconfirmed boot whose pane fails identity (a reissued pane id running another OMP) instead of re-arming, once its socket refuses'",
        "processes.test.ts (end-to-end through ProcessManager): 'retires an unconfirmed boot at the first watchdog interval -- never re-arming -- once its pane id has been reissued to another OMP process and its socket refuses' (retired with fake clock advanced < 2000 ms; locator undefined; launchFailures 1; queued; no kill-pane -t %1)",
        "proc-stat.test.ts: 'counts fields only after the last `)`, so a comm containing spaces or parentheses cannot shift them' (`(tmux: server)`, `(a) b (c))`), plus ordinary line, malformed-line throws, and this process's own /proc/self/stat"
      ],
      "command": "same JUnit run as line 1",
      "output": "All PASS (worker-boot-watchdog.test.ts and proc-stat.test.ts included in the 219-test run).",
      "result": "pass",
      "notes": "The watchdog dep is boolean, so the unit test cannot tell 'reissued' from 'dead' by itself; the ProcessManager test is what proves the reissued pane (list-panes %1 777 vs recorded 12345) is retired rather than re-armed (pre-change: isOmpPane(777) true -> re-arm forever)."
    }
  ],
  "packageChecks": {
    "command": "cd packages/daemon && bunx biome check --error-on-warnings src/ && bunx tsc --noEmit && LEGION_E2E=1 LEGION_TMUX_LIVE=1 bun test",
    "biome": "Checked 145 files in 287ms. No fixes applied. exit 0",
    "tsc": "exit 0 (no output)",
    "bunTestCiEnv": "LEGION_E2E=1 LEGION_TMUX_LIVE=1 bun test -> 899 pass, 0 fail, 0 skip; Ran 899 tests across 57 files. [31.99s] (+1 vs 7d13d3bf: the controller reissued-pane case is now two tests)",
    "processesAndWatchdog": "bun test processes.test.ts worker-boot-watchdog.test.ts -> 213 pass, 3 skip (LEGION_TMUX_LIVE-gated), 0 fail; Ran 216 tests across 2 files. [2.46s]",
    "flakes": "None observed in this round."
  },
  "audit": {
    "droppedAssertions": [
      "processes.test.ts 'retires the pane instead of writing a claim when closeTree closes the tree while a launch is still in flight': `expect(managedState.trees[root]?.status).toBe(\"closed\")` was deleted. Re-added temporarily and run 3x: the status IS 'closed' (contract survived), so the deletion was unnecessary; recommend restoring it. In the same test `expect(commands.some(kill-pane || kill-window)).toBeTrue()` was replaced by the stronger `toContainEqual([... 'kill-pane', '-t', '%1'])` (the tester's own pane, with its shim refusing) -- strengthened, not weakened.",
      "processes.test.ts 'backfills a tree locator's missing pane id once probe confirms it alive, and a controller locator's once controllerAlive confirms it alive' deleted whole (plan rule (b)): the backfill it pinned no longer exists; replaced by the legacy-locator test.",
      "Rule (c) applied without dropping any kill-pane-presence assertion: 'clears completed-tree phases ... shuts down its recorded tmux tree' and 'treats a kill-pane that finds no server ...' now make list-panes report the recorded identity and the socket refuse, so `kill-pane -t %0` is still attempted and PANE_GONE_STDERR is still exercised. No kill-pane-ABSENCE or locator-clearing assertion was weakened or removed anywhere in the diff (grep of every removed `expect`/`toContainEqual`/`toBeTrue`/`toBeFalse` line).",
      "worker-boot-watchdog.test.ts 'probes the worker's own pane pid, not its window's first pane ...' rewritten as the call-order contract; the own-row selection it covered lives in tmux.panePid and is pinned by tmux.test.ts ('is undefined for a pane id missing from the listing, never a sibling pane's pid')."
    ],
    "logWordingPins": [
      "Identity-only assertions everywhere the identity exists: /pid 777/, /pid 2001/, /recorded pid 12345 start 4242/, /recorded pid 1001 start 3000/, /%1/. Nothing pins 'not killing pane' or 'treating ... as dead'.",
      "Legacy case pins the reason phrase /no recorded process identity/ (the implementer cites the architect's amendment B as allowing it; there is no identity to assert in that case).",
      "processes.test.ts T2 uses the pre-existing watchdog log text 're-arming the watch' as its flushEventLoopUntil wait condition (a rename would time the test out); the pre-existing re-arm test at ~3544 observes re-arm via elapsed fake time + locator still set instead. worker-boot-watchdog.test.ts asserts `.not.toMatch(/re-arming the watch/)` -- a negative wording match that is weightless after any rename; `retired` toEqual([token]) is the real proof there."
    ],
    "legacyIdentityLessLocator": "Covered: 'treats a locator recorded before identity tracking as dead on its first probe ...' asserts probe dead on a live OMP pane, log /no recorded process identity/, no kill-pane, new-window with --resume=<session file>, fresh locator with panePid 555. 'Logged once' is asserted as at-least-once; in practice one onProbe tick logs three lines for a legacy root (probe, resurrectDeadTree's re-probe, stopProcess's gate) and never again once the fresh locator is recorded.",
    "launchTimeUnreadableStat": "Covered for both roles: 'fails a root launch through the ordinary launch-failure rollback when the pane's process is already gone before its identity can be recorded' (rejects /exited before its process identity could be recorded/; tree rolled back to queued, launchFailures 1, admission queue [root]) and 'fails a worker launch through the ordinary launch-failure path ...' (claim.launchFailures 1, locator undefined). No controller-launch variant (spec Errors row 5 does not name one)."
  },
  "observations": [
    "All five findings from the 7d13d3bf audit are addressed at 3ff0bbf4 (see correctiveRound.items); no test or src change was made by the tester.",
    "The `expect(log).not.toMatch(/re-arming the watch/)` in worker-boot-watchdog.test.ts and the `re-arming the watch` wait condition in the reconnectWorkers sibling test still couple to the pre-existing watchdog log text; both were noted last round as weightless-after-rename, neither is a criterion gap (`retired toEqual([token])` and the locator/launchFailures assertions carry those tests).",
    "reconcileTmuxWindows' `knownPanes` still includes stale pane ids of locators that failed identity but have not yet cleared (plan concern #4, pre-existing narrowing hazard) -- unchanged, out of scope.",
    "Throwaway drivers (/tmp/legion27-test/*.ts) were not committed; the two throwaway tmux servers from the first round were killed and none was opened this round (no live-tmux driver was needed to re-verify test-only changes; the LEGION_TMUX_LIVE/LEGION_E2E suites ran on their own throwaway sockets). `.omp/config.yml` in the working copy left untouched."
  ],
  "documentationFeedback": "All changed AGENTS.md sentences read true against processes.ts and worker-boot-watchdog.ts, including the corrected watchdog retirement clause.",
  "previousHeadSha": "7d13d3bf0809f90ca5e599a8de3815466b00c2fd",
  "correctiveRound": {
    "head": "3ff0bbf46315fd7d4e762430d3bd001ce478b1a3",
    "commit": "test(daemon): identity-gate controller case, deterministic reissued-pane deadline waits, restored closed assertion",
    "scopeCheck": "jj diff -r 'b2b4116b..3ff0bbf4' --stat: .legion/implement.json (+2/-1), packages/daemon/src/daemon/AGENTS.md (+1/-1), packages/daemon/src/daemon/__tests__/processes.test.ts (+123/-37). `jj diff --summary | grep packages/daemon/src | grep -v __tests__ | grep -v AGENTS.md` -> empty: no src change.",
    "items": [
      {
        "item": 1,
        "finding": "trees[root].status toBe('closed') restored in the in-flight-launch closeTree test",
        "diff": "processes.test.ts:7813 `expect(managedState.trees[root]?.status).toBe(\"closed\");` re-added directly after the TreeClosingError assertion.",
        "execution": "bun test processes.test.ts -t 'retires the pane instead of writing a claim when closeTree closes the tree while a launch is still in flight' -> PASS (JUnit).",
        "result": "confirmed"
      },
      {
        "item": 2,
        "finding": "both reissued-pane deadline tests await the resurrection's own persist via a resolver, not a tick-bounded wait",
        "diff": "`reissuablePanes(state, resurrectedGeneration, commands, sessionExists)` now returns `resurrected` (Promise.withResolvers) and a `saveState` that resolves it when `trees[root]` is generation N, status active, and its locator's panePid equals the pid the fake tmux handed that pane. Both tests pass `saveState: tmuxFake.saveState` and replace `flushEventLoopUntil(() => windowCount() >= N, 20_000)` with `await tmuxFake.resurrected`. grep of lines 10118-10260: 0 occurrences of flushEventLoopUntil, 2 of `await tmuxFake.resurrected`.",
        "execution": "30x isolated `bun test processes.test.ts -t \"reconnectRoots' re-armed deadline treats a restart-surviving locator whose pane id was reissued as dead\"` -> 30/30 pass, 0 fail. 30x isolated for the spawnRoot sibling `resurrects a root once, without killing the pane, when its registration deadline elapses on a pane id since reissued` -> 30/30 pass, 0 fail. (Previous round: 2 failures in the first 6 isolated runs of the reconnectRoots test.)",
        "result": "confirmed"
      },
      {
        "item": 3,
        "finding": "controller reissued-pane case split: identity-gate case with a refusing socket, graceful sibling kept",
        "diff": "New `reissuedControllerFixture(connectWorkerRpc)` (locator %1 recorded pid 12345, list-panes reports %1 777, new-window -> @44 %3 3333). Gate case `never kills a controller pane whose id was reissued to another OMP process when its recorded process is unreachable: the identity gate treats it as already gone, logs both identities, and a fresh controller spawns` passes a connectWorkerRpc that throws ECONNREFUSED and asserts no kill-pane, `new-window controller`, fresh locator @44/%3/3333/4242, log /pid 777/ and /recorded pid 12345 start 4242/. Graceful sibling `asks the recorded controller process to shut down over its own socket before replacing a locator whose pane id was reissued, spawning a fresh controller once it closes` keeps the fakeWorkerRpcClient shutdown spy and `shutdowns toEqual([socketPath])`.",
        "execution": "Both PASS (JUnit). Throwaway control (/tmp/legion27-test/controller-gate-control.ts, not committed) reproducing the gate fixture against the real ProcessManager: kill-pane commands = [], log carries `not killing pane %1 for legion-omp-controller, treating it as already gone: pane %1 now runs pid 777 (recorded pid 12345 start 4242)` -- the stopProcess identity gate is what declined the kill, the graceful branch never ran. Positive control already in the suite: `ensureController retires a stuck controller pane and spawns a fresh one once its registration deadline elapses with no role claim` uses the same refusing socket with list-panes reporting the recorded pid 12345 and asserts kill-pane IS issued; observed pid is the only difference.",
        "result": "confirmed"
      },
      {
        "item": 4,
        "finding": "the start-mismatch it.each case asserts the observed 999999 start ticks",
        "diff": "`reissuedPaneCases` rows gain `observed: RegExp`: /pid 777/ (pid case), /pid 12345 .*999999/ (start-mismatch case), /pid 12345 .*\\/proc/ (unreadable-stat case); the assertion is now `expect(log).toMatch(reissued.observed)` beside the unchanged /recorded pid 12345 start 4242/. describePaneVerdict's start-mismatch line is `pane %0 runs pid 12345 started at 999999 (recorded pid 12345 start 4242)`, so the regex is satisfied only by the observed half.",
        "execution": "All three it.each cases PASS (JUnit).",
        "result": "confirmed"
      },
      {
        "item": 5,
        "finding": "AGENTS.md watchdog sentence no longer uses 'gone' alone for a present-but-mismatched pane",
        "diff": "packages/daemon/src/daemon/AGENTS.md l.59: `Only a boot whose pane is gone -- or present but no longer running the process its locator recorded -- *and* whose socket refuses a connection is retired, ...`. Reads true against WorkerBootWatchdog.probeAlive (paneProcessVerified, then socket probe) and retireUnconfirmedBoot.",
        "execution": "n/a (doc); read against worker-boot-watchdog.ts probeAlive.",
        "result": "confirmed"
      }
    ]
  },
  "schemaVersion": 1,
  "phase": "test",
  "completed": "2026-09-12T21:46:19.868Z"
}