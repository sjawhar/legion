{
  "verdict": "FAIL",
  "head": "e24acdc6bee8cb574db39656726b99a5e8a80a6e",
  "pr": {
    "number": 1018,
    "mergeable": "MERGEABLE",
    "mergeStateStatus": "BLOCKED",
    "note": "BLOCKED = awaiting review, not a conflict; #975 changed the same two lines identically so git merges them clean"
  },
  "passed": 5,
  "failed": 2,
  "failures": [
    {
      "criterion": "Acceptance 7 — PR body Deployment: 'a daemon restarted first refuses … (package 1.22.2) speaks daemon API contract 1' and 'the release version to install is reported after merge'",
      "evidence": "main already carries contract 2: PR #975 (LEGION-20, merged 2026-09-13 10:56 UTC, 52 min after the implementer's handoff) bumped LEGION_DAEMON_API_VERSION and legion.daemonApiVersion 1→2 for the stateGate/GatesRegister shape change; release 1.23.0 declares 2 and is installed in ~/.omp/profiles/legion (manifest: version 1.23.0, daemonApiVersion 2). Proof: `LEGION52_REPO=$LEGION_WORKSPACE /tmp/legion52-smoke/run.sh main` — the tip daemon against profile legion52-main (cp -a of the live plugin tree, 1.23.0 / contract 2) passed the gate (0 refusal lines in /tmp/legion52-smoke/main/start.log) and proceeded to NATS connect (`NatsError: CONNECTION_REFUSED` at scratch port 14252). A merged daemon restarted against today's profile starts; no plugin reinstall or ordering is required by this change (1.23.0 already writes the credential file). Fix: rewrite the Deployment section to say the installed 1.23.0 already satisfies the merged daemon; keep the general install-first-then-restart rule as the rule for future bumps."
    },
    {
      "criterion": "Contract-history text this PR adds (constant doc comment legion-daemon-api.ts:21-22, verifyLegionPluginContract doc comment, daemon AGENTS.md 'Contract history:', pi-envoy AGENTS.md 'Daemon contract', CHANGELOG 'Changed' entry, PR summary and title) — says 2 is the credential file (LEGION-54, bumped by LEGION-52)",
      "evidence": "On main, 2 was introduced by LEGION-20 (#975) for the stateGate/GatesRegister HTTP shapes; 1.23.0 is the first release declaring 2; this change only widens what the number covers to the pane contract. `jj diff --from 52c5e03e7cb3 --to b29711980a2b --git packages/contracts/src/legion-daemon-api.ts packages/pi-envoy/package.json` shows #975's bump and shape change; `jj file show -r main@origin packages/contracts/src/legion-daemon-api.ts` shows `= 2`. The text as written would land on main as an incomplete history. No bump to 3 (architect agreed): it would refuse 1.23.0 for no behavior change."
    }
  ],
  "passedCriteria": {
    "3 negative control": "`LEGION52_REPO=$LEGION_WORKSPACE timeout 300 /tmp/legion52-smoke/run.sh neg` on the tip (working copy = tip; `jj diff --from e24acdc6bee8 --to @ --summary` shows only an untracked empty .omp/config.yml), profile legion52-neg (cp -a of the live tree with released plugin 1.22.2 / contract 1): exit 1; start.log line: `[legion] pi-legion-envoy at /home/ubuntu/.omp/profiles/legion52-neg/plugins/node_modules/@sjawhar/pi-legion-envoy/package.json (package 1.22.2) speaks daemon API contract 1; this daemon requires 2. Install the @sjawhar/pi-legion-envoy release built from this daemon's commit into the active profile.` thrown from verifyLegionPluginContract (boot-probes.ts:273) inside startDaemonLocked (index.ts:286). Nothing else started: `find /tmp/legion52-smoke/neg -name state.json -o -name legions.json` empty; `ss -H -ltn 'sport = :19452'` empty; `pgrep -af 'start sjawhar/5252'` none. Only the pre-gate files exist (daemon/bin/legion, daemon.lock, secrets/dispatch-token, worker-bin/gh), per the documented boot order.",
    "5 bump rule text": "legion-daemon-api.ts doc comment: 'Bump rule: any change to a `LegionDaemonApi` request or response shape, OR to the pane contract, bumps this constant AND the plugin manifest's `legion.daemonApiVersion` in the same commit', with the variable list LEGION_GRANT_FILE, LEGION_BOOT_TOKEN_FILE, LEGION_CONTROLLER_SECRET_FILE, DISPATCH_TOKEN_FILE, DISPATCH_URL, LEGION_DAEMON_URL, LEGION_* identity variables. daemon AGENTS.md carries the same rule and list (plus the identity variables spelled out). verifyLegionPluginContract's doc comment names both surfaces.",
    "6 tests": "packages/contracts `bun test`: 103 pass, 0 fail (8 files). packages/daemon `bun test`: 1117 pass, 7 skip, 0 fail (63 files); `bun test src/daemon/__tests__/index.test.ts -t 'refuses to start when the installed pi-legion-envoy'`: 3 pass, 0 fail. packages/pi-envoy `bun test`: 166 pass, 0 fail (7 files); `bun test src/legion/daemon-api-version.test.ts`: 1 pass (pins manifest legion.daemonApiVersion === LEGION_DAEMON_API_VERSION, which is 2). grep for daemonApiVersion|LEGION_DAEMON_API_VERSION across the tree: no test pins a literal 1 — index.test.ts uses the constant and `+ 1`; the only `= 1` left is the historical docs/solutions/daemon/plugin-daemon-api-contract-version-gate.md (not a test).",
    "8 docs": "packages/daemon/src/daemon/AGENTS.md contract-gate paragraph and packages/pi-envoy/AGENTS.md new 'Daemon contract' section describe the widened rule; packages/pi-envoy/CHANGELOG.md has the entry under '### Changed'.",
    "7 text presence": "PR body Deployment section states install-first-then-restart and 'This supersedes LEGION-54's deploy instruction to install 1.20.1' — present, but its factual claim fails (see failures[0])."
  },
  "positiveBootWithBranchPlugin": "Profile legion52-rig = cp -a of the live plugin tree (secretsd, superpowers, knives, codegraph intact) with @sjawhar/pi-legion-envoy replaced by the plugin packed from the tip: `bun build extensions/envoy.ts extensions/legion.ts --outdir dist --target bun --format esm --external @oh-my-pi/pi-coding-agent --external @oh-my-pi/pi-tui --external @oh-my-pi/pi-utils` + `cp -r skills dist/skills` + manifest with omp.extensions=[dist/envoy.js,dist/legion.js], version 1.22.2-legion52.e24acdc6, legion.daemonApiVersion 2, devDependencies/scripts stripped (a file: dep otherwise fails on workspace:* devDeps). Agent dir mirrors legion21-smoke's layout (config.yml copied; WATCHDOG.md/keybindings.yml/models.yml/agents/hooks/prompts symlinked). `run.sh rig` against a throwaway NATS (container legion52-scratch-nats on 14252, removed afterwards): gate passed, both OMP probes passed (`await probes` at index.ts:674 precedes `legion daemon listening on 127.0.0.1:19482`), state.json written, no pane opened.",
  "rigRunAcceptance4": "NOT RUN this round by architect decision (11:24 UTC): run once on the corrected head. Staged: profile ~/.omp/profiles/legion52-rig; launcher /tmp/legion52-smoke/rig-up.sh [up|down|checkpoints n] (SMOKE_REPO=sjawhar/legion-smoke, SMOKE_PROJECT=sjawhar/52, SMOKE_WEBHOOK_MODE=envoy, SMOKE_DIR=/tmp/legion-smoke-52, NATS_PORT=14252, ENVOY_PORT=19252, LEGION_DAEMON_PORT=19520; scrubs this pane's LEGION_*/DISPATCH_TOKEN_FILE/OMP_PROFILE/PI_CODING_AGENT_DIR; DISPATCH_URL/DISPATCH_TOKEN from ~/.config/opencode/envoy.json via the environment; rig secrets ENVOY_GITHUB_WEBHOOK_SECRET GH_AGENT_APP_PRIVATE_KEY_B64 GH_REVIEW_APP_PRIVATE_KEY_B64 are agent-tier). Next round: repack /tmp/legion52-smoke/pi-legion-envoy-e24acdc6 from the new head (same recipe) and `bun install` in the profile's plugins dir; before up.sh check `docker ps --filter name=legion-smoke-nats`, ports 14252/19252/19520, and message the LEGION-61 implementer (their rig uses the same container name and both envoy-mode rigs cross-triage LEGSMOKE roots; they ran down.sh at 11:26 UTC and had retired by 12:15 UTC). Evidence to collect: worker transcript (session file recorded in /tmp/legion-smoke-52/daemon/state.json roles[].locator.ompSessionFile) showing the first `jj git push` and `legion gh` outputs, the 0600 grant file <state_dir>/secrets/<role token>-grant, and absence of `LEGION_GRANT is missing`/403 in transcript and daemon.log.",
  "mainMoved": "PR #975 (LEGION-20) merged 10:56 UTC; release commit 63dac108397d 'chore: release pi-envoy v1.23.0'; main@origin LEGION_DAEMON_API_VERSION = 2, manifest daemonApiVersion 2, version 1.23.0. Branch fork point 52c5e03e7cb3 (#981). Transient hazard noted, not a defect: 1.23.0 (contract 2, #975 shapes) against this branch's daemon (contract 2, pre-#975 shapes) passes the gate with mismatched stateGate/GatesRegister shapes — resolved at merge since the merged code carries main's shapes; the rig must use the branch-packed plugin, never the live 1.23.0.",
  "scratchArtifacts": "/tmp/legion52-smoke/{neg,pos,main,rig}/ (configs, start.log, scratch daemon dirs), /tmp/legion52-smoke/pi-legion-envoy-e24acdc6, /tmp/legion52-smoke/rig-up.sh, /tmp/legion52-smoke/pr-body-*.md, profiles ~/.omp/profiles/legion52-{neg,pos,main,rig}. Keep legion52-neg (negative control) and legion52-rig (positive run) for the next round; the rest is deletable after the tree closes.",
  "schemaVersion": 1,
  "phase": "test",
  "completed": "2026-09-13T12:17:07.661Z"
}