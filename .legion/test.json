{
  "summary": "LEGION-71 test phase, round 2 (full round after the conflict-forced rebase onto main at 9a6708feb9cd following #1011): certifies code head cf78801fc1ce (tip 0c2f279a2f12; branch head 6cc9145fbe5c adds only the implement handoff's rebase record) for pull request sjawhar/legion#1029. All six spec Acceptance lines PASS on independent reproduction from /tmp with the real mise on this box; the implementer's fingerprint-isolation claim is confirmed (the change is the up.test.sh header hunk alone, every other hunk byte-identical, so round 1's six negative controls on those hunks still stand) and four fresh negative controls on the merged hunk each fail the harness at the guarding assertion. shellcheck's finding set equals main@origin's, CI is green at the tip, and the PR is MERGEABLE (BLOCKED = the review/checks gate). No finding: #1010 (LEGION-41) is MERGEABLE and #1026 (LEGION-61) CONFLICTING, both still open ahead of this PR; another conflict-forced rebase may come when they land.",
  "verdict": "PASS at code head cf78801fc1ce (tip 0c2f279a2f12).",
  "passed": 6,
  "failed": 0,
  "failures": [],
  "headTested": "cf78801fc1ce (code head after the #1011 rebase; tip 0c2f279a2f12 differs from it only by .legion/implement.json and .legion/test.json; branch head 6cc9145fbe5c differs from the tip only by .legion/implement.json)",
  "previousRound": {
    "codeHead": "55a042fff733",
    "verdict": "PASS (round 1, 2026-09-13 12:57Z); superseded by this round because the fingerprint changed",
    "controlsStillValid": "The six round-1 scratch controls (mise stderr swallowed; remedy unconditional; mise called on the override path; start_process argv write removed; fake go exit 1; stored_webhook_mode branches swapped) exercised hunks whose added/removed lines are byte-identical between 55a042fff733 and cf78801fc1ce (see fingerprint.isolation); the fake-go control was re-run here anyway."
  },
  "fingerprint": {
    "recipe": "jj diff --from \"fork_point(main@origin | <sha>)\" --to <sha> --git --context 0 '~(.legion | docs/solutions)' | sed -e '/^@@/d' -e '/^index /d' | sha256sum",
    "old": "5e997e330af104ddad21ed6af7f8dcff81721826f15060edf428e05080f45059 (55a042fff733, fork point 5db1957059f4)",
    "new": "15311147c7877e2aeaeb15d581d39ce4fbaa6ea9eae4dad56ab2cc389e06c373 (cf78801fc1ce, fork point 9a6708feb9cd)",
    "matchesImplementer": true,
    "isolation": "Excluding scripts/smoke/up.test.sh ('~(.legion | docs/solutions | scripts/smoke/up.test.sh)'): 9688970744b60be0d9ddd313ab6974a8253c22a2315030d16aa0d1e6a96e4fa6 at both heads. up.test.sh alone: 48b6556c… -> 76a285f8…. `diff` of the two up.test.sh --context 0 diffs with hunk positions stripped (22 hunks -> 23): the only change is the header — `-sed '$d' \"$up_script\" >\"$source_file\"` / `+sed -e '$d' -e \"s|^repo_root=.*|repo_root=\\\"${project_root}\\\"|\" \"$up_script\" >\"$source_file\"` became `-sed '$d' \"$up_script\" >\"${source_dir}/up.sh\"` / `+sed -e '$d' -e \"s|^repo_root=.*|…|\" \"$up_script\" >\"${source_dir}/up.sh\"`, the comment merged with #1011's two lines, and the fixture grep now reads \"${source_dir}/up.sh\" (a separate hunk because #1011's `cp dispatch-config.sh` sits between). Every other hunk identical."
  },
  "acceptance": {
    "1 mise stderr passthrough": {
      "verdict": "PASS",
      "harness": "up.test.sh PASS lines `a mise failure other than not-installed stops preflight on mise's own message, without the mise install remedy` and `an explicit LEGION_OMP_PATH override passes through; otherwise … names mise install when it is not` (the latter asserts `mise ERROR <pin> not installed` reached the output).",
      "realMiseForm": "The plan's `source <(sed \"\\$d\" scripts/smoke/up.sh)` now fails: `/dev/fd/63: line 30: /dev/fd/dispatch-config.sh: No such file or directory` (up.sh sources dispatch-config.sh beside itself by BASH_SOURCE since #1011). Used the harness's form: `d=$(mktemp -d); sed '$d' scripts/smoke/up.sh >$d/up.sh; cp scripts/smoke/dispatch-config.sh $d/; bash -c \"source $d/up.sh; unset LEGION_OMP_PATH; …\"`.",
      "realMiseConfigError": "mise 2026.8.10 linux-x64. `MISE_CONFIG_FILE=/tmp/legion71-bad-mise.toml resolve_omp_path` (file content `not toml [[[`) -> exit=1; stderr: `Error loading settings file: TOML parse error at line 1, column 5` (twice, with caret block), `mise::config::parse_error`, `× Invalid TOML in config file: /tmp/legion71-bad-mise.toml` (miette frame), then `error: mise where github:sjawhar/oh-my-pi@18.1.18-sami.20260912-104423 failed; mise's own message is above`; `grep -c 'mise install'` = 0.",
      "realMiseNotInstalled": "`SMOKE_OMP_PIN=github:sjawhar/oh-my-pi@0.0.0-nonexistent … resolve_omp_path` -> exit=1; stderr: `mise ERROR github:sjawhar/oh-my-pi@0.0.0-nonexistent not installed`, `mise ERROR Version: 2026.8.10 linux-x64 (2026-08-20)`, `mise ERROR Run with --verbose or MISE_VERBOSE=1 for more information`, `error: OMP pin github:sjawhar/oh-my-pi@0.0.0-nonexistent is not installed (mise where failed); run: mise install github:sjawhar/oh-my-pi@0.0.0-nonexistent`; `grep -c 'mise install'` = 1.",
      "realMiseSuccess": "installed pin -> exit=0, stdout `/home/ubuntu/.mise/installs/github-sjawhar-oh-my-pi/18.1.18-sami.20260912-104423/bin/omp`, stderr 0 bytes."
    },
    "2 harness runs from /tmp, (set -e; …) form": {
      "verdict": "PASS",
      "upTest": "`cd /tmp && bash \"$LEGION_WORKSPACE/scripts/smoke/up.test.sh\"` -> exit=0, `grep -c '^PASS:'` = 20, non-PASS lines = 0. `comm` against the round-1 output: only-in-new = `DISPATCH_URL and DISPATCH_TOKEN fall back to .dispatch.serverUrl/.dispatch.token in envoy.json`, `an exported DISPATCH_URL/DISPATCH_TOKEN wins over envoy.json`, `with neither source present, resolution stops naming the variable, the envoy.json path, and the key`; only-in-old = none. All three are present in main@origin's up.test.sh and absent (0 `envoy.json` matches) at the old fork point 5db1957059f4: #1011's cases.",
      "checkpointsTest": "-> exit=0, 23 PASS, non-PASS lines = 0. only-in-new = `checkpoints read DISPATCH_URL and DISPATCH_TOKEN from envoy.json when the variables are unset`, `missing-token error names the variable, the envoy.json path, and the key`; only-in-old = `missing-token error names the exact secrets-wrapped invocation` (#1011 reworded it). Both new lines are in main@origin's checkpoints.test.sh: net +1, #1011's.",
      "downTest": "-> exit=0, 3 PASS, non-PASS lines = 0 (unchanged).",
      "prerequisite": "`bun install --frozen-lockfile`: `Checked 598 installs across 696 packages (no changes)`.",
      "negativeControls": "Scratch up.test.sh (project_root pinned to the workspace): (a) fake go `exit 0` -> `exit 1`: exit 1 after 14 PASS at `expected up.sh main() to succeed on the installed pin (exit 1); output: GREEN OMP build: …/mise-install/bin/omp (mise where <pin>)`. (b) the final fake curl's issues-create branch made to `exit 1`: exit 1 after 14 PASS at the same assertion, output showing both GREEN lines then `error: could not create Dispatch root issue in LEGSMOKE` — a failure deep inside main() is caught by the `(set -e; …)` form. Unmutated pinned scratch harness: exit 0, 20 PASS."
    },
    "2b repo_root pin (the merged hunk)": {
      "verdict": "PASS",
      "negativeControls": "Scratch up.test.sh line 25 replaced with #1011's unpinned `sed '$d' \"$up_script\" >\"${source_dir}/up.sh\"`: (a) fixture grep kept -> exit 1, 0 PASS at `fixture error: repo_root was not pinned in the sourced copy`; (b) fixture grep block also removed -> from /tmp exit 1, 0 PASS at `error: Module not found \"//packages/daemon/src/daemon/omp-pin.ts\"` / `error: could not read the OMP pin from packages/daemon/src/daemon/omp-pin.ts (bun required)` — the pre-pin failure mode; the pin is what makes the harness cwd-independent."
    },
    "3 fake-mise call log; empty-glob guard": {
      "verdict": "PASS",
      "evidence": "Hunks byte-identical to 55a042fff733 (fingerprint.isolation); harness cases `with no override, preflight verifies the omp-pin.ts pin under mise and exports no LEGION_OMP_PATH to the daemon` and `an explicit LEGION_OMP_PATH override is named in the daemon env block and in no other start_process argv …` PASS at this head. Round-1 controls (mise called on the override path -> `must not invoke mise; calls: where <pin>`; argv write removed -> `fixture error: main() recorded no start_process argv for listener`) stand."
    },
    "4 checkpoints precedence; scratch dir hygiene": {
      "verdict": "PASS",
      "evidence": "checkpoints.test.sh PASS line `the recorded webhook-mode file outranks an exported SMOKE_WEBHOOK_MODE in both directions` at this head; `grep -n 'rm -rf' scripts/smoke/checkpoints.test.sh` -> `14:trap 'rm -rf \"$temporary_dir\"' EXIT`, `123:trap 'kill \"$clean_pid\" \"$planted_pid\" 2>/dev/null; rm -rf \"$temporary_dir\"' EXIT` only; `473:bare_smoke_dir=\"${temporary_dir}/bare-smoke\"`. Hunks byte-identical to round 1; the swapped-precedence control stands."
    },
    "5 wording": {
      "verdict": "PASS",
      "evidence": "up.sh / README.md / checkpoints.test.sh hunks byte-identical to round 1 (only-place wording at up.sh:87-88, up.sh:679, README.md:78; README.md:126 `uses … when there is no record`; GREEN line labels). `grep -n '#[0-9]'` over the four files at cf78801fc1ce -> no output, exit 1. The up.test.sh:573 message hunk is unchanged."
    },
    "6 lands after #1011/#1010/#1026; T4 at open": {
      "verdict": "PASS",
      "prView": "`legion gh -- pr view 1029 --json mergeable,mergeStateStatus,headRefOid,statusCheckRollup,state` -> state=OPEN, headRefOid=6cc9145fbe5c657273a8ce1f6a80b81c1206a421, mergeable=MERGEABLE, mergeStateStatus=BLOCKED (checks/review gate, not a conflict); rollup pr-title/lint/typecheck/test all COMPLETED SUCCESS (run 34758821318).",
      "ciAtTip": "`gh run view 34758696071`: Tests head=0c2f279a2f123d67f6a8155ad243c7dc129653ae completed/success, jobs test/lint/pr-title/typecheck all success. cf78801fc1ce itself has no check-runs (the rebase pushed the tip directly); it differs from the tip only by .legion/ files, which the Tests workflow does not read.",
      "rebaseComment": "https://github.com/sjawhar/legion/pull/1029#issuecomment-5653441724 by legion-implementer[bot] at 13:02:21Z: `rebase d542ee97fc5b → 0c2f279a2f12; fingerprint 5e997e33… → 15311147…; changed.` with the Legion footer — both SHAs and both full fingerprints named; matches my recomputation.",
      "predecessors": "#1011 merged (9a6708feb9cd, now the fork point); #1010 OPEN MERGEABLE; #1026 OPEN CONFLICTING. The body's Lands-after paragraph is unchanged from round 1."
    }
  },
  "noBehaviourChange": {
    "filesInDiff": "`jj diff --from 'fork_point(main@origin | cf78801fc1ce)' --to cf78801fc1ce --summary` (fork point 9a6708feb9cd): A .legion/plan.json, M scripts/smoke/README.md, M scripts/smoke/checkpoints.test.sh, M scripts/smoke/up.sh, M scripts/smoke/up.test.sh — nothing else.",
    "shellcheck": "shellcheck 0.11.0 `-S warning` on up.sh, up.test.sh, checkpoints.test.sh at head: only `scripts/smoke/up.sh line 22 … SC2155 (warning)`. Default-severity finding sets (code, count) for the three files at main@origin vs cf78801fc1ce are identical: 1x SC2086 note, 1x SC1091 note (#1011's `source dispatch-config.sh` line, on main too), 1x SC2155 warning; `diff` empty."
  },
  "observations": [
    "The plan's proofCommands.realMise* recipe (`source <(sed \"\\$d\" scripts/smoke/up.sh)`) is stale since #1011: up.sh sources dispatch-config.sh beside itself by BASH_SOURCE, which is /dev/fd/63 under process substitution. The working form is the harness's stripped copy in a temp dir with dispatch-config.sh copied beside it (recorded in acceptance 1 above). Not a defect in this PR; a note for anyone reusing the plan.",
    "Both `mergeStateStatus=BLOCKED` readings are the checks/review gate, not a conflict; `mergeable=MERGEABLE` throughout this round.",
    "#1010 (LEGION-41) MERGEABLE and #1026 (LEGION-61) CONFLICTING, both open and both edit scripts/smoke; this PR lands after them, so another conflict-forced rebase is likely (plan.rebaseHotSpots[1..2] name the lines). If a later rebase leaves the fingerprint unchanged, bare gates only; otherwise another full round.",
    "PR body E2E section rewritten to this round's evidence at cf78801fc1ce; CI, Threads, Thermo, Fast-follow, Chain lines left as the implementer maintains them.",
    "Every mutation lived under /tmp/legion71-ctl (round-2 script run-controls-round2.sh, log controls-round2.log); `jj diff --from cf78801fc1ce --to @ -- scripts/` was empty after all runs — the pushed head was not edited.",
    "Working copy carries the untracked provisioned .omp/config.yml; not committed."
  ],
  "documentationFeedback": "No change requested. The one remaining `falls back` in the four files (README.md:85) is about model fallback under a missing provider key, outside this batch.",
  "environment": "Linux 7.0.0-1011-aws x64; harnesses run from /tmp; mise 2026.8.10 linux-x64 (2026-08-20); shellcheck 0.11.0; bun 1.3.14; jj workspace at $LEGION_WORKSPACE, fork point 9a6708feb9cd.",
  "learningsInjected": [
    "docs/solutions/legion/fast-follow-pr-mechanics-and-queue-registration.md",
    "docs/solutions/legion/worker-pane-shell-gotchas.md"
  ],
  "learningsHelpful": [
    "docs/solutions/legion/worker-pane-shell-gotchas.md"
  ],
  "schemaVersion": 1,
  "phase": "test",
  "completed": "2026-09-13T13:11:45.582Z"
}