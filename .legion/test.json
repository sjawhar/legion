{
  "pullRequest": {
    "repo": "sjawhar/legion",
    "number": 1089,
    "url": "https://github.com/sjawhar/legion/pull/1089",
    "branch": "legion/LEGION-99",
    "head": "1ce204c8b08094c7cae07e0e8fdf60dbd9e8892f",
    "codeHead": "5943ced3558008cfd2e72cd73ee3e642a1ee705c",
    "forkPoint": "617b778ec9f0 (chore: release pi-envoy v1.29.0); main@origin had moved to 372412f8 at test time and GitHub still reports MERGEABLE"
  },
  "passed": 4,
  "failed": 0,
  "failures": [],
  "implementerProof": {
    "verdict": "verified",
    "how": "Re-ran every command in .legion/implement.json independently at head 1ce204c8 (identical to the code head 5943ced3 outside .legion/), not copying its numbers. actionlint (`mise x go@1.26.8 -- go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.7`, nothing installed into the profile) exit 0 on pr-and-main.yaml and pr-title.yaml; js-yaml structural read: on keys [push, merge_group, pull_request], merge_group.types [checks_requested], lint/typecheck/test `if` null, pr-title.yaml `on` = [pull_request] only. `go test ./internal/webhook/ -run TestGitHubHandlerMergeGroup -v -count=1` PASS x3; `go test ./internal/webhook/ ./internal/contracts/ -count=1` ok/ok; gofmt -l and go vet print nothing. Reproduced the implementer's red run by reverting githubSkip to the pre-fix `return false` locally: both signed subtests fail `published = 1, want 0; first topic \"notifications.github.sjawhar.legion.comment\"`, the unsigned subtest still passes; restored with `jj restore` (working copy back to only .omp/config.yml). The implementer's cited CI runs were at 5943ced3; at the current head 1ce204c8 I read the superseding runs with `legion gh -- run view`: Tests 34830388734 (lint, typecheck, test success), PR Title 34830388744 (pr-title success), Legion Envoy and Contracts 34830388735 (changes, envoy-go success; six path-filtered jobs skipped). Skip placement checked in source: githubSkip is called at github.go:136, after verify.Github at :121."
  },
  "proof": [
    {
      "criterion": "Spec Testing, last paragraph (pre-merge half 1): the Tests workflow parses, lists the merge_group trigger, and its lint, typecheck, and test jobs are not conditioned away, so they run on merge-group commits under exactly the names the main ruleset requires; the PR-title check is pull-request-only.",
      "surface": "The committed workflow files at head 1ce204c8, parsed by actionlint v1.7.7 and by a YAML parser, plus GitHub Actions' own acceptance of the file: the PR's Tests run at that head (GitHub answers an invalid trigger block with a failed 'workflow file issue' run instead of a green run). The merge_group run itself cannot exist until an administrator adds the merge queue rule.",
      "command": "cd -- \"$LEGION_WORKSPACE\" && mise x go@1.26.8 -- go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.7 .github/workflows/pr-and-main.yaml .github/workflows/pr-title.yaml; bun -e '<js-yaml load of both files, printing Object.keys(on), on.merge_group, on.pull_request, and each job's if>'; legion gh -- run view 34830388734 --repo sjawhar/legion --json workflowName,headSha,jobs",
      "observed": "actionlint: no findings, exit 0. pr-and-main.yaml: on keys [push, merge_group, pull_request]; merge_group {types: [checks_requested]}; pull_request {types: [opened, reopened, synchronize]}; jobs lint if=null, typecheck if=null, test if=null. pr-title.yaml: on keys [pull_request] (types opened, edited, reopened, synchronize), job pr-title if=null, no merge_group key. Tests run 34830388734 at 1ce204c8, event pull_request: test=success, typecheck=success, lint=success.",
      "headSha": "1ce204c8b08094c7cae07e0e8fdf60dbd9e8892f",
      "negativeControl": "sed 's/merge_group:/merge_grup:/' .github/workflows/pr-and-main.yaml > /tmp/legion99-tester-bad.yaml && actionlint /tmp/legion99-tester-bad.yaml -> `7:3: unknown Webhook event \"merge_grup\". see https://docs.github.com/... [events]`, exit status 1 (file removed afterwards)."
    },
    {
      "criterion": "Spec Testing, last paragraph (pre-merge half 2) and Requirements row 7: Envoy's GitHub webhook, sent a recorded merge_group delivery, answers 200 and publishes nothing, and the skip does not bypass signature verification.",
      "surface": "The Go webhook handler served over HTTP (httptest) with the recorded merge_group fixture (packages/envoy/internal/webhook/testdata/merge_group_checks_requested.json) and a destroyed payload, locally at head 1ce204c8, and the same suite in the PR's Legion Envoy and Contracts run (job envoy-go). No real merge_group delivery exists yet because the queue is not on.",
      "command": "cd -- \"$LEGION_WORKSPACE/packages/envoy\" && mise x go@1.26.8 -- go test ./internal/webhook/ -run TestGitHubHandlerMergeGroup -v -count=1 && mise x go@1.26.8 -- go test ./internal/webhook/ ./internal/contracts/ -count=1; legion gh -- run view 34830388735 --repo sjawhar/legion --json jobs",
      "observed": "PASS: checks_requested_is_acknowledged_and_not_published, destroyed_is_acknowledged_and_not_published, an_unsigned_merge_group_is_still_refused; `ok github.com/sjawhar/envoy/internal/webhook`, `ok github.com/sjawhar/envoy/internal/contracts`; gofmt -l and go vet clean. Legion Envoy and Contracts run 34830388735 at 1ce204c8: changes=success, envoy-go=success (attempt 1, no re-run needed).",
      "headSha": "1ce204c8b08094c7cae07e0e8fdf60dbd9e8892f",
      "negativeControl": "Reverted the fix locally (`return event == \"merge_group\"` -> `return false` in github.go, not committed) and re-ran the same test: FAIL with `github_test.go:410: published = 1, want 0; first topic \"notifications.github.sjawhar.legion.comment\"` on both signed subtests, the unsigned subtest still PASS (401 precedes the skip); `jj restore` put the fix back and the suite is green again. Second control inside the suite: the same fixture with `X-Hub-Signature-256: sha256=invalid` -> 401 `invalid signature`, nothing published."
    }
  ],
  "checks": {
    "ciAtHead": {
      "Tests": {
        "runId": 34830388734,
        "attempt": 1,
        "jobs": {
          "lint": "success",
          "typecheck": "success",
          "test": "success"
        }
      },
      "PR Title": {
        "runId": 34830388744,
        "attempt": 1,
        "jobs": {
          "pr-title": "success"
        },
        "note": "a second PR Title run 34831413524 at the same head (body edit) also success"
      },
      "Legion Envoy and Contracts": {
        "runId": 34830388735,
        "attempt": 1,
        "jobs": {
          "changes": "success",
          "envoy-go": "success"
        },
        "skipped": [
          "pi-envoy",
          "contracts",
          "dispatch",
          "envoy-plugin",
          "claude-envoy-bridge",
          "envoy-client"
        ]
      }
    },
    "mergeability": "legion gh -- pr view 1089 --json mergeable,mergeStateStatus -> mergeable=MERGEABLE, mergeStateStatus=BLOCKED (required review not yet submitted; not CONFLICTING) with main@origin at 372412f8",
    "scope": "legion gh -- pr view 1089 --json files and jj diff --from fork_point(main@origin | 1ce204c8) --to 1ce204c8 --summary agree: M .github/workflows/pr-and-main.yaml, M AGENTS.md, M packages/envoy/internal/webhook/github.go, M packages/envoy/internal/webhook/github_test.go, A packages/envoy/internal/webhook/testdata/merge_group_checks_requested.json, A .legion/plan.json, A .legion/implement.json — nothing else; no ruleset, organizer, envoy-and-contracts.yaml, or plan/spec markdown.",
    "agentsMd": "AGENTS.md hunk at lines 125-133 (Issue Lifecycle, before 'Review signaling') adds the sentence: every merge into main goes through GitHub's merge queue, naming the Tests workflow's lint/typecheck/test jobs and the merge_group trigger, no rebase and no new commit on the branch."
  },
  "notProvableBeforeMerge": "Spec acceptance lines 1-3 (a stale-green PR is held by a failed merge-group check; a compatible PR merges with its branch untouched; main's Tests green at every merge commit) exist only once an administrator adds the merge queue rule to the main ruleset, which the spec orders after this PR merges. Not attempted, per the spec's Testing section: they are the implementer's production verification after the switch, and the issue closes only when they are proven.",
  "deviations": [
    "No smoke rig, scratch daemon, stand-in, kind cluster, NATS container, or scratch tmux server was started: Sami's standing order (2026-09-13 19:00 UTC) relayed by the architect overrides any plan or skill text asking for one; this PR's proof set needs none.",
    "The plan's `envoy` requiredSkill (subscribe to notifications.github.sjawhar.legion.comment to observe a merge-group cycle) was not exercised: no merge_group delivery can reach the listener before the ruleset change, so there is nothing to observe pre-merge; the Go test is the pre-merge proof and the implementer observes the topic in production after the switch.",
    "The plan's yq commands were run as js-yaml (via `bun -e`) + JSON output because yq is not installed; same structural assertions.",
    "The push of this handoff is refused for this role by design (review App holds no contents permission); the commit rides the implementer's next push."
  ],
  "observations": [
    "GitHub's merge_group run cannot be provoked on this PR before the ruleset change, so the trigger's acceptance by GitHub is proven indirectly: the file with the new block was parsed and run by Actions (a malformed trigger block yields a failed 'workflow file issue' run) and actionlint accepts the event name.",
    "The reviewer's `Thermo` line is still to be written; this PR is not docs-only (Go handler + workflow), so the reviewer runs both thermo agents once at the reviewed head.",
    "Production activation of the Envoy fix needs the operator to restart the listener container on the image Release Envoy Listener publishes at the merge SHA (autodeploy redeploys only dispatch); recommended before the switch, not a blocker for it (plan concern 2)."
  ],
  "schemaVersion": 1,
  "phase": "test",
  "completed": "2026-09-14T10:10:56.444Z"
}