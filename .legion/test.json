{
  "passed": 22,
  "failed": 0,
  "failures": [],
  "implementerProof": {
    "verdict": "verified",
    "how": "Round 4: confirmed `jj diff --from dcc86ae8 --to 11113a3a --summary` (the four rig scripts, the three harnesses, README, the CI workflow, the three handoffs; test-lib.sh and envoy-bridge.ts untouched) and CI green at 11113a3a (Tests 34943451992) and at ae359bc1 (run 34943625612 attempt 2 after the LEGION-174 flake). Re-ran the implementer's 18 proof entries in kind on my own instances legion26v/legion26w with the same image: up.sh exit 0 in 1m18s / 1m17s with the same READY blocks; admitted, architect-pod, spec-posted, tree-moved OK; kill-pod-resume OK with the new `landed: pod … Failed (worker exit 137)` detail; pod-hygiene OK (3 pods); done SKIPPED-BLOCKED exit 3; worker-cap OK with the same detail shape; down.sh KIND SMOKE DOWN on both, four absence checks empty; 0 rendered* files during (a 1 s watcher over the whole up.sh, 240 ticks) and after the run; the 12 secret values (tokens, Postgres password, provider keys, six PEM body lines) found only under secrets/ and overlay/secrets/ (600 in 700) and 0 files after down.sh; box-wide 0 App PEM / provider-key values under ~/.local/state/legion-smoke (every prior instance dir included); host processes: recorded pids are the binaries (readlink /proc/<pid>/exe → bin/envoy-listener, bin/envoy-dispatch; the port-forward and keeper are their own bash loops), parent systemd --user, none of the five secret names, no App PEM / provider-key value in any environ (the listener holds its own ENVOY_API_TOKEN and Dispatch its own ENVOY_TOKEN/DATABASE_URL/DISPATCH_AGENT_TOKEN — their configuration by design; the loops hold none); the port-base rerun refusal names both bases and every live resource with records unchanged; a never-started instance gains no state dir from down.sh or checkpoints.sh; the foreign cluster-record refusal on the real surface (`refusing to delete cluster 'legion-smoke-someone-else': the record does not name this instance's cluster legion-smoke-legion26w`, cluster and kubeconfig left, exit 1); the harnesses OK, bun test 8 pass, SC2251 0, the CI bridge step present; my round-1/2 canaries and a reinstated `kubectl kustomize … >rendered.yaml` in a copy of up.sh each fail the harness naming the line (313 for the existence check; 65 for the by-value scan with 313 removed). Observations matched the implementer's in kind; verdict verified. Rounds 1–3 stand.",
    "round1": {
      "verdict": "verified",
      "how": "Re-ran every command of the implementer's eight proof entries on fresh throwaway instances of my own (legion26t on SMOKE_PORT_BASE=31300, legion26u on 31400 for worker-cap, legion26n for the negative controls) at head 090f950b, with the newest cli-v* image at test time (cli-v1.18.0, sha256:31c79105…, built from 453f6a42; the implementer used cli-v1.17.0 sha256:16fb8cd1…) and compared in kind: up.sh exit 0 in 1m23s with the same KIND SMOKE READY block (controller: none, keeper started); admitted, architect-pod, spec-posted, tree-moved OK with the same detail shapes; kill-pod-resume OK in 1m39s with the same kill method (docker exec <kind node> kill -9 <pid>), generation 1→2, --resume=<recorded ompSessionFile>, session id unchanged, the implementer claimed afterwards, corroborated in the daemon log (`pod …-architect-g1 Failed` → `resurrecting … by resuming OMP session …`) and read independently from the g2 pod's command and PVC; pod-hygiene OK (3 pods); done SKIPPED-BLOCKED exit 3; worker-cap OK (queue held SLEGION26U-1/planner while 1 pod ran; promoted; peak 2, longest excess 0s); down.sh KIND SMOKE DOWN exit 0 and the four absence checks empty on both; the tag, port-in-use, and no-record negative controls produced the same refusal texts. The harness/biome/tsc/shellcheck claims re-ran green; the implementer's `shellcheck … the harnesses carry no error/warning either` is accurate at the warning level — the SC2251 info-level findings are what failure 1 is about. One divergence from the implementer's first run: every phase worker here came up at generation 1 with launchFailures 0 (the keeper's unset landed before each provisioning), where the implementer's first run saw the planner at g8 before the keeper existed."
    },
    "round2how": "Round 2: confirmed the changed-file set two ways (`jj diff --from 090f950b --to 21f288b7 --summary` and `legion gh -- api repos/sjawhar/legion/compare/090f950b...21f288b7`), sha256-compared the five rig scripts at 090f950b and 21f288b7 (all identical), re-ran the three harnesses (OK), `bun test scripts/kind-smoke` (8 pass), `shellcheck -x` on test-lib.sh and the three harnesses (SC2251 count 0; only 3× SC2016 on the `printf %q` fake template and 1× SC2015 on the setup `[ -e ] && cp || ln -s` idiom remain, both info), re-planted my two round-1 canaries on a throwaway copy — `FAIL up.test.sh:183: expected failure, but succeeded: grep -Fq anthropic-canary-value …/calls.log …/last.txt` exit 1 and `FAIL checkpoints.test.sh:337: expected failure, but succeeded: grep -Fq dispatch-secret-value-0123456789 …/out.txt` exit 1 (copy line = real line + 1, matching the implementer's recorded lines) — plus a plant of my own in down.test.sh (`listener_pid=$$` before line 146: `FAIL down.test.sh:147: expected failure, but succeeded: alive 2612384`, exit 1), then removed the plants and got all three OK again with the copies byte-identical to the real files. CI: Tests run 34935787660 (lint/typecheck/test success) and PR Title run 34935787711 at 21f288b7; PR MERGEABLE. Round 1 verdict stands (rig re-run of every implementer proof on fresh instances; unchanged rig).",
    "round3how": "Round 3: confirmed the changed-file set (`jj diff --from 21f288b7 --to dcc86ae8 --summary`, GitHub compare) and sha256-compared the eight unchanged files; read test-lib.sh's new refute (`case $status: 1 → ok; 0 → 'expected failure, but succeeded'; * → 'refuted command errored (exit N)'`) and the up.test.sh split; re-ran the three harnesses (OK), bun test (8 pass), shellcheck -x on test-lib.sh + harnesses (SC2251 count 0; 3× SC2016 and 1× SC2015 info remain); planted on a throwaway copy — (1) `rm -f \"$tmp/state/pids/listener.pid\"` before the loop → `FAIL up.test.sh:176: [ -f \"$tmp/state/pids/$name.pid\" ]` exit 1 (copy line = real 175 + 1, the .pid test, previously exempt); (2) `rm -f \"$tmp/last.txt\"` before the provider-key refute → `grep: … No such file or directory` then `FAIL up.test.sh:186: refuted command errored (exit 2): grep -Fq anthropic-canary-value …/calls.log …/last.txt` exit 1; `rm -f \"$tmp/out.txt\"` before the FAILED-text refute → `FAIL checkpoints.test.sh:337: refuted command errored (exit 2): grep -Fq dispatch-secret-value-0123456789 …/out.txt` exit 1; the round-1 canary still bites (`FAIL up.test.sh:186: expected failure, but succeeded: …`); plants removed → copies diff-identical to the real files. Sense: the 30 refutes use four shapes (`alive` ×4, `grep -Eq` ×6, `grep -Fq` ×16, `grep -q` ×4); proven each yields exactly 1 on a clean negative — grep no-match on one file, two files, an empty file, and an empty `<(tail …)` stream → 1; `kill -0` on a reaped pid and on a dead pgid → 1 — and the real-file runs are green, so none became a false FAIL. The implementer's recorded lines and texts match mine. Round 1 (rig) and round 2 (lock bites) verdicts stand."
  },
  "proof": [
    {
      "criterion": "Acceptance 1: up.sh creates the instance and prints one summary block; controller: none on today's main; the LEGION-177 keeper started",
      "surface": "the real kind smoke on the Legion dev box, throwaway instance legion26t (SMOKE_PORT_BASE=31300), image ghcr.io/sjawhar/legion-worker@sha256:31c791054b7644f97a6762d94cd0def56bc29c528e780c031f7b1fac5459c9fb (cli-v1.18.0, built from 453f6a42dee0 = #1108 merged; no LEGION-177, no #1110), dev-box recipe, kind v0.24.0, kubectl v1.31.0, Go 1.26.8 via mise",
      "command": "cd $LEGION_WORKSPACE && PATH=~/.local/bin:$PATH SMOKE_INSTANCE=legion26t SMOKE_PORT_BASE=31300 SMOKE_WORKER_IMAGE=ghcr.io/sjawhar/legion-worker@sha256:31c791054b7644f97a6762d94cd0def56bc29c528e780c031f7b1fac5459c9fb SMOKE_IMPLEMENT_APP_KEY_FILE=/etc/legion/implementer.pem SMOKE_REVIEW_APP_KEY_FILE=/etc/legion/reviewer.pem SMOKE_OMP_LAUNCH_PREFIX= mise x go@1.26 -- /home/legion/.local/bin/legion-pane-env bash scripts/kind-smoke/up.sh (05:07:44Z)",
      "observed": "exit 0 in 1m23s: CREATED cluster legion-smoke-legion26t / CREATED container legion-smoke-legion26t-nats / CREATED container legion-smoke-legion26t-postgres / STARTED listener (pid 3198202) / STARTED dispatch (pid 3201672) / CREATED Dispatch project SLEGION26T / deployment \"legion-daemon-demo\" successfully rolled out / STARTED port-forward (pgid 3218444) / STARTED legion-177-keeper (pgid 3228161) / SKIPPED controller: the checkout has no legion controller start (pull request #1110) / CREATED root issue SLEGION26T-1 (todo); KIND SMOKE READY: gateway 172.30.0.1, nats://172.30.0.1:31300, listener http://172.30.0.1:31301, dispatch http://172.30.0.1:31302 (project SLEGION26T; login smoke), postgres 172.30.0.1:31303, daemon http://127.0.0.1:31304 → svc/legion-daemon-demo:13370, image … (daemon API contract 5), session store pvc, worker cap 6, controller: none (…#1110), github ingress: none (checkpoint done will report SKIPPED-BLOCKED), legion-177: keeper (pgid 3228161, every 3s), root issues SLEGION26T-1. kubectl get pods 36 s later: legion-daemon-demo-… 1/1 Running and legion-slegion26t-1-architect-g1 1/1 Running.",
      "headSha": "090f950bf503a0e73d6d198dc2670ed4cfe29c8c",
      "negativeControl": "kind, go, and jq absent from PATH (an env -i shell with only the other tools) → `error: missing required tools: kind go jq — see docs/kubernetes.md, Runbook: the kind smoke, Prerequisites`, exit 1, no cluster, container, or records dir for the instance; a tag (`ghcr.io/sjawhar/legion-worker:latest`) → `error: SMOKE_WORKER_IMAGE must be pinned by digest (…@sha256:<64 hex>); got ghcr.io/sjawhar/legion-worker:latest`, exit 1, nothing created; SMOKE_PORT_BASE=31302 (the live instance's Dispatch port) → `error: port 31302 (nats) is already in use and is not this instance's; choose another SMOKE_PORT_BASE (current 31302)`, exit 1, nothing created"
    },
    {
      "criterion": "Acceptance 2: admitted, architect-pod, spec-posted, tree-moved OK; done SKIPPED-BLOCKED exit 3 naming what the run lacks",
      "surface": "instance legion26t through the port-forward, the scratch Dispatch, and kubectl with the instance kubeconfig, checkpoints run in the runbook order right after up.sh (05:09Z–05:13Z)",
      "command": "SMOKE_INSTANCE=legion26t bash scripts/kind-smoke/checkpoints.sh admitted | architect-pod | spec-posted | tree-moved | done",
      "observed": "CHECKPOINT admitted OK: SLEGION26T-1 status=in_progress tree=active controller=none; CHECKPOINT architect-pod OK: SLEGION26T-1 in_progress; tree active generation 1; pod legion-slegion26t-1-architect-g1 Running with its legion.dev labels; claim legion-slegion26t-1 Bound and mounted; CHECKPOINT spec-posted OK: spec 5258bad9-de67-424b-a5ff-2a944d7581c4 posted; gate off, none registered; phase worker: planner; CHECKPOINT tree-moved OK: phase worker planner on SLEGION26T-1 pod legion-slegion26t-1-planner-g1 Running; no child is a tree of its own (generation 1: the keeper's unset preceded the planner's provisioning; every phase worker of the run came up at g1, launchFailures 0); CHECKPOINT done SKIPPED-BLOCKED: the run has no controller (the checkout has no legion controller start (pull request #1110)), exit 3. Also run as the runbook's `for c in …` loop 16 minutes later: admitted OK (status=testing), architect-pod FAILED: SLEGION26T-1 has Dispatch status 'retro', expected in_progress (after its 600 s budget), spec-posted OK, tree-moved OK, pod-hygiene OK, done SKIPPED-BLOCKED — every verdict true to the state at that time, none a false green.",
      "headSha": "090f950bf503a0e73d6d198dc2670ed4cfe29c8c",
      "negativeControl": "SMOKE_KILL_ROLE=tester → `CHECKPOINT kill-pod-resume SKIPPED-BLOCKED: SMOKE_KILL_ROLE=tester is not supported: a crashed phase worker is only retired until something addresses it, so only the root architect (SMOKE_KILL_ROLE=architect) is resurrected by the daemon itself`, exit 3; worker-cap on this 1-root run → `CHECKPOINT worker-cap SKIPPED-BLOCKED: worker-cap needs a run started with SMOKE_ROOT_ISSUES=2 SMOKE_WORKER_CAP=1 (this run: SMOKE_ROOT_ISSUES=1 SMOKE_WORKER_CAP=6)`, exit 3"
    },
    {
      "criterion": "Acceptance 3: kill-pod-resume crashes the root architect's pod mid-phase from the kind node; generation +1; --resume=<recorded ompSessionFile>; same session id; the tree keeps working; the WORKAROUND LEGION-177 line",
      "surface": "instance legion26t, the planner mid-phase (planner g1 Running 32 s, tree generation 1, session 01a0a378-2fc4-7000-9111-219a1f09defc), 05:10:22Z–05:12:01Z",
      "command": "SMOKE_INSTANCE=legion26t bash scripts/kind-smoke/checkpoints.sh kill-pod-resume; then kubectl … logs deploy/legion-daemon-demo | grep -E 'Failed|resurrecting'; kubectl … get pod legion-slegion26t-1-architect-g2 -o json | jq -r '.spec.containers[0].command[] | select(startswith(\"--resume=\"))'",
      "observed": "WORKAROUND LEGION-177 applied (credential.interactive was already unset) / CHECKPOINT kill-pod-resume OK: SLEGION26T-1 architect pod legion-slegion26t-1-architect-g1 → legion-slegion26t-1-architect-g2 generation 1→2 (kill: docker exec legion-smoke-legion26t-control-plane kill -9 4842 (container f9fd3414cd77); LEGION-177 workaround applied (already unset), keeper running (pgid 3228161)) session 01a0a378-2fc4-7000-9111-219a1f09defc unchanged; --resume=/home/legion/.omp/profiles/legion/agent/sessions/--legion-workspaces-sjawhar-legion-smoke-slegion26t-1--/2026-09-15T05-09-17-892Z_01a0a378-2fc4-7000-9111-219a1f09defc.jsonl; tree moved after the replacement registered — claims changed: > legion-demo-slegion26t-1-implementer 1 implementer SLEGION26T-1; real 1m39s, exit 0. Daemon log: `[legion] pod legion-slegion26t-1-architect-g1 Failed: last lines of worker: …` → `[legion] resurrecting SLEGION26T-1 by resuming OMP session /home/legion/.omp/…/2026-09-15T05-09-17-892Z_01a0a378-2fc4-7000-9111-219a1f09defc.jsonl` → `[legion] resync probed 1 active roots`. Independent read of the g2 pod: command carries exactly that --resume= value; its volume claim is legion-slegion26t-1; state afterwards: trees[SLEGION26T-1] generation 2, readyConfirmedAt set, same ompSessionFile; roles: implementer g1 pod legion-slegion26t-1-implementer-g1. The tree then reached `retro` through planner, implementer, tester, and reviewer pods across the kill.",
      "headSha": "090f950bf503a0e73d6d198dc2670ed4cfe29c8c",
      "negativeControl": "a second kill-pod-resume run 16 minutes later, with the tree at `retro` and every worker idle: generation 2→3, --resume and session unchanged, then `CHECKPOINT kill-pod-resume FAILED: the tree of SLEGION26T-1 has not moved since the replacement registered (claims: implementer/SLEGION26T-1@1,planner/SLEGION26T-1@1,reviewer/SLEGION26T-1@1,tester/SLEGION26T-1@1; statuses: SLEGION26T-1 retro)`, exit 1 after SMOKE_WAIT_KILL_COMPLETE — a true report (the daemon lost the retro implementer's completion in the resurrection window; LEGION-182), never OK"
    },
    {
      "criterion": "Acceptance 4a: pod-hygiene — profiles exact, no secret value in env/command/args, PID 1 clean, daemon unlabelled",
      "surface": "instance legion26t with the architect g2, implementer g1, and planner g1 pods Running (05:13:25Z), and again with only the architect g3 (05:56Z)",
      "command": "SMOKE_INSTANCE=legion26t bash scripts/kind-smoke/checkpoints.sh pod-hygiene; plus an independent sweep: grep -rlIF -f <the 8 secret values> over <state>/logs, <state>/records, <state>/overlay/{legion,kustomization}.yaml, the tester's transcript logs, the daemon log, and every Pod/Deployment string in `kubectl get pods,deploy -o json`",
      "observed": "CHECKPOINT pod-hygiene OK: 3 pods checked; profiles match; no secret in env/command/args; PID 1 clean; daemon unlabelled (exit 0); later CHECKPOINT pod-hygiene OK: 1 pods checked; …. The independent sweep found no secret value anywhere for either instance (legion26t and legion26u): 0 hits in logs/records/manifests/tester files, 0 hits in pod/deployment specs. Secret files are 0600 under 0700 directories.",
      "headSha": "090f950bf503a0e73d6d198dc2670ed4cfe29c8c",
      "negativeControl": "the harness fixtures for a planted token in a container env (`… env DISPATCH_TOKEN_CANARY contains a secret value`), a provider key in PID 1's environ, and a requests.memory mismatch all print FAILED exit 1 in checkpoints.test.sh — but see failure 1: the harness's own assertion that the FAILED text never contains the token value (line 331) is inert; on the live rig the OK line carried no value"
    },
    {
      "criterion": "Acceptance 4b: worker-cap on a run started with SMOKE_ROOT_ISSUES=2 SMOKE_WORKER_CAP=1",
      "surface": "throwaway instance legion26u (SMOKE_PORT_BASE=31400 SMOKE_ROOT_ISSUES=2 SMOKE_WORKER_CAP=1 SMOKE_WORKER_IDLE_RETIRE=60), same image, project SLEGION26U, root issues SLEGION26U-1 SLEGION26U-2, 05:13:25Z–05:17:26Z",
      "command": "SMOKE_INSTANCE=legion26u SMOKE_PORT_BASE=31400 SMOKE_ROOT_ISSUES=2 SMOKE_WORKER_CAP=1 SMOKE_WORKER_IDLE_RETIRE=60 … bash scripts/kind-smoke/up.sh; SMOKE_INSTANCE=legion26u bash scripts/kind-smoke/checkpoints.sh admitted; SMOKE_INSTANCE=legion26u bash scripts/kind-smoke/checkpoints.sh worker-cap",
      "observed": "up.sh exit 0 in 1m26s (CREATED root issue SLEGION26U-1 (todo), CREATED root issue SLEGION26U-2 (todo), `worker cap: 1`); CHECKPOINT admitted OK: SLEGION26U-1 status=in_progress tree=active controller=none; CHECKPOINT worker-cap OK: queue held SLEGION26U-1/planner while 1 worker pod(s) ran; promoted to pod legion-slegion26u-1-planner-g1; worker pods peaked at 2 with worker_cap 1 (an excess is idle lingering, allowed up to worker_idle_retire_seconds 60 + 30s; longest 0s), sampled every 5s — real 2m34s, exit 0. Pods afterwards: two architects Running, legion-slegion26u-2-planner-g1 (104 s old, finished), legion-slegion26u-1-planner-g1 (5 s old, the promoted head); daemon log: `retiring idle worker legion-demo-slegion26u-2-planner` twice at the 60 s window.",
      "headSha": "090f950bf503a0e73d6d198dc2670ed4cfe29c8c",
      "negativeControl": "the same checkpoint on the 1-root instance legion26t → `CHECKPOINT worker-cap SKIPPED-BLOCKED: worker-cap needs a run started with SMOKE_ROOT_ISSUES=2 SMOKE_WORKER_CAP=1 (this run: SMOKE_ROOT_ISSUES=1 SMOKE_WORKER_CAP=6)`, exit 3"
    },
    {
      "criterion": "Acceptance 5: down.sh tears down by record; the four absence checks afterwards; nothing the rig did not create was touched",
      "surface": "instances legion26t and legion26u (05:57Z), the partial instance legion26n after its bridge refusal (05:12Z), and the box: a `docker ps -a` baseline taken before the first up.sh, ~/.kube/config's mtime, the dev-box daemon's pid",
      "command": "SMOKE_INSTANCE=legion26t bash scripts/kind-smoke/down.sh; SMOKE_INSTANCE=legion26u bash scripts/kind-smoke/down.sh; then for each: kind get clusters; docker ps -a --filter label=legion-smoke.instance=<i>; tmux -L legion-smoke-<i> ls; grep -c legion-smoke ~/.kube/config; plus diff of `docker ps -a --format '{{.Names}}' | sort` before/after, stat ~/.kube/config, legion legions",
      "observed": "Both: STOPPED legion-177-keeper (pgid …) / STOPPED port-forward (pgid …) / STOPPED listener (pid …) / STOPPED dispatch (pid …) / DELETED cluster legion-smoke-<i> / shredded …/kubeconfig / REMOVED container legion-smoke-<i>-nats / REMOVED container legion-smoke-<i>-postgres / ten `shredded secrets/…` lines / KIND SMOKE DOWN, exit 0. Afterwards: `kind get clusters` → `No kind clusters found.`; `docker ps -a --filter label=legion-smoke.instance=<i>` empty for both; `tmux -L legion-smoke-<i> ls` → `error connecting to /tmp/tmux-1003/legion-smoke-<i> (No such file or directory)`; `grep -c legion-smoke ~/.kube/config` → 0, the file's mtime 04:32:03Z unchanged from before the run; no pid files and no host process left for either instance; the container diff against the pre-run baseline shows only other agents' unlabelled containers (inspect-task-*, testcontainers-ryuk-*), nothing of the rig; the dev-box daemon (pid 1559294, started 01:06Z) untouched. The partial legion26n: down.sh → `GONE envoy-bridge (pid … is not the recorded process; record removed)` / STOPPED listener / STOPPED dispatch / DELETED cluster / REMOVED both containers / KIND SMOKE DOWN exit 0.",
      "headSha": "090f950bf503a0e73d6d198dc2670ed4cfe29c8c",
      "negativeControl": "SMOKE_INSTANCE=nothing bash scripts/kind-smoke/down.sh → `/home/legion/.local/state/legion-smoke/nothing has no instance record: this directory never started a kind smoke; stopping nothing, deleting nothing`, exit 0, both live clusters still listed; down.sh again on the already-torn-down legion26n → `cluster legion-smoke-legion26n is already gone` / `container … is already gone` ×2 / KIND SMOKE DOWN exit 0"
    },
    {
      "criterion": "Acceptance 6 and the Errors table: the runbook's commands run as written; the troubleshooting rows match; AGENTS.md rows present; README variables complete; the bridge refuses before the daemon",
      "surface": "the checkout at 090f950b and the instances above",
      "command": "every command of docs/kubernetes.md 'Running the kind smoke' once (the dev-box up.sh line with the `mise x go@1.26 --` prefix, the `for c in …` checkpoint loop, down.sh, the worker-cap instance lines, the four absence checks); grep -ohE 'SMOKE_[A-Z0-9_]+' over lib.sh/up.sh/down.sh/checkpoints.sh/envoy-bridge.ts vs README.md; grep of the AGENTS.md rows; SMOKE_GITHUB_INGRESS=envoy SMOKE_UPSTREAM_NATS=nats://127.0.0.1:9 … up.sh for instance legion26n",
      "observed": "Every runbook command ran except the `secrets … --` line (this box has no `secrets` CLI, which the runbook states). Troubleshooting rows reached: `controller: none (…)` (the #1110 reason), the port-in-use text (matches the row verbatim), the LEGION-177 keeper and `WORKAROUND LEGION-177 applied` line, state read through the port-forward, the IPv6-first IPAM entry with gateway 172.30.0.1 chosen by regex. README: every SMOKE_* the scripts read is documented (SMOKE_RIG_NATS is set by up.sh for the bridge, not an input). Root AGENTS.md: the Architecture sentence, the three Commands lines, the WHERE TO LOOK row; daemon AGENTS.md: the `Live proof` bullet — all present. CI: `.github/workflows/pr-and-main.yaml` has the `Test kind smoke harnesses` step; Tests run 34931087847 lint/typecheck/test success and PR Title run 34931100269 success at 090f950b; GitHub: mergeable true, mergeable_state clean. Bridge refusal: CREATED cluster/containers, STARTED listener, STARTED dispatch, STARTED envoy-bridge (pid 3389437), then `error: the GitHub bridge could not subscribe upstream (nats://127.0.0.1:9); see …/logs/envoy-bridge.log` (the log: `BRIDGE UNHEALTHY CONNECTION_REFUSED`), exit 1 in 1m6s, no `legion` namespace and no deployment in the cluster — the daemon was never applied.",
      "headSha": "090f950bf503a0e73d6d198dc2670ed4cfe29c8c",
      "negativeControl": "the `for c in …` loop run 16 minutes after up.sh (tree at `retro`): `architect-pod` → `CHECKPOINT architect-pod FAILED: SLEGION26T-1 has Dispatch status 'retro', expected in_progress` exit 1 after its 600 s budget — correct per the spec's assertion, and the reason the runbook loop must start right after up.sh"
    },
    {
      "criterion": "Round 2: the harness regression lock can fail (the round-1 failure), SC2251 = 0, green on the real files",
      "surface": "the checkout at 9fe181ff on the dev box: the real harnesses, and a throwaway copy of scripts/kind-smoke in a fake repo root (deploy/ and packages/ symlinked to the workspace) for the plants",
      "command": "bash scripts/kind-smoke/{up,down,checkpoints}.test.sh; bun test scripts/kind-smoke; shellcheck -x scripts/kind-smoke/{up,down,checkpoints}.test.sh scripts/kind-smoke/test-lib.sh | grep -c SC2251; on the copy: sed -i '182i echo \"docker run --token anthropic-canary-value\" >>\"$FAKE_LOG\"' up.test.sh; sed -i '336i echo dispatch-secret-value-0123456789 >>\"$tmp/out.txt\"' checkpoints.test.sh; sed -i '146i listener_pid=$$' down.test.sh; bash each; then sed -i '/# PLANTED/d' and bash each again",
      "observed": "Real files: `up.test.sh: OK`, `down.test.sh: OK`, `checkpoints.test.sh: OK`, 8 pass 0 fail, SC2251 count 0 (remaining shellcheck: 3× SC2016 info, 1× SC2015 info). Plants: `FAIL up.test.sh:183: expected failure, but succeeded: grep -Fq anthropic-canary-value /tmp/tmp.99DqdIavKw/calls.log /tmp/tmp.99DqdIavKw/last.txt` exit 1 after `refusals OK`; `FAIL checkpoints.test.sh:337: expected failure, but succeeded: grep -Fq dispatch-secret-value-0123456789 /tmp/tmp.RHtErDGKN5/out.txt` exit 1 after `kill-pod-resume OK`; `FAIL down.test.sh:147: expected failure, but succeeded: alive 2612384` exit 1 after `refusal OK`. Plants removed (copies diff-identical to the real files): all three OK. Sense check: an instrumented refute reporting any guarded command exiting ≥ 2 printed nothing across all 30 refutes in the normal fixture — each is a clean exit-1 negative that should hold.",
      "headSha": "9fe181ffe8438d1b580cc0120f318de772136c26",
      "negativeControl": "the two survivors above: up.test.sh:174's first `&&` operand (a missing listener.pid with .start present reaches the end of the script, exit 0), and `refute grep …` with its target file removed (harness exit 0 with only a stray `grep: … No such file or directory` on stderr)"
    },
    {
      "criterion": "Round 2 docs: the checkpoint-order note (README and runbook) and the shim-connect troubleshooting row are accurate against round 1",
      "surface": "the checkout at 9fe181ff, `jj diff --from 090f950b --to 21f288b7 scripts/kind-smoke/README.md docs/kubernetes.md`",
      "command": "read both diffs against the round-1 observations (architect-pod FAILED after 600 s with status retro; kill-pod-resume FAILED `has not moved` after its SMOKE_WAIT_KILL_COMPLETE budget at retro; the `architect shim connect … negotiate_protocol timed out` retries on every root start, ending in `stopping … retries: the tree moved to generation 2`)",
      "observed": "Both notes say what round 1 showed: run the loop right after up.sh and kill-pod-resume right after tree-moved; architect-pod asserts in_progress; a kill at retro or between phases lands on an idle tree or a lost completion (LEGION-182) and the checkpoint prints FAILED truthfully. The troubleshooting row for the shim-connect lines matches the daemon log of both instances (7 retry lines per root start, `/process/ready` landed, trees confirmed). One wording inaccuracy in both additions: the `600 s` wait is architect-pod's budget (SMOKE_WAIT_ARCHITECT_POD); the kill-pod-resume `has not moved` FAILED arrives after SMOKE_WAIT_KILL_COMPLETE, 1800 s (my second kill: 05:25Z kill → 05:56Z FAILED). Fast-follow wording.",
      "headSha": "9fe181ffe8438d1b580cc0120f318de772136c26",
      "negativeControl": "grep of every `SMOKE_*` the scripts read against the README (unchanged since round 1: complete; SMOKE_RIG_NATS bridge-internal); the rig scripts are byte-identical so no runbook command was re-run"
    },
    {
      "criterion": "Round 3: the two round-2 survivors are fixed (up.test.sh:174 split; refute accepts exactly exit 1), no exemption survives, harnesses/bun/shellcheck green, docs numbers correct",
      "surface": "the checkout at 51dfa6ff on the dev box: the real harnesses, and a throwaway copy of scripts/kind-smoke in a fake repo root for the plants",
      "command": "bash scripts/kind-smoke/{up,down,checkpoints}.test.sh; bun test scripts/kind-smoke; shellcheck -x scripts/kind-smoke/{up,down,checkpoints}.test.sh scripts/kind-smoke/test-lib.sh | grep -c SC2251; audit: grep -nE '^\\s*!' / 'set +e' / assertion-first ' && ' / line-continued chains over the three harnesses; plants on the copy: `rm -f \"$tmp/state/pids/listener.pid\"` before line 174, `rm -f \"$tmp/last.txt\"` before line 185, `rm -f \"$tmp/out.txt\"` before checkpoints.test.sh:336, `echo \"docker run --token anthropic-canary-value\" >>\"$FAKE_LOG\"` before line 185; each harness run, then `sed -i '/# PLANTED/d'` and diff against the real files; exit-code proof of each refuted shape in a clean bash script",
      "observed": "Real files: `up.test.sh: OK`, `down.test.sh: OK`, `checkpoints.test.sh: OK`, 8 pass 0 fail, SC2251 count 0. Audit: no unguarded bare `!` (the six `! cmd || { …; exit 1; }` remain and work), no `set +e`, no line-continued chain, no assertion-first `&&` chain (up.test.sh 136–138 are conditional assignments inside the fake curl body; 70/113–115 are setup and fake-docker bodies). Plants: `FAIL up.test.sh:176: [ -f \"$tmp/state/pids/$name.pid\" ]` exit 1; `FAIL up.test.sh:186: refuted command errored (exit 2): grep -Fq anthropic-canary-value /tmp/tmp.YXAv7payLC/calls.log /tmp/tmp.YXAv7payLC/last.txt` exit 1; `FAIL checkpoints.test.sh:337: refuted command errored (exit 2): grep -Fq dispatch-secret-value-0123456789 /tmp/tmp.hEAc4hgZdm/out.txt` exit 1; `FAIL up.test.sh:186: expected failure, but succeeded: grep -Fq anthropic-canary-value …` exit 1; plants removed → identical to the real files. Shapes: grep -Fq/-Eq/-q no-match → 1 (one file, two files, empty file, empty process substitution); kill -0 reaped pid → 1; kill -0 -- -dead pgid → 1. Docs: README `after its 1800 s SMOKE_WAIT_KILL_COMPLETE budget … architect-pod alone waits its 600 s SMOKE_WAIT_ARCHITECT_POD`; runbook the same — both match round 1 (kill 05:25Z → FAILED 05:56Z; architect-pod FAILED after 600 s at retro). CI: dcc86ae8 lint/typecheck/test success (Tests 34937318672), pr-title success (34937318549); PR MERGEABLE. Head 51dfa6ff (handoff only): Tests run 34937678477 red on `real-deployment-instructions-e2e.test.ts` (`tmux window ownership marker failed (exit 1): server exited unexpectedly` in spawnController — LEGION-174, a file the PR does not touch; 1653 pass, 1 fail), re-run with `legion gh -- run rerun 34937678477 --failed` (accepted).",
      "headSha": "51dfa6ffbdf684426fa802c1fef6a478cb1a9196",
      "negativeControl": "`grep -Fq '' <file>` exits 0, so a `refute grep -Fq \"$(cat <secret file>)\" …` whose secret file is empty or missing reports `expected failure, but succeeded` — loud (misleading text, but never a silent pass); and the three `refute grep … <(tail -n +N \"$FAKE_LOG\")` lines (up.test.sh 190/380/404) would read an empty stream if $FAKE_LOG vanished (tail's error is hidden by the process substitution; grep exits 1) — they guard rerun idempotence, not secrets, and $FAKE_LOG is created by the harness itself"
    },
    {
      "criterion": "Round 4, review blocking 1: the rendered manifest (every secret base64) is never on disk; secret values live only under secrets/ and are shredded by down.sh; the box carries no leftover",
      "surface": "instance legion26v on the dev box (cli-v1.18.0), a 1 s find-watcher over the state dir for the whole up.sh, by-value greps, and the harness on a throwaway copy",
      "command": "watcher: `while …; do find <state> -name 'rendered*'; sleep 1; done` for 240 s across up.sh; after up.sh: find <state> -name 'rendered*'; grep -rlF -f <12 values: dispatch/envoy tokens, postgres password, provider keys, three body lines of each App PEM> <state>; stat -c %a on secrets/ and overlay/secrets/ and every file; grep -rlF -f <PEM+provider values> ~/.local/state/legion-smoke/; after down.sh the same greps; harness: bash scripts/kind-smoke/up.test.sh on a copy of up.sh with `kubectl kustomize \"$o\" >\"$state/rendered.yaml\"` reinstated, then with the harness's `[ ! -e rendered.yaml ]` line removed too",
      "observed": "watch done: 240 ticks, rendered hits=0; after up.sh: no rendered* file; the 12 values hit exactly 9 files — overlay/secrets/{github-app-implement.pem,github-app-review.pem,providers.env}, secrets/{dispatch-token,dispatch-token-auth-header,envoy-token,envoy-token-auth-header,postgres-password,postgres.env} — all 600 in 700 directories, none elsewhere; box-wide: 0 files under ~/.local/state/legion-smoke hold an App PEM or provider-key value outside legion26v's own secret dirs, every prior instance dir (legion26, 26b, 26c, 26d, 26e, 26t, 26u) 0 value hits and 0 rendered; after down.sh: 0 files hold a secret value, 0 rendered*, and after legion26w's down.sh 0 App PEM / provider-key values anywhere under ~/.local/state/legion-smoke. Harness: `FAIL up.test.sh:313: [ ! -e \"$tmp/state/rendered.yaml\" ]` exit 1 with the render written; `FAIL up.test.sh:65: expected failure, but succeeded: grep -lF -f …/values <every file outside secrets/>` exit 1 with the existence check removed (the by-value scan alone catches the Secret-bearing file); the real harness OK.",
      "headSha": "ae359bc1356118026df0baf2fbdd8724c921c9ca",
      "negativeControl": "the reinstated render write above, and my round-1/2 canaries: a provider key planted in the fake argv log → `FAIL up.test.sh:60: expected failure, but succeeded: grep -Fq -f …/values …/calls.log …/last.txt`; the runner output removed → `FAIL up.test.sh:60: refuted command errored (exit 2): …`"
    },
    {
      "criterion": "Round 4, review blocking 2: exactly one verdict per checkpoint on a transient and on a persistent Dispatch failure",
      "surface": "instance legion26v: the instance's own scratch Dispatch process paused with SIGSTOP (my process), the real `admitted` checkpoint",
      "command": "kill -STOP <dispatch pid>; SMOKE_INSTANCE=legion26v bash scripts/kind-smoke/checkpoints.sh admitted (with `sleep 15; kill -CONT` in the background); then kill -STOP again and SMOKE_WAIT_ADMITTED=12 SMOKE_POLL_INTERVAL=2 … checkpoints.sh admitted; kill -CONT; curl the issue",
      "observed": "transient: one line, `CHECKPOINT admitted OK: SLEGION26V-1 status=in_progress tree=active controller=none`, exit 0, real 15.2 s (the read blocked until CONT and the retry answered); persistent: one line, `CHECKPOINT admitted FAILED: Dispatch did not answer GET /api/v1/issues/SLEGION26V-1 (see …/logs/dispatch.log)`, exit 1; grep -c '^CHECKPOINT ' = 1 in both; Dispatch answered 200 after CONT. Harness: the fail-once and fail-always fixtures (checkpoints.test.sh 182–188, 223, 340) pin the same.",
      "headSha": "ae359bc1356118026df0baf2fbdd8724c921c9ca",
      "negativeControl": "the old checkpoints.sh (dcc86ae8) against the new harness reproduces the reviewer's `FAILED … then OK` pair (the implementer's proof); the persistent run overran its 12 s budget to 152 s because poll checks the budget between attempts and each attempt hits curl's --max-time 20 (plus daemon_state's own retries) — an observation on wall-clock accuracy under a hung Dispatch, never a second verdict"
    },
    {
      "criterion": "Round 4, review blocking 3: pod-hygiene with a denied kubectl exec is FAILED naming the pod, never `PID 1 clean`",
      "surface": "instance legion26v on the real API server: a ServiceAccount (labels app.kubernetes.io/*, never legion.dev/project) bound to a Role with get/list pods and deployments and no pods/exec, its token in a 0600 kubeconfig, handed to the checkpoint through a `kubectl` PATH wrapper that swaps only the --kubeconfig argument (real kubectl, real cluster)",
      "command": "kubectl apply of SA/Role/RoleBinding; kubectl create token …; kubectl … auth can-i create pods/exec → no; kubectl … exec <pod> -- true → Forbidden; RESTRICTED_KUBECONFIG=… PATH=<wrapper>:$PATH SMOKE_INSTANCE=legion26v bash scripts/kind-smoke/checkpoints.sh pod-hygiene; then kubectl delete rolebinding,role,serviceaccount -l app.kubernetes.io/name=tester-hygiene-probe",
      "observed": "`Error from server (Forbidden): pods \"legion-slegion26v-1-planner-g1\" is forbidden: User \"system:serviceaccount:legion:tester-hygiene-probe\" cannot create resource \"pods/exec\"`; the checkpoint: exactly one line, `CHECKPOINT pod-hygiene FAILED: could not read the PID 1 environment of pod legion-slegion26v-1-architect-g2 (kubectl exec failed); nothing is claimed clean for it`, exit 1, `PID 1 clean` absent; with the instance kubeconfig the same checkpoint printed `CHECKPOINT pod-hygiene OK: 3 pods checked; profiles match; no secret in env/command/args; PID 1 clean; daemon unlabelled`. My RBAC objects deleted afterwards.",
      "headSha": "ae359bc1356118026df0baf2fbdd8724c921c9ca",
      "negativeControl": "checkpoints.test.sh 395/399: the environ fixture removed → the same FAILED text; emptied → `pod … PID 1 environment read back empty`"
    },
    {
      "criterion": "Round 4, review blocking 4 and the harness surface: every negation is a refute, SC2251 0, the earlier canaries still bite, the kustomize fake emits a Secret and the leak scan catches a Secret-bearing file outside secrets/",
      "surface": "the checkout at ae359bc1: the real harnesses, and a throwaway copy in a fake repo root for the plants",
      "command": "bash scripts/kind-smoke/{up,down,checkpoints}.test.sh; bun test scripts/kind-smoke; shellcheck -x scripts/kind-smoke/*.sh; grep -nE '(^|[;&|] *)! ' and '! grep.*\\|\\|' and 'set +e' over the three harnesses; plants: the round-1 argv canary and the round-2 rm -f before refute_secret_leak (up.test.sh:325), the token value into out.txt before checkpoints.test.sh's FAILED-text refute, `listener_pid=$$` before down.test.sh's `refute alive`, the reinstated rendered.yaml write (see blocking 1); `sed -i '/# PLANTED/d'` and diff against the real files",
      "observed": "up.test.sh: OK, down.test.sh: OK, checkpoints.test.sh: OK; 8 pass 0 fail; shellcheck SC2251 count 0 (3× SC2016 and 1× SC2015 info remain); zero `! ` negations and zero `! grep … ||` forms, no set +e; refute counts 32/9/8; plants: `FAIL up.test.sh:60: expected failure, but succeeded: grep -Fq -f …`, `FAIL up.test.sh:60: refuted command errored (exit 2): …`, `FAIL up.test.sh:313: [ ! -e \"$tmp/state/rendered.yaml\" ]`, `FAIL up.test.sh:65: …grep -lF -f …`, `FAIL checkpoints.test.sh:383: expected failure, but succeeded: grep -Fq dispatch-secret-value-0123456789 …/out.txt`, `FAIL down.test.sh:153: expected failure, but succeeded: alive 321121`, each exit 1; plants removed → copies identical, all OK. CI: `.github/workflows/pr-and-main.yaml` carries `Test kind smoke harnesses` (the three) and the new `Test kind smoke bridge: bun test scripts/kind-smoke`.",
      "headSha": "ae359bc1356118026df0baf2fbdd8724c921c9ca",
      "negativeControl": "the plants above; the control run of the unmodified copies OK"
    },
    {
      "criterion": "Round 4, the eight fix-here items on their surfaces",
      "surface": "instances legion26v and legion26w, the box's /proc, the harnesses",
      "command": "kill-pod-resume OK detail; worker-cap OK detail + checkpoints.test.sh 441–445; `done` on legion26w with records/controller and records/github-ingress temporarily set to `tmux …`/`envoy` (my instance; restored after), once with GH_HOST=bogus.invalid and once with gh absent from PATH; up.sh rerun on the live legion26v with SMOKE_PORT_BASE=31750; for each host process: readlink /proc/<pid>/exe, tr '\\0' '\\n' </proc/<pid>/environ | cut -d= -f1, grep -F -f <values> over environ, the parent's cmdline; after down.sh kill -0 on every recorded pid and pgrep -af legion-smoke/legion26; the CI workflow diff; SMOKE_INSTANCE=never26x down.sh and checkpoints.sh admitted; down.sh on legion26w with records/cluster set to legion-smoke-someone-else (restored after) and down.test.sh 191/205",
      "observed": "kill landed from pod0: `… (kill: docker exec legion-smoke-legion26v-control-plane kill -9 6294 (container 0c383b4a3cb4), landed: pod legion-slegion26v-1-architect-g1 Failed (worker exit 137); …)`; worker-cap: `… worker pods peaked at 2 with worker_cap 1 (an excess is idle lingering, allowed up to worker_idle_retire_seconds 60 + 30s; longest 0s), sampled every 5s` (no zero-running tick occurred on the real run; the harness pins one tick tolerated, two → `the worker queue held a task while no worker pod ran for 2 consecutive samples`); done: `CHECKPOINT done SKIPPED-BLOCKED: gh cannot list pull requests on sjawhar/legion-smoke (unauthenticated, rate-limited, or offline): needed to confirm the pull requests merged` in 0.18 s, and `… gh is not on PATH …` in 0.09 s, exit 3 both — no 7200 s wait; port-base rerun: `error: SMOKE_PORT_BASE is 31750 but this instance was started with 31700 and its container legion-smoke-legion26v-nats container legion-smoke-legion26v-postgres process listener process dispatch process port-forward process legion-177-keeper are still live; run scripts/kind-smoke/down.sh first (or rerun with SMOKE_PORT_BASE=31700)`, exit 1, every record byte-identical, nothing listening on 31750–31754 (the agreed behaviour: refused while live, re-derived only when nothing is); environments: listener exe=bin/envoy-listener, dispatch exe=bin/envoy-dispatch, port-forward and keeper exe=/usr/bin/bash running their own `bash -c` loops, parent systemd --user (no wrapper); none of GH_AGENT_APP_PRIVATE_KEY_B64/GH_REVIEW_APP_PRIVATE_KEY_B64/ANTHROPIC_API_KEY/GEMINI_API_KEY/OPENAI_API_KEY in any of the four; secret values present only as the process's own configuration — listener: ENVOY_API_TOKEN; dispatch: ENVOY_TOKEN, DATABASE_URL, DISPATCH_AGENT_TOKEN; port-forward and keeper: none; after down.sh none of the four recorded pids answers kill -0 and no process mentions the instance; CI: `- name: Test kind smoke bridge` / `run: bun test scripts/kind-smoke`; never26x: down.sh → `… has no instance record: this directory never started a kind smoke; stopping nothing, deleting nothing` exit 0, checkpoints.sh → `error: record port-base missing under …: run scripts/kind-smoke/up.sh first` exit 1, and the directory does not exist after either; foreign cluster record: `error: refusing to delete cluster 'legion-smoke-someone-else': the record does not name this instance's cluster legion-smoke-legion26w`, the cluster and kubeconfig left in place, the processes and containers of the instance torn down, exit 1; with the record restored the next down.sh deleted the cluster and ended KIND SMOKE DOWN exit 0.",
      "headSha": "ae359bc1356118026df0baf2fbdd8724c921c9ca",
      "negativeControl": "the old lib.sh/up.sh/down.sh at dcc86ae8 fail the new harnesses at up.test.sh:308 (App key names in the listener's environment) and down.test.sh:108 (the state tree created) — the implementer's proof [14], consistent with what the new code shows on the box"
    },
    {
      "criterion": "Round 4, the standard set (Acceptance 1–5) on fresh instances after the rig-script changes, and three Errors-table negatives",
      "surface": "legion26v (SMOKE_PORT_BASE=31700) and legion26w (31800, two roots, cap 1, idle retire 60), image ghcr.io/sjawhar/legion-worker@sha256:31c791054b7644f97a6762d94cd0def56bc29c528e780c031f7b1fac5459c9fb (cli-v1.18.0, still the newest cli-v* release), dev-box recipe, 08:03Z–08:15Z",
      "command": "up.sh; checkpoints.sh admitted architect-pod spec-posted tree-moved kill-pod-resume pod-hygiene done (kill right after tree-moved); on legion26w: up.sh, admitted, worker-cap; down.sh on both + kind get clusters / docker ps -a --filter label=legion-smoke.instance=<i> / tmux -L legion-smoke-<i> ls / grep -c legion-smoke ~/.kube/config; negatives: SMOKE_KILL_ROLE=tester, worker-cap on the 1-root instance, a tag, kind (and others) absent from PATH",
      "observed": "legion26v up.sh exit 0 in 1m18s, KIND SMOKE READY (controller: none, keeper pgid 7003); CHECKPOINT admitted OK: SLEGION26V-1 status=in_progress tree=active controller=none; architect-pod OK (pod legion-slegion26v-1-architect-g1 Running; claim legion-slegion26v-1 Bound and mounted); spec-posted OK (spec 5552af52-… posted; gate off, none registered; phase worker: planner); tree-moved OK (planner g1 Running; no child is a tree of its own); WORKAROUND LEGION-177 applied (credential.interactive was already unset) + kill-pod-resume OK: … g1 → g2 generation 1→2 (kill: docker exec … kill -9 6294 …, landed: pod … Failed (worker exit 137); …) session 01a0a418-0536-7000-91f6-19c2590ee913 unchanged; --resume=…/2026-09-15T08-03-52-758Z_01a0a418-0536-7000-91f6-19c2590ee913.jsonl; tree moved after the replacement registered — status changed: < in_progress > testing (daemon log: `pod legion-slegion26v-1-architect-g1 Failed` at 08:08:36 then the resurrection; 0 init failures, launchFailures 0); pod-hygiene OK: 3 pods checked …; done SKIPPED-BLOCKED: the run has no controller (…) exit 3. legion26w up.sh exit 0 in 1m18s (root issues SLEGION26W-1 SLEGION26W-2, worker cap 1); admitted OK; worker-cap OK: queue held SLEGION26W-1/planner while 1 worker pod(s) ran; promoted to pod legion-slegion26w-1-planner-g1; worker pods peaked at 2 with worker_cap 1 (… longest 0s), sampled every 5s (2m18s). down.sh both → KIND SMOKE DOWN exit 0; `No kind clusters found.`, no labelled container, tmux socket absent, `grep -c legion-smoke ~/.kube/config` 0 (mtime unchanged through the run), no host process of any legion26* instance, the docker ps diff against the pre-run baseline holds no legion-smoke container. Negatives: SMOKE_KILL_ROLE=tester → SKIPPED-BLOCKED exit 3; worker-cap on legion26v → `SKIPPED-BLOCKED: worker-cap needs a run started with SMOKE_ROOT_ISSUES=2 SMOKE_WORKER_CAP=1 (this run: SMOKE_ROOT_ISSUES=1 SMOKE_WORKER_CAP=6)` exit 3; a tag → `error: SMOKE_WORKER_IMAGE must be pinned by digest (…@sha256:<64 hex>); got ghcr.io/sjawhar/legion-worker:latest` exit 1; kind, bun, tmux, ss, openssl, shred, setsid, mise absent → `error: missing required tools: kind bun tmux ss openssl shred setsid mise — see docs/kubernetes.md, Runbook: the kind smoke, Prerequisites` exit 1, no state dir created.",
      "headSha": "ae359bc1356118026df0baf2fbdd8724c921c9ca",
      "negativeControl": "the four negatives above; plus the pause of Dispatch and the RBAC-denied exec recorded under blocking 2 and 3"
    }
  ],
  "observations": [
    "LEGION-182 (filed by this tester): a root crashed while a worker's phase-complete was in flight never learns of it — daemon log 05:25:48Z `legion-demo-slegion26t-1-architect: receipt_timeout for event … on a live process; the holder is slow, not gone — nothing re-sent` five seconds after `resurrecting SLEGION26T-1`; the resumed architect's transcript ends at 05:25:52Z with `catchup-overseer snapshot: no new completions — retro still running… Parked for the retro wake`; the tree sat at `retro` with every worker retired for the next 31 minutes. The rig reported it truthfully as `kill-pod-resume FAILED: the tree … has not moved`.",
    "The `architect shim connect for <KEY> failed (attempt k/6, cycle 1) … negotiate_protocol timed out after 5000ms` retry lines appeared after every root start on both instances, ending in `stopping architect shim connect … retries: the tree moved to generation 2` or the best-effort give-up — LEGION-39's documented ready-connect behaviour; /process/ready landed and the trees worked.",
    "With the newer image (cli-v1.18.0, #1108 merged) under SMOKE_SESSION_STORE=pvc nothing changed: probe line `daemon-api-version=5`, every checkpoint as on cli-v1.17.0.",
    "The checkpoints are lifecycle-sensitive: `architect-pod` asserts `in_progress` and `kill-pod-resume`'s mid-phase precondition (a worker holds a claim with a pod) is satisfied by idle finished workers too, so both are meaningful only when run right after `up.sh`/`tree-moved` as the runbook orders them. A one-sentence note in the README/runbook would spare an operator a 600 s wait and a confusing FAILED.",
    "jq is a shell builtin in this OMP pane (`type jq` → builtin), which is why an `env -i` negative-control PATH built from `command -v` lost it; the rig's own `command -v jq` check behaved correctly in both environments.",
    "Round 2: the surviving bare `!` lines (up.test.sh 57/168/180/269/334, checkpoints.test.sh 117) are all `! cmd || { …; exit 1; }` — the `||` guard makes them work; shellcheck does not flag them and they need no change.",
    "Round 2: `refute grep -Fq \"$(cat secret-file)\" …` (up.test.sh:375 and the `for s in …` loops) fails loudly if the secret file itself is missing: an empty pattern matches every line, so refute reports `expected failure, but succeeded` — misleading text, but not silent.",
    "Round 2: PR #1114 at 9fe181ff is MERGEABLE (mergeStateStatus BLOCKED = awaiting review), CI green at 21f288b7 (Tests 34935787660, PR Title 34935787711) and a ci-green wake arrived for 9fe181ff.",
    "Round 3: the handoff head 51dfa6ff's first Tests run (34937678477) failed only `real deployment instructions fragment (real tmux …) > hands the launched process one --append-system-prompt value` with `tmux window ownership marker failed (exit 1): server exited unexpectedly` in spawnController — the CI flake already filed as LEGION-174; the code head dcc86ae8 was green on the same suite and 51dfa6ff differs by .legion/implement.json alone. Re-run with --failed per the skill (never a push); the reviewer should read the rerun's verdict at the head.",
    "Round 3: residual, not a finding — `refute grep … <(tail …)` (three lines) cannot distinguish a missing $FAKE_LOG from no new lines, because the process substitution swallows tail's exit; these guard rerun idempotence and $FAKE_LOG always exists.",
    "Round 4: the transient-Dispatch check passes by design, but the poll budget is judged between attempts: with Dispatch hung, one `admitted` attempt takes curl's --max-time 20 s (plus daemon_state's own retries), so SMOKE_WAIT_ADMITTED=12 ran 152 s before its single FAILED. Wall-clock accuracy only; not a spec requirement.",
    "Round 4: the listener and Dispatch processes necessarily hold their own tokens in their environment (ENVOY_API_TOKEN; ENVOY_TOKEN, DATABASE_URL, DISPATCH_AGENT_TOKEN) — the Go binaries are configured by environment; the scrub removes what they do not need (both App PEMs, the provider keys), and the loops carry no secret at all.",
    "Round 4: ~/.kube/config on this box was rewritten to an empty `kind: Config` at 07:24:45Z by something other than this rig (the rig only ever passes --kubeconfig); its mtime did not change through my two instances.",
    "Round 4: SMOKE_KILL_ROLE=architect only, the LEGION-177 keeper/WORKAROUND line, LEGION-39 shim-connect lines, `done` SKIPPED-BLOCKED, and LEGION-182 are known, not findings."
  ],
  "documentationFeedback": "Runbook and README are accurate for a run in the prescribed order. Two wording additions (fast-follow): (1) say that the `for c in …` loop and `kill-pod-resume` are meant to run right after `up.sh` — `architect-pod` requires the root still `in_progress`, and a kill during `retro` lands on an idle tree; (2) add the `architect shim connect … negotiate_protocol timed out` daemon-log lines to the troubleshooting table as expected (LEGION-39), since an operator reading the daemon log during a run sees them on every root start. Round 2: both additions landed and read correctly; one wording fix — the `600 s` attached to the kill-pod-resume FAILED should be `SMOKE_WAIT_KILL_COMPLETE` (1800 s); 600 s is architect-pod's wait. Round 3: the numbers are now right in both places (1800 s SMOKE_WAIT_KILL_COMPLETE for the late-kill FAILED; 600 s SMOKE_WAIT_ARCHITECT_POD for architect-pod). Nothing further. Round 4: README's record table and the CI sentence (the bridge test runs in CI) now match the workflow; nothing further.",
  "rounds": [
    {
      "round": 1,
      "headSha": "090f950bf503a0e73d6d198dc2670ed4cfe29c8c",
      "passed": 6,
      "failed": 1,
      "verdict": "real surface passed on every criterion; one failure: the harnesses' bare `!` negatives were no-ops (fixed in round 2 at 21f288b7)",
      "failures": [
        {
          "criterion": "Acceptance 5/6 and the Testing table: the three shell harnesses (up.test.sh, down.test.sh, checkpoints.test.sh) as the regression lock for the shell logic — their negative assertions must be able to fail",
          "evidence": "Every bare `! grep …` / `! kill -0 …` line in the harnesses is a no-op under `set -euo pipefail`: bash exempts a `!`-inverted command from errexit (shellcheck SC2251 flags each: up.test.sh 164, 180, 186, 216, 227, 230, 238, 251, 265, 300, 313, 334, 339, 359, 379; down.test.sh 146, 147, 165, 178, 180; checkpoints.test.sh 269, 275, 331, 335). Proven three ways: `bash -euo pipefail -c 'echo canary >f; ! grep -Fq canary f; echo REACHED'` prints REACHED with exit 0; a copy of up.test.sh (in a fake repo root with the real deploy/ and packages/) with `echo \"docker run --token anthropic-canary-value\" >>\"$FAKE_LOG\"` planted immediately before line 180 still prints `refusals OK`, `host services OK`, `overlay and daemon OK`, `controller, bridge, root issues, summary OK`, `up.test.sh: OK`, exit 0; checkpoints.test.sh with `echo dispatch-secret-value-0123456789 >>\"$tmp/out.txt\"` planted before line 331 still prints `pod-hygiene OK` and `checkpoints.test.sh: OK`. The inert lines are exactly the ones that lock the security contract (a provider key never in an argv or stdout, the pod-hygiene FAILED output never carrying the token value, the kubeconfig never under ~/.kube/config, no tmux call on `controller: none`, down.sh's stopped processes actually dead). The guarded lines (`… || { echo …; exit 1; }` at up.test.sh 58/169/178/253/315 and checkpoints.test.sh 118) do work, and the live rig itself leaked nothing (sweep below), so this is an inert regression lock — a test that cannot fail hides an error — not a product leak. Fix in this PR: append `|| { echo '<what leaked>' >&2; exit 1; }` to each, or a `refute() { if \"$@\"; then echo \"refute: $*\" >&2; exit 1; fi; }` helper; the CI step `Test kind smoke harnesses` then guards what it claims to."
        }
      ]
    },
    {
      "round": 2,
      "headSha": "9fe181ffe8438d1b580cc0120f318de772136c26",
      "codeSha": "21f288b75be25941e4fe5151b6ce8cd48cc04a08",
      "surface": "harness only: lib.sh, up.sh, down.sh, checkpoints.sh, envoy-bridge.ts are byte-identical to 090f950b (sha256 compared per file; `jj diff --from 090f950b --to 21f288b7 --summary` lists only .legion/test.json, docs/kubernetes.md, scripts/kind-smoke/README.md, the three harnesses, and the new test-lib.sh) — no kind re-run",
      "passed": 9,
      "failed": 2,
      "verdict": "the round-1 finding is fixed (every planted leak now fails the harness naming file:line; SC2251 = 0; every refute has the right sense in the fixture); two small survivors of the same class remain, listed in failures (both fixed in round 3 at dcc86ae8)",
      "failures": [
        {
          "criterion": "Round 2, the corrective fix's own rule (`every A && B assertion chain split to one assertion per line`) and the round-1 criterion (harness assertions that can fail)",
          "evidence": "One `&&` assertion chain survived: scripts/kind-smoke/up.test.sh:174 `for name in listener dispatch; do [ -f \"$tmp/state/pids/$name.pid\" ] && [ -f \"$tmp/state/pids/$name.start\" ]; done` — the `.pid` test is the first operand and is exempt from errexit. Proven: `bash -Eeuo pipefail -c 'trap … ERR; for name in listener dispatch; do [ -f /tmp/l174/pids/$name.pid ] && [ -f /tmp/l174/pids/$name.start ]; done; echo REACHED'` with listener.pid missing and listener.start present prints REACHED, exit 0, no trap; with .start missing instead the trap fires and exit is 1. So a start_process regression that writes the .start record without the .pid record passes this line. Fix: two lines, `[ -f \"$tmp/state/pids/$name.pid\" ]` and `[ -f \"$tmp/state/pids/$name.start\" ]`. Audit method: grep of the three harnesses for `^\\s*!` (six survivors, all the working `! cmd || { …; exit 1; }` form: up.test.sh 57/168/180/269/334, checkpoints.test.sh 117), `set +e` (none), and every ` && `/` || ` line (this is the only assertion chain left; the rest are setup, fake-binary logic, `cmd || status=$?` runners, or `X || { …; exit 1; }` guards); no line-continued chains."
        },
        {
          "criterion": "Round 2 (d): a `refute` around a command that fails for an unrelated reason must not pass silently — the harness must still report something",
          "evidence": "`refute CMD…` (test-lib.sh) treats any non-zero exit of CMD as the expected negative, so `refute grep -Fq <secret> <file>` passes silently when <file> is missing (grep exits 2, not 1). Proven on a throwaway copy of the harnesses in a fake repo root: `rm -f \"$tmp/last.txt\"` planted right before up.test.sh:182 → the harness prints `grep: …/last.txt: No such file or directory` on stderr and then `host services OK` … `up.test.sh: OK`, exit 0; `rm -f \"$tmp/out.txt\"` before checkpoints.test.sh:336 → `grep: …/out.txt: No such file or directory`, `pod-hygiene OK`, `checkpoints.test.sh: OK`, exit 0. (An unreadable file — `chmod 000` — is caught only later by unrelated positive assertions: `FAIL up.test.sh:185: wc -l < \"$FAKE_LOG\"`, `FAIL checkpoints.test.sh:116: cat \"$tmp/out.txt\"`.) A future rename of `last.txt`/`out.txt`/`calls.log` in the runner but not in the refute lines would therefore disarm every secret-leak lock again without a failing harness. In today's fixture every guarded command exits exactly 1: an instrumented `refute` that additionally reports exit ≥ 2 printed nothing across all 30 refutes, so the fix is green immediately. Fix: in `refute`, capture the status (`local st=0; \"$@\" || st=$?`), fail on 0 as now, and also fail (or at least print FAIL) on st ≥ 2 for grep-shaped negatives — e.g. a `refute_grep` used by the 26 grep refutes — so a missing target is a harness failure, not a silent pass."
        }
      ]
    },
    {
      "round": 3,
      "headSha": "51dfa6ffbdf684426fa802c1fef6a478cb1a9196",
      "codeSha": "dcc86ae8b9eb",
      "surface": "harness only: `jj diff --from 21f288b7 --to dcc86ae8 --summary` and the GitHub compare list exactly .legion/implement.json, .legion/test.json, docs/kubernetes.md, scripts/kind-smoke/README.md, test-lib.sh, up.test.sh; lib.sh, up.sh, down.sh, checkpoints.sh, envoy-bridge.ts, down.test.sh, checkpoints.test.sh, envoy-bridge.test.ts sha256-identical to 21f288b7 — no kind re-run",
      "passed": 10,
      "failed": 0,
      "verdict": "both round-2 survivors fixed and proven; no exemption survives the audit; docs numbers correct. CI at the code head dcc86ae8 green (Tests 34937318672, PR Title 34937318549); the handoff head 51dfa6ff's first Tests run 34937678477 was red on the unrelated real-tmux flake LEGION-174 (real-deployment-instructions-e2e.test.ts, `tmux window ownership marker failed: server exited unexpectedly`, a file the PR does not touch) and was re-run with --failed per the skill, never a push"
    },
    {
      "round": 4,
      "headSha": "ae359bc1356118026df0baf2fbdd8724c921c9ca",
      "codeSha": "11113a3a584d",
      "surface": "FULL round (lib.sh, up.sh, down.sh, checkpoints.sh changed by review round 1): fresh instances legion26v (SMOKE_PORT_BASE=31700) and legion26w (31800, SMOKE_ROOT_ISSUES=2 SMOKE_WORKER_CAP=1 SMOKE_WORKER_IDLE_RETIRE=60), image cli-v1.18.0 sha256:31c79105… (still the newest cli-v* release), dev-box recipe; plus the harness surface on a throwaway copy",
      "passed": 22,
      "failed": 0,
      "verdict": "every blocking item and every fix-here item verified on the real surface and the harness; the standard checkpoint set OK; teardown clean; no finding"
    }
  ],
  "schemaVersion": 1,
  "phase": "test",
  "completed": "2026-09-15T08:16:45.981Z"
}