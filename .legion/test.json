{
  "verdict": "pass",
  "passed": 7,
  "failed": 0,
  "failures": [],
  "headSha": "0c6f3c18d45afd1367ef09147f38afe4d570076f",
  "codeCommit": "a3f40d856997 daemon: a failed list-panes proves nothing about the pane -- never alive, never gone",
  "testsCommit": "5f795f98f796 daemon tests: a fake gone pane must say so on stderr; waits on real events",
  "mergeBase": "0f744bc8811e (chore: release pi-envoy v1.20.0) -- #957, #993, #1002, #992 included; main has since moved to 419a0590 (#1001 + two releases) touching no file this branch touches",
  "previousHeadSha": "883cd3dfcff0564f4c469683e3e8816b20712f64",
  "pr": "https://github.com/sjawhar/legion/pull/981",
  "reviewRound": "https://github.com/sjawhar/legion/pull/981#pullrequestreview-5188762941 (CHANGES_REQUESTED at ccf037d5: B3, B4, rebase, four non-blocking)",
  "ci": "pr-checks-result at 0c6f3c18: lint/pr-title/test/typecheck SUCCESS across runs 34742308108, 34742341712, 34742420479; GitHub mergeable=MERGEABLE, mergeStateStatus=BLOCKED (review required) after its recompute (`legion gh -- pr view 981 --json mergeable,mergeStateStatus,statusCheckRollup`).",
  "blocking": {
    "B3": {
      "thread": "https://github.com/sjawhar/legion/pull/981#discussion_r3998125105",
      "code": "tmux.ts `lookupPane(server, paneId): {status:'present', pid} | {status:'absent'} | {status:'failed', detail}` -- `absent` only for a nonzero exit whose stderr matches the exported `PANE_GONE_STDERR` (can't find pane | no server running | error connecting ... (No such file or directory)) or a successful listing missing the row; any other nonzero exit or an unparseable pid is `failed` with argv+stderr in detail. runtime-tmux.ts `verifyPaneProcess` maps `failed` to `{verified:false, reason:'listing-failed', detail}`; `probe` THROWS `Error('cannot verify pane %N: <detail>')` on it (never alive, never gone; pane-gone -> gone, every other unverified -> not-recorded-process); `stop` order is graceful ask -> `if (refuseKill) return` -> verify -> verified: kill-pane (PANE_GONE_STDERR tolerated) / listing-failed: `throw new ProcessStopFailed(locator, detail)` / other unverified: return, logging only when it decided itself. Callers: `probeLocator` (probeTree / controllerAlive / resurrectDeadTree) rethrows -> resync `probeActiveRoots` Promise.all rejects -> `runResync` rejects -> index.ts logs `resync failed:` and reschedules (a probe effect is emitted only by resync, never by a durable reducer, so it is never DurableFatalFailure); `closeTreeLocked` probes INSIDE the try around its stop, so a throw is `anyFailed` -> tree left `lingering`, locator untouched, `StopFailed` (502); `retireUnconfirmedRoot` catches, logs, re-arms the registration deadline for the same generation; `WorkerBootWatchdog.probeAlive` catches, logs `liveness probe did not complete; re-arming`, returns true (no launchFailures increment; the registration deadline still bounds it). Worker stops (closeTreeLocked's worker loop, retireWorkerLocator via markWorkerDeadLocked/retireUnconfirmedBoot/launchWorker) take ProcessStopFailed -> StopFailed through the pre-existing failed-kill handling: claim and locator untouched.",
      "tests": [
        "tmux.test.ts > lookupPane: 'returns the target pane's own pid from its window's listing, over the private server'; 'reports a pane id missing from a successful listing as absent, never a sibling pane's pid'; it.each 'reports a nonzero exit whose stderr says the pane or server is not there as absent: %s' (3 PANE_GONE shapes); 'reports any other nonzero exit as a failed listing that proves nothing, naming the exit and stderr' (exit 1 + stderr; exit 143 empty stderr); 'reports a row whose pid is not a pid as a failed listing, never as absent'",
        "runtime.contract.test.ts > TmuxRuntime: 'stop throws ProcessStopFailed, killing nothing, when list-panes itself fails for a reason that does not prove the pane gone' (exit 1 empty stderr and 'server not responding'; commands exactly [list-panes]; pane still pid 12345; probe alive afterwards); it.each 'stop treats a list-panes failure whose stderr proves the pane gone as an already-gone pane: %s' (3 shapes; probe -> gone); 'probe throws, never alive and never gone, when list-panes fails for a reason that proves nothing'",
        "processes.test.ts: 'throws StopFailed and leaves the tree lingering with its locator intact when list-panes itself fails while closing, killing nothing' (first listing = probeTree alive, second = kill gate exit 1; StopFailed; inner error message `list-panes -t %0 exited 1`; status lingering; locator toEqual seeded clone; kill-pane []); 'probe rejects, clearing nothing and resurrecting nothing, when list-panes itself fails for a reason that proves nothing' (onProbe composition rejects with `cannot verify pane %0: list-panes -t %0 exited 1: tmux: server not responding`; tree active gen 1; locator unchanged; no new-window/kill-pane)",
        "worker-boot-watchdog.test.ts: 're-arms, never retires, a boot whose runtime probe could not complete -- even with its socket refusing -- and still honors the registration deadline' (2 probes, 1 retire only at the deadline, tail `probe -> retire` with no connect after the last probe)"
      ],
      "execution": "All named tests PASS (JUnit run: processes + runtime.contract + worker-boot-watchdog + tmux + legion-state + proc-stat = 355 pass / 3 gated-skip / 0 fail). The two ProcessManager B3 tests + the B4 test ran 20/20 isolated (the graceful-ask timing they depend on is deterministic). Repro against the ROUND-1 runtime (5a3e715c's runtime-tmux.ts/tmux.ts copied to /tmp and driven with the same fixture -- identity-bearing live locator, list-panes -> {exitCode:1, stderr:''}): round-1 `stop` RESOLVED with commands [list-panes] and no kill (the bug; the regression test's StopFailed/lingering/locator assertions would have failed there), round-1 `probe` returned {status:dead, reason:gone}; head `stop` THREW ProcessStopFailed 'list-panes -t %0 exited 1' with no kill, head `probe` threw 'cannot verify pane %0: ...'; a PANE_GONE stderr on head still resolves as gone. Real tmux (driver below, Part B): closeTree with the kill gate's listing faulted (exit 143, empty stderr) -> StopFailed aggregate, inner log `root process failed to stop while closing LEGION-42: StopFailed: list-panes -t %2 exited 143`, tree lingering, locator identical by reference, zero kill-pane, pane process alive; fault cleared -> closed, locator cleared, exactly `kill-pane -t %2`, process gone, stranger alive.",
      "result": "confirmed"
    },
    "B4": {
      "thread": "https://github.com/sjawhar/legion/pull/981#discussion_r3998125111",
      "code": "The `missing a pane id` throw is deleted from `TmuxRuntime.stop`; a locator without `tmuxPaneId` is `no-identity` in `verifyPaneProcess` (never touches tmux) so `probe` answers not-recorded-process (`pane @42 has no recorded process identity ...`) and `stop` sends the graceful ask over its socket, refuses the kill, returns normally -> `closeTreeLocked` clears the locator and closes the tree. The doc comment's two contradictory sentences are reconciled.",
      "tests": [
        "runtime.contract.test.ts > TmuxRuntime: 'stop on a locator without a pane id never throws: the graceful shutdown still goes out over its socket, the kill is refused as for any identity-less locator' (graceful confirmed: shutdowns 1, zero tmux commands, no log; with skipGraceful+refuseKill: zero commands, no log; skipGraceful alone: zero commands, one refusal log naming 'no recorded process identity'; pane %1 untouched)",
        "processes.test.ts: 'closes a tree whose root locator has no pane id: the graceful shutdown still goes out over its socket, nothing is killed, nothing throws' (shutdowns == ['/state/workers/architect.sock']; kill-pane []; kill-window []; status closed; locator undefined; exactly one `treating LEGION-42's root as dead: pane @42 has no recorded process identity ...` line; no 'failed to stop' line) -- replaces the round-1 test that pinned the throw"
      ],
      "execution": "Both PASS (JUnit; the ProcessManager one 20/20 isolated).",
      "result": "confirmed"
    }
  },
  "nonBlocking": [
    {
      "thread": "r3998125116 removeTreeProcess stop-then-clear",
      "confirmed": "processes.ts removeTreeProcess: `const locator = tree.locator; if (!locator) return; try { stopProcessSerialized(...) } catch { log } finally { if (tree.locator === locator) delete tree.locator; }` -- the root's window stays in recordedLocators() for the whole graceful wait; a newer generation's locator recorded mid-stop is never deleted. Covered by the FakeRuntime resurrect test and the real-tmux driver (Part A: resurrect with the recorded pane reissued; no kill-window/kill-pane)."
    },
    {
      "thread": "r3998125119 isProcessGoneError",
      "confirmed": "runtime-tmux.ts `isProcessGoneError(error)` = code ENOENT || ESRCH; the only catch in `readPaneStartTicks` and `isOmpPane`; everything else rethrows; the redundant `kill -0` in isOmpPane is gone (the old `expect(commands.some(c => c[0] === 'kill'))` assertion became a list-panes assertion). Throwaway execution (/tmp/legion27-test/r4/proc-error-repro.ts, not committed): EACCES/EIO/EMFILE from readProcessStat -> `probe` and `stop` both reject with that error (no kill); ENOENT/ESRCH -> not-recorded-process 'has no readable /proc stat'; EACCES from readProcessCmdline -> rejects; ENOENT cmdline -> not-omp. Round-1 runtime on EACCES stat returned not-recorded-process (read as a verdict) -- fixed on head. GAP (non-blocking, no test pins the propagation): recommend a runtime.contract.test.ts TmuxRuntime row `probe and stop propagate a /proc read error other than ENOENT/ESRCH instead of reading it as a verdict` (readProcessStat throwing {code:'EACCES'} -> probe rejects with it, stop rejects with it, no kill-pane), and its ENOENT/ESRCH sibling asserting stat-unreadable."
    },
    {
      "thread": "r3998125122 legion-state.ts comment",
      "confirmed": "legion-state.ts:307 now says a locator without identity is never confirmed by `TmuxRuntime.verifyPaneProcess`, its first probe reports it dead and it is resurrected with --resume; no 'backfillable' wording remains (grep: 0 hits)."
    },
    {
      "thread": "r3998125124 recordedLocators dedupe",
      "confirmed": "runtime-tmux.ts recordedLocators: `if (opened && !found.includes(opened)) found.push(opened);` (by reference); method doc now matches the field doc (kept until its pane fails to verify)."
    },
    {
      "item": "drainSpawns awaits resurrections",
      "confirmed": "processes.ts drainSpawns: `while (this.spawns.size > 0 || this.resurrecting.size > 0) await Promise.allSettled([...this.spawns, ...this.resurrecting.values()])`."
    },
    {
      "item": "windowCounter().reached(n) replacing tick-bounded windowCount waits",
      "confirmed": "processes.test.ts `windowCounter()` (count/increment/reached(n): a promise the fake `run` settles on the n-th new-window); every `flushEventLoopUntil(() => windowCount >= N, 20_000)` in the registration-deadline/controller tests is now `await windows.reached(N)` (+ `await processes.drainSpawns()` / a saveState resolver where the event is a persist); `expect(windowCount).toBe(N)` -> `expect(windows.count).toBe(N)` (value-identical assertions)."
    },
    {
      "item": "AGENTS.md + docs/solutions",
      "confirmed": "AGENTS.md tmux.ts row names lookupPane's present/absent/failed verdict and the PANE_GONE_STDERR contract; graceful-stop bullet gains 'a locator without a pane id is the same identity-less case, never a throw' (B4) and 'never at all when the pane could not be verified either way: a list-panes that fails for a reason that does not prove the pane gone ... makes TmuxRuntime.stop throw ProcessStopFailed ... and TmuxRuntime.probe throw ... so every probe caller ... logs and retries instead of clearing anything' (B3); the PANE_GONE_STDERR pointer moved to tmux.ts. docs/solutions/daemon/tmux-list-panes-target-is-its-window.md describes lookupPane (formerly panePid), the three-way verdict, and the runner-timeout `failed` case."
    }
  ],
  "rebaseAudit": {
    "mergeBase": "abaf0817 (#973 idle-worker retirement, #980 launch hold / per-claim reconnect isolation / workspace clone, #983 pin bump all included)",
    "idleRetire973": "`idleWorkerFixture` (processes.test.ts:154) seeds `...paneIdentity()` with the comment that a 'no kill-pane' assertion must mean the graceful stop confirmed, not that an identity-less kill was refused; its 16 idle-retire tests (e.g. 'retires a confirmed idle worker past the idle window when phases[issue] is absent: one graceful stop, locator cleared, claim kept with its session file, no failure counted, no worker-died'; 'resumes a retired worker with --resume on its next spawn_worker') PASS in the JUnit run; `retireIdleWorker -> retireWorkerLocator -> stopProcess -> Runtime.stop` is identity-gated by construction.",
    "launchHold980": "index.test.ts:440 'binds the API and loads state while the OMP probe is still timing out, launches nothing during the hold, and promotes the queued root and worker once it passes' fakes `list-panes -> %1 4242` (the pid `fakeProcStat` reports) and `readProcessCmdline: async () => 'omp\\0'`, with comments explaining that the worker's split into the root's window is the window-reuse-by-identity contract; every `startDaemon` gets `readProcessStat: fakeProcStat`. #980's reconnect-isolation test carries `...paneIdentity()` + livePanes; its held-recoveries test uses `paneGone()`.",
    "fixtureSweep": "`paneGone()` = {stdout:'', stderr:\"can't find pane\", exitCode:1} replaces every bare `{stdout:'', exitCode:1}` list-panes fake that meant 'gone' (33 sites in processes.test.ts + the three LEGION_TMUX_LIVE fixtures now returning stdout/stderr/exitCode separately) -- required because a bare exit 1 is now a listing failure the runtime refuses to act on. Removed `expect` lines in 7eb31188..246f5e7c: `destroyed`/`strangers.size` assertions on FakeRuntime cases (per the reviewer's fast-follow: decided by the fake's own gate; the `options` equality that discriminates ProcessManager's decision is kept), `expect(commands.some(c => c[0] === 'kill'))` (kill -0 removed; replaced by a list-panes assertion), the round-1 'missing a pane id' pins (B4 contract inverted by design), `windowCount` -> `windows.count`, and `panePid` -> `lookupPane` rewraps. No kill-pane-ABSENCE, locator-clearing, or --resume assertion was weakened: every identity test at this head still asserts them (enumerated in acceptance below), and the B3 tests add `locator toEqual seeded`/`=== before` and `status lingering`.",
    "flakeRemediation": "The park-then-re-admit secret-file test (#980's) lost only `sleep: async () => {}` and gained a comment: with an instant sleep, generation 2's root registration deadline fired the moment its locator landed, `retireUnconfirmedRoot` killed `%2`, cleared the locator, and `persist()`'s prune removed the very secret file the assertions read -- a real race between the deadline and the test's fs reads, fixed by leaving the deadline a real (long) timer that `dispose()` cancels. Its assertions are unchanged (the hunk touches one dep line and a comment)."
  },
  "acceptance": [
    {
      "line": 1,
      "criterion": "A locator carries a process identity beyond the pane id; a probe against a pane whose identity does not match reports dead, never alive.",
      "tests": [
        "runtime.contract.test.ts > TmuxRuntime: 'probe reports a pane whose process is not the recorded one with both identities, and a gone pane as gone'; 'probe never trusts a locator without a recorded identity, and never asks tmux about it'; 'sameProcess treats a reissued pane -- same id, other pid or start ticks -- as a different process'; describe.each(FakeRuntime, TmuxRuntime) 'probe distinguishes a process that is gone from a handle another process now occupies, and stop never destroys that other process'",
        "processes.test.ts: it.each 'probe reports a root dead when its recorded pane now runs another process (%s), and resurrecting it resumes the session without killing that pane' (3 cases); 'treats a locator recorded before identity tracking as dead on its first probe ...'",
        "legion-state.test.ts: 'round-trips a tmux locator's recorded process identity on a tree, a worker claim, and the controller'; 'migrates v24 state to v25 as a pure version bump, keeping identity-less tmux locators'"
      ],
      "command": "cd -- \"$LEGION_WORKSPACE\" && bun test packages/daemon/src/daemon/__tests__/{processes,runtime.contract,worker-boot-watchdog,tmux,legion-state,proc-stat}.test.ts --reporter=junit --reporter-outfile=/tmp/legion27-test/r4/junit.xml",
      "output": "355 pass, 3 skip (LEGION_TMUX_LIVE-gated), 0 fail; Ran 358 tests across 6 files. Every named test PASS.",
      "result": "pass"
    },
    {
      "line": 2,
      "criterion": "reconnectWorkers/reconnectRoots treat a mismatched identity as dead and clear the locator so the claim stops counting toward worker_cap; only the stale one of two claims sharing a pane id is cleared.",
      "tests": [
        "processes.test.ts: 'reconnectWorkers clears a stale unconfirmed claim whose pane id was reissued to a live sibling -- without killing that pane -- and the live claim's re-armed watchdog verifies its pane at the next interval'; 'reconnectRoots' re-armed deadline treats a restart-surviving locator whose pane id was reissued as dead: resumes the root once, never killing that pane'; 'resurrects a root once, without killing the pane, when its registration deadline elapses on a pane id since reissued to another OMP process'; FakeRuntime 'retires an unconfirmed worker boot at the first watchdog interval once its handle belongs to another process and its socket refuses, relaunching the same role'"
      ],
      "command": "same JUnit run",
      "output": "All PASS.",
      "result": "pass"
    },
    {
      "line": 3,
      "criterion": "A daemon whose tmux server was recreated under it resurrects every root with --resume and never adopts another role's pane.",
      "tests": [
        "processes.test.ts: 'resurrects every located root with --resume, killing nothing, when the private tmux server was recreated under the daemon and reissued its pane ids'; FakeRuntime 'resurrects a root whose handle now belongs to another process: asks the recorded process to exit with the destroy step refused, resumes onto a fresh process, and logs the decision once'; LEGION_TMUX_LIVE=1 'probes a real tmux pane as alive, detects its death, and resurrects it once' (real /proc)"
      ],
      "command": "LEGION_E2E=1 LEGION_TMUX_LIVE=1 bun test (packages/daemon); bun /tmp/legion27-test/r4/runtime-recreate-driver.ts",
      "output": "Gated tests pass inside the 1032/0 package run. Driver (REAL ProcessManager over the REAL TmuxRuntime at 246f5e7c, real /proc, OMP swapped for `sleep 999`, throwaway `tmux -L legion-smoke71941`; the deployment server legion-sjawharlegion never targeted): spawnRoot -> @1/%1 pid 3639509 start 164064973 == real stat field 22; probe alive; `kill-server`; another role opened -> @1/%1 pid 3641291 (tmux.lookupPane('%1') = present/3641291: a pane-id-only probe would have said alive); onProbe composition -> probe dead; EXACTLY ONE log line `treating LEGION-42's root as dead: pane %1 now runs pid 3641291 (recorded pid 3639509 start 164064973)`; zero kill-pane; new-window carries --resume=<session file>; fresh @2/%2 with fresh identity; generation +1; the other role's pane listed and `kill -0` alive; probe alive on the fresh pane. Then Part B (B3 fault) as recorded above. RESULT: PASS; server killed after (`ls` -> no server running). Smoke-rig artifact assessed separately below.",
      "result": "pass"
    },
    {
      "line": 4,
      "criterion": "Daemon AGENTS.md operational invariant gains 'and is the process this locator recorded'.",
      "tests": [],
      "command": "read packages/daemon/src/daemon/AGENTS.md l.50 (trust invariant), l.52 (graceful-stop bullet incl. B3/B4 clauses), tmux.ts row, phase-worker bullet, watchdog bullet against runtime-tmux.ts/tmux.ts/processes.ts/worker-boot-watchdog.ts at this head",
      "output": "All read true: verifyPaneProcess order pid -> ticks -> OMP; probe's gone / not-recorded-process / throw mapping; stop's ask -> refuseKill -> verify -> kill|throw|refuse; lookupPane's three-way verdict with PANE_GONE_STDERR; probedWindowId never rewriting a window id; watchdog re-arming on an incomplete probe.",
      "result": "pass"
    },
    {
      "line": 5,
      "criterion": "No daemon-issued kill-pane ever lands on a pane whose process is not the one the locator recorded.",
      "tests": [
        "runtime.contract.test.ts > TmuxRuntime: 'stop never kills a pane whose process is not the one recorded, logging both identities once, and stays silent when the caller already decided'; 'stop on a legacy locator without identity sends the graceful shutdown over its socket and never kills'; 'stop on a locator without a pane id never throws ...' (B4); 'stop throws ProcessStopFailed, killing nothing, when list-panes itself fails ...' (B3); 'stop tolerates a pane that is already gone and surfaces every other kill-pane failure'; describe.each both runtimes 'stop with refuseKill asks the process to exit but never destroys it'",
        "processes.test.ts: 'never kills a pane whose process is not the one the locator recorded on tree close ...'; 'never kills a controller pane whose id was reissued ... when its recorded process is unreachable ...' + graceful sibling; FakeRuntime 'closes a tree whose root's handle now belongs to another process ...' (options {skipGraceful:false, refuseKill:true}) and 'replaces a controller whose handle now belongs to another process ...' ({refuseKill:true}); B3 'throws StopFailed and leaves the tree lingering ... killing nothing'",
        "real-shutdown-e2e.test.ts (LEGION_E2E=1): 'never kills a real pane whose recorded identity is not the process running in it ...' (start ticks off by one) beside its positive control 'kills a real pane wrapping a real worker-shim after its stop timeout ...'"
      ],
      "command": "same JUnit run; LEGION_E2E=1 LEGION_TMUX_LIVE=1 bun test",
      "output": "All PASS; e2e file green inside the 1032/0 run.",
      "result": "pass"
    },
    {
      "line": 6,
      "criterion": "A recorded window id is reused for a new worker pane only when a recorded pane in that window verifies as its recorded process.",
      "tests": [
        "runtime.contract.test.ts > TmuxRuntime: 'never splits into a recorded window whose pane now runs another process, even when the window is live'; 'splits a second process for the same issue into its recorded window once that window's pane verifies'; 'opens a fresh window when no recorded pane verifies, leaves every recorded locator's window alone, and splits later spawns into the fresh one'; 'a concurrent second spawn on an issue splits into the window the first one is opening'",
        "processes.test.ts: 'opens a fresh window for a new worker when the issue's recorded window is live but none of its recorded panes is still the process its locator recorded' + control 'splits a new worker into the issue's recorded window when a recorded pane in it still verifies ...'; 'opens a fresh window when every recorded pane is gone, leaves the stale locators naming their dead window, and splits later workers into the fresh window'; B1 legacyLiveTreeFixture pair"
      ],
      "command": "same JUnit run",
      "output": "All PASS.",
      "result": "pass"
    },
    {
      "line": 7,
      "criterion": "The worker boot watchdog never re-arms on a reissued pane; an unconfirmed boot whose pane id now belongs to another OMP process is retired at the interval (socket refused).",
      "tests": [
        "worker-boot-watchdog.test.ts: 'retires a boot only after the runtime reports it dead and its socket refuses a connection' (the byte-identical not-recorded-process row was folded into it per the reviewer's fast-follow); 're-arms, never retires, a boot whose runtime probe could not complete ...' (B3)",
        "processes.test.ts: 'retires an unconfirmed boot at the first watchdog interval -- never re-arming -- once its pane id has been reissued to another OMP process and its socket refuses' (TmuxRuntime end-to-end: reissued %1 777 vs recorded 12345 -> retired within the first interval, locator cleared, launchFailures 1, queued, no kill-pane -t %1); FakeRuntime 'retires an unconfirmed worker boot ... relaunching the same role'",
        "proc-stat.test.ts: field-22 parser incl. comm with spaces/parentheses and this process's own /proc/self/stat"
      ],
      "command": "same JUnit run",
      "output": "All PASS.",
      "result": "pass"
    }
  ],
  "packageChecks": {
    "command": "cd packages/daemon && bunx biome check --error-on-warnings src/ && bunx tsc --noEmit && LEGION_E2E=1 LEGION_TMUX_LIVE=1 bun test",
    "biome": "Checked 149 files in 666ms. No fixes applied. exit 0",
    "tsc": "exit 0 (no output)",
    "bunTestCiEnv": "LEGION_E2E=1 LEGION_TMUX_LIVE=1 bun test -> 1032 pass, 0 fail, 0 skip; Ran 1032 tests across 58 files. [65.69s] (matches the implementer's 1032/1032)",
    "flakes": "Neither LEGION-15/LEGION-35 flake fired; no rerun needed."
  },
  "smokeRigArtifact": {
    "source": "dispatch://LEGION-27/artifact/smoke-rig-evidence.md",
    "consistency": "Internally consistent. S2 baseline (v24: controller %1 pid 585212 start 163587985; root %2 pid 679088 start 163600729, no panePid) -> v25 + .v24.bak; first tick logs `pane %2 has no recorded process identity` and resumes the SAME session file into gen 2 @3/%3 pid 800726 start 163616468 -- exactly the locator S1-cycle-1's 'before' shows. Cycle 1: strangers %0-%3 pids 898645/898829/898935/899077; the root decision line names `pane %3 now runs pid 899077 (recorded pid 800726 start 163616468)` = stranger3 exactly; controller (still legacy %1) `no recorded process identity`; result root gen 3 @4/%4 902351/163631737, controller @5/%5 902434/163631750. Cycle 2 'before' matches those; strangers %0-%5 965739..966750; decision lines `%4 now runs pid 966458 (recorded 902351 start 163631737)` = stranger4 and `%5 now runs pid 966750 (recorded 902434 start 163631750)` = stranger5; result gen 4 @6/%6 971579/163641095, controller @7/%7 971682/163641131. Every `resurrecting ... resuming OMP session .../01a098cf-b3b5-7000-8bed-38902cf55045.jsonl` line names the same session file, so --resume of one session across generations 2 -> 3 -> 4 holds; start ticks increase monotonically with pid order across the whole exercise; the full decision-log dump contains exactly the three root and two controller decision lines the narrative cites and no `not killing pane` line, consistent with the caller-decided `refuseKill` contract.",
    "strangersAlive": "Proven for S1 (both cycles): stranger pids are listed as alive after each transition, and the daemon issues no kill on a refuseKill path by construction.",
    "notProvenHonest": "Yes. The 'Not proven here' section correctly limits the S2 claim: the daemon logs no kill-pane command, so 'no kill-pane was issued for %2' is by construction of the refuseKill path (pinned by the contract tests), and the pane's exit ~2 min later is attributed to the graceful ask without an independent observer -- stated as such. It also separates that from the S1 stranger-liveness claims, which are observed.",
    "caveats": "(1) The rig ran the branch's daemon 'when the branch sat on main 4d516a1f' and the artifact says the later rebase onto abaf0817 changed no src/daemon file; jj's evolog shows the branch was rebased more than once between 03:29Z and the 04:01Z code-commit split, so that equivalence is the implementer's word -- the independent proof for the pushed head is this round's unit/contract suites (1032/0) and my real-tmux driver at 246f5e7c. (2) The artifact's state excerpts are hand-trimmed JSON (the S1 excerpts omit `readyConfirmedAt`/status), fine for the identity fields they cite. (3) `failed to connect architect shim socket on ready ... negotiate_protocol timed out` after each resurrection is noted as pre-existing best-effort behaviour under load ~200; not identity-related. (4) The two rig adaptations (NATS container name / machine id parameterization, Dispatch event relay) were made outside the PR in a throwaway main checkout and are named for the architect -- appropriate scoping."
  },
  "observations": [
    "Round 3 at 0c6f3c18: see round3_0c6f3c18 (verdict pass; one non-blocking test gap on handleException). Earlier sections carried forward for what round 3 did not touch.",
    "Test-quality gap (non-blocking): the r3998125119 fix -- a non-ENOENT/ESRCH /proc read error propagates from probe/stop instead of reading as gone -- is verified here by code and a throwaway run, but no committed test pins it; exact test proposed under nonBlocking[1].",
    "worker-boot-watchdog.test.ts 're-arms, never retires, a boot whose runtime probe could not complete ...' counts `liveness probe did not complete` log lines (2) beside its discriminating event-sequence assertions; the wording count is redundant with `probe` x2 and could be dropped without loss.",
    "A resync tick in which one tree's probe throws rejects `runResync` as a whole (`Promise.all` over the probe effects): the other trees' probes already in flight still complete, but that tick's resync payload is not published to the controller and `reportRootAnomalies` for that tick is lost until the next interval. Acceptable for a transient tmux failure (the daemon logs `resync failed:` and reschedules); noted for the reviewer.",
    "Throwaway drivers (/tmp/legion27-test/r4/*.ts, a /tmp copy of 5a3e715c's runtime-tmux.ts for the B3 repro) not committed; throwaway servers legion-smoke54884 and legion-smoke71941 killed (`ls` -> no server running); deployment server legion-sjawharlegion untouched; `.omp/config.yml` left as provisioned."
  ],
  "documentationFeedback": "AGENTS.md (tmux.ts row, trust invariant, graceful-stop bullet with the B3/B4 clauses, watchdog bullet) and docs/solutions/daemon/tmux-list-panes-target-is-its-window.md read true against the code at this head.",
  "delta_883cd3df": {
    "scope": "Narrow re-verification after the rebase onto main 195674ab. The PASS at 246f5e7c stands for everything the rebase did not touch; the six items below are what it did touch.",
    "verdict": "pass",
    "items": [
      {
        "item": 1,
        "check": "no change under packages/daemon/src outside __tests__ and AGENTS.md",
        "method": "Compared the branch's delta against its base before the rebase (`jj diff --git -r abaf0817..2219c53f <file>`) with its delta against the new base after (`jj diff --git -r 195674ab..883cd3df <file>`), hunk headers/index lines stripped, per src file.",
        "result": "Identical file sets (proc-stat.ts added; index.ts, legion-state.ts, processes.ts, runtime-tmux.ts, runtime.ts, tmux.ts, worker-boot-watchdog.ts, AGENTS.md modified). The diff-of-diffs is byte-identical for all 8 src files -- context lines included, so the rebase neither changed a branch line nor landed a branch hunk on different surrounding code. Zero source conflicts confirmed.",
        "status": "confirmed"
      },
      {
        "item": 2,
        "check": "AGENTS.md conflict resolution",
        "method": "grep for conflict markers; sorted line-set diff of head vs main's AGENTS.md; sentence-level md5 of the bullets main changed since the old base; `aborted: true` sentence read against packages/daemon/src/state/fetch.ts.",
        "result": "0 conflict markers. Lines in head not in main = exactly this branch's 7 identity lines (runtime-tmux.ts row, tmux.ts row with lookupPane's three-way verdict, trust invariant, graceful-stop bullet incl. the B3 listing-failed and B4 pane-id-less clauses, phase-worker bullet, watchdog bullet, PANE_GONE_STDERR pointer moved to tmux.ts); lines in main not in head = exactly their 7 predecessors. Main's #995 DaemonConfig bullet (`... comes back with timedOut: {limitMs, elapsedMs} (exit 143) ...; one it killed because the caller's signal aborted comes back with aborted: true instead ... At most one of the two is present, the first kill to land deciding which.`), the Workspace-clone bullet, and the deployment-instructions.ts row are byte-identical to main (md5 3d4189f0 / 8f109d13 / 0e50f59d). The `aborted: true` sentence reads true against fetch.ts (`aborted?: true` set on a signal abort, `timedOut` on the budget kill, doc: at most one present). The graceful-stop bullet reads true against runtime-tmux.ts stop/probe at this head (src unchanged since 246f5e7c, see item 1).",
        "status": "confirmed"
      },
      {
        "item": 3,
        "check": "f20c3072: #956's real-deployment-instructions e2e builds TmuxRuntime without the deleted `persist` dep",
        "method": "Read the one-line diff; read main's runtime-tmux.ts for every `persist` use; read the test's assertions; ran it under LEGION_E2E=1.",
        "result": "The commit deletes only `persist: async () => {},` from the TmuxRuntimeDeps literal. On main, `TmuxRuntimeDeps.persist` existed solely for the pane-id backfill (`runtime-tmux.ts:262` in the probe), which this branch removes; the e2e test supplied a no-op and asserted nothing through it -- its assertions are the controller's argv (`--mode rpc`, the two `--append-system-prompt` fragments in order, the deployment-instructions fragment last) and the controller locator shape. No asserted behaviour was dropped. `LEGION_E2E=1 bun test src/daemon/__tests__/real-deployment-instructions-e2e.test.ts` -> 1 pass / 0 fail (real tmux + real worker-shim + argv-recorder OMP).",
        "status": "confirmed"
      },
      {
        "item": 4,
        "check": "80c9cfb1: the isProcessGoneError contract rows are discriminating",
        "method": "Read the two it.each rows; ran them on the real tree; ran them against a MUTANT copy of runtime-tmux.ts (isProcessGoneError widened to `return true`) in /tmp with the workspace untouched.",
        "result": "Rows: `probe and stop propagate a %s from the /proc stat read instead of reading it as a verdict, and stop issues no kill` over EACCES/EIO/EMFILE (asserts `rejects.toBe(fault)` -- the very error object -- for probe AND stop, tmux commands exactly two `verifyArgv('%1')` listings and no kill-pane, pane pid unchanged, and the same locator probes alive again once the fault clears) and `probe reads a %s from the /proc stat read as the recorded process being gone, with both identities` over ENOENT/ESRCH (not-recorded-process with `pid 12345.*recorded pid 12345 start <ticks>`). Real tree: 5 pass / 0 fail. Mutant (catch widened): the three EACCES/EIO/EMFILE rows FAIL with `Expected promise that rejects / Received promise that resolved`; the two ENOENT/ESRCH rows still pass -- the row fails exactly when the catch is widened and only then. Mutant directory removed; `jj status` shows the workspace untouched (only the provisioned .omp/config.yml).",
        "status": "confirmed"
      },
      {
        "item": 5,
        "check": "#956's other new fixtures pass unchanged under the identity contract",
        "method": "Listed #956's (0c0bdd8e) test additions; diff-of-diffs of every branch-touched test file across the rebase; read the #956 tests' fixtures and assertions.",
        "result": "#956 added `appends the deployment instructions file as the last --append-system-prompt for root, worker, and controller windows when configured` (processes.test.ts) and `refuses to boot on an unreadable instructions path ...` + `materializes the instructions file under state_dir at boot ...` (index.test.ts), plus config/deployment-instructions/cli tests and the e2e. The branch's delta to processes.test.ts and index.test.ts is byte-identical across the rebase (so the #956 tests arrived verbatim from main and were not edited); runtime.contract.test.ts differs only by 80c9cfb1's added rows. The processes.test.ts one asserts only launch command strings (its fake `list-panes` answers exit 0 / empty stdout, which lookupPane reads as `absent`, so the worker opens a fresh window -- the test does not assert split vs new-window, and passes either way); the two index tests go through `daemonTestDependencies`, which supplies `readProcessStat: fakeProcStat` (index.test.ts:205). None of them carry a kill-pane-absence, locator-clearing, or --resume assertion, so none could have been weakened; all pass in the package run.",
        "status": "confirmed"
      },
      {
        "item": 6,
        "check": "package checks and GitHub state",
        "result": "cd packages/daemon && bunx biome check --error-on-warnings src/ -> Checked 154 files, no fixes, exit 0; bunx tsc --noEmit -> exit 0; LEGION_E2E=1 LEGION_TMUX_LIVE=1 bun test -> 1051 pass / 0 fail / 0 skip, Ran 1051 tests across 61 files [63.02s] (matches the implementer's 1051/1051, 61 files). No LEGION-15/35 flake fired. GitHub at 883cd3df: mergeable=MERGEABLE, mergeStateStatus=BLOCKED (awaiting review), lint/pr-title/test/typecheck SUCCESS on runs 34739339255 and 34739406529.",
        "status": "confirmed"
      }
    ],
    "observations": [
      "The two #956 index tests reach identity only through the shared `daemonTestDependencies` default (`readProcessStat: fakeProcStat`); fine as long as that default stays, noted for anyone later building daemon deps by hand in that file.",
      "No throwaway server was opened this round (nothing tmux-facing changed); the LEGION_TMUX_LIVE/LEGION_E2E suites ran on their own sockets inside the package run. The mutant copy under /tmp/legion27-test/r5/mutant was removed."
    ]
  },
  "round3_0c6f3c18": {
    "review": "https://github.com/sjawhar/legion/pull/981#pullrequestreview-5189671202 (CHANGES_REQUESTED at 8d5855cf: B5 + two land-with items + fast-follow list)",
    "scope": "The PASS at 246f5e7c/883cd3df stands for everything round 3 did not touch; the round-3 delta (34907851..0c6f3c18: processes.ts +96/-, runtime-tmux.ts, worker-boot-watchdog.ts, AGENTS.md, four test files, one docs/solutions note) and the three in-round rebases are verified below.",
    "verdict": "pass",
    "findings": [
      {
        "severity": "non-blocking gap",
        "item": 2,
        "finding": "No committed test covers the round-3 `handleException` catch (commit 02fd8a8d, thread r3998846683). The implementer's reply names the fix and the doc changes but no test; grep of every __tests__ file for `failed to recover` / `has no redelivery` is empty, and none of the ten existing `handleException` call sites in processes.test.ts drives a probe that throws. The code is correct (verified by a throwaway, below) and a test asserting it discriminates (a mutant with the catch removed rejects instead of resolving).",
        "testThatShouldExist": "processes.test.ts: `handleException logs once, naming the role token, and clears nothing when the root architect's liveness probe cannot complete` -- seed an active, ready-confirmed root with an identity-bearing locator; fake `list-panes` -> {exitCode:1, stderr:'tmux: server not responding'}; `await processes.handleException(exception(roleToken('omp', root, 'architect')))` must RESOLVE (not reject); exactly one console.error line containing the role token and the probe's `cannot verify pane %0: list-panes -t %0 exited 1: tmux: server not responding`; `state.trees[root].locator` identical by reference; no `new-window`, no `kill-pane`; generation unchanged. A sibling for the controller branch (`controllerLocator` seeded, `ensureController` path) with the same shape.",
        "evidence": "Throwaway /tmp/legion27-test/r6/handle-exception-probe.ts (not committed) against a copy of the head: handleException RESOLVED, 1 console.error `[legion] failed to recover legion-omp-legion-42-architect after a delivery exception; nothing was cleared, and this lane has no redelivery -- ... : Error: cannot verify pane %0: list-panes ...`, locator same object, new-window 0, kill-pane 0. Same script against a mutant with both `logFailure(error)` sites replaced by `throw error`: REJECTED `cannot verify pane %0: ...`, 0 log lines -- the proposed test fails there."
      }
    ],
    "items": [
      {
        "item": 1,
        "check": "B5 (r3998846681), aea534f2: escalateOrRetryUnconfirmedRoot wraps `await onRetry()`",
        "code": "processes.ts escalateOrRetryUnconfirmedRoot: `try { await onRetry(); } catch (error) { console.error('[legion] failed to resurrect an unconfirmed root for <tree>; re-arming its registration deadline rather than leaving it unwatched:', error); if (this.treeStillUnconfirmed(this.deps.state.trees[treeKey], generation)) this.armRootRegistrationDeadline(treeKey, generation); }` -- logged once there (the timer's .catch never sees it); a spawn failure that queued/launch-failed the tree, or a newer generation, makes treeStillUnconfirmed decline the re-arm.",
        "test": "processes.test.ts 're-arms the same generation's registration deadline when the resurrection's own re-probe cannot complete, leaving the locator intact, and resurrects normally once it can' -- PASS. Its second arm is `await secondArm.promise` (resolved by the 2nd injected sleep); without the wrap no 2nd sleep ever happens, so the test hangs there.",
        "preFix": "Mutant (/tmp copy of the head, try/catch replaced by bare `await onRetry()` = the pre-aea534f2 shape): the test FAILS on bun's 5 s timeout at that await (`[5020 ms]`); restored copy: PASS.",
        "realTmux": "Throwaway /tmp/legion27-test/r6/b5-real-tmux-driver.ts (not committed) on `tmux -L legion-smoke17837` (killed after; deployment server never targeted): REAL ProcessManager + REAL TmuxRuntime, injected sleep gates the deadlines. spawnRoot gen 1 -> `%1` pid 3791728 recorded, deadline armed (sleep #1). Operator `kill-pane %1` out of band (real tmux; the session ends with its only pane). Deadline 1 fires: the deadline's probe sees the pane gone through real tmux (`can't find pane`/no server -> gone), launchFailures -> 1, resurrect starts, and the resurrection's re-probe `list-panes` is faulted (exit 1, `tmux: server not responding`). Observed: exactly ONE log line for the root -- `failed to resurrect an unconfirmed root for LEGION-42; re-arming its registration deadline rather than leaving it unwatched: Error: cannot verify pane %1: list-panes -t %1 exited 1: tmux: server not responding`; locator identical by reference; generation 1; launchFailures 1; status active, unconfirmed; zero kill-pane; zero new-window; sleep #2 armed (same generation). Fault cleared, deadline 2 fires: generation 2, launchFailures 2, `new-window` carries `--resume=<session file>`, fresh identity (pid 3792977 vs 3791728; the recreated server reissued `%1` -- the very shape this PR exists for), zero kill-pane, sleep #3 (gen 2's own deadline), probe alive on the fresh pane. RESULT: PASS.",
        "status": "confirmed"
      },
      {
        "item": 2,
        "check": "r3998846683, 02fd8a8d: handleException catches and logs a probe/recovery throw naming the role token; docs read true",
        "code": "processes.ts handleException: `logFailure = (error) => console.error('[legion] failed to recover <roleToken> after a delivery exception; nothing was cleared, and this lane has no redelivery -- the resync backstop, the registration deadline, or the next exception retries:', error)`; the controller branch wraps `ensureController()`, the architect branch wraps `probe -> controlDirective | resurrect`; both clear nothing.",
        "docsQuoted": [
          "probeLocator: `... every caller's own failure handling logs and retries later without clearing anything: the resync onProbe on its next tick, a registration deadline by re-arming itself, the boot watchdog by re-arming its interval, the linger sweep on its next pass. handleException only logs -- the core-NATS exception lane has no redelivery -- and leaves the retry to those.` -- TRUE (matches index.ts resync catch, escalateOrRetryUnconfirmedRoot/retireUnconfirmedRoot re-arms, watchdog probeAlive catch, linger sweep's own catch, handleException's logFailure).",
          "probeTree: `This is the decision point and the only place that logs it: the resurrection that follows (resurrectDeadTree) re-probes through the silent probeLocator -- so a root that came back alive between the two is never replaced -- and hands that fresh verdict to its stop, which decides nothing again.` -- TRUE (resurrectDeadTree probes via probeLocator, returns on alive, passes the verdict to removeTreeProcess).",
          "resurrectDeadTree: `The probe is taken afresh here -- a caller's earlier verdict may be stale by now ... but silently ... A probe that cannot complete throws through to the caller, which re-arms or retries (escalateOrRetryUnconfirmedRoot, the resync tick, handleException's log) without touching the tree.` -- TRUE.",
          "AGENTS.md process-failure-recovery bullet: `... every probe caller logs and retries later without clearing anything — the resync onProbe on its next tick, the boot watchdog by re-arming its interval, the controller and root registration deadlines by re-arming themselves (including when it is the resurrection's own re-probe or stop that throws, after the deadline's probe already found the root dead: escalateOrRetryUnconfirmedRoot catches the retry, logs once, and re-arms the same generation's deadline while the tree is still active and unconfirmed, since the resync backstop probes only confirmed roots), the linger sweep on its next pass; handleException only logs, naming the role token — the core-NATS exception lane has no redelivery, so that log is the whole record and the retry is whichever of the others reaches the root next.` -- TRUE; the probe-throw clause now lives in this bullet, and the linger bullet keeps only the stop side (`... the probe side of the same verdict is in the process-failure-recovery bullet above`)."
        ],
        "test": "NONE committed -- see findings[0]. Behaviour verified by throwaway on head and shown discriminating against a mutant.",
        "status": "code confirmed; test gap (non-blocking)"
      },
      {
        "item": 3,
        "check": "r3998846684: recordedLocators dedupes by sameProcess",
        "code": "runtime-tmux.ts: `if (opened && !found.some((locator) => sameProcess(locator, opened))) found.push(opened);` with the doc explaining state holds a spread copy after /process/started or /worker/started.",
        "test": "runtime.contract.test.ts > TmuxRuntime 'verifies a recorded pane once even when state holds a copy of the launched locator, not the object spawn returned' -- state re-keyed to `{...root}` via the new `registerLocator`, the pane reissued so every recorded window fails, `list-panes -t %1` asserted exactly once. PASS on head; MUTANT (`!found.includes(opened)`, the pre-fix shape): FAIL `Expected length: 1 / Received length: 2` -- discriminating. A different pane counts separately by construction (sameProcess compares pane id + pid + ticks; the existing 'opens a fresh window when no recorded pane verifies ...' and 'splits a second process ...' rows still PASS).",
        "status": "confirmed"
      },
      {
        "item": 4,
        "check": "fast-follow items",
        "results": [
          "Watchdog re-arm on an unconfirmable retirement: worker-boot-watchdog.ts `retire()` helper removes the armed entry before the call, catches, logs `could not be retired; re-arming the watch rather than leaving its boot unwatched`, puts the entry back under the same generation, returns false so the loop continues one more interval. Test worker-boot-watchdog.test.ts 're-arms the watch when a retirement's stop cannot be confirmed, and retires the boot on the next interval once it can' -- PASS; MUTANT (catch -> rethrow, the pre-fix shape): FAIL (`retire:1` only, no `probe, retire:2` tail). AGENTS.md watchdog bullet's new clause quoted above reads true.",
          "stop's listing-failed message via describePaneVerdict: runtime-tmux.ts `throw new ProcessStopFailed(locator, describePaneVerdict(tmuxLocator, verdict))`; contract row 'stop throws ProcessStopFailed, killing nothing, when list-panes itself fails ...' now asserts the full sentence `cannot verify pane %1: <detail>` (a strengthening); the ProcessManager closeTree B3 row asserts `expect.stringContaining('list-panes -t %0 exited 1')` (widened from exact-equal only because the sentence grew a prefix; the argv/exit contract is still pinned).",
          "isOmpPane rethrow row: runtime.contract.test.ts 'probe propagates an EACCES from the /proc cmdline read, and reads an ENOENT there as the process no longer being OMP' -- PASS (EACCES -> rejects.toBe(fault); ENOENT -> not-recorded-process with both identities; cleared -> alive).",
          "Dead fixture branches removed: two `#{pane_id}`-only list-panes branches (never matched the real two-token format), `kill` argv branches (the removed `kill -0`), two bare `{stdout:'', exitCode:1}` list-panes fakes -> `paneGone()`; `${treeKey}` template no-ops -> `treeKey`; docs/solutions/testing/mutation-proof-probe-tests.md shows `lookupPane`.",
          "Wording pins dropped: the B4 test's `failed to stop` negative match and the watchdog test's `liveness probe did not complete` count (both tests keep their behavioural assertions: shutdown over the socket / kill-pane [] / closed / locator cleared / one decision line; probe x2, retire x1, tail `probe -> retire`).",
          "Removed-assertion audit of the whole round-3 test diff: every removed `expect` is a wording pin, a rewrap (panePid -> lookupPane in the docs note), a relocation onto the retirement's save snapshot (the same three assertions `locator undefined`, `launchFailures 1`, `queue == [token]` now read from the snapshot taken at that save; `not.toContainEqual(kill-pane -t %1)` kept), or a strengthening (stop message). No kill-pane-absence, locator-clearing, or --resume assertion weakened; the B5 test adds locator-by-reference, `kill-pane` absence twice, and `--resume` implicitly via the gen-2 relaunch path (asserted explicitly by the real-tmux driver)."
        ],
        "status": "confirmed"
      },
      {
        "item": 5,
        "check": "rebases: state version composition, #957 boot-retire test, formerly-flaky test",
        "migration": "legion-state.ts: `migrateV24State` (v24 -> v25, #993's pure bump) and `migrateV25State` (v25 -> v26, this PR's pane-identity bump) both in the chain; schema literal / newLegionState / loadState check at 26; `.bak` named by the source version (`${file}.v${sourceVersion}.bak`, flag wx). Executed (/tmp/legion27-test/r6/migrate-check.ts, not committed): a v24 file with an identity-less tmux locator -> version 26, `state.json.v24.bak` byte-identical to the input, legacy locator kept identity-less; a v25 file -> version 26, `state.json.v25.bak`, same. legion-state.test.ts 'migrates v24 state to v25 as a pure version bump, leaving PR records untouched', 'migrates v25 state to v26 as a pure version bump, keeping identity-less tmux locators', 'initializes empty v26 state ...' all PASS. PR body explains the numbering: `#993 took v24 -> v25 while this PR was in review, so a v24 file walks both; the smoke-rig evidence below records v24 -> v25 and .v24.bak because that was the numbering when it ran`.",
        "bootRetire957": "index.test.ts 'retires a ready-confirmed worker whose shim socket is dead at boot: retirement dereferences the live api without throwing, pane killed once, locator cleared, secret file removed' (45a7776e): the seeded `%7` now carries `panePid: 7777, paneStartTicks: 4242`, the fake `list-panes` answers `%7 7777`, `readProcessCmdline` reads omp; the assertions `killedPanes toEqual(['%7'])`, `persisted.locator toBeUndefined`, `exposed.locator toBeUndefined`, secret file ENOENT are unchanged -- the kill is still asserted, now on a verified pane (contract moved, not weakened). PASS.",
        "flakyTest": "processes.test.ts 'retires an unconfirmed boot at the first watchdog interval -- never re-arming -- once its pane id has been reissued to another OMP process and its socket refuses' now resolves a `Promise.withResolvers` from `saveState` when the save carries `locator === undefined && launchFailures === 1`, snapshots state at that save (structuredClone), and asserts on the snapshot; the `flushEventLoopUntil(..., 20_000)` poll is gone. 20/20 isolated runs pass.",
        "status": "confirmed"
      },
      {
        "item": 6,
        "check": "package checks and GitHub",
        "result": "cd packages/daemon && bunx biome check --error-on-warnings src/ -> Checked 156 files, no fixes, exit 0; bunx tsc --noEmit -> exit 0; LEGION_E2E=1 LEGION_TMUX_LIVE=1 bun test -> 1088 pass / 0 fail / 0 skip, 1088 tests across 62 files [49.53s] (matches the implementer's 1088/1088). Round-3 file run (processes + runtime.contract + worker-boot-watchdog + legion-state + index): 392 pass / 3 gated-skip / 0 fail. No LEGION-15/35 flake. GitHub at 0c6f3c18: lint/pr-title/test/typecheck SUCCESS on runs 34742308108, 34742341712, 34742420479; mergeable=MERGEABLE, mergeStateStatus=BLOCKED (review required) -- read UNKNOWN for ~30 s while GitHub recomputed after main advanced to 419a0590 (#1001 + two releases, no file overlap with this branch), then MERGEABLE.",
        "status": "confirmed"
      }
    ],
    "observations": [
      "Item 2 is a test gap, not a behaviour gap: the catch exists and works; the exact test is spelled out in findings[0]. Non-blocking under the reviewer's own classification of the item ('land with B5'), but the implementer's thread reply implies a test that is not there.",
      "The B5 real-tmux driver incidentally reproduced the original bug shape end to end: after the operator killed the sole pane the session ended, and the resurrected generation received `%1` again from the recreated server -- verified alive only because the fresh locator records the new pid/ticks.",
      "Throwaway artifacts (/tmp/legion27-test/r6/*.ts, the /tmp mutant copy) not committed; mutant directory removed; throwaway servers legion-smoke21505 and legion-smoke17837 killed (`ls` -> no server running); deployment server legion-sjawharlegion untouched; `.omp/config.yml` left as provisioned."
    ]
  },
  "schemaVersion": 1,
  "phase": "test",
  "completed": "2026-09-13T06:30:21.723Z"
}