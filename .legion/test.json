{
  "passed": 4,
  "failed": 0,
  "failures": [],
  "implementerProof": {
    "verdict": "verified",
    "how": "Re-ran the implementer's live command at the PR head 9bae4ce5e03f950a29b9729855151699a84e7192: `cd -- \"$LEGION_WORKSPACE\" && LEGION_TMUX_LIVE=1 bun test packages/daemon/src/daemon/__tests__/processes.test.ts -t \"LEGION-179\\)\"` -> 2 pass, 0 fail, 20 expect() calls (924 ms), both death-path log lines present ('its stream closed and the one reconnect was refused' and 'the resync probe found its process gone'), each followed by 'respawning LEGION-42 by resuming OMP session /tmp/legion-processes-*/tester.jsonl'. Its kind-run records at /tmp/legion-179/proof on this box (daemon log, kill log, tester-g2 pod spec, state excerpt, listener lines) match the handoff's proof[2] and cover Acceptance 3 — the tester kill, the --resume relaunch naming the recorded session, the phase completing, phase-complete forwarded, no spawn_worker for the tester — and I repeated that run independently on my own instance (proof[3] below) with the same outcome."
  },
  "proof": [
    {
      "criterion": "Acceptance 1(a)/1(b) and 4 on a real process: the implementer's two LEGION_TMUX_LIVE=1 rows reproduce at the PR head",
      "surface": "real tmux server (tmux -L legion-smoke<timestamp>, tmux 3.7b), real legion worker-shim over its unix socket, real kill-pane, real /proc — the repository's real-process fixture surface (processes.test.ts under LEGION_TMUX_LIVE=1)",
      "command": "cd -- \"$LEGION_WORKSPACE\" && LEGION_TMUX_LIVE=1 bun test packages/daemon/src/daemon/__tests__/processes.test.ts -t \"LEGION-179\\)\"",
      "observed": "2 pass, 0 fail, 20 expect() calls, 924 ms. Logged: '[legion] legion-smoke1789456681366-legion-42-tester: worker process died (the resync probe found its process gone); launch failure 1/3; relaunching the same agent with --resume and its catch-up' + 'respawning LEGION-42 by resuming OMP session /tmp/legion-processes-MB2U1G/tester.jsonl'; '[legion] legion-smoke1789456681473-legion-42-tester: worker process died (its stream closed and the one reconnect was refused); launch failure 1/3; relaunching the same agent with --resume and its catch-up' + 'respawning LEGION-42 by resuming OMP session /tmp/legion-processes-2eRPx4/tester.jsonl'.",
      "headSha": "9bae4ce5e03f950a29b9729855151699a84e7192",
      "negativeControl": "Inside each row, probeWorkerClaim on the live pane before the kill (a real list-panes + pid/start-ticks identity check) left the claim at generation 1, the same process (sameProcess true), and opened nothing; in the resync-probe row a second probe of the relaunched pane spawned nothing more (still exactly two pane opens, generation 2)."
    },
    {
      "criterion": "Acceptance 1(a)-(d), 2 (counter and the never-relaunch cases), and 4 as regression locks: bun test packages/daemon green with the named tests present and passing; biome and tsc clean",
      "surface": "Bun test runner over packages/daemon at the PR head (fake clock, FakeRuntime with kubernetes-shaped locators); bunx biome check src/; bunx tsc --noEmit",
      "command": "cd -- \"$LEGION_WORKSPACE\" && bun test packages/daemon; cd packages/daemon && bunx biome check src/ && bunx tsc --noEmit",
      "observed": "1663 pass / 16 skip / 0 fail, 7692 expect() calls, 68 files, 33.7 s; biome: 172 files, no fixes; tsc exit 0. Present and passing in processes.test.ts: 1(a) 'relaunches a confirmed mid-task worker with --resume and its catch-up when its stream closes and the one reconnect is refused (LEGION-179, acceptance 1a)'; 1(b) 'relaunches a confirmed worker the resync probe finds dead while its stream never reported the close (LEGION-179, acceptance 1b)' with resync.test.ts 'probes every worker claim with a recorded locator and a confirmed ready, and none without (LEGION-179)' / 'still probes a located, confirmed worker of a lingering tree: the handler owns the tree verdict (LEGION-179)' and events.test.ts 'dispatches a probe-worker effect to onProbeWorker before saving (LEGION-179)'; 1(c) it.each 'a probe answering alive|unknown|a throw leaves the claim byte-for-byte untouched, spawns nothing, and keeps the cached client (acceptance 1c)'; 1(d) 'does not relaunch a dead worker whose token already holds a queue entry: the drain relaunches it exactly once (acceptance 1d)'. Acceptance 2 counter: 'a relaunched worker that confirms ready zeroes launchFailures and receives its catch-up, leaving the phase untouched (acceptance 2, counter reset)', 'a relaunch that never confirms is retired by the boot watchdog and reaches worker-died at the threshold on the same counter (acceptance 2, escalation)', 'publishes worker-died and relaunches nothing when the death is the third launch failure'. Never-relaunch cases: idle retirement — it.each 'retires a confirmed idle worker past the idle window when phases[issue] is absent|completed|naming another role: one graceful stop, locator cleared, claim kept with its session file, no failure counted, no worker-died' (asserts the stop's own socket close reaches markWorkerDead as a no-op with no relaunch decision); closeTree — 'closes a tree with a confirmed live worker without relaunching it: the stop's own socket close is a no-op through the identity check or the tree check (acceptance 2)'; prompt-failure breaker — 'retires a worker whose acknowledged prompts start no turn three times, then relaunches the assignment cold with --resume' (exactly one cold relaunch, the breaker's own); finished bystander — 'leaves a finished worker retired, not relaunched, when it dies after its phase moved on (acceptance 2, bystander)'; lingering tree — 'relaunches nothing when a worker dies while its tree is lingering'. Also 'relaunches a sub-architect whose process died even though its child's phase names a worker: an architect is never a bystander', 'enqueues a dead worker holding a pending prompt with no queue entry instead of handing it a catch-up, and the drain delivers that prompt', 'reconnectWorkers relaunches a confirmed worker whose socket refuses at restart — queued under the launch hold, launched once launches are enabled (restart-reconnect)', 'logs a not-recorded-process verdict once with the runtime's detail and treats the worker as dead', 'touches nothing when a probe's dead verdict arrives after a newer generation replaced the locator', 'probeWorkerClaim skips an unconfirmed boot, a locator-less claim, and an unknown token without probing'.",
      "headSha": "9bae4ce5e03f950a29b9729855151699a84e7192",
      "negativeControl": "The 1(c) it.each rows are the negative controls in the suite: a probe answering unknown or throwing 'API down' leaves the claim byte-for-byte equal to its pre-probe copy, spawns nothing, keeps the cached client, and logs exactly one 'could not probe or retire worker legion-omp-legion-42-tester; leaving its claim for the next resync' line; the alive row logs nothing."
    },
    {
      "criterion": "Acceptance 3: on a throwaway kind instance this tester owns, a running tester pod's process killed from the node is relaunched with --resume naming the recorded session, the tester's phase completes, and the architect receives phase-complete with no spawn_worker in between",
      "surface": "throwaway kind cluster legion-smoke-t179 (created 07:23Z and deleted 07:43Z by this tester; never the dev-box daemon, no pane or pod I did not start): this branch's daemon at the PR head run on the host from the issue workspace (`bun packages/daemon/src/cli/index.ts start demo --config /tmp/legion-179-test/legion.yaml` under env -i: runtime.kubernetes.kubeconfig at the kind kubeconfig, daemon_url http://172.30.0.1:34380 at the kind gateway, bind 0.0.0.0, port 34380 / worker_stream_port 34381, resync_interval_seconds 60, worker_cap 3, worker_idle_retire_seconds 600, gates.design off), pods from the published image ghcr.io/sjawhar/legion-worker@sha256:31c791054b7644f97a6762d94cd0def56bc29c528e780c031f7b1fac5459c9fb (probe pod legion-probe-demo-31c791054b76 passed 'probe-image: OK … session-storage=probed daemon-api-version=5'), the instance's own NATS, Postgres, Envoy listener and scratch Dispatch (project ST179) stood up with LEGION-26's rig (scripts/kind-smoke from the legion/LEGION-26 bookmark, read-only, byte-identical to the implementer's copy) with SMOKE_STOP_AFTER=overlay and only the Namespace and providers Secret applied to the cluster, the LEGION-177 credential.interactive keeper loop running (kubeconfig-scoped); sandbox repository sjawhar/legion-smoke (the run opened PR #57, closed afterwards)",
      "command": "root issue ST179-1 created and released to todo at 07:26:08Z; the tree ran architect -> planner -> implementer -> tester (Dispatch in_progress 07:26:08Z, testing 07:33:05Z; tester pod legion-st179-1-tester-g1 ready-confirmed 07:33:20Z, session 01a0a3fc-0bb5-7000-8359-c1a2cb770a78, phases[ST179-1] = tester); at 07:33:59Z: node=$(kind get nodes --name legion-smoke-t179); cid=$(docker exec $node crictl ps -q --name worker --label io.kubernetes.pod.name=legion-st179-1-tester-g1); pid=$(docker exec $node crictl inspect --output go-template --template '{{.info.pid}}' $cid); docker exec $node kill -9 $pid  (container 9063b5dada27, pid 10959). LEGION-177 keeper: unset credential.interactive through the architect pod at 07:34:07Z, 8 s after the kill and before tester-g2's init fetch.",
      "observed": "Daemon log, seconds after the kill: '[legion] legion-demo-st179-1-tester: worker process died (its stream closed and the one reconnect was refused); launch failure 1/3; relaunching the same agent with --resume and its catch-up' then '[legion] respawning ST179-1 by resuming OMP session /home/legion/.omp/profiles/legion/agent/sessions/--legion-workspaces-sjawhar-legion-smoke-st179-1--/2026-09-15T07-33-19-413Z_01a0a3fc-0bb5-7000-8359-c1a2cb770a78.jsonl' — exactly the claim's recorded ompSessionFile. Pod legion-st179-1-tester-g2 Running at 07:34:04Z with LEGION_GENERATION=2 / legion.dev/generation=2 and its container command carrying --resume=<exactly that path>; the claim moved to generation 2 with the SAME sessionId 01a0a3fc-0bb5-7000-8359-c1a2cb770a78, readyConfirmedAt 07:34:11Z, launchFailures reset (null), pendingAssignment cleared (the catch-up delivered). At 07:35:44Z the listener logged 'listener received' then 'listener role forwarded' for {\"type\":\"phase-complete\",\"issue\":\"ST179-1\",\"role\":\"tester\",\"summary\":\"ST179-1 verified PASS on PR #57 head dd29bdee…\"} on notifications.role.legion-demo-st179-1-architect to holder 01a0a3f5-a0a3-7000-8eec-420fba66ffbc (the architect's session); Dispatch event seq 5 moved ST179-1 testing -> needs_review at 07:35:44Z (actor legion-daemon:ST179); the architect's reviewer spawn settled 07:35:52Z and phases[ST179-1] became reviewer at 07:35:57Z. The daemon's durable spawnRequests ledger holds exactly one tester request (5ba6e967…, result spawned, settled 07:33:14Z — before the kill) and none after it; the relaunch's prompt was the death path's catch-up. Run record: dispatch://LEGION-179/artifact/legion-179-tester-kind-run-md (daemon log, kill log, api-outage log, tester-g1/g2 pod specs, claim snapshots, listener lines, Dispatch events, spawn ledger, teardown log); also /tmp/legion-179-test/proof on the dev box. Torn down with the rig's down.sh (listener and Dispatch stopped by recorded pid+start ticks, cluster deleted, kubeconfig and both containers removed, every recorded secret shredded), plus the daemon's own state_dir secrets, the rendered overlay, and the apply subset shredded; kind get clusters / docker ps / ss show nothing of the instance.",
      "headSha": "9bae4ce5e03f950a29b9729855151699a84e7192",
      "negativeControl": "Three. (1) Live pods probed alive: every 60 s the daemon logged 'resync probed 1 active roots and N confirmed workers' and the architect, implementer, and reviewer pods stayed at generation 1 for the whole run. (2) At 07:36:48Z the FINISHED planner's pod (legion-st179-1-planner-g1, idle after its phase; active phase = reviewer) was killed the same way (container 37417bab340a, pid 5426): the daemon logged '[legion] legion-demo-st179-1-planner: worker process died (its stream closed and the one reconnect was refused) after finishing: ST179-1's active phase is reviewer and nothing is queued for this role; retired, not relaunched — only spawn_worker resumes a finished worker'; no legion-st179-1-planner-g2 pod appeared, the claim stayed generation 1 with launchFailures untouched, its locator cleared and resumeSessionFile kept. (3) 07:38:47Z-07:40:56Z the daemon's path to the kube-apiserver was rejected (docker exec <node> iptables -I INPUT -p tcp --dport 6443 -s 172.30.0.1 -j REJECT --reject-with tcp-reset — the host-gateway source only, so kubelet, the control plane, and the pods were untouched; host kubectl failed 'connection reset by peer' / EOF): the two resync ticks inside the window still logged 'resync probed 1 active roots and 3 confirmed workers', the daemon's own API calls failed ('orphan sweep: failed to list pods: The socket connection was closed unexpectedly'), no 'worker process died' line, no relaunch, the claims byte-identical before and after (diff of the two snapshots empty), pods unchanged; on this runtime an API failure probes alive while the pod's stream is registered, else unknown (runtime-kubernetes.ts probe), and neither is a death verdict."
    }
  ],
  "observations": [
    "Environment: the host daemon and the rig ran under env -i (PATH, HOME, USER, LANG, TERM, XDG_STATE_HOME/XDG_DATA_HOME, DISPATCH_TOKEN only), so nothing of this tester's Legion pane (LEGION_*, LEGION_GRANT_FILE, ENVOY_*, the dev-box DISPATCH_*) reached the daemon or its pods.",
    "Unrelated to this issue, seen again as in the implementer's and LEGION-26's runs: the root architect's ready connect timed out three times ('architect shim connect for ST179-1 failed (attempt n/6, cycle 1) … negotiate_protocol timed out after 5000ms') while the architect worked normally; no effect on the proof.",
    "The daemon's spawnRequests ledger prunes settled entries over time (by the end of the run the planner's and implementer's first requests were gone while the tester's remained), so 'exactly one tester request' was read both right after the tester's ready (three entries: planner, implementer, tester) and at the end (tester, reviewer, two resumes)."
  ],
  "schemaVersion": 1,
  "phase": "test",
  "completed": "2026-09-15T07:46:56.552Z"
}