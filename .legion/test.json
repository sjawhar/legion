{
  "issue": "LEGION-13",
  "pr": 978,
  "prUrl": "https://github.com/sjawhar/legion/pull/978",
  "headSha": "c185b4614e547c3374b8670e3c43ce6471ba927b",
  "specVersion": 6,
  "verdict": "PASS",
  "passed": 8,
  "failed": 0,
  "failures": [],
  "testerStatus": {
    "context": "tester",
    "state": "success",
    "sha": "c185b4614e547c3374b8670e3c43ce6471ba927b",
    "statusId": 54054257927
  },
  "verdictComment": "https://github.com/sjawhar/legion/pull/978#issuecomment-5648478864",
  "prBody": "CI line names Tests runs 34716356904/34716344284 (no pr-checks-result job exists in this repo) and the tester status; E2E line replaced with this run; Negative control line rewritten. Threads/Thermo/Fast-follow/Chain untouched.",
  "paneEnvironment": {
    "cwd": "$LEGION_WORKSPACE/packages/daemon at c185b461",
    "DISPATCH_URL": "http://localhost:8766",
    "DISPATCH_TOKEN": "<unset>",
    "DISPATCH_TOKEN_FILE": "<state_dir>/secrets/dispatch-token",
    "note": "the exact bug shape from the issue title; every unprefixed run below used it as-is"
  },
  "criteria": {
    "1_acceptance1": {
      "paneUnprefixed": "bun test src/cli/__tests__/index.test.ts → 18 pass / 0 fail, exit 0",
      "urlPlusTokenFile": "DISPATCH_URL=http://127.0.0.1:1 DISPATCH_TOKEN_FILE=/dev/null bun test src/cli/__tests__/index.test.ts → 18 pass / 0 fail, exit 0"
    },
    "2_acceptance2": {
      "urlPlusToken": "DISPATCH_URL=http://127.0.0.1:1 DISPATCH_TOKEN=t bun test src/cli/__tests__/index.test.ts → 18 pass / 0 fail, exit 0",
      "unsetAll": "env -u DISPATCH_URL -u DISPATCH_TOKEN -u DISPATCH_TOKEN_FILE bun test src/cli/__tests__/index.test.ts → 18 pass / 0 fail, exit 0"
    },
    "3_daemonSuite": "bun test (packages/daemon, pane unprefixed) → 872 pass / 5 skip / 0 fail, 877 tests, 56 files, 3506 expect() calls, exit 0 — matches implement.json gates.bunTest",
    "4_revertGuard": {
      "where": "/tmp/legion13-test/packages/daemon: cp -a of packages/daemon at the head; node_modules, packages/contracts, packages/workspace symlinked to the workspace; sanity run 18/0 before the revert",
      "revert": "sed -i '244s/^    env,$/    env: process.env,/' src/cli/index.ts — diff vs head is exactly that line",
      "ciShape": "DISPATCH_URL=http://127.0.0.1:1 DISPATCH_TOKEN=t → 17 pass / 1 fail, exit 1; sole failure: legion start --check-config > resolves Dispatch settings from the injected env, not the process environment",
      "plainShell": "env -u DISPATCH_URL -u DISPATCH_TOKEN -u DISPATCH_TOKEN_FILE → 17 pass / 1 fail; same sole failure",
      "paneUnprefixed": "16 pass / 2 fail: the injected-env test plus `validates github_apps.<role>.private_key_command without executing it`, both `dispatch_url is set but DISPATCH_TOKEN is not; the dispatch tools would not register`",
      "branchUntouched": "jj status in $LEGION_WORKSPACE: only the extension's uncommitted .omp/config.yml; the revert never left /tmp"
    },
    "5_mainNegativeControl": {
      "main": "main@origin d59417f7 (chore: release pi-envoy v1.12.0); only src/cli/index.ts, src/cli/__tests__/index.test.ts and docs/solutions/legion/worker-pane-shell-gotchas.md differ from the head",
      "where": "/tmp/legion13-main/packages/daemon: same layout, those two files replaced by main@origin's via jj file show",
      "scrubActive": "pane unprefixed → 17 pass / 0 fail, exit 0",
      "scrubDisabled": "main's beforeEach (test file lines 167–174) commented out → 16 pass / 1 fail: validates github_apps.<role>.private_key_command fails with the Dispatch error — main is green in a pane only through #967's scrub",
      "headHasNoScrub": "grep -n 'beforeEach\\|afterEach' packages/daemon/src/cli/__tests__/index.test.ts → no matches; bun:test import is { describe, expect, it }",
      "headCheckConfigDescribe": "no process.env mutation; env = { PATH: process.env.PATH, HOME: process.env.HOME } (line 167), spread per test",
      "remainingProcessEnvWrites": "lines 19–20 and 46–49, the pre-existing GH_TOKEN save/delete/restore inside the `legion gh` test — byte-identical on main, outside this issue's spec, not touched by the PR; the test's subject is the process env itself (grant redeemed only into the gh child). Noted for the architect; not counted as a failure."
    },
    "6_operatorSurface": {
      "yaml": "/tmp/legion13-op-Zems/legion.yaml — project acme99, envoy_url, dispatch_project, repos, nats_urls, gates.design off; no dispatch_url",
      "paneAsIs": "bun src/cli/index.ts start acme99 --check-config --config <yaml> → exit 1, `error: dispatch_url is set but DISPATCH_TOKEN is not; the dispatch tools would not register` (resolveDaemonConfig config.ts:708 ← loadStartConfig index.ts:243 ← cmdCheckConfig index.ts:255)",
      "explicitNoToken": "env -u DISPATCH_TOKEN DISPATCH_URL=http://127.0.0.1:1 bun src/cli/index.ts start acme99 --check-config --config <yaml> → same message, exit 1",
      "withToken": "DISPATCH_URL=http://127.0.0.1:1 DISPATCH_TOKEN=t bun src/cli/index.ts start acme99 --check-config --config <yaml> → `Config OK: project=acme99`, exit 0",
      "unchangedVsMain": "the same three inputs against main@origin's index.ts in /tmp/legion13-main give the same three outputs and the same Bun uncaught-error stack shape"
    },
    "7_gatesAndCi": {
      "biome": "bunx biome check src/ → Checked 143 files in 360ms. No fixes applied. exit 0",
      "tsc": "bunx tsc --noEmit → exit 0",
      "ci": "Tests workflow (.github/workflows/pr-and-main.yaml: lint, typecheck, test, pr-title) runs 34716356904 and 34716344284 — success at c185b461, event pull_request",
      "prChecksResult": "no such job exists in this repository's workflows; the PR body template line was rewritten to name the Tests runs"
    },
    "8_docs": {
      "envU": "grep -n 'env -u' docs/solutions/legion/worker-pane-shell-gotchas.md → no matches",
      "section2": "line 141 `## 2. bun test in a pane: inject the env the code reads, and run it from packages/daemon`; LEGION-29 packages/pi-envoy paragraph at lines 150–159; LEGION-14 `Run it from packages/daemon` paragraph at lines 161–167; related_issues still lists LEGION-14 and LEGION-29"
    }
  },
  "observations": [
    "The PR body template's `pr-checks-result` run is not a job this repository has; the PR gate is the `Tests` workflow. Reviewer: verify CI against runs 34716356904/34716344284.",
    "The only process.env writes left in index.test.ts are the pre-existing GH_TOKEN save/delete/restore in the `legion gh` test (lines 19–20, 46–49), identical on main and outside the spec — flagged, not failed.",
    "worker-pane-shell-gotchas § 1 failure shape: the shadowed `export` pins the first grant until grant_release runs; releasing only after credentialed calls left the lock on an expired grant, and 17 plain calls later a `legion gh` call 403'd with 18 stacked grants counted. Release at the end of every call.",
    "Two `export LEGION_GRANT` blocks appeared on several calls where the command text started at `cd -- \"$LEGION_WORKSPACE\"`; the shadow kept the first (injected) one each time, consistent with the LEGION-29 finding that the extra blocks are re-emitted model text."
  ],
  "scratchArtefacts": [
    "/tmp/legion13-test (head + the one-line revert)",
    "/tmp/legion13-main (main@origin's index.ts + index.test.ts, scrub commented out)",
    "/tmp/legion13-op-Zems/legion.yaml",
    "/tmp/legion13-verdict.md, /tmp/legion13-pr-body.md, /tmp/legion13-pr-body-new.md"
  ],
  "environmentNotes": [
    "Grant stacking (LEGION-12 unmerged): sourced /tmp/legion9-grant-trap.sh; grant-consuming command first in its call; `; grant_release` at the end of every call after the 403 above.",
    "node_modules already present in the workspace (planner's bun install); no install run."
  ],
  "schemaVersion": 1,
  "phase": "test",
  "completed": "2026-09-12T20:26:24.187Z"
}