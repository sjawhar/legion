{
  "verdict": "pass",
  "verdictSummary": "Every acceptance criterion in the tester's scope (1 through 4) holds on the branch head, reproduced independently on the tester's own rig directory and Oh My Pi profile. The one blocking fact for merging is not a behavior defect: pull request #974 is CONFLICTING against main in exactly one file, docs/solutions/legion/worker-pane-shell-gotchas.md, and GitHub has started no workflow run for its head (a pull request GitHub cannot merge gets no pull_request-triggered CI), so the implementer must rebase once to get CI and a mergeable head. Auto-merging main and the branch with git merge-tree conflicts only in that document; the merged tree passes the full pi-envoy suite (155 pass, 0 fail) and tsc.",
  "testedHead": "5667f1ba6806 (implement: record handoff). The pull request head c8587dfdf6f1 (implement: record pull request) differs from it only in .legion/implement.json (jj diff --stat: 1 file, 7 insertions, 5 deletions); every code, test, script, and doc file is identical.",
  "pullRequest": {
    "number": 974,
    "url": "https://github.com/sjawhar/legion/pull/974",
    "title": "fix(pi-envoy): deliver the worker's one-time credential through the shell tool's environment, not the command text",
    "head": "c8587dfdf6f1f52779211ab5c923c10f9685783c",
    "base": "main",
    "mergeable": "CONFLICTING (mergeStateStatus DIRTY) as read with `legion gh -- pr view 974 --json mergeable,mergeStateStatus` at 2026-09-12T20:00Z",
    "conflictingFiles": [
      "docs/solutions/legion/worker-pane-shell-gotchas.md"
    ],
    "conflictEvidence": "`git --git-dir=$(jj git root) merge-tree --write-tree --name-only fd4fce51204a (main@origin) c8587dfdf6f1` -> exit 1, CONFLICT (content) in docs/solutions/legion/worker-pane-shell-gotchas.md only; legion.ts, legion.test.ts and CHANGELOG.md auto-merge. Cause: the branch rewrote section 1 from the v1.8.1 base (e01661365b2b) while main later added sections 7-9 and three section-1 sub-workarounds (#953 LEGION-18, #952, #970). The merged tree 604932bbf779 (exported to /tmp/l12rig.tester.Y9Lp/merged) passes `bun test` in packages/pi-envoy: 155 pass, 0 fail, 495 expect calls; `bunx tsc --noEmit` exit 0.",
    "ci": "No check runs and no workflow runs exist for head c8587dfdf6f1 (`legion gh -- api repos/sjawhar/legion/commits/<head>/check-runs` -> total_count 0; `actions/runs?head_sha=<head>` -> total_count 0), while other legion/* pull-request heads pushed in the same hour do have Tests runs. A conflicting pull request has no merge commit for the pull_request trigger to build, so CI will appear only after the rebase.",
    "testerStatusCheck": "posted by this tester on head c8587dfdf6f1 as commit status context `legion/tester` (no prior convention exists in the repository; chosen and named here), state success, description pointing at .legion/test.json.",
    "e2eSectionUpdated": true
  },
  "acceptance": {
    "1_headlessAndTerminal": "PASS. Headless leg (omp --mode rpc, branch extension via the l12rigtest profile symlinks): 31 bash tool calls, 3 task spawns; 0 `export LEGION_GRANT=` lines and 0 occurrences of the string LEGION_GRANT in any persisted arguments.command; 31 POST /legion/v1/grants (all 200), one per call, in order; arguments.env.LEGION_GRANT equals that call's mint on all 31; all 25 record-grant commands ran under their own call's mint (seen-grants.log). Terminal leg (interactive omp under tmux -L l12rig, 8 bash calls, 1 task spawn): the same five checks all hold (8 calls, 8 mints, 0 text lines, env equals mint on all 8, all 6 record-grant commands ran under their own mint).",
    "2_handoffComplete": "PASS. Headless leg call 31 `legion handoff complete --summary 'rig run complete'; echo exit=$?` -> `[handoff] Reported phase completion`, `exit=0`; stand-in log: POST /legion/v1/phase/complete 200 with grant 6f4139bf-419f-40d6-890d-67a1f6a3f2bb, the id minted for call 31. Terminal leg call 8: same, 200 with the grant minted for call 8. No shell workaround was sourced in either worker (the prompt never names the variable or the word export).",
    "3_credentialAndGh": "PASS on the stand-in (the real-GitHub half is acceptance 5, post-merge, out of scope). Headless calls 27 and 29 `legion credential get` -> `username=x-access-token / password=rig-token / exit=0`, git-credential 200 with each call's own mint; call 28 `legion gh -- --version` -> `gh version 2.98.0`, `exit=0`, gh-token 200 twice with call 28's mint (the shim re-enters `legion gh`, the recorded gotcha; both 200). Call 16 `gh --version` (the bare shim) -> gh-token 200 with call 16's mint. Never `Unable to redeem`.",
    "4_regressionTestsAndMechanism": "PASS. On main's tree (main@origin fd4fce51204a exported with git archive, bun install --frozen-lockfile) with the branch's legion.test.ts copied in: `bun test extensions/legion.test.ts -t 'tool env|re-feeds|recovers a live worker'` -> 0 pass, 4 fail (test A: expect(input.command).toBe(command) received the six-line prelude `export LEGION_GRANT='grant-1' / unset GH_TOKEN / unset GITHUB_TOKEN / unset GH_HOST / export GH_CONFIG_DIR=... / export PATH=...`; test B, the shim test, and the recovery test: `worker shell was not given a daemon grant` because the result had no env). On the branch head: 4 pass, 0 fail, 24 expect calls. Full `bun test` in packages/pi-envoy on the branch head: 139 pass, 0 fail, 429 expect calls across 6 files, run from inside this Legion pane with no env -u; `bunx tsc --noEmit` exit 0. The pull request body names the mechanism (Oh My Pi's write-back of the hook's revised input, then imitation). The unfixed reproduction transcript is at the path under rigRuns.unfixedRpc.",
    "5_postMergeUpgrade": "out of scope for this phase (second assignment after merge)."
  },
  "unfixedReproduction": {
    "how": "Reproduced by the tester, not taken from the implementer's record: main@origin (fd4fce51204a, pi-envoy v1.11.1) exported to /tmp/l12rig.tester.Y9Lp/main, plus only (a) the branch's scripts/grant-rig directory and (b) the two debug log lines (instance id on `extension instance loaded`, one `legion tool_call hook` line per invocation) added to extensions/legion.ts; gh-shim.ts is main's, so the credential is still prepended as command text. Verified with diff against `jj file show -r main@origin`: 3 hunks, all logging. Separate rig dir /tmp/l12rig.tester.Y9Lp/unfixed, profile l12rigunfixed, stand-in on port 13497.",
    "result": "31 bash calls, 31 mints, H=1/1 and G=1 on every call (the hook never multiplies). Credential lines in the command text per call: 1,1,2,3,5,1,2,4,1,2,1,3,2,2,2,2,2,2,3,3,4,4,5,3,5,4,5,6,7,8,9 (26 of 31 calls carry more than one). The first line on every call is the hook's own mint; the 73 extra ids are all v4-shaped and none was ever minted. 20 of 25 record-grant commands ran under an imitated id (calls 3,4,5,7,8,10,12,13,14,17-26,30). Redemptions: gh-token 403, git-credential 403, gh-token 403, git-credential 403, phase/complete 403, every one with a never-minted id. Tool results: call 16 `gh --version` -> `Unable to redeem LEGION_GRANT (403)` (isError); calls 27, 28, 29 -> `Unable to redeem LEGION_GRANT (403)`, `exit=1`; call 31 `legion handoff complete` -> `Failed to report phase completion: Unable to report phase completion (403): {\"error\":\"Invalid or expired grant\"}`, `exit=1`. Analyzer verdicts A, B(redemptions), C, D FAIL; E PASS."
  },
  "modelImitationOnFixedCode": {
    "finding": "The model still imitates on the fixed code, and the fix holds anyway. In the headless leg the model itself supplied an `env` object on 26 of 31 calls (every call from 6 onward): all six of the hook's keys copied verbatim (GH_CONFIG_DIR and PATH exact, the token keys empty) with a fabricated v4-shaped LEGION_GRANT that was never minted (26 of 26). This is visible in the streamed toolcall_end events in events.jsonl (the model's arguments before the hook) versus the persisted transcript (after the hook). Because the hook spreads its keys last, the persisted and executed env.LEGION_GRANT equals the hook's mint on all 26, every record-grant ran under the mint, and every redemption was 200. The terminal leg showed the same: the tmux pane rendered call 5 as `LEGION_GRANT=\"b3c1b7a0-84d1-4f22-9d8f-c\" record-grant; echo step-6` (the model's streamed env, a truncated fabrication) while the persisted call carries the hook's 8659b844... and seen-grants.log shows the command ran under 8659b844.... This is the live case for the spec's precedence requirement, beyond regression test B.",
    "commandTextStaysClean": "On neither fixed leg did the model ever put the credential into the command text (0 occurrences of LEGION_GRANT in 39 persisted commands); the imitation moved entirely into the env object, where the hook wins."
  },
  "standIn": {
    "rule": "Verified against a fresh stand-in on port 13498: a minted grant redeemed 200 five times within 0.2 s (gh-token x3, git-credential, phase/complete) -> not single-use; the same id 38 s after mint -> 200; at 101 s -> 403 `Invalid or expired grant` on gh-token, git-credential, and phase/complete. A fabricated id -> 403 `Invalid or expired grant`; a mint with a wrong session secret -> 403 `Invalid session secret`. Matches AuthState.resolveGrant (existence and expiry only) and GRANT_TTL_MS = 60_000 in packages/daemon/src/daemon/api.ts.",
    "negativeControl": "`printf 'protocol=https\\nhost=github.com\\n' | LEGION_GRANT=00000000-0000-4000-8000-000000000000 LEGION_DAEMON_URL=http://127.0.0.1:13498 /tmp/l12rig.tester.Y9Lp/state/bin/legion credential get` -> `Unable to redeem LEGION_GRANT (403)`, exit 1; stand-in log line: git-credential 403 grantId 00000000-0000-4000-8000-000000000000."
  },
  "instrumentation": "Headless fixed leg: `extension instance loaded` 8 lines (4 envoy.ts + 4 legion.ts), legion instance ids f89e4e08 (parent, loaded 19:29:52 before the first call), e3196231, 1d521cc0, 83bb10ce (one per task spawn, loaded at 19:30:35, 19:34:03, 19:37:31); `legion tool_call hook` exactly 1 line per bash call for all 31, all from f89e4e08; the parent also logged 3 task, 3 hub lines; the three subagent instances logged only their own `yield` (4 lines total) and none of the 31 mints (all mints carry the parent session id 01a09718-fc88-...). Terminal leg: 2 legion instances (d867786b parent, f28a9f71 for the one task spawn), 1 hook line per bash call for all 8, subagent instance logged 2 yield lines and minted nothing. Unfixed leg: also H=1/1 on all 31 with 4 instances, which is what separates the model-imitation cause from a hook-multiplication cause.",
  "behaviorEdges": {
    "pathAndShim": "arguments.env.PATH on all 39 fixed calls = <rig>/state/worker-bin + ':' + the pane's PATH (which itself starts with <rig>/state/bin), the shim directory appearing exactly once; headless call 15 `which gh` -> /tmp/l12rig.tester.Y9Lp/state/worker-bin/gh; call 16 `gh --version` -> gh version 2.98.0 via the shim (gh-token 200 with call 16's mint). The shim script strips its own directory from PATH before `exec legion gh -- \"$@\"`.",
    "tokenClearing": "Persisted env on all 39 fixed calls has GH_TOKEN, GITHUB_TOKEN, GH_HOST = '' and GH_CONFIG_DIR = <rig>/state/gh. Real-surface probe (throwaway driver /tmp/l12rig.tester.Y9Lp/probe-env-clear.ts, one extra headless session on the fixed extension whose pane environment carried GH_TOKEN=ambient-personal-token, GITHUB_TOKEN=ambient-personal-token, GH_HOST=ambient.example.invalid): command 1 `echo \"T=[$GH_TOKEN] G=[$GITHUB_TOKEN] H=[$GH_HOST] C=[$GH_CONFIG_DIR]\"; which gh` -> `T=[] G=[] H=[] C=[/tmp/l12rig.tester.Y9Lp/state/gh]` and the shim path; command 2 `printenv GH_TOKEN; echo printenv-exit=$?` -> empty line, `printenv-exit=0` (set to empty for the command, which gh treats as absent). 2 mints, both 200. Run dir runs/tester-env-clear-2026-09-12T20-04-03-878Z.",
    "noLeakBetweenCalls": "Across 25 consecutive record-grant calls in the headless leg and 6 in the terminal leg, each command saw its own call's mint and never the previous call's (0 repeats), so call N's env does not survive into call N+1. Oh My Pi source (fork worktree ~/.worktrees/oh-my-pi/feat-extension-agent-revival at 8ac937a04ed0, closest to the pinned 18.1.18-sami.20260912-104423 build): the bash tool passes env per run to a persistent native shell, which pushes a Command-scoped exported overlay and pops it after the command (crates/pi-shell/src/shell.rs apply_command_env; packages/coding-agent/src/exec/bash-executor.ts executionShell.run({env: commandEnv})).",
    "hookWinsOverModelEnv": "See modelImitationOnFixedCode: 26 live cases in the headless leg plus 1 observed in the terminal leg; regression test B covers the re-fed case."
  },
  "docsAndChangelog": {
    "section1": "docs/solutions/legion/worker-pane-shell-gotchas.md section 1 states the cause (Oh My Pi writes the hook's revised input back into the assistant message; the model imitates it), states the 60-second lifetime and repeat-redeem rule with the source references, describes the fix (env channel, keys spread last, per-command scope), retires the `export()` shadow-function and `pickgrant` workarounds ('do not source the old workaround ... delete them'; the base text's `trap ... DEBUG` remark went with it), and carries the resumed-session caveat.",
    "changelog": "packages/pi-envoy/CHANGELOG.md [Unreleased] > Fixed has the LEGION-12 entry.",
    "findingNonBlocking": "Section 1 says the credential was command text 'until plugin 1.8.1' and the front-matter symptom says '(plugin 1.8.1 and earlier)'. main has since released pi-envoy 1.9.0, 1.10.0, 1.11.0 and 1.11.1 without this fix, so a reader on 1.10.x would wrongly conclude they have it. The '1.8.x' placeholder for the fix version is the post-merge task's to fill; the 'until 1.8.1' claim should become 'until the release that carries LEGION-12 (every version through 1.11.1)'. Belongs in the rebase the conflict already forces."
  },
  "gotchasConfirmed": [
    "`legion gh -- --version` redeems the grant twice (two gh-token 200 lines with call 28's mint at 19:39:27.807Z and 19:39:28.234Z): the shim is still first on PATH when legion gh spawns gh. Harmless.",
    "The stand-in's role token is roleToken('l12rig','RIG-1','implementer') = legion-l12rig-rig-1-implementer; the boot handshake succeeded on the real Envoy listener in all four worker sessions.",
    "The profile's bash interceptor is Oh My Pi's default rule set (DEFAULT_BASH_INTERCEPTOR_RULES in packages/coding-agent/src/config/settings-schema.ts, enabled by the legion profile's config.yml `bashInterceptor.enabled: true`); the rig prompt avoids `> file` redirections and no call was blocked in any tester run.",
    "The tester's own pane runs the OLD extension and showed the bug: `legion gh` 403'd twice when this session's command text carried a second, imitated credential line; the doc's `export()` shadow function was sourced in the persistent shell (once, then again after the shell was reset) and every later `legion gh` call succeeded with `[grants this call: 2; used <first>]`. The daemon test suite was not run (nothing under packages/daemon changed), so the `env -u DISPATCH_URL -u DISPATCH_TOKEN_FILE` note did not apply."
  ],
  "rigRuns": {
    "rigDir": "/tmp/l12rig.tester.Y9Lp (tester's own; profile l12rigtest with extensions symlinked to the workspace at 5667f1ba6806; kept for the reviewer). Each run dir holds prompt.txt, table.txt, report.json, transcript.jsonl, omp.log, standin.log, seen-grants.log (plus events.jsonl or pane.txt).",
    "fixedRpc": "/tmp/l12rig.tester.Y9Lp/runs/tester-fixed-rpc-2026-09-12T19-29-09-763Z",
    "fixedTui": "/tmp/l12rig.tester.Y9Lp/runs/tester-fixed-tui-2026-09-12T19-48-37-913Z",
    "envClearProbe": "/tmp/l12rig.tester.Y9Lp/runs/tester-env-clear-2026-09-12T20-04-03-878Z",
    "unfixedRpc": "/tmp/l12rig.tester.Y9Lp/unfixed/runs/tester-unfixed-rpc-2026-09-12T19-52-13-681Z (profile l12rigunfixed, extensions symlinked to /tmp/l12rig.tester.Y9Lp/main)",
    "ttlProbeLog": "/tmp/l12rig.tester.Y9Lp/standin-ttl.log",
    "mainExport": "/tmp/l12rig.tester.Y9Lp/main (main@origin fd4fce51204a + rig scripts + two logging lines in legion.ts)",
    "mergedTree": "/tmp/l12rig.tester.Y9Lp/merged (git merge-tree 604932bbf779 of main@origin and the pull request head)",
    "implementerRunsForComparison": "/tmp/l12rig.kDlj/runs (not reused; the tester's numbers above are from the tester's own runs)",
    "commands": [
      "RIG_PROFILE=l12rigtest sh packages/pi-envoy/scripts/grant-rig/setup.sh $LEGION_WORKSPACE /tmp/l12rig.tester.Y9Lp",
      "bun packages/pi-envoy/scripts/grant-rig/daemon-standin.ts 13499 $RIG/standin.log $RIG/state/secrets/boot",
      "bun packages/pi-envoy/scripts/grant-rig/run.ts drive --rig $RIG --port 13499 --omp $(mise where github:sjawhar/oh-my-pi@18.1.18-sami.20260912-104423)/bin/omp --profile l12rigtest --label tester-fixed-rpc",
      "bun packages/pi-envoy/scripts/grant-rig/run.ts tui --rig $RIG --port 13499 --omp <same> --profile l12rigtest --short --label tester-fixed-tui",
      "git --git-dir=$(jj git root) archive fd4fce51204a | tar -x -C $RIG/main; (cd $RIG/main && bun install --frozen-lockfile); cp -r packages/pi-envoy/scripts/grant-rig $RIG/main/packages/pi-envoy/scripts/; add the two debug log lines to $RIG/main/packages/pi-envoy/extensions/legion.ts; RIG_PROFILE=l12rigunfixed sh setup.sh $RIG/main $RIG/unfixed; stand-in on 13497; run.ts drive --rig $RIG/unfixed --port 13497 --profile l12rigunfixed --label tester-unfixed-rpc",
      "cd packages/pi-envoy && bun test extensions/legion.test.ts -t 'tool env|re-feeds|recovers a live worker' (branch: 4 pass; main tree + branch test file: 4 fail); bun test (139 pass); bunx tsc --noEmit (clean)"
    ]
  },
  "forTheArchitect": [
    "Blocking for merge, not a behavior defect: rebase legion/LEGION-12 onto main (permitted: GitHub reports CONFLICTING). One file conflicts, docs/solutions/legion/worker-pane-shell-gotchas.md; carry main's sections 7-9 and fold the branch's section-1 rewrite over main's section-1 workaround additions. Code merges clean and the merged tree is green. CI will only start after that.",
    "Fix the 'until plugin 1.8.1' / '(plugin 1.8.1 and earlier)' wording in the same rebase (see docsAndChangelog.findingNonBlocking).",
    "Retest after the rebase is a re-run of the two regression commands and the headless rig leg (about 15 minutes); the tester session stays alive for it."
  ],
  "schemaVersion": 1,
  "phase": "test",
  "completed": "2026-09-12T20:14:14.658Z"
}