{
  "summary": "PASS at code head ad7d2444da88195572fc8b0a1c515ff272f0f730 (PR #1120 head 8c771678200f6a95a71eecd73d2e2a08d698c213 = code head + .legion/implement.json). Review round 1 fixed the one Blocking thread (a pre-pause probe that throws left the ResendLedger entry in flight): `resendToRootArchitect` now settles in a try/catch around the pre-pause probe and rethrows, and the new lock pins that the throw is logged once, nothing is armed or sent, and the next exception for the same message is attempt 2 with a 15 s pause rather than dropped as in-flight. Full packages/daemon suite 1643 pass / 14 skip / 0 fail; biome and tsc clean; every spec-v7 lock (kept-until-TTL x2, capped-message, P14 both variants) and every earlier LEGION-107 lock still green; source audit finds no unsettled path between claim() and settle(); the diff from the fork point is the three daemon files plus two tests only; PR MERGEABLE with CI green at both the code head and the tip. The `E2E (tester)` block was added to the PR body by `legion gh -- pr edit` (accepted for the review App; verified verbatim round-trip). No smoke rig run, production daemon untouched (Sami, 2026-09-13). No findings for the implementer.",
  "round": "corrective-1, review round 1 (PR #1120; earlier tester PASS at 53ec3879/b6914208 and at #1085's d13ea8c9 and b35589ae)",
  "pullRequest": "https://github.com/sjawhar/legion/pull/1120",
  "codeHeadSha": "ad7d2444da88195572fc8b0a1c515ff272f0f730",
  "prHeadSha": "8c771678200f6a95a71eecd73d2e2a08d698c213",
  "passed": 1643,
  "failed": 0,
  "failures": [],
  "implementerProof": {
    "verdict": "verified",
    "how": "Re-ran the implementer's gates and named tests independently at the PR head (working copy @ has no changes over 8c771678; `jj diff --from ad7d2444 --to 8c771678 --summary` is `.legion/implement.json` only): `bun test` 1643 pass / 14 skip / 0 fail (matches the thread reply's numbers); biome and tsc clean. Ran and read the new lock (processes.test.ts:10141-10208): a `run` that answers the first `list-panes` with exitCode 1 / stderr 'tmux: server not responding' then liveRun; after exception 1 it asserts clock.pending [], publications [], controlRequests [], tree unchanged, exactly one `failed to recover <token> after a delivery exception` line; exception 2 arms [15000], fires it, and the test asserts zero 'arrived while its re-send is pending' lines, exactly one 're-sending event' line containing 'attempt 2 of 3', one publication, one directive. That matches the thread's claim (retry is attempt 2 / 15 s, settle refunds nothing). Audited the thread's four-bullet path list against the source (checks.4_audit) and found it accurate. The red-first claim (timed out on the un-armed pause before the fix) is consistent with the pre-fix code at 53ec3879 (no catch around the pre-pause probe) but was not re-executed against it; the diff is the evidence."
  },
  "checks": {
    "1_bunTest": "cd packages/daemon && bun test  (tip 8c771678) -> ' 1643 pass\\n 14 skip\\n 0 fail\\n 7609 expect() calls\\nRan 1657 tests across 68 files. [35.15s]'",
    "1_biome": "cd packages/daemon && bunx biome check src/ -> 'Checked 172 files in 609ms. No fixes applied.' exit 0",
    "1_tsc": "cd packages/daemon && bunx tsc --noEmit -> (no output) exit 0",
    "2_newLock": "bun test src/daemon/__tests__/processes.test.ts -t 'a pre-pause probe that throws settles the ledger entry' -> ' 1 pass\\n 0 fail\\nRan 1 test across 1 file. [152.00ms]'. Asserts: one `failed to recover … after a delivery exception` line after the throwing probe; clock.pending [], publications [], controlRequests [], trees[root] {generation 1, active}; second exception arms exactly [15000]; no 'arrived while its re-send is pending' line; one 're-sending event' line with 'attempt 2 of 3'; publications 1, controlRequests 1.",
    "2_specV7Locks": "resend-ledger.test.ts -> 6 pass / 0 fail; -t 'keeps the entry capped until the TTL elapses' -> 1 pass; -t 'a capped claim refreshes the TTL clock' -> 1 pass; processes.test.ts -t 'later delivery_failed exceptions for a capped message' -> 1 pass; -t 'integration: bounded re-sends' -> 2 pass (both it.each variants).",
    "3_earlierLocks": "processes.test.ts -t 'receipt_timeout' -> 5 pass; -t 'resurrects a dead root architect on a' -> 2 pass; -t 're-send' -> 9 pass; -t 'worker-queued' -> 5 pass; P7 'drops, without counting, an exception for a message whose re-send pause is still running' -> 1 pass; P8 'resurrects a root that dies during the re-send pause instead of directing it' -> 1 pass; P9 'dispose\\(\\) cancels a pending re-send pause so firing it afterwards sends nothing' -> 1 pass; P6 -> 1 pass; P6b -> 1 pass; index.test.ts -t 'envoyPublishBody' (I1) -> 1 pass; events.test.ts E1 'parses a receipt_timeout exception…' + E2 'ignores an exception whose reason is not one of the three known ones' -> 2 pass. All 0 fail.",
    "4_audit": "resendToRootArchitect (processes.ts:3095-3153), every exit between claim() -> resend (entry.inFlight = true, 3100) and settle(): (a) pre-pause probe throws -> catch settles (3125) and rethrows; handleException's catch logs once — SETTLED (the fix). (b) pre-pause probe not alive -> settle (3129) then resurrect (3130); a resurrect throw strands nothing — SETTLED. (c) the pause: boundedWait (runtime.ts:207-214) never rejects — injected sleep is `sleep(ms).then(() => true)`; real timer is createCancellableSleep whose cancel() `resolvePending?.()` resolves the pending promise (cancellable-sleep.ts:27-33) — so `await timedOut` cannot throw. (d) the `disposed || resendWaits.get(key) !== wait` early return (3141) does NOT settle, but is reachable only via dispose(): dispose() runs `resendLedger.clear()` (processes.ts:552) so there is no entry to strand (P9 pins this); a second arm for the same key would need claim() to answer resend while the first is in flight, which the in-flight guard (resend-ledger.ts:80) refuses until the first settles, and a pause of at most 45 s can never outlive the 5-min TTL prune, so `resendWaits.get(key) !== wait` without disposed is unreachable in production — SAFE. (e) post-pause settle (3143) precedes the re-probe (3144), resurrect (3145), and controlDirective (3148); any throw there happens with the entry already settled — SAFE. No unsettled path; the thread's audit (discussion_r4012953905, implementer reply 4013004767) is accurate.",
    "5_scope": "jj diff --from cb87231d --to ad7d2444 --summary (cb87231d = fork_point(main@origin | ad7d2444) = the PR base) -> A .legion/{implement,review,test}.json, M packages/daemon/src/daemon/AGENTS.md, M __tests__/processes.test.ts, M __tests__/resend-ledger.test.ts, M processes.ts, M resend-ledger.ts — nothing else; GitHub's file list is exactly those eight. The literal `jj diff --from main@origin --to ad7d2444` additionally lists packages/contracts/package.json, packages/dispatch/**, packages/envoy-plugin/package.json, packages/pi-envoy/package.json: those are main's own commits since the fork point (#1116 129c4ee5, #1118 6042e1c4, and the two release commits e7ee6469 / fef7803e) shown in reverse, not files this PR touches. `jj file list -r ad7d2444 .omp` -> nothing. Round 1's own delta (53ec3879 -> ad7d2444) is processes.ts + processes.test.ts (+ .legion handoffs).",
    "6_pr": "legion gh -- pr view 1120 --json headRefOid,mergeable,mergeStateStatus -> 8c771678200f6a95a71eecd73d2e2a08d698c213 mergeable=MERGEABLE state=BLOCKED (awaiting review; not CONFLICTING). Tests run 34941076958: completed success at ad7d2444 (lint, typecheck, test); successor Tests run 34941286722: completed success at 8c771678 (lint, typecheck, test); PR Title run 34941286686 success.",
    "7_prBody": "`legion gh -- pr edit 1120 --body-file …` accepted for the review App. Added one `E2E (tester)` block immediately before `**Fast-follow:**`; the rest of the body is verbatim (the pre-edit body with the block spliced in compares equal to the post-edit body read back, modulo GitHub's one appended trailing newline). Not posted as a comment."
  },
  "proof": [
    {
      "criterion": "Review round 1 (thread discussion_r4012953905): a pre-pause liveness probe that throws must not leave the ResendLedger entry in flight — the throw is logged once by handleException's existing line, the entry is settled, and the next exception for the same message is a normal attempt (attempt 2 with a 15 s pause; settle refunds no budget), never dropped as in-flight.",
      "surface": "The packages/daemon unit and integration suite (fake tmux runtime whose `run` answers the first `list-panes` with exitCode 1 / 'tmux: server not responding', injected manual clock) plus PR #1120's CI job `test`. No smoke rig, stand-in or scratch daemon, throwaway broker, or scratch tmux server was run, and the production daemon was never restarted, signalled, or edited (Sami's standing order, 2026-09-13); no journal read was required. The production proof — the daemon journal's re-sending:cap ratio after the operator's next restart — is owned by LEGION-101.",
      "command": "cd packages/daemon && bun test src/daemon/__tests__/processes.test.ts -t \"a pre-pause probe that throws settles the ledger entry\"; CI Tests run 34941076958 (ad7d2444) and 34941286722 (8c771678)",
      "observed": "1 pass / 0 fail. Read: after exception 1 with the throwing probe — clock.pending [], publications [], controlRequests [], tree {generation: 1, status: active}, exactly one 'failed to recover legion-omp-legion-42-architect after a delivery exception' line; exception 2 arms exactly [15000], fired, zero 'arrived while its re-send is pending' lines, one 're-sending event' line containing 'attempt 2 of 3', publications 1, controlRequests 1. Source: processes.ts 3121-3127 wraps the pre-pause probe in try/catch calling resendLedger.settle(key) before rethrowing. CI job test success at both SHAs.",
      "headSha": "ad7d2444da88195572fc8b0a1c515ff272f0f730",
      "negativeControl": "The same lock's second exception is judged `attempt 2 of 3` with a 15 s pause — not `attempt 1` with 5 s — so settle() reopens the chain without refunding the claim the thrown probe consumed; the budget still bounds the chain at three."
    },
    {
      "criterion": "Regression: spec v7's kept-until-TTL rule and every earlier LEGION-107 lock hold with the settle-on-throw fix in place — capped fifth/sixth failures arm nothing, probe nothing, send nothing, log nothing; a seventh after a whole quiet TTL arms 5 s; P14 both variants; receipt_timeout on a live process logged once; in-flight drop uncounted (P7); root dying during the pause resurrected (P8); dispose() cancelling a pending pause (P9); worker-queued once per enqueue and only for an assignment; dedupe_key only when given (I1); three-reason allow-list (E1/E2).",
      "surface": "packages/daemon unit suite plus CI. No smoke rig (Sami, 2026-09-13).",
      "command": "cd packages/daemon && bun test src/daemon/__tests__/resend-ledger.test.ts; bun test src/daemon/__tests__/processes.test.ts -t \"later delivery_failed exceptions for a capped message\"; -t \"integration: bounded re-sends\"; -t \"receipt_timeout\"; -t \"resurrects a dead root architect on a\"; -t \"re-send\"; -t \"worker-queued\"; -t \"dispose\\(\\) cancels a pending re-send pause\"; bun test src/daemon/__tests__/index.test.ts -t \"envoyPublishBody\"; bun test src/daemon/__tests__/events.test.ts -t \"parses a receipt_timeout exception|ignores an exception whose reason is not one of the three known ones\"",
      "observed": "6 pass; 1 pass; 2 pass; 5 pass; 2 pass; 9 pass; 5 pass; 1 pass; 1 pass; 2 pass — 0 fail everywhere. Source audit (checks.4_audit): no path between claim() -> resend and settle() is left unsettled; the one non-settling early return is reachable only after dispose() cleared the ledger.",
      "headSha": "ad7d2444da88195572fc8b0a1c515ff272f0f730",
      "negativeControl": "The capped-message lock's fifth (+1 s) and sixth (+61 s) failures leave clock.pending [], publications 3, controlRequests 3, console.error count and list-panes count unchanged, while its seventh after RESEND_LEDGER_TTL_MS+1 arms exactly [5000] — the ledger forgets after silence, never silences forever; and events.test.ts E2: reason \"timeout\" reaches no recovery."
    }
  ],
  "e2e": "Surfaces: the packages/daemon unit and integration suite at code head ad7d2444da88195572fc8b0a1c515ff272f0f730 plus PR #1120's CI (Tests run 34941076958 at ad7d2444; run 34941286722 at the tip 8c771678). No smoke rig, stand-in or scratch daemon on any port, kind cluster, throwaway NATS container, or scratch tmux server was run, and the production daemon was not restarted, signalled, or edited, per Sami's standing order of 2026-09-13; no journal read was required for this round (the unit tests are the proof). The production proof — the journal's re-sending:cap ratio at most 3:1 per chain with no `attempt 1` inside five minutes of a cap line for the same message — is owned by LEGION-101 after the operator's next restart. The same evidence is in the PR body's `E2E (tester)` block.",
  "observations": [
    "The `resendWaits.get(key) !== wait` guard at processes.ts:3141 is, in production, only ever true together with `this.disposed` (see checks.4_audit); it is defence in depth, not a live path, and the comment above it ('only the current arm acts') is accurate for the dispose case. Not a finding.",
    "The literal `jj diff --from main@origin` scope check is misleading whenever main has advanced past the PR's fork point (it did here: #1116, #1118, two release commits); compare from `fork_point(main@origin | <code head>)` or GitHub's file list instead. Same artifact as in round 1 at #1085.",
    "PR body edit by the review App succeeded (`legion gh -- pr edit` on a PR opened by the implement App); no comment fallback was needed."
  ],
  "learningsInjected": [],
  "learningsHelpful": [],
  "schemaVersion": 1,
  "phase": "test",
  "completed": "2026-09-15T07:26:42.687Z"
}