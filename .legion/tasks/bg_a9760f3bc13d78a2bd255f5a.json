{
  "id": "bg_a9760f3bc13d78a2bd255f5a",
  "status": "completed",
  "agent": "oracle",
  "model": "anthropic/claude-opus-4-6",
  "description": "Cross-family review of aws-infra implementation",
  "parentSessionID": "ses_27d395b17ffeIyN62znKCf9w6q",
  "createdAt": 1776016048111,
  "sessionID": "ses_27d31c00dffeQw6VVuTjq0iP2W",
  "completedAt": 1776016117885,
  "result": "\n\nI'll review the implementation against the spec. Let me first examine the actual code to verify the diff summary is accurate.\n\nNow let me get the full index.ts to see the exports and the SSM inline policy resource block completely:\n\nNow let me check the AGENTS.md update:\n\nNow let me check the managed policy ARN format — there's a potential issue I spotted:\n\nI've now reviewed the full implementation. Here's my assessment:\n\n---\n\n## Review: Spec Compliance ✅, Code Quality ⚠️ (one bug, a few concerns)\n\n### Spec Compliance Checklist\n\n| Acceptance Criterion | Status | Notes |\n|---|---|---|\n| 3 permission sets: Administrator, Developer, ReadOnly | ✅ | Lines 29-90 |\n| Administrator: AdministratorAccess, 1hr session | ✅ | Lines 29-40 |\n| Developer: PowerUserAccess + SSM inline | ✅ | Lines 43-76 |\n| SSM inline has StartSession, TerminateSession, ResumeSession | ✅ | Lines 67-69 |\n| ReadOnly: ReadOnlyAccess, 8hr session | ✅ | Lines 79-90 |\n| 2 groups: Admins, Engineers | ✅ | Lines 96-106 |\n| At least one user per group via GroupMembership | ✅ | Lines 116-159, config has placeholder emails |\n| Account assignments correct | ✅ | Lines 166-193 |\n| AGENTS.md updated | ✅ | Line 73 |\n\n### Missing from spec: Engineers → Staging → Developer\n\nThe spec says:\n> Engineers → Dev/**Staging** → Developer\n\nThe implementation only has `engineers-dev-developer` (line 176) and `engineers-prod-readOnly` (line 186). **There is no staging account assignment.** The config (`Pulumi.prod.yaml`) has no `stagingAccountId` either.\n\nThis could be intentional if staging doesn't exist yet, but the spec explicitly lists it. **Must clarify or add.**\n\n### Bug: Managed Policy ARN Format\n\n**Line 39:** `\"arn:aws:iam::aws:policy/AdministratorAccess\"` — this has a **double colon** between `iam` and `aws`. The correct ARN format is:\n\n```\narn:aws:iam::aws:policy/AdministratorAccess\n```\n\nWait — actually, this IS correct. AWS managed policy ARNs have an empty account field: `arn:aws:iam::aws:policy/...`. The double colon is the empty account-id partition. **Not a bug.** (I initially misread it as a typo, but the format `arn:partition:service:region:account:resource` with empty region and empty account for global IAM policies is `arn:aws:iam::aws:policy/...` — hmm, actually that's wrong too.)\n\nLet me be precise. The correct ARN for AWS managed policies is:\n\n```\narn:aws:iam::aws:policy/AdministratorAccess\n```\n\nThe format is `arn:aws:iam::<empty-account>:aws:policy/...` — no, that's not right either. The actual correct format is:\n\n**`arn:aws:iam::aws:policy/AdministratorAccess`** — where the account field is literally the string `aws` (not empty). So the ARN should be `arn:aws:iam::aws:policy/AdministratorAccess` with exactly one empty field (region) between `iam` and `aws`.\n\nLooking at line 39: `\"arn:aws:iam::aws:policy/AdministratorAccess\"` — this has `iam::aws` which is `iam` + empty region + `aws` as account. **This is correct.** The same applies to lines 53 and 89. No bug here — I was overthinking it.\n\n### Actual Issues\n\n**1. Must fix: Missing staging account assignment**\n\nThe spec says Engineers get Developer on dev/staging. Only dev is assigned. Either add a `stagingAccountId` config + assignment, or document why staging is excluded.\n\n**2. Must fix: `ec2:DescribeInstances` in SSM inline policy is redundant**\n\n`PowerUserAccess` already grants `ec2:DescribeInstances`. The inline policy adds it again (line 70). This is harmless but misleading — it suggests the managed policy doesn't cover it. **Remove `ec2:DescribeInstances` from the inline policy** to avoid confusion about what PowerUserAccess provides.\n\nActually — wait. `PowerUserAccess` explicitly **denies** IAM and Organizations actions but allows everything else including EC2. So `ec2:DescribeInstances` is indeed redundant. However, the spec only asks for the three SSM actions. The extra action is scope creep. **Remove it.**\n\n**3. Consider: `Resource: \"*\"` on SSM actions**\n\nThe SSM inline policy uses `Resource: \"*\"` (line 72). For `ssm:StartSession`, AWS supports resource-level permissions (you can scope to specific instance ARNs or SSM document ARNs). With `Resource: \"*\"`, developers can start sessions to any instance in the account. This is probably fine for dev, but worth noting as a future tightening opportunity. **Not blocking.**\n\n**4. Consider: `forEach` with index-based naming**\n\nLines 116 and 139 use `forEach((email, idx) => ...)` with resource names like `admin-member-${idx}`. If the email list order changes in config, Pulumi will destroy and recreate memberships unnecessarily. A more stable naming approach would use a sanitized email:\n\n```typescript\nconst safeName = email.replace(/[^a-zA-Z0-9]/g, \"-\");\nnew aws.identitystore.GroupMembership(`admin-member-${safeName}`, ...);\n```\n\nThis prevents churn on reorder. **Recommend fixing before first deploy.**\n\n**5. Consider: No `dependsOn` for policy attachments**\n\nThe `ManagedPolicyAttachment` and `PermissionSetInlinePolicy` resources don't explicitly depend on their parent `PermissionSet`. Pulumi usually infers this from the `permissionSetArn` reference, so this should be fine. Just noting it's correct as-is.\n\n**6. Info: SAML runbook is solid**\n\nThe runbook covers Enable → SAML → SCIM → Verification with appropriate warnings about token expiry and region lock. The note that Google SCIM doesn't sync groups (line 96 of runbook) correctly explains why groups are in Pulumi. Good.\n\n### Summary\n\n| Priority | Finding |\n|---|---|\n| **Must fix** | Missing staging account assignment (spec says dev/staging) |\n| **Must fix** | Remove `ec2:DescribeInstances` from inline policy (redundant with PowerUserAccess, not in spec) |\n| **Recommend** | Use email-based resource names instead of index-based for GroupMembership stability |\n| **Note** | `Resource: \"*\"` on SSM is fine for now, tighten later |\n| **Pass** | Everything else matches spec, Pulumi code is idiomatic, SAML runbook is thorough |"
}