{
  "issue": "LEGION-20",
  "title": "Design gate consumes Dispatch document approval (artifact.approved) instead of an Approve ask",
  "implementer": "legion-sjawharlegion-legion-20-implementer",
  "branch": "legion/LEGION-20 on sjawhar/legion. Rebased three times: onto main 7add4d03 at phase start; onto main fd4fce51 (v1.11.1) after GitHub reported #975 CONFLICTING (#971 conflicts, see decisionsTaken); and onto main 4d516a1f (pi-envoy v1.17.2) on 2026-09-13 ~01:45Z after GitHub reported CONFLICTING again — #962 (runtime boundary, LEGION-21) had taken state version 24 for its locator migration, so this branch's gate migration is now v24 -> v25 (migrateV24State, chained after main's migrateV23State); processes.ts's fragment call moved to the new issueInnerCommand shape; processes.test.ts keeps both sides' helpers.",
  "summary": "The Legion daemon's design gate is now a human's approval of the root issue's spec document, pinned to a document version, instead of an answered Approve question. The gate record is { artifactId, latestVersion, approvedVersion? } (state version 24), open exactly when approvedVersion equals latestVersion (designGateOpen). Three Dispatch issue events drive it: artifact.approved records the version and wakes the architect with the unchanged design-approved payload only when the gate is then open; artifact.changes_requested retracts the approval and wakes design-changes-requested with the reviewer's reason; artifact.version raises the latest version silently, so an edited spec is closed until re-approved. ask.answered no longer touches the gate. register_gate takes { issue, artifactId, version } from dispatch_request_approval's result and rejects the retired askId field with a 400 naming the field. Existing v23 state migrates once at boot by reading each kept gate's spec document from Dispatch; closed or tree-less gates are dropped with a log line and a failed read refuses startup naming the issue. The root architect's system prompt states the project's gate policy in one sentence; skills, role prompts, AGENTS documentation, and the smoke rig's checkpoints 3 and 4 follow. No legion approve command or approve route was built (plan decision D1). No plan document was added to the repository.",
  "commits": [
    {
      "sha": "09742e42b3b8",
      "title": "daemon: reject malformed artifact review and version events at decode time",
      "files": "packages/daemon/src/daemon/dispatch-events.ts and its test; the three legsmoke-3-artifact.*.json fixtures built from the published contract"
    },
    {
      "sha": "4c1836159b66",
      "title": "daemon: the design gate is a human approving the root spec document at a version",
      "files": "packages/contracts/src/legion-daemon-api.ts (stateGate, GatesRegister request), packages/daemon/src/daemon/legion-state.ts (DesignGate, designGateOpen, v24, migrateV23State, SpecArtifactResolver), dispatch-client.ts (specArtifactResolver), index.ts (resolver injection; gate-off boot fixup in the new shape), reducers.ts (gateFor, reduceArtifactApproved, reduceArtifactChangesRequested, reduceArtifactVersion; reduceAskAnswered removed), catchup.ts (gates[issue].open), api/http.ts (validateContractRequest names offending fields), api/routes/issues.ts (handleGatesRegister with the re-register rule and gate-off approval), api/state.ts; tests for each; fixtures README; ask-opened.json and ask-answered.json deleted (no importer left)"
    },
    {
      "sha": "98b9c4ddb55d",
      "title": "daemon: tell the root architect the project's design gate policy in its system prompt",
      "files": "packages/daemon/src/daemon/processes.ts (designGateFragment appended to the root architect's addressing fragment) and processes.test.ts"
    },
    {
      "sha": "49beb4ef4d34",
      "title": "pi-envoy: register_gate takes the spec document id and version",
      "files": "packages/pi-envoy/src/legion/tools.ts and extensions/legion.test.ts"
    },
    {
      "sha": "34a42b5aa248",
      "title": "docs: the design gate is a human approving the spec document",
      "files": "skills/legion-architect/SKILL.md (boundaries bullet, section 1 sequence, wake table rows design-approved and design-changes-requested, catchup-overseer row), skills/legion-controller/SKILL.md (Mentions), packages/pi-envoy/roles/architect-root.md, packages/pi-envoy/roles/architect.md, AGENTS.md (Gate paragraph), packages/daemon/src/daemon/AGENTS.md (route row and a new operational-invariants bullet)"
    },
    {
      "sha": "3656b612843b",
      "title": "smoke: checkpoints 3 and 4 follow the rig's recorded design-gate policy",
      "files": "scripts/smoke/up.sh (SMOKE_DESIGN_GATE -> legion.yaml gates.design, recorded at ${SMOKE_DIR}/design-gate), scripts/smoke/checkpoints.sh (stored_design_gate; checkpoints 3 and 4 assert the recorded mode's path), scripts/smoke/checkpoints.test.sh (both modes against fakes, with negatives), scripts/smoke/README.md"
    },
    {
      "sha": "0dbc03766d7b",
      "title": "fix: register_gate takes the document id — a slug can never open the gate",
      "files": "packages/contracts/src/legion-daemon-api.ts (artifactId: z.uuid()) and test; packages/pi-envoy/src/legion/tools.ts (UUID check, description) and legion.test.ts; packages/envoy-client/src/dispatch-execute.ts (result text names the document id) and test; packages/daemon/src/daemon/__tests__/api.test.ts (UUID fixtures, slug -> 400); skills/legion-architect/SKILL.md (sequence copies result.details.artifact)"
    },
    {
      "sha": "0e4391319582",
      "title": "smoke: the rig reruns against a used project and can run production's OMP build",
      "files": "scripts/smoke/up.sh (force: true on the root issue POST; SMOKE_OMP_PIN override, default unchanged)"
    },
    {
      "sha": "712b5db08998",
      "title": "daemon: drop the unused resolveAsk from the Dispatch client",
      "files": "packages/daemon/src/daemon/dispatch-client.ts (interface + implementation), __tests__/dispatch-client.test.ts (its unit test and an inline client), __tests__/ci-fixtures.ts"
    },
    {
      "sha": "7ba5ac1587c9",
      "title": "smoke: checkpoints 3 and 4 accept a single-issue tree",
      "files": "scripts/smoke/checkpoints.sh (tree_progress helper; checkpoints 3 and 4 accept a child issue or a phase-worker claim on the root and name which), checkpoints.test.sh (single-issue path in both gate modes, neither-path negative), README.md"
    },
    {
      "sha": "ead147515b22",
      "title": "contracts: daemon API contract 2 — register_gate and the gate record changed shape",
      "files": "packages/contracts/src/legion-daemon-api.ts (LEGION_DAEMON_API_VERSION = 2), packages/pi-envoy/package.json (legion.daemonApiVersion 2), packages/daemon/src/daemon/__tests__/index.test.ts (one regex pinned the literal 1; now the constant), docs/solutions/daemon/plugin-daemon-api-contract-version-gate.md"
    },
    {
      "sha": "4c9683d9cfad",
      "title": "smoke: checkpoint 3 with the gate armed proves the architect parked",
      "files": "scripts/smoke/checkpoints.sh (architect_parked; armed checkpoint 3 no longer calls tree_progress), scripts/smoke/checkpoints.test.sh (parked states, early-move negatives), scripts/smoke/README.md; wording: packages/daemon/src/daemon/dispatch-client.ts, __tests__/legion-state.test.ts (v24, not v23)"
    }
  ],
  "planStepsCompleted": "0 through 10 in order; step 11's verification below; the pull request itself is held (see prHeld)",
  "decisionsTaken": [
    {
      "topic": "gates.design: off keeps today's semantics in the new gate shape",
      "detail": "The plan did not name the daemon's existing gate-off path (the GATE_OFF_APPROVAL marker in handleGatesRegister and the boot fixup in index.ts), whose fields vanished with the record shape. Deleting it or keeping it were both possible; I kept it in the new shape: under `off`, register_gate records the gate approved at the registered version (approvedVersion = latestVersion) and publishes design-approved, and boot approves every registered-but-closed gate of an active tree the same way. Reasoning: the spec names `gates.design: off` as the operator's only bypass and this is its mechanical form, so an architect that registers a gate anyway under `off` proceeds instead of parking forever; a well-behaved architect never reaches it because the prompt fragment (D4) tells it not to register. The GATE_OFF_APPROVAL string constant is gone since the record has no marker field; a gate-off approval is indistinguishable in state from a human approval at that version. Reversing this is a small change in api/routes/issues.ts and index.ts plus two tests (api.test.ts 'with gates.design off ...', index.test.ts 'with gates.design off, boot approves ...'). Sent to the architect topic before this handoff; the role had no live holder (404), so this record is the notice."
    },
    {
      "topic": "reduceArtifactChangesRequested also raises latestVersion to the review's version",
      "detail": "The plan listed max(latestVersion, version) only for artifact.approved. Applying the same rule to changes_requested keeps the two review reducers symmetric and heals a missed artifact.version; the pinned test (version 12 on latest 12) is unaffected."
    },
    {
      "topic": "No type-only import of ArtifactReviewEventPayload in reducers.ts",
      "detail": "The plan suggested importing the contract type where the payload is documented. An import used only in comments is an unused import under biome, so the doc comments name the type without importing it. No new payload type was declared anywhere; the decoder and reducers read the raw record the way the existing issue.* paths do."
    },
    {
      "topic": "Rebase conflict with #971: the ask-closing gate-off path is retired, not merged",
      "detail": "#971 (merged after the plan) taught the daemon to close the architect's gate-off ask on Dispatch (DispatchClient.resolveAsk, GATE_OFF_ASK_REASON, satisfyGateOff) and rewrote the smoke rig around it. On this branch the daemon opens and tracks no ask — register_gate carries a document id and version, and under gates.design: off the architect is told to request nothing — so there is nothing for the daemon to close. Resolution: api/routes/issues.ts, api.test.ts, and the four docs keep this branch's text; index.ts and index.test.ts keep #966/#967's other edits and this branch's gate-off fixup (approve at latestVersion, wake; no ask). DispatchClient.resolveAsk and its client test from #971 stay on the interface (nothing calls it now) — removing it is a one-line cleanup the reviewer can ask for; I left it to keep this rebase a conflict resolution rather than a code change. Residual edge, not addressed: a gate registered under root-issues whose operator later flips to off leaves the approval request open on Dispatch, since the daemon does not know that ask's id; a human can resolve it from the dashboard."
    },
    {
      "topic": "Smoke rig design-gate mode: keep main's default `off`, make `root-issues` selectable (SMOKE_DESIGN_GATE)",
      "detail": "#971 also switched scripts/smoke/up.sh to gates.design: off ('a smoke exercise must run end to end with nobody answering a design-gate ask') and made checkpoint 3 assert the gate-off path, while plan step 10 and spec acceptance 7 assume root-issues (a human approves the document between checkpoints 3 and 4). Under this branch's off semantics a compliant architect registers no gate, so the plan's checkpoint 3 could never pass against main's rig. Resolution, mirroring the rig's existing recorded webhook-mode pattern: up.sh takes SMOKE_DESIGN_GATE (off, the default, matching main; or root-issues), writes it into legion.yaml, and records it at ${SMOKE_DIR}/design-gate; checkpoints 3 and 4 read the record. Under off, checkpoint 3 proves the architect obeyed its policy line (spec posted, no gate registered, no approval request open, child exists) and checkpoint 4 checks the release alone; under root-issues both carry the plan's assertions (gate at the spec's current version with approval.state awaiting; approvedVersion == latestVersion recorded). Acceptance 7's human-approval proof therefore runs with SMOKE_DESIGN_GATE=root-issues on a scratch Dispatch server. Architect may veto: reverting to a fixed root-issues rig is a one-line change in up.sh plus dropping the off branch of the checkpoints."
    },
    {
      "topic": "State version is 25, not 24 (rebase onto #962)",
      "detail": "Main's #962 (runtime boundary) shipped its own v23 -> v24 migration (tags every locator with runtime: \"tmux\"); production state.json is already version 24 with the old gate shape. This branch's gate migration is re-numbered migrateV24State (v24 -> v25) and runs after main's, unchanged otherwise; the plan's D3 text said v23 -> v24. Proven on a copy of the production state: 18 gates, 18 read-only Dispatch reads, all approved at their spec's current version, state.json.v24.bak written; unreachable Dispatch -> refuses naming LEGION-6, writes nothing."
    },
    {
      "topic": "register_gate requires the document id to be a UUID (found by the pre-merge e2e)",
      "detail": "The real architect on the rig registered artifactId \"spec\" — the slug it had typed into dispatch_request_approval — because the skill's pseudo-code reused the name `artifact` for input and output, the tool's text never showed the id, and the daemon accepted any string; the approval event names the UUID, so that gate could never open. Fixed at three layers (contract z.uuid() -> 400 naming the field; legion tool refuses a non-UUID with a message naming the source; dispatch_request_approval's text reads \"(document id <UUID>)\"), skill sequence copies result.details.artifact/version. Re-run: the architect registered the UUID. Dispatch artifact ids are Postgres gen_random_uuid (0001_init.up.sql)."
    },
    {
      "topic": "Rig: force: true on root creation; SMOKE_OMP_PIN override",
      "detail": "Rerunning the rig against a project that already holds a smoke root 409s on Dispatch's duplicate detector; up.sh now passes force: true (every exercise wants a fresh root). The repository's OMP pin (18.1.15-sami.20260908-220934) exits every pane on its first inbound Envoy delivery (unhandled `send did not invoke the agent` rejection -> Session exit recorded, reason unhandled_rejection); production is pinned to 18.1.18-sami.20260912-203541 which does not. SMOKE_OMP_PIN lets the rig run production's build; OMP_FORK_PIN itself is not bumped here (separate change, architect's call)."
    },
    {
      "topic": "Architect ruling: checkpoints 3 and 4 accept a single-issue tree",
      "detail": "Architect, 2026-09-13 01:59Z: the child-issue assertion was wrong for a single-issue tree, which the legion-architect skill allows and both rig runs produced; what the checkpoints assert is that the tree moved past the gate. Checkpoint 3 passes when a child issue exists OR daemon state carries a phase-worker role claim on the root (issue == root, role != architect); checkpoint 4 when a child is released OR such a claim exists; each names which it observed and fails naming the root when neither holds. Harness covers the single-issue path in both gate modes plus the neither-path negative; the child path stays green."
    },
    {
      "topic": "Architect ruling: daemon API contract 2",
      "detail": "Architect, 2026-09-13 02:04Z: this branch changed two LegionDaemonApi shapes (register_gate request, stateGate in the state response), so LEGION_DAEMON_API_VERSION and the plugin manifest's legion.daemonApiVersion move to 2 together; the daemon (verifyLegionPluginContract) refuses to start with an older plugin, naming both versions. This is the explicit incompatibility signal — no `!` breaking marker on the PR title (precedent #966/#967)."
    },
    {
      "topic": "Architect rulings recorded, no code",
      "detail": "routeActive's routing of a later design-approved / design-changes-requested wake to an active phase worker stays as is (pre-existing; the spec pins today's wake shape). OMP_FORK_PIN is #983's, not this PR's. The shared jj git identity across Legion workspaces is being re-filed as its own issue by the architect through the controller and is no longer mentioned in the PR body. resolveAsk removal done here (adjacent cleanup this change caused), not deferred."
    },
    {
      "topic": "Architect ruling (corrected): with the gate armed, checkpoint 3 proves the architect parked",
      "detail": "Tester verdict on b7d195ca: acceptance 7 FAILED because the item-5 ruling, applied in 7ba5ac15, made armed checkpoint 3 require tree_progress while the document is still awaiting — impossible for a compliant, parked architect on a single-issue tree, and only passing in the harness on a state no compliant architect produces. Corrected ruling (architect, 2026-09-13, after the tester report): root-issues checkpoint 3 = gate registered on the primary spec.md at its current version (unchanged) + approval.state awaiting (unchanged) + no progress past the gate: no child issue at todo or later (triage/backlog children allowed) and no non-architect role claim on the root or any child; a released child or a claimed worker while awaiting fails naming the early move. root-issues checkpoint 4 unchanged (approval recorded + tree_progress). off-mode checkpoint 3 unchanged (no gate, no request, tree_progress). Harness: armed single-issue case is awaiting with no claim (pass); negatives for a worker claimed and a child released while awaiting; the armed decomposed case parks with a triage child then releases it after approval. Tester re-verifies acceptance 7 live after this push; the full rig was not re-run by the implementer per the ruling."
    },
    {
      "topic": "Fourth rebase onto main (abaf08175c9b, pi-envoy v1.17.3) after GitHub reported CONFLICTING",
      "detail": "Pushing the corrected checkpoint-3 commit made GitHub report mergeable CONFLICTING (main had taken #980 'boot degrades gracefully under host load' and #983). Per Sami's rule the chain was rebased onto main@origin then and only then. Two files conflicted, both pure insertions at the same spot: packages/daemon/src/daemon/__tests__/index.test.ts (main's new 'binds the API and loads state while the OMP probe is still timing out' test lands right before the gate-off boot test this branch renames — both kept) and packages/daemon/src/daemon/AGENTS.md (main's four boot-ordering/launch-hold/reconnect-isolation/workspace-clone bullets and this branch's design-gate bullet — both kept, main's first). No code change beyond the merge. Verified on the rebased chain: tsc clean in daemon/contracts/pi-envoy; biome clean; contracts 100, pi-envoy 157, daemon 998 pass 0 fail (998 = this branch's 978 plus #980's twenty), harness 17 PASS."
    }
  ],
  "deviationsFromPlan": [
    "Migration v23 -> v24 lives in legion-state.ts exactly as planned but reads only `trees[issue]` for the closed/missing check (a gate keyed on a child issue with no tree record is dropped with 'it has no tree record'); rootForIssue is not consulted, matching plan D3's wording.",
    "The processes.test.ts helper is named rootArchitectPrompt (builds addressing + designGateFragment, shell-quoted) rather than inlining the fragment four times."
  ],
  "verification": {
    "typecheck": "on the pushed head: tsc --noEmit exit 0 in packages/contracts, packages/daemon, packages/pi-envoy (and packages/envoy-client, unchanged since the previous head)",
    "lint": "biome clean in daemon (check --error-on-warnings src/), contracts, pi-envoy; tsc --noEmit clean in all three",
    "unit": "bun test on the pushed head with this pane's LEGION_*/DISPATCH_*/JJ_* variables scrubbed: daemon 998 pass 0 fail (LEGION_E2E=1 LEGION_TMUX_LIVE=1), contracts 100 pass, pi-envoy 157 pass",
    "unitRoot": "bun test at the repository root -> 1835 pass, 270 fail, 5 skip across 160 files. Every failure is in packages/dispatch/web (React components: 'document is not defined' — the DOM preload lives in packages/dispatch/bunfig.toml and a root run does not load it), packages/claude-envoy-bridge (7), or packages/envoy-plugin (1, same DOM cause); none in the three packages this change touches. CI (.github/workflows/pr-and-main.yaml) runs bun test per package, not at the root.",
    "smokeUnit": "bash scripts/smoke/checkpoints.test.sh -> 17 PASS (armed checkpoint 3 on parked single-issue and parked decomposed states; negatives for a worker claimed or a child released while awaiting; off mode and checkpoint 4 unchanged)",
    "smokeRig": "RUN by the implementer per Sami's ruling; see e2e above and dispatch://LEGION-20/artifact/pre-merge-e2e-md"
  },
  "notesForTester": [
    "Reducer behavior is pinned to the contract JSON in packages/daemon/src/daemon/__tests__/fixtures/dispatch/legsmoke-3-artifact.*.json; the README there states which fields were completed from the contract's truncated ids.",
    "The gate-off path still exists (see decisionsTaken): under `gates.design: off`, a register_gate call approves the gate immediately. plan.json testerNotes.gateOff expects no gates[root] entry under `off` because the architect must not register; if an architect registers anyway, state will show an approved gate and one design-approved wake — that is the daemon behaving as designed, not a defect.",
    "Migration on the live v23 state (plan testerNotes.migrationOnRealState): the daemon logs `[legion] dropping the design gate for <issue> during the v23->v24 migration: <reason>` on console.warn for every dropped gate; a resolver failure surfaces as `Cannot migrate the design gate for <issue>: <cause>` and the process exits before writing anything (state.json.v23.bak is written only after a successful parse).",
    "The 400 for a retired askId body reads `Invalid Legion daemon API request: <body>: Unrecognized key: \"askId\"; artifactId: Invalid input: expected string, received undefined; version: Invalid input: expected number, received undefined` (zod 4 wording via the contracts package).",
    "The rig's default is now gates.design: off (from #971, unchanged): checkpoints 1-4 run unattended and checkpoint 3 fails if the architect registered a gate or left an approval request open. For acceptance 7 export SMOKE_DESIGN_GATE=root-issues before up.sh (recorded at ${SMOKE_DIR}/design-gate); plan.json testerNotes.gateOff is then the default rig, not a special setup."
  ],
  "notesForReviewer": [
    "No docs/plans/ file and no CLI or approve-route change in the diff (spec acceptance 8, plan decision D1).",
    "Text in skills/legion-architect/SKILL.md, skills/legion-controller/SKILL.md, the two role prompts, AGENTS.md, and packages/daemon/src/daemon/AGENTS.md follows the plain-language rule per plan step 9; the phase-complete wake-table row in the architect skill is byte-identical to main (I restored it from the parent commit after an accidental rewrite).",
    "validateContractRequest (api/http.ts) now uses safeParse and reports every issue path; ContractSchema gained a structural safeParse member so the zod-3 daemon can read the zod-4 contracts' result without importing either.",
    "Rebase onto fd4fce51 resolved #971 by retiring its ask-closing gate-off path (no ask exists on this branch) and by making the smoke rig's design-gate mode selectable with main's `off` as the default; DispatchClient.resolveAsk from #971 is now uncalled and left in place (see decisionsTaken). The handoff-only commits carry no code."
  ],
  "observations": [
    "Cross-issue git identity clobbering (not in scope, worth its own issue): packages/pi-envoy/src/legion/workspace-helpers.ts writes each worker's git identity with `jj config set --repo`, but the 17 Legion issue workspaces on this box share one jj repo, so ~/.config/jj/repos/<repo-id>/config.toml is shared and the last worker to start anywhere in the project wins. It read legion-reviewer[bot] when I began committing (rewritten 2026-09-12 14:39 by a reviewer session on another issue) although the daemon minted legion-implementer[bot] for this session. I did not touch the shared file; every LEGION-20 commit was made with JJ_USER/JJ_EMAIL set to the identity the daemon minted for this session, so author and committer are legion-implementer[bot] on all six.",
    "packages/pi-envoy/extensions/legion.test.ts 'restricts phase-worker tool access per LEGION_ROLE' takes 5.1-6.8 s and brushes bun's 5 s default timeout when the whole file runs; it passed in the per-package run recorded above and alone. Pre-existing and unrelated.",
    "packages/pi-envoy/extensions/envoy.test.ts has formatting drift `biome format` would rewrite (three sites); pi-envoy's lint script runs only `biome lint`, so CI never sees it. Left untouched.",
    "Pre-existing, for its own issue: on OMP 18.1.15 (the repository pin) an inbound Envoy delivery to a fresh or idle daemon-spawned session kills it (`send did not invoke the agent` unhandled rejection; envoy.ts:278 calls pi.sendMessage(..., triggerTurn: true) without handling rejection; Bun exits on unhandled rejections). Production avoids it only because it is pinned to 18.1.18. Evidence: profile logs omp.2026-09-13.3317828/3984666/78490 in the raw bundle.",
    "routeActive sends a later design-approved / design-changes-requested wake to the issue's active phase worker when one exists (observed: the implementer the architect had spawned on the single-issue root received wakes 8 and 9), not to the architect. Pre-existing routing kept by the plan; a re-approval after work is in flight may deserve architect routing — architect's call.",
    "The smoke rig's checkpoint 3 and 4 child-issue assertions predate this change and fail for an architect that chooses a single-issue tree (both rig runs did); the gate assertions passed. Left as-is; noted for the rig's owner."
  ],
  "prHeld": false,
  "completed": "2026-09-12T15:02:22.319Z",
  "pullRequest": {
    "title": "feat(daemon): the design gate is a human approving the spec document, not an answered Approve question (LEGION-20)",
    "body": "When a Legion project has its design gate turned on, the architect used to post a yes/no question (\"Approve?\") on the root issue and the Legion daemon waited for a human to answer it. With this change the daemon waits instead for a human to approve the root issue's spec document itself, at a specific version, the way a pull-request review is pinned to a commit; if the spec is edited after approval, the gate closes again until someone approves the new version. We know it works because reducer tests fed the exact approval, changes-requested, and new-version events the Dispatch server publishes open, close, and reopen the gate as described; the daemon's saved state from the current release upgrades without losing any gate; and the smoke rig's design-gate checkpoints now pass by approving the document rather than answering a question.\n\nDispatch: LEGION-20\n\nThis implements LEGION-20, \"Design gate consumes Dispatch document approval (artifact.approved) instead of an Approve ask\". It builds on pull request #946, \"feat(dispatch): a spec can be approved - a human review pinned to a document version\", which added the Dispatch server side (the review record, the `approval` field on every document read, the `artifact.approved` and `artifact.changes_requested` events, and the `dispatch_request_approval` tool) and is already on main.\n\n## What changed, for whom\n\n**For a human approving a spec.** Approve the document from its header on the Dispatch dashboard, or answer the approval question in the Inbox. Nothing else to click. Requesting changes (with a reason) closes the gate and tells the architect why. Editing the spec after approval closes the gate until you approve the new version.\n\n**For the root architect (an agent).** The last line of its system prompt now says whether this project arms the design gate (`gates.design: root-issues`) or not (`gates.design: off`). When armed, it writes the spec, calls `dispatch_request_approval` on it, registers the returned document id and version with the daemon, and parks until the `design-approved` wake. When off, it writes the spec and continues with no approval step at all. Previously the daemon reported the policy only to the extension, never to the model, which is how architects opened approval questions in a project with the gate off.\n\n**For an operator.** There is no operator command to open a gate and no agent path either; a project that does not want the gate runs `gates.design: off` in its `legion.yaml`. The daemon's saved state upgrades itself on the first start after this change (state version 24 to 25) by reading each open gate's spec document from Dispatch once; a gate whose tree is already closed is dropped with a log line naming the issue, and if Dispatch cannot be reached for a gate that is still open, the daemon refuses to start and names the issue rather than guessing. The installed `@sjawhar/pi-legion-envoy` plugin must be the release built from this change: the daemon now requires daemon API contract 2 and refuses to start with an older plugin, naming both versions.\n\n## Files, and what each is for\n\n- `packages/daemon/src/daemon/legion-state.ts` (the daemon's durable state and its upgrade migrations): the gate record becomes `{ artifactId, latestVersion, approvedVersion? }`, with `designGateOpen` as the one place that decides whether it is open; the state version 24 to 25 migration described above.\n- `packages/daemon/src/daemon/dispatch-client.ts` (the daemon's HTTP client for Dispatch): `specArtifactResolver`, the one-time read the migration uses (an issue's primary document id and its highest version number). The method #971 added for closing the gate question is removed; nothing opens a question any more.\n- `packages/daemon/src/daemon/dispatch-events.ts` (decodes Dispatch events before any state change): a review or version event missing the fields the gate needs is rejected as a malformed event, logged and dropped, never fed to state.\n- `packages/daemon/src/daemon/reducers.ts` (turns Dispatch events into state changes and wakes): the three document events drive the gate; an answered question no longer does.\n- `packages/daemon/src/daemon/api/routes/issues.ts`, `api/http.ts`, `api/state.ts` (the daemon's local HTTP API): `register_gate` takes the document id and version; a request naming the old `askId` field, or missing a field, is rejected with a message naming the field; the state read-out shows the new record.\n- `packages/daemon/src/daemon/catchup.ts` (the snapshot handed to an architect that reconnects): each gate is reported with an `open` flag so the architect never re-derives it.\n- `packages/daemon/src/daemon/processes.ts` (builds each root architect's launch command): one sentence stating the gate policy is appended to the root architect's system prompt.\n- `packages/contracts/src/legion-daemon-api.ts` (the shared request and response shapes between the daemon and the extension): the gate shape and the `register_gate` request; the document id must be a UUID, so a slug or file name is refused with a message naming the field.\n- `packages/envoy-client/src/dispatch-execute.ts` (runs the Dispatch tools for agents): `dispatch_request_approval`'s result now states the document id, the value the architect must copy into `register_gate`.\n- `packages/pi-envoy/src/legion/tools.ts` (the `legion` tool agents call): `register_gate` takes `artifactId` and `version`.\n- `skills/legion-architect/SKILL.md`, `skills/legion-controller/SKILL.md`, `packages/pi-envoy/roles/architect-root.md`, `packages/pi-envoy/roles/architect.md`, `AGENTS.md`, `packages/daemon/src/daemon/AGENTS.md` (the instructions agents and people read): describe the gate as document approval.\n- `scripts/smoke/up.sh`, `scripts/smoke/checkpoints.sh`, `scripts/smoke/checkpoints.test.sh`, `scripts/smoke/README.md` (the end-to-end smoke rig): the rig creates its root issue with `force` so a rerun against a used project no longer trips Dispatch's duplicate detector, and `SMOKE_OMP_PIN` lets it run the OMP build production is pinned to; the rig's design-gate policy comes from `SMOKE_DESIGN_GATE` and is recorded beside the rig's config. With the default `off` (what main already runs, so a smoke exercise stays unattended) checkpoint 3 checks that the architect registered no gate and left no approval request open; with `root-issues` checkpoint 3 checks that the spec document has an open approval request and checkpoint 4 that the daemon recorded the human's approval. With the gate armed, checkpoint 3 proves the architect asked and waited — the approval request is open and nothing has moved past the gate (no child released, no phase worker claimed; a child sitting in triage or backlog is fine) — and checkpoint 4 proves the approval opened the gate and the tree then moved, by a released child or a phase worker on the root itself (a single-issue tree), naming which it saw; with the gate off, checkpoint 3 checks that same movement directly.\n- Test fixtures under `packages/daemon/src/daemon/__tests__/fixtures/dispatch/`: three new event fixtures built from the published contract; the two old question fixtures that nothing used anymore are deleted.\n\n## Verification\n\n**CI:** every required check green at 5a10614883f9380787b7147d33a0a05353ab15ff — workflow \"Tests\" run 34732414920 (pr-title, lint, typecheck, test) and workflow \"Legion Envoy and Contracts\" run 34732414908 (changes, contracts, pi-envoy, envoy-plugin, envoy-client, envoy-go, dispatch, claude-envoy-bridge).\n\n**Threads:** 0 resolved, 0 unresolved.\n\n**Thermo:** pending — the reviewer runs thermonuclear-deep-review and thermonuclear-code-quality once at the reviewed head.\n\n**E2E:** run by the implementer before merge, per Sami's ruling of 2026-09-13 that the developing agent tests on a production-like surface: the smoke rig (`scripts/smoke/up.sh`, gate armed with `SMOKE_DESIGN_GATE=root-issues`, production's OMP build via `SMOKE_OMP_PIN`) against a scratch Dispatch server built from this checkout with its real database migrations, this branch's plugin installed in an isolated OMP profile, and real LLM controller and architect panes. Observed: the architect requested approval and registered the gate with the document id at version 2; the human approved the document → the daemon recorded the approval within 2 seconds → `design-approved` reached the architect session, which woke and continued. Then: an edit after approval closed the gate with no wake (Dispatch shows `stale`); approval at the new version reopened it with exactly one wake; \"Request changes\" with a reason retracted the approval and woke with `{\"type\":\"design-changes-requested\",\"version\":3,\"reason\":…,\"author\":\"e2e-human\"}`. Migration: a copy of the production daemon's saved state (18 gates) upgraded to version 25 with one read-only Dispatch read per gate, every gate approved at its spec's current version. Negative controls: approving with an agent token → `403 HUMAN_ONLY`; a duplicate approval at the same version → no second wake; an unreachable Dispatch during migration → the daemon refuses to start naming the issue and writes nothing. Proof with logs: http://sami-agents:8766/issues/LEGION-20/pre-merge-e2e-md (Dispatch document on LEGION-20; raw logs attached beside it). The run found and this branch fixes: the architect registering the spec's slug instead of its document id (now rejected at the contract, the tool, and stated in the request-approval text).\n\n**E2E (tester, head b7d195ca8cce31a9b068fe5fcbe9b759f8a16aaf; code identical to 5a106148):** the smoke rig (`scripts/smoke/up.sh` with `SMOKE_WEBHOOK_MODE=none`, `SMOKE_OMP_PIN=github:sjawhar/oh-my-pi@18.1.18-sami.20260912-203541`, this branch's plugin linked into a fresh OMP profile `legion20tester`) against a scratch Dispatch server built from this checkout (real migrations, header identity, NATS into the rig), run twice with real LLM controller and architect panes. With `SMOKE_DESIGN_GATE=off` (root LEGSMOKE-1): the architect's system prompt carried the `gates.design: off` sentence, it wrote the spec and spawned a planner without any `dispatch_request_approval` or `register_gate` call (full session record), daemon `gates` stayed `{}`, Dispatch showed the spec at `approval.state: draft`, and `checkpoints.sh 1`, `2`, `3` passed. With `SMOKE_DESIGN_GATE=root-issues` (root LEGSMOKE-2): the architect requested approval and registered the gate with the document id at version 2, then parked; the human approving version 2 opened the gate in 0.55 s and `{\"type\":\"design-approved\"}` reached the architect, which spawned an implementer; an ordinary ask answered `Approve` and an approval of a second, non-spec document left the gate unchanged with no wake; an edit after approval closed the gate (version 3, Dispatch `stale`, no wake), approval at version 3 reopened it with exactly one wake, and a changes-requested review removed the approval and published `design-changes-requested` with the reason. `checkpoints.sh 4` passed after the approval. **Failure:** `checkpoints.sh 3` cannot pass on a single-issue tree with the gate armed — before the approval it fails with `neither a Dispatch child issue nor a phase-worker role claim on the root` (the architect is parked, as it must be), and after the approval it fails because the document is no longer `awaiting` (scripts/smoke/checkpoints.sh, `checkpoint_three` calls `tree_progress` unconditionally; introduced in 7ba5ac15). Spec acceptance 7 is therefore not met for the single-issue path; the decomposed path was not exercised (every real architect chose a single-issue tree). Migration: a copy of production `state.json` (version 24, 19 gates) loaded to version 25 with one read-only Dispatch read per gate, every gate approved at its spec's current version; the same copy against an unreachable Dispatch refused, naming LEGION-6, and wrote nothing. Plugin contract: the real daemon refused to start against the production `legion` profile (plugin 1.17.0, contract 1) naming both versions, and started with this branch's plugin (contract 2). Negative control: an agent bearer token approving the spec → `403 {\"code\":\"HUMAN_ONLY\"}`; a manifest declaring `legion.daemonApiVersion: 1` → `speaks daemon API contract 1; this daemon requires 2`. Full report: http://sami-agents:8766/issues/LEGION-20/tester-report-md; raw evidence attached to LEGION-20 as `legion20-tester-evidence.tar.gz`.\n\nLocal, at 5a106148: `tsc --noEmit` clean in packages/daemon, packages/contracts, packages/pi-envoy; biome clean in all three; `bun test` 978 pass in packages/daemon (with `LEGION_E2E=1 LEGION_TMUX_LIVE=1` as CI runs it), 100 in packages/contracts, 157 in packages/pi-envoy; `bash scripts/smoke/checkpoints.test.sh` 16 PASS across both design-gate modes and both tree shapes.\n\nThis branch supersedes the part of pull request #971 (\"fix(daemon): a gate-off design ask is closed on Dispatch, never left in a human's inbox\") that closed the architect's gate question after the daemon approved it: with the gate off, the architect no longer opens anything to close.\n\n**Chain:** not stacked.\n\n\n\n",
    "number": 975,
    "url": "https://github.com/sjawhar/legion/pull/975",
    "titleNote": "Retitled after opening: CI's pr-title check requires a conventional-commit subject (type(scope): summary) because squash merges use the PR title as the commit subject and the release workflows version from it. The plan's recorded plain-language title failed that check. Scope `daemon` follows #966/#967's precedent for cross-package Legion changes; no `!` (major bump) was added even though register_gate's input and the daemon state shape changed — the architect or reviewer decides whether a breaking marker is warranted.",
    "headSha": "(set after push)",
    "ci": "green at 5a106148: Tests run 34732414920, Legion Envoy and Contracts run 34732414908"
  },
  "schemaVersion": 1,
  "phase": "implement",
  "e2e": {
    "ruling": "Sami 2026-09-13 00:48Z (dispatch://AGENTC-79/ask/4d04158b-9e41-43e5-8479-1fa90be62c81): the developing agent tests on a production-like surface before merging.",
    "proof": "dispatch://LEGION-20/artifact/pre-merge-e2e-md",
    "proofHttp": "http://sami-agents:8766/issues/LEGION-20/pre-merge-e2e-md",
    "raw": "dispatch://LEGION-20/artifact/pre-merge-e2e-raw-tar-gz",
    "surface": "scripts/smoke/up.sh with SMOKE_DESIGN_GATE=root-issues, SMOKE_WEBHOOK_MODE=none, SMOKE_OMP_PIN=github:sjawhar/oh-my-pi@18.1.18-sami.20260912-203541, OMP_PROFILE=legion20e2e (this branch's plugin installed with the release recipe), against a scratch Dispatch server built from this checkout (real Postgres migrations, header identity human e2e-human, NATS into the rig) — real LLM controller and architect panes.",
    "result": "Acceptance 1: human approval -> gate approvedVersion==latestVersion in 2.0s -> design-approved forwarded to the live architect (01:37:32.830), which woke. Acceptance 3: agent edit -> v3 -> gate closed, Dispatch stale, no wake; human approval at v3 -> open, exactly one wake. Acceptance 2: changes_requested with reason -> approvedVersion deleted, wake {type, version 3, reason, author e2e-human}. Negative controls: agent bearer approve -> 403 HUMAN_ONLY; redundant re-approvals at v2 -> no second wake. Checkpoints 1, 2 pass; 3 and 4 pass their gate assertions and fail only the pre-existing child-issue assertion (single-issue tree). Migration on the real production state (v24, 18 gates) -> v25 with 18 reads; unreachable Dispatch -> refuses naming LEGION-6.",
    "notProven": "checkpoint 3/4 child-issue assertions (architect chose a single-issue tree); later phases on the sandbox repo (out of scope)."
  }
}
