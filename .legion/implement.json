{
  "pr": 1026,
  "prUrl": "https://github.com/sjawhar/legion/pull/1026",
  "branch": "legion/LEGION-61",
  "headSha": "9b241b02e8722a61bd735e4203fe05fe943b908f",
  "base": "main@9a3bb41b (rebased a second time, conflict-forced, after #1011 LEGION-40 and #1010 LEGION-41 merged; first rebase onto 63dac108 was forced by LEGION-20 #975)",
  "filesChanged": [
    "docs/solutions/legion/smoke-rig-modes-gate-checkpoints-and-the-pin-has-one-home.md",
    "docs/solutions/testing/scratch-daemon-rig-proves-what-unit-tests-cannot.md",
    "docs/solutions/testing/smoke-rig-fakes-and-live-run-notes.md",
    "scripts/smoke/README.md",
    "scripts/smoke/checkpoints.sh",
    "scripts/smoke/checkpoints.test.sh",
    "scripts/smoke/down.sh",
    "scripts/smoke/down.test.sh",
    "scripts/smoke/issue-relay.test.ts",
    "scripts/smoke/issue-relay.ts",
    "scripts/smoke/up.sh",
    "scripts/smoke/up.test.sh"
  ],
  "trickyParts": [
    "Dispatch caps GET /api/v1/issues/<key>/events?limit= at 200 (parseEventLimit), not the plan's 500; the relay pages at 200 and the fake Dispatch in the unit test rejects a larger limit with 400 so the cap cannot regress silently.",
    "Within one tick the relay fetches events for every tracked key BEFORE discovering their children, then fetches the newly discovered keys, repeating per level: a root row that names a child was appended after that child's issue.created, so the child is always discovered in the same tick and its lower-id rows sort ahead of the row that named it (orderEvents by global id). Rig B's log shows the child's dispatch-4643 landing between the root's 4642 and 4645.",
    "The envelope is a field-for-field port of publisher.go envelope()/payloadSummary including in_reply_to and urgency where the outbox sets them; the JetStream msgID is nats.go's `<dedupe_key>:<topic>` so a relay restart replays as duplicate=true and the daemon's consumed-event count does not move (proved live on rig A).",
    "RELAY READY is logged after the first tick that fetched AND published without error, so RIG READY in isolated mode means the root's history is already in the rig NATS; the README/harness key on that.",
    "The root-issue existence probe (GET /api/v1/issues/<root>, 404 -> RELAY UNHEALTHY exit 1) runs once inside the retry loop, so a Dispatch outage at boot is retried while a missing root is definitive; the events endpoint itself answers [] for an unknown key and would never expose that.",
    "The unit test's fake sleep cannot tell a backoff from the poll interval by duration (the second backoff step is also 2000 ms): it classifies by whether the last log line is a RELAY RETRY, which is the contract the checkpoint precheck also relies on (newest `RELAY ` line).",
    "Running the rig from a Legion worker pane: the pane's own LEGION_* identity (including LEGION_OMP_PATH, which up.sh would treat as an operator override) and TMUX must be scrubbed from the environment before up.sh, otherwise the smoke daemon inherits them; the throwaway launcher /tmp/legion61-rig.sh did that. Host load ~100/32 cores needed LEGION_WORKER_RPC_TIMEOUT_SECONDS=60 and LEGION_WORKER_BOOT_TIMEOUT_SECONDS=600 (documented daemon env keys, no code change) for controller/architect panes to complete their RPC handshake.",
    "up.test.sh sources up.sh from a temp copy, so up.sh's repo_root there is `/` and the recorded relay command ends in `//scripts/smoke/issue-relay.ts`; the assertion compares against the sourced repo_root, exactly as the envoy-bridge argv is built."
  ],
  "deviations": [
    "Checkpoint 12 gained a `CHECKPOINT 12 OK: <root> released its admission slot and <queued> was promoted` line; it previously passed silently, contradicting the README's one-OK-line contract, and the harness case for 12 needed an observable pass.",
    "The relay validates each Dispatch row with a passthrough zod schema at the boundary (repository rule: schema-parse outside data) and imports ISSUE_KEY_PATTERN from legion-state.ts rather than copying the regex; the plan described a plain cast.",
    "EVENT_PAGE_LIMIT is 200 (Dispatch's maximum), not the plan's 500.",
    "Two-rig proof was sequential (rig A, down, rig B) as the architect decided for spec v3: LEGION-41 (#1010) is not in main, so up.sh still hardcodes the legion-smoke-nats container name and listener machine id. The tester owns the concurrent run once #1010 lands. Both rigs' smoke architects were single-turn users of provider credit; rig A chose a single-issue tree (planner on the root), rig B decomposed into LEGSMOKE-124, so checkpoints 3 and 4 were exercised on both branches of main's new tree_progress/root_phase_worker logic.",
    "Second conflict-forced rebase (architect-directed) onto main 9a3bb41b after #1011/#1010 merged: down.sh (LEGION-41's rewritten main() vs the relay stop), up.test.sh (both sides appended at end of file), README (start block; runtime/two-rigs paragraphs) conflicted and were merged keeping both sides. The diff is CHANGED (fingerprints above), in two steps: the merged resolutions themselves (down.sh reads the rig identity before terminate_pid_file issue-relay; the README two-rigs paragraph gains the isolated sentence; the start block keeps LEGION-40's sjawhar values with isolated as the mode), then integration commit 9b241b02: up.test.sh proves the relay env block carries the Dispatch URL/token resolve_dispatch_config resolved from envoy.json exactly as the daemon's does; up.sh names those sources at the relay start; README no longer claims a none-mode rig spawns its controller (LEGION-44's tester proved a fresh none rig opens no pane until an event arrives) and says checkpoint 13 has something to inspect only once a pane exists. No relay or script behaviour changed: the relay's NATS URL was already the per-rig nats_url, and its Dispatch values were already the ones resolve_dispatch_config assigns in main().",
    "The first rebase's deviation, kept for the record: rebased onto main 63dac108 before the proof because the rig daemon on the 52c5e03e base refused to boot against pi-legion-envoy 1.23.0 (contract 2 vs required 1) and #975 rewrote the same checkpoints/README regions; 3 conflicted files merged keeping both sides; SMOKE_DISPATCH_INGRESS=rig and isolated coexist."
  ],
  "openQuestions": [
    "None. Both dependency PRs are in main; the branch sits on 9a3bb41b. The concurrent two-rig run (acceptance 1 as written) is now possible on LEGION-41's shape and remains the tester's E2E line."
  ],
  "liveProof": {
    "rigA": {
      "root": "LEGSMOKE-122",
      "dir": "/tmp/legion-smoke-61",
      "consumedKeys": {
        "LEGSMOKE-122": 6
      },
      "relayed": 6,
      "checkpoints": {
        "1": "OK",
        "2": "OK",
        "3": "OK",
        "4": "OK",
        "12": "reached its own assertion (gate open), exit 1 not 3"
      },
      "negativeControl": "relay killed -> CHECKPOINT 1 FAILED naming issue-relay.pid; up.sh rerun REUSED daemon/listener/NATS/root, STARTED issue-relay, replay all duplicate=true, checkpoint 1 OK again"
    },
    "rigB": {
      "root": "LEGSMOKE-123",
      "child": "LEGSMOKE-124",
      "dir": "/tmp/legion-smoke-62",
      "consumedKeys": {
        "LEGSMOKE-123": 10,
        "LEGSMOKE-124": 3
      },
      "relayed": 13,
      "retryRecovered": "3 RELAY RETRY lines (Dispatch unreachable) then RELAY RECOVERED after 3 attempts",
      "checkpoints": {
        "1": "OK",
        "2": "OK",
        "3": "OK",
        "4": "OK",
        "12": "reached its own assertion (gate open), exit 1 not 3"
      }
    },
    "isolation": "22 other LEGSMOKE issues were live on the shared server; each daemon log carries only its own keys; LEGSMOKE-122 never appears in rig B's log and LEGSMOKE-123/124 never in rig A's.",
    "teardown": "LEGSMOKE-122/123/124 closed as done via the API (the relay carried issue.closed within one poll; trees went lingering); down.sh ran for both rigs; the deployment daemon and the LEGION-43 tester's legion-smoke-nats-legion43 rig were untouched."
  },
  "localChecks": {
    "unit": "bun test scripts/smoke/issue-relay.test.ts: 22 pass (at 9b241b02)",
    "lint": "packages/daemon/node_modules/.bin/biome check scripts/smoke/issue-relay.ts scripts/smoke/issue-relay.test.ts: clean; CI invocation `bunx biome check --error-on-warnings src/` in packages/daemon: clean",
    "types": "tsc --noEmit over the relay and its test via a throwaway tsconfig extending packages/daemon/tsconfig.json (types: bun): exit 0; `bunx tsc --noEmit` in packages/daemon: exit 0",
    "harnesses": "up.test.sh (incl. the new envoy.json relay case), checkpoints.test.sh (4 isolated PASS lines), down.test.sh (LEGION-41's rewritten cases plus the relay case) all PASS with LEGION_TREE/LEGION_ISSUE/LEGION_ROLE/LEGION_OMP_PATH/TMUX unset"
  },
  "subPlanningNeeded": false,
  "rebase": {
    "at": "2026-09-13T13:43Z",
    "tipBefore": "83b64a80f539",
    "tipAfter": "9b241b02e872",
    "fingerprintBefore": "45abf9b4c796009cf6d06ccb54e721b622938f8a59de72bcbb916f8f23ed26de",
    "fingerprintRebasedOnly": "ccc5ded1b8caf8c048f2d8ea09a978d986e2048d877133dd018a1454d4353f81 (head d0b2e12c1541, before the integration commit)",
    "fingerprintAfter": "3193064de16c1457abccf2775c0820c2453743bc461a0390fb6659e623e37add",
    "verdict": "changed",
    "conflictedFiles": [
      "scripts/smoke/down.sh",
      "scripts/smoke/up.test.sh",
      "scripts/smoke/README.md"
    ],
    "comment": "https://github.com/sjawhar/legion/pull/1026#issuecomment-5653653339",
    "ci": "Tests run 34760576651 success at 9b241b02e872"
  },
  "schemaVersion": 1,
  "phase": "implement",
  "completed": "2026-09-13T13:45:32.794Z"
}