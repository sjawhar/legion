{
  "issue": "LEGION-11",
  "spec": "dispatch://LEGION-11/spec (version 5; Design 5 = no behaviour change in processes.ts/api.ts/api/auth.ts, comment-only edits in scope)",
  "plan": "docs/plans/2026-09-12-reconnect-workers-after-api.md (commit 5b732e1251a8)",
  "pr": {
    "number": 955,
    "url": "https://github.com/sjawhar/legion/pull/955",
    "title": "fix(daemon): probe workers after the API starts so a dead worker's cleanup no longer crashes on restart",
    "base": "main",
    "head": "legion/LEGION-11"
  },
  "commits": [
    {
      "change": "lxkxplmn",
      "commit": "4ed969ed8136",
      "title": "fix(daemon): run reconnectWorkers after the API exists so a dead worker's retirement can revoke its capability",
      "files": [
        "packages/daemon/src/daemon/index.ts",
        "packages/daemon/src/daemon/__tests__/index.test.ts"
      ],
      "note": "Plan tasks 1+2. reconnectWorkers try/console.error, pruneSecretFiles(), and the controllerPendingNotices drain/ensureController block moved verbatim to after `api = startLegionApi(...)` and before enableWorkerPromotion(); the 'needs no api reference' comment replaced by the ordering invariant. Regression test added under describe(\"startDaemon\") after the registration-deadline test."
    },
    {
      "change": "pvvsloly",
      "commit": "70e0adf42d20",
      "title": "docs(daemon): comments and invariants follow reconnectWorkers to its post-API position",
      "files": [
        "packages/daemon/src/daemon/__tests__/processes.test.ts",
        "packages/daemon/src/daemon/AGENTS.md",
        "docs/solutions/daemon/refcounted-hold-guards-a-derived-prune.md"
      ],
      "note": "Plan task 3, comment-only. After rebase4 this commit no longer touches processes.ts or worker-admission.ts: #980 rewrote the comments it edited, and the rewrites already describe the current order (see rebase4.conflicts)."
    }
  ],
  "redBeforeFix": {
    "command": "bun test packages/daemon/src/daemon/__tests__/index.test.ts -t \"retires a ready-confirmed worker\"",
    "result": "0 pass, 1 fail",
    "failureText": "error: expect(received).toEqual(expected)\n\n- []\n+ [\n+   [\n+     \"[legion] worker reconnection failed:\",\n+     [TypeError: undefined is not an object (evaluating 'api.revokeSessionCapability')],\n+   ],\n+ ]\n\n- Expected  - 1\n+ Received  + 6\n\n      at <anonymous> (.../packages/daemon/src/daemon/__tests__/index.test.ts:625:24)\n(fail) startDaemon > retires a ready-confirmed worker whose shim socket is dead at boot: capability revoked through the live api, locator cleared, secret file removed"
  },
  "greenAfterFix": "bun test packages/daemon/src/daemon/__tests__/index.test.ts: 19 pass, 0 fail, 87 expect() calls",
  "gates": {
    "biome": "cd $LEGION_WORKSPACE && bunx biome check packages/daemon/src -> exit 0, no diagnostics",
    "tsc": "cd $LEGION_WORKSPACE/packages/daemon && bunx tsc --noEmit -> exit 0",
    "bunTest": "After rebase onto main 8f6fad36: cd $LEGION_WORKSPACE && env -u DISPATCH_URL -u DISPATCH_TOKEN_FILE bun test packages/daemon -> 816 pass, 5 skip, 0 fail, 3333 expect() calls, 821 tests across 53 files, exit 0 (pre-rebase: 812 pass / 0 fail).",
    "bunTestInPaneEnv": "With the worker pane's DISPATCH_URL/DISPATCH_TOKEN_FILE present: 811 pass, 1 fail — `legion start --check-config > validates github_apps.<role>.private_key_command without executing it` (packages/daemon/src/cli/__tests__/index.test.ts) throws `dispatch_url is set but DISPATCH_TOKEN is not` from resolveDaemonConfig because cmdCheckConfig reads the real process.env. Pre-existing and environment-sensitive: the file passes 14/14 with those two variables unset, and this branch changes nothing under packages/daemon/src/cli or config.ts. Not fixed here (assignment constraint: no executable changes outside index.ts and the new test); CI runners do not carry DISPATCH_URL."
  },
  "deviationsFromPlan": [
    "PR title is `fix(daemon): probe workers after the API starts so a dead worker's cleanup no longer crashes on restart` rather than the assignment's illustrative `Daemon: …` — the Tests workflow's pr-title job requires `^[a-z]+(\\([^)]+\\))?!?: .+`; the wording is otherwise the plain-words title the architect asked for.",
    "PR body gives the closure audit as one plain-English sentence (per the architect's assignment: no line numbers, expand identifiers) instead of pasting the plan's line-numbered table; the full table remains in the plan document.",
    "`jj bookmark set legion/LEGION-11 -r @-` needed `--allow-backwards`: in this jj the bookmark had followed the working-copy commit, so pointing it at @- is a local backwards move (still a fast-forward from origin). Push and PR head are the described commits only; .omp/config.yml is not committed.",
    "index.ts: one blank line added between the `);` closing startLegionApi(...) and the moved block, for readability; otherwise the inserted text is the plan's verbatim.",
    "Rebased onto main (8f6fad36) before CI could run: main had moved (#947 gates-off boot block, #954 boot-probe retries) and GitHub reported the PR CONFLICTING, which suppresses every pull_request workflow — no Tests run ever appeared for the original head c5cdfc04. Only index.test.ts conflicted (main also added `roleToken` to the @legion/contracts import; merged into one import line). index.ts merged cleanly: main's changes are all above the moved region. Gates re-run on the rebased tree: biome exit 0, tsc exit 0, bun test 816 pass / 5 skip / 0 fail (main added 4 tests). Pushed as a sideways bookmark move; nothing was stacked on the branch."
  ],
  "closureAudit": "Confirmed while editing: the span from `new ProcessManager({...})` to `api = startLegionApi(...)` in the new index.ts contains no await (only closure/object construction, the synchronous startEventPump, and createCiStatusFetcher). mintControllerCapability/mintBootToken/mintWorkerBootToken/revokeSessionCapability are reachable only via HTTP handlers, NATS callbacks, timers, promotion cascades gated by enableWorkerPromotion(), or the three moved blocks.",
  "ci": "Tests run 34734366745 (https://github.com/sjawhar/legion/actions/runs/34734366745) at 7596ffc4ef2de20af4870b11d06804da7a5f87c2: pr-title, lint, typecheck, test all success. GitHub: MERGEABLE against main abaf0817. The run at the final head is named in the PR body's Verification/CI line.",
  "testerNotes": [
    "Follow plan §Tester exactly (docs/plans/2026-09-12-reconnect-workers-after-api.md, 'Tester' section): scratch project sjawhar/11, state under /tmp/legion11-smoke/<main|branch>/daemon, `tmux -L legion-sjawhar11`, port 19371, isolated NATS docker legion11-nats on 127.0.0.1:14223. Main run first from /home/ubuntu/legion-ws-RunDaemon (read-only), branch run from $LEGION_WORKSPACE at the PR head (code head 66bcbb6b; the implement handoff commit sits on top of it).",
    "Expected main signature: `[legion] worker reconnection failed:` followed by `TypeError: undefined is not an object (evaluating 'api.revokeSessionCapability')`, locator retained, secret file retained. Expected branch: only the per-worker `[legion] failed to reconnect worker <token>: … Failed to connect` line, locator cleared in `legion state`, secret file gone.",
    "Never restart, stop, or reconfigure the production daemon (legion-sjawharlegion, port 13370). Verify `tmux -L legion-sjawharlegion list-panes -a` is identical before/after.",
    "Fill the PR body's **E2E** line (command/run, observation, head sha, negative control) via `legion gh -- pr edit 955 --body-file …`; keep the rest of the body."
  ],
  "reviewerNotes": [
    "Behaviour diff is index.ts only (`jj diff --from main --to 4ed969ed -- packages/daemon/src/daemon/index.ts`): the block move plus its comments; every executable line matches the tested diff across all four rebases. After rebase4 the PR no longer touches processes.ts or worker-admission.ts. The moved block sits after the WorkerStreamListener block and before the launch hold — see rebase4.conflicts[0].placementRationale.",
    "Spec Testing row 2: the three processes.test.ts reconnect cases pass unmodified; only their comments changed.",
    "PR body carries the what-moved list and the closure-audit sentence (spec Acceptance 4) and `Dispatch: LEGION-11`."
  ],
  "base": "main@origin abaf08175c9b (chore: release pi-envoy v1.17.3) after the fourth rebase; previously dc464ee7, fd4fce51, 8f6fad36, originally 7db2cd83.",
  "rebase2": {
    "reason": "GitHub reported PR #955 CONFLICTING/DIRTY against main after main moved from 8f6fad36 to fd4fce51 (32 commits, including #967 reverse-dial worker-shim + WorkerStreamListener, #952, #971), which also suppresses every pull_request workflow run. Architect instruction: conflict-only rebase, change nothing else.",
    "command": "jj rebase -s omvmmzoo -d main@origin (rebased all 7 branch commits; commit shape preserved: plan doc, plan handoff, fix+test, comment sweep, implement handoff, test handoff)",
    "conflicts": [
      "packages/daemon/src/daemon/index.ts: NONE. jj auto-merged: main (#967) inserted its `startWorkerStreamListener` try/catch immediately after `api = startLegionApi(...)`, the same anchor as the moved block; jj placed main's block first, then the moved block, then `enableWorkerPromotion()`. That is the correct order: the listener is bound with the API and its bind failure is startup-fatal (`api.stop(); throw`), so it must fail before any process-manager work or a fire-and-forget `ensureController` spawn; it is synchronous, so the stated invariant (no top-level await between `new ProcessManager(...)` and the moved block) still holds — verified by an awk scan of index.ts lines 275-431: every `await` in that span is inside a closure (provisioningToken, emitOverseerCatchup, onUndeliverable, onProbe, emitResync, onControllerReady). No line of the moved block or of main's block was edited.",
      "packages/daemon/src/daemon/AGENTS.md: one 2-sided conflict in the docs commit — main appended a shim-reachability bullet at the end of Operational invariants, the same anchor as the boot-ordering bullet. Resolved by keeping both, main's bullet first, mine second; neither bullet's text changed. Resolution squashed into the docs commit (pvvsloly).",
      "packages/daemon/src/daemon/__tests__/index.test.ts, processes.test.ts, processes.ts: auto-merged cleanly (main's edits are elsewhere in those files)."
    ],
    "diffIdentity": {
      "tested": "jj diff --from 8f6fad36 --to 66bcbb6b --git  (the code the tester ran at PR head f5cf98f5)",
      "rebased": "jj diff --from main@origin --to 74e6c432 --git  (new code head)",
      "method": "Stripped both to their added/removed lines (^[+-], excluding the +++/--- file headers) and diffed.",
      "result": "IDENTICAL — 1074 added/removed lines in each, byte-for-byte equal; same nine files. The only differences in the full unified diffs are context lines: in AGENTS.md the line preceding the boot-ordering bullet is now main's shim-reachability bullet (was the queued-idle-resume bullet); in index.ts the lines preceding the moved block are now the WorkerStreamListener block's `throw error; }` (were `apiDeps );`). No +/- line differs."
    },
    "gates": {
      "install": "bun install --frozen-lockfile -> exit 0 (20 packages added by main's lockfile)",
      "biome": "bunx biome check packages/daemon/src -> exit 0",
      "tsc": "cd packages/daemon && bunx tsc --noEmit -> exit 0",
      "bunTest": "env -u DISPATCH_URL -u DISPATCH_TOKEN_FILE bun test packages/daemon -> 872 pass, 5 skip, 0 fail, 3514 expect() calls, 877 tests across 56 files, exit 0 (main added 56 tests since the last run)"
    },
    "heads": {
      "codeHead": "74e6c4321d2ed2516373bf42813bd01248d800b7 (docs commit; the fix commit is 3987da66f3199a3011a80600db8d33a35e07a6b9)",
      "pushedHeadBeforeHandoffCommit": "f8e9a541ce91ae640a785807c2ffef667573f0d4 (rebased `test: record handoff`)",
      "finalHead": "the `implement: record handoff` commit carrying this file, pushed after it; .legion/-only on top of f8e9a541"
    },
    "ci": "Tests run 34714945221 (https://github.com/sjawhar/legion/actions/runs/34714945221) at f8e9a541ce91ae640a785807c2ffef667573f0d4: pr-title, lint, typecheck, test all success. GitHub: MERGEABLE. The run at the final head (the .legion/-only commit on top) is named in the PR body's Verification/CI line.",
    "testerEvidence": "Unchanged and still valid: .legion/test.json (PASS on all four acceptance lines) was gathered at the pre-rebase head f5cf98f5 / code head 66bcbb6b, whose diff is identical to the rebased one as proven above."
  },
  "rebase3": {
    "reason": "GitHub reported PR #955 CONFLICTING/DIRTY after main moved from fd4fce51 to dc464ee7 (31 commits, including #962 LEGION-21 runtime boundary — ProcessManager drives an injected Runtime, discriminated `Locator` union with `runtime: \"tmux\"` — and #973 LEGION-30 idle-worker retirement). Architect instruction: conflict-only rebase; minimal test-harness adaptation allowed if #962 changed the harness, every changed line recorded.",
    "command": "jj rebase -s omvmmzoo -d main@origin — all 9 branch commits rebased, shape preserved: plan doc, plan handoff, fix+test, comment sweep, implement handoff, test handoff, implement rebase record, test round-2 handoff (then this handoff commit on top).",
    "conflicts": [
      {
        "file": "packages/daemon/src/daemon/__tests__/index.test.ts",
        "commit": "fix+test (lxkxplmn)",
        "kind": "2-sided conflict on the `@legion/contracts` import: main (#962) turned it into a multi-line import and added `LEGION_DAEMON_API_VERSION`; ours added `type DaemonStateResponse`.",
        "resolution": "Kept main's multi-line import and inserted `type DaemonStateResponse,` as one line between `controllerToken,` and `LEGION_DAEMON_API_VERSION,`. Same named import as before; only the line shape differs."
      },
      {
        "file": "packages/daemon/src/daemon/processes.ts",
        "commit": "comment sweep (pvvsloly)",
        "kind": "2-sided conflict on `retireUnconfirmedBoot`'s doc-comment paragraph: main (#962) changed `pane` to `process` in two phrases of that paragraph; ours had removed the `api`-before-exists clause and reflowed the same paragraph.",
        "resolution": "The paragraph still exists and the clause still applied. Re-applied our clause removal on top of main's wording: our 16-line paragraph with `a second pane racing` -> `a second process racing` and `different generation or pane)` -> `different generation or process)`. Both affected lines stay within 100 columns, so no further reflow. Main's code untouched; the file's diff against main remains one doc comment."
      },
      {
        "file": "packages/daemon/src/daemon/index.ts",
        "commit": "fix+test (lxkxplmn)",
        "kind": "NO conflict — auto-merged. Main's #962 now constructs `new TmuxRuntime({...})` (synchronous) immediately before `new ProcessManager({...})`, binds the API and worker-stream listener to `config.bind`, and renames `reconcileTmuxWindows` to `reconcileOrphans`; none of that touches the moved block.",
        "invariantAfterMerge": "Order in the merged file: `const runtime = new TmuxRuntime(` (line 296) -> `const processManager = new ProcessManager(` (308) -> `api = startLegionApi(` (436) -> `workerStream = startWorkerStreamListener(` (449, synchronous, startup-fatal on bind failure) -> the moved block's leading comment (462) -> `await processManager.reconnectWorkers()` (480) -> `pruneSecretFiles()` -> pending-notice drain/spawn -> `processManager.enableWorkerPromotion()` (522). An awk scan of lines 296-462 finds no top-level `await`: every `await` in that span (lines 325, 334-335, 344, 351, 365, 380, 403, 427-428) is inside a closure (`provisioningToken`, `emitOverseerCatchup`, `onUndeliverable`, `onProbe`, `emitResync`, `onControllerReady`). Invariant holds; no judgement call was needed."
      }
    ],
    "testHarnessAdaptation": {
      "required": true,
      "why": "#962 replaced `WorkerLocator` with the discriminated `Locator` union (`TmuxLocator` requires `runtime: \"tmux\"`; `migrateV23State` back-fills it for persisted state, but the regression test seeds its claim in memory). Without the tag: `tsc` fails (TS2322 `Property 'runtime' is missing ... required in type '{ runtime: \"tmux\"; }'`) and the test fails at runtime (`locatorHandles` in runtime.ts takes the non-tmux branch, so `reconcileOrphans` in runtime-tmux.ts throws `TypeError: undefined is not an object (evaluating 'handle.startsWith')` during boot).",
      "changedLines": [
        "packages/daemon/src/daemon/__tests__/index.test.ts, inside the seeded `state.roles[token].locator` object of the test `retires a ready-confirmed worker whose shim socket is dead at boot …`: ADDED one line `        runtime: \"tmux\",` as the object's first property (before `tmuxSession`). No other line of the test changed; the five assertions are unchanged: no `worker reconnection failed`/TypeError in console.error; `killedPanes` equals [\"%7\"]; persisted claim has no `locator`, keeps `resumeSessionFile`, keeps `sessionId`; `GET /legion/v1/state` shows the role with no `locator`; secret file stat rejects ENOENT."
      ]
    },
    "diffIdentity": {
      "tested": "jj diff --from 8f6fad36 --to 66bcbb6b --git (the code the tester ran at f5cf98f5 / e053af7f)",
      "rebased": "jj diff --from main@origin --to 47b782f24b42 --git (new code head)",
      "method": "Both stripped to ^[+-] lines excluding +++/--- file headers, then diffed. 1074 lines each; same nine files.",
      "result": "NOT byte-identical. index.ts: IDENTICAL (checked separately — the behaviour change is unchanged). Every differing line, by file:",
      "differingLines": [
        "index.test.ts — tested: `-import { controllerToken, roleToken, roleTopic } from \"@legion/contracts\";` and `+import { controllerToken, type DaemonStateResponse, roleToken, roleTopic } from \"@legion/contracts\";`; rebased: `+  type DaemonStateResponse,` (main's import is now multi-line; same named import).",
        "index.test.ts — rebased only: `+        runtime: \"tmux\",` (the harness adaptation above).",
        "processes.ts — `-   * over-admission, or a second pane racing a concurrent same-role spawn — and would also` -> rebased removes the same line with `process` instead of `pane` (main's #962 wording).",
        "processes.ts — `-   * different generation or pane), or deleted entirely (`closeTree`) means this retirement is` -> rebased removes the same line with `process` instead of `pane`.",
        "processes.ts — `+   * per-role launch lock — over-admission, or a second pane racing a concurrent same-role spawn.` -> rebased adds the same line with `process` instead of `pane`.",
        "processes.ts — `+   * (`readyConfirmedAt` now set), superseded by a newer launch (a different generation or pane),` -> rebased adds the same line with `process` instead of `pane`."
      ]
    },
    "gates": {
      "install": "bun install --frozen-lockfile -> exit 0 (no changes)",
      "biome": "bunx biome check packages/daemon/src -> exit 0",
      "tsc": "cd packages/daemon && bunx tsc --noEmit -> exit 0 (after the one-line adaptation; exit 2 with TS2322 before it)",
      "namedTest": "env -u DISPATCH_URL -u DISPATCH_TOKEN_FILE bun test packages/daemon/src/daemon/__tests__/index.test.ts -t \"retires a ready-confirmed worker\" -> 1 pass, 0 fail, 9 expect() calls",
      "bunTest": "env -u DISPATCH_URL -u DISPATCH_TOKEN_FILE bun test packages/daemon -> 959 pass, 5 skip, 0 fail, 3851 expect() calls, 964 tests across 57 files, exit 0"
    },
    "heads": {
      "fixCommit": "3aa0256fe571",
      "codeHead": "47b782f24b42 (comment-sweep commit)",
      "pushedHeadBeforeThisHandoff": "87f541a8ece64e31d8e1c7e1a1537009384c4346 (rebased `test: record round 2 at the rebased head`)",
      "finalHead": "the `implement: record rebase3` commit carrying this file, .legion/-only on top of 87f541a8"
    },
    "ci": "Tests run 34733404008 (https://github.com/sjawhar/legion/actions/runs/34733404008) at 87f541a8ece64e31d8e1c7e1a1537009384c4346: pr-title, lint, typecheck, test all success. GitHub: MERGEABLE against main dc464ee7. The run at the final head is named in the PR body's Verification/CI line.",
    "testerEvidence": "The tester's rounds 1 and 2 (.legion/test.json) were gathered at heads whose index.ts diff is identical to this one; the only source-level differences since are the one-line locator tag in the regression test and main's two pane->process words in a doc comment."
  },
  "rebase4": {
    "reason": "While waiting for CI on rebase3's head 7b3b39fd, main moved from dc464ee7 to abaf0817 (3 commits: #980 `fix(daemon): boot degrades gracefully under host load: probe retry with a launch hold, per-claim reconnect isolation, budgeted provisioning` plus two release commits) and GitHub reported the PR dirty again. Architect instruction: one more rebase in this phase rather than completing on a conflicting head.",
    "whatMainChanged": "#980 restructured startDaemonLocked around a launch hold: the OMP boot probes now start early and are awaited at `await probes` after `stop` is defined; `enableWorkerPromotion()` became `processManager.enableLaunches()` and moved below that hold together with reconnectRoots/reconcileAdmission/reconcileWorkerAdmission and a new replayHeldRecoveries(); reconnectWorkers got per-claim isolation (a throw is logged `[legion] failed to reconcile worker <token>:` and that claim is left as found). main still ran reconnectWorkers before `api` (the LEGION-11 bug) but edited the comments of both blocks this PR moves, and its AGENTS.md bullet explicitly says the move is LEGION-11's to make.",
    "command": "jj rebase -s omvmmzoo -d main@origin (all 10 branch commits; shape preserved: plan doc, plan handoff, fix+test, comment sweep, implement handoff, test handoff, implement rebase2 record, test round-2 handoff, implement rebase3 record, then this handoff commit)",
    "conflicts": [
      {
        "file": "packages/daemon/src/daemon/index.ts",
        "commit": "fix+test (lxkxplmn)",
        "kind": "three 2-sided conflicts",
        "resolution": [
          "Conflict 1 (old reconnectWorkers position, after `new ProcessManager`): main had edited two comment lines there (`enableWorkerPromotion()` -> `enableLaunches()` — boot's launch hold); ours removes the whole block from this position. Resolved: block removed here (ours); main's wording carried into the moved copy (below).",
          "Conflict 2 (old pending-controller-notice position, after `startEventPump`): main appended one sentence to that comment; ours removes the block from this position. Resolved: removed here; main's sentence carried into the moved copy.",
          "Conflict 3 (insertion site after the WorkerStreamListener block): ours inserted the moved block immediately before the old `enableWorkerPromotion()` tail; main deleted that tail (it now lives below the launch hold as `enableLaunches()`). Resolved: moved block kept at the insertion site — immediately after the listener block, before `let stopped`, the `stop` definition, and the launch hold — and the deleted tail not re-added.",
          "Comment lines changed inside the moved block, each a merge of main's #980 wording (no code line changed): (a) `awaited before \\`enableWorkerPromotion()\\` below because` -> `awaited before \\`enableLaunches()\\` below because it` (and the next line reflowed by one word); (b) `\\`enableWorkerPromotion()\\` until the probe has settled. \\`Bun.serve\\` is already accepting` -> `\\`enableLaunches()\\` — boot's launch hold, released only once the OMP probes pass below.` followed by the `Bun.serve` sentence reflowed over three lines with identical words; (c) the pending-notice comment gains main's sentence `Boot's launch hold is still on here, so \\`ensureController\\` records the request instead of opening a pane; \\`replayHeldRecoveries()\\` below the hold spawns it once the probes pass.` after ours (`Runs after \\`api\\` is assigned because \\`ensureController\\` mints the controller capability through it.`)."
        ],
        "placementRationale": "Two placements satisfied 'after api and the listener, before enableLaunches()': (a) right after the listener, before the launch hold; (b) after the hold, right before enableLaunches(). (a) is the one main itself specifies for the reconnect ('state load, NATS, the API bind, and the worker reconnect all proceed while a probe is still retrying' — #980's comment and AGENTS.md bullet); (b) would silently delay the reconnect behind the probes, a behaviour change. So (a) is forced by conflict-only, not a judgement call. reconnectWorkers is still awaited before enableLaunches(): moved block -> stop definition -> await probes -> enableLaunches().",
        "invariantAfterMerge": "Order in the merged file: `const probes = (async () => …)()` (279, started, not awaited) -> `await deps.loadState` (297) -> `await deps.createNatsTransport` (341) -> `new TmuxRuntime` (349) -> `new ProcessManager` (361) -> `startEventPump` (426) -> `api = startLegionApi(` (489) -> `startWorkerStreamListener` (502) -> moved block: leading comment (515), `await processManager.reconnectWorkers()` (533), `pruneSecretFiles()` (538), pending-notice drain/spawn (554) -> `let stopped` (566) -> `const stop` (580) -> `await probes` (654, the launch hold) -> `processManager.enableLaunches()` (674) -> `reconnectRoots()` (679) -> `replayHeldRecoveries()` (685). An awk scan of lines 349-515 finds no statement at top-level indentation containing `await`; every `await` in that span (378, 387-388, 397, 404, 418, 433, 456, 480-481) is inside a closure. `enableWorkerPromotion` no longer appears in index.ts. Invariant holds; no judgement call needed."
      },
      {
        "file": "packages/daemon/src/daemon/worker-admission.ts",
        "commit": "comment sweep (pvvsloly)",
        "kind": "two 2-sided conflicts (the `workerPromotionEnabled` doc comment and the `enqueueForRetry` doc comment)",
        "resolution": "#980 rewrote both comments; its versions no longer state the old order (`called from ProcessManager.enableLaunches() after the daemon's boot probes pass and its HTTP api is assigned`; `reconnectWorkers, run during boot's launch hold`). Our edits dropped; file now equals main."
      },
      {
        "file": "packages/daemon/src/daemon/processes.ts",
        "commit": "comment sweep (pvvsloly)",
        "kind": "2-sided conflict on retireUnconfirmedBoot's doc paragraph",
        "resolution": "#980 rewrote the clause we had removed: it now reads `and would also open a process during boot's launch hold when this runs from reconnectWorkers (before enableLaunches())` — no api-before-exists claim. Our edit dropped; file now equals main (the PR no longer touches processes.ts at all)."
      },
      {
        "file": "packages/daemon/src/daemon/__tests__/processes.test.ts",
        "commit": "comment sweep (pvvsloly)",
        "kind": "two 2-sided conflicts (comment-only on our side)",
        "resolution": "Conflict 1 (the `manager()` helper): main renamed `skipEnablePromotion`/`enableWorkerPromotion()` to `skipEnableLaunches`/`enableLaunches()` in code and comment but kept `index.ts calls this immediately after api is assigned`, which is false after the move (and, post-#980, false on main as well since enableLaunches() runs after the launch hold). Resolved: main's code line and identifier, our wording: `this once reconnectWorkers has settled, after api is assigned) — only the dedicated` / `boot-ordering test passes skipEnableLaunches to exercise the gate itself.` Conflict 2 (the `skipEnablePromotion models exactly what reconnectWorkers runs under` comment): #980 rewrote it (`during boot, before enableLaunches() releases the launch hold`) — no old-order claim; our edit dropped. Our other two comment edits in this file merged cleanly."
      },
      {
        "file": "packages/daemon/src/daemon/AGENTS.md",
        "commit": "comment sweep (pvvsloly)",
        "kind": "no jj conflict, but #980 added its own `Boot ordering in startDaemonLocked` bullet listing reconnectWorkers before the API and ending `This branch does not change where reconnectWorkers runs (before the API, as before); moving it after the API … is LEGION-11's fix, which rebases over this restructure.`",
        "resolution": "Edited that bullet minimally so no doc states the old order (the architect's standing ruling; the bullet's own text hands the update to LEGION-11): the sequence `NATS, ProcessManager, reconnectWorkers, pruneSecretFiles, the event pump, the pending-controller-notice drain/spawn (…), the API, the worker-stream listener; then the launch hold` became `NATS, ProcessManager, the event pump, the API, the worker-stream listener, reconnectWorkers, pruneSecretFiles, the pending-controller-notice drain/spawn (…); then the launch hold`, and the hand-off sentence became `reconnectWorkers runs after the API so a dead confirmed worker's capability revoke has an api to go through (LEGION-11; the invariant is the boot-ordering bullet below).` Our own invariant bullet: `before enableWorkerPromotion()` -> `before enableLaunches()`."
      },
      {
        "file": "docs/solutions/daemon/refcounted-hold-guards-a-derived-prune.md",
        "commit": "comment sweep (pvvsloly)",
        "kind": "no conflict",
        "resolution": "Historical note's identifier follows main's rename: `before enableWorkerPromotion()` -> `before enableLaunches()`."
      }
    ],
    "diffIdentity": {
      "tested": "jj diff --from 8f6fad36 --to 66bcbb6b --git (the code the tester ran in rounds 1 and 2)",
      "rebased": "jj diff --from main@origin --to 70e0adf42d20 --git (new code head)",
      "method": "Both stripped to ^[+-] lines excluding +++/--- file headers, then diffed per file. 1074 lines tested vs 1022 rebased.",
      "result": "NOT identical. No executable line differs anywhere; index.ts's differences are comment text only. Every differing line, by file:",
      "differingLines": {
        "packages/daemon/src/daemon/index.ts": [
          "REMOVED-side text differs because main (#980) had edited the comments in the old positions before we removed them: `-  // stay gated behind \\`processManager.enableWorkerPromotion()\\` below until \\`api\\` exists: nothing` / `-  // here is protected by call *ordering*, only by the gate.` are now `-  // stay gated behind \\`processManager.enableLaunches()\\` — boot's launch hold, released only` / `-  // once the OMP probes pass below: nothing here is protected by call *ordering*, only by the gate.`; and `-  // boot depends on either finishing.` is now that line plus main's two added lines about the launch hold.",
          "ADDED-side: `+  // revokes its capability through it, and awaited before \\`enableWorkerPromotion()\\` below because` / `+  // it is the source of truth …` -> `+  // … awaited before \\`enableLaunches()\\` below because it` / `+  // is the source of truth …`.",
          "ADDED-side: `+  // \\`enableWorkerPromotion()\\` until the probe has settled. \\`Bun.serve\\` is already accepting` + 3 lines -> `+  // \\`enableLaunches()\\` — boot's launch hold, released only once the OMP probes pass below.` + the same Bun.serve sentence reflowed over 3 lines.",
          "ADDED-side: `+  // mints the controller capability through it.` -> that line continued with main's launch-hold sentence over 3 lines."
        ],
        "packages/daemon/src/daemon/__tests__/index.test.ts": [
          "The two import hunks (`spyOn` from bun:test; `type DaemonStateResponse` from @legion/contracts) no longer appear: main's file already imports both.",
          "`+        runtime: \"tmux\",` — the rebase3 locator-tag adaptation, unchanged."
        ],
        "packages/daemon/src/daemon/__tests__/processes.test.ts": [
          "`skipEnablePromotion` -> `skipEnableLaunches` in the helper comment's -/+ pair (main's rename).",
          "The `-    // daemon calls it before \\`api\\` exists, hence before \\`enableWorkerPromotion()\\`.` / `+    // daemon calls it before \\`enableWorkerPromotion()\\`.` pair is gone (our edit dropped; main rewrote that comment)."
        ],
        "packages/daemon/src/daemon/processes.ts": [
          "No longer in the diff (our comment edit dropped; file equals main)."
        ],
        "packages/daemon/src/daemon/worker-admission.ts": [
          "No longer in the diff (our two comment edits dropped; file equals main)."
        ],
        "packages/daemon/src/daemon/AGENTS.md": [
          "Our invariant bullet: `before \\`enableWorkerPromotion()\\`` -> `before \\`enableLaunches()\\``.",
          "New -/+ pair: the edit to #980's own boot-ordering bullet described under conflicts."
        ],
        "docs/solutions/daemon/refcounted-hold-guards-a-derived-prune.md": [
          "`+  \\`enableWorkerPromotion()\\`).` -> `+  \\`enableLaunches()\\`).`"
        ]
      }
    },
    "redOnMainRecheck": "With main's index.ts restored into the working copy (test file kept), the named test fails: `[legion] failed to reconcile worker legion-acme1-widgets-42-tester:` + `TypeError: undefined is not an object (evaluating 'api.revokeSessionCapability')` (#980's per-claim isolation changed the log prefix; the test's `instanceof TypeError` filter still catches it). index.ts restored to the code head afterwards; nothing committed.",
    "gates": {
      "install": "bun install --frozen-lockfile -> exit 0 (no changes)",
      "biome": "bunx biome check packages/daemon/src -> exit 0",
      "tsc": "cd packages/daemon && bunx tsc --noEmit -> exit 0",
      "namedTest": "env -u DISPATCH_URL -u DISPATCH_TOKEN_FILE bun test packages/daemon/src/daemon/__tests__/index.test.ts -t \"retires a ready-confirmed worker\" -> 1 pass, 0 fail, 9 expect() calls",
      "bunTest": "env -u DISPATCH_URL -u DISPATCH_TOKEN_FILE bun test packages/daemon -> 979 pass, 5 skip, 0 fail, 3957 expect() calls, 984 tests across 57 files, exit 0"
    },
    "heads": {
      "fixCommit": "4ed969ed8136",
      "codeHead": "70e0adf42d20 (comment-sweep commit)",
      "pushedHeadBeforeThisHandoff": "7596ffc4ef2de20af4870b11d06804da7a5f87c2 (rebased `implement: record rebase3 onto main`)",
      "finalHead": "the `implement: record rebase4` commit carrying this file, .legion/-only on top of 7596ffc4"
    },
    "ci": "Tests run 34734366745 (https://github.com/sjawhar/legion/actions/runs/34734366745) at 7596ffc4ef2de20af4870b11d06804da7a5f87c2: pr-title, lint, typecheck, test all success. GitHub: MERGEABLE against main abaf0817. The run at the final head is named in the PR body's Verification/CI line.",
    "testerEvidence": "Rounds 1 and 2 (.legion/test.json) were gathered at heads whose index.ts executable lines are identical to this one; since then only comment text (main's renames and launch-hold sentences merged into the moved block), the one-line locator tag in the regression test, and doc/comment merges changed."
  },
  "schemaVersion": 1,
  "phase": "implement",
  "completed": "2026-09-13T03:04:09.990Z"
}