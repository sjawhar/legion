{
  "summary": "Part B of LEGION-25 on pull request #1110 (branch legion/LEGION-25, cut fresh from main at e7eb2f8a which carries #961). Seven plan tasks, one commit each in order, plus a formatting commit and a rebuilt claude-envoy-bridge bundle: (1) LegionDaemonApi.ControllerSecret, the external controller record on the state response, daemon-API contract 5 -> 6 with the plugin manifest, operator_token_file (required under kubernetes, refused under tmux, never read by --check-config); (2) ExternalControllerLocator / ControllerLocator admitted only on controllerLocator (type and zod), sameProcess by session id, locatorHandles empty, api/state.ts allowlist projection; (3) Runtime.controllerLaunch (daemon|operator) and controllerReadyLocator, KubernetesRuntime's Envoy-backed probe (alive/gone/unknown, controllerLivenessMs = max(boot timeout, 2 x 120 s)), stop no-op for the record; (4) ensureController under operator: mints nothing, arms no deadline, logs 'controller not registered; run legion controller start' once per worker_boot_timeout_seconds; recordControllerReady; controller-environment.ts shared by spawnController and the CLI; (5) POST /legion/v1/controller/secret behind the operator bearer (constant time), 403 for wrong/missing/unconfigured, 400 for a non-empty body; (6) legion controller start CLI with a strict twelve-key operator-side file, 0600 token-file check, secret fetch, state-dir setup, foreground OMP launch, exit code passthrough; (7) manifests (operator Secret mount, operator_token_file in base legion.yaml, kind overlay secretGenerator + operator.env.example), controller.yaml.example, docs/kubernetes.md 'Operator-launched controller', daemon and pi-envoy AGENTS.md, root AGENTS.md Commands, legion-controller skill subsection. Round 2 (reviewer REQUEST_CHANGES at e82da691, review 5204795546): three correctness threads fixed in two commits — 25404b2e (kind overlay legion.yaml carries operator_token_file; the kind walkthrough copies operator.env; a config test loads both shipped legion.yaml files under --check-config) and cb8fa7a7 (legion controller start runs every local check — token files, resolveRolePromptsDir, readDeploymentInstructions — before fetchControllerSecret, so a local failure never revokes the incumbent controller's secret; two regression tests).",
  "pullRequest": "https://github.com/sjawhar/legion/pull/1110",
  "headSha": "cb8fa7a787c94a1ec3335125cc8be387cde090a8",
  "contractVersion": "LEGION_DAEMON_API_VERSION = 6 and packages/pi-envoy/package.json legion.daemonApiVersion = 6 (main was 5 at branch time and is still 5 at 73a526e3; re-read at any merge rebase). The dogfood profile must install the first @sjawhar/pi-legion-envoy release declaring legion.daemonApiVersion: 6, cut by the Release workflow from this pull request's merge commit (the merger names the tag; 1.35.0, 1.35.1, and 1.36.0 shipped at contract 5), before the daemon restarts on this commit.",
  "filesChanged": [
    "packages/contracts/src/legion-daemon-api.ts",
    "packages/contracts/src/legion-daemon-api.test.ts",
    "packages/pi-envoy/package.json",
    "packages/pi-envoy/CHANGELOG.md",
    "packages/pi-envoy/AGENTS.md",
    "packages/daemon/src/daemon/config.ts",
    "packages/daemon/src/daemon/__tests__/config.test.ts",
    "packages/daemon/src/daemon/runtime.ts",
    "packages/daemon/src/daemon/runtime-tmux.ts",
    "packages/daemon/src/daemon/runtime-kubernetes.ts",
    "packages/daemon/src/daemon/legion-state.ts",
    "packages/daemon/src/daemon/api/state.ts",
    "packages/daemon/src/daemon/api/context.ts",
    "packages/daemon/src/daemon/api/routes/controller.ts",
    "packages/daemon/src/daemon/api.ts",
    "packages/daemon/src/daemon/index.ts",
    "packages/daemon/src/daemon/processes.ts",
    "packages/daemon/src/daemon/controller-environment.ts",
    "packages/daemon/src/daemon/environment.ts",
    "packages/daemon/src/daemon/AGENTS.md",
    "packages/daemon/src/daemon/__tests__/fake-runtime.ts",
    "packages/daemon/src/daemon/__tests__/legion-state.test.ts",
    "packages/daemon/src/daemon/__tests__/runtime.contract.test.ts",
    "packages/daemon/src/daemon/__tests__/runtime-kubernetes.test.ts",
    "packages/daemon/src/daemon/__tests__/processes.test.ts",
    "packages/daemon/src/daemon/__tests__/api.test.ts",
    "packages/daemon/src/daemon/__tests__/api.thermo-auth.test.ts",
    "packages/daemon/src/daemon/__tests__/worker-stream-e2e.test.ts",
    "packages/daemon/src/cli/controller-start.ts",
    "packages/daemon/src/cli/__tests__/controller-start.test.ts",
    "packages/daemon/src/cli/index.ts",
    "packages/daemon/src/cli/__tests__/index.test.ts",
    "packages/claude-envoy-bridge/dist/envoy-channel.js",
    "packages/claude-envoy-bridge/dist/open-asks-hook.js",
    "deploy/kubernetes/daemon/base/deployment.yaml",
    "deploy/kubernetes/daemon/base/legion.yaml",
    "deploy/kubernetes/daemon/overlays/kind/kustomization.yaml",
    "deploy/kubernetes/daemon/overlays/kind/secrets/operator.env.example",
    "deploy/kubernetes/daemon/controller.yaml.example",
    "docs/kubernetes.md",
    "skills/legion-controller/SKILL.md",
    "AGENTS.md",
    "packages/daemon/src/daemon/deployment-instructions.ts",
    "deploy/kubernetes/daemon/overlays/kind/legion.yaml"
  ],
  "proof": [
    {
      "criterion": "Acceptance 3: legion controller start launches the controller on the operator's machine with LEGION_CONTROLLER, LEGION_CONTROLLER_SECRET_FILE (fetched from POST /legion/v1/controller/secret), LEGION_DAEMON_URL — the files it writes and their modes, the exact environment and prompt arguments, a 403, an unreachable daemon, a missing/blank/group-readable token file, --daemon-url, an unknown key.",
      "surface": "The legion CLI (packages/daemon/src/cli/controller-start.ts, wired at legion controller start) against a fake daemon and a real temp filesystem — the unit/integration suite, per Sami's 2026-09-13 19:00Z standing order (no smoke rig, scratch daemon, kind cluster, or throwaway NATS). Also the real binary: bun src/cli/index.ts controller start.",
      "command": "cd packages/daemon && bun test src/cli/__tests__/controller-start.test.ts; and bun src/cli/index.ts controller start [--config <file>] against a 0644 token file and against daemon_url http://127.0.0.1:1",
      "observed": "18 pass, 0 fail (round 2 adds the two ordering regression tests). The happy path records exactly one POST http://daemon.test:13370/legion/v1/controller/secret with authorization 'Bearer <trimmed token file contents>', content-type application/json, body '{}'; writes secrets/legion-demo-controller (0600, contents s3cret) under a 0700 secrets/, worker-bin/gh 0700, bin/legion executable, deployment-instructions.md starting '# Deployment instructions (demo)', and spawns with cwd <stateDir>/controller; launches exactly `<withOmpLaunchPrefix(prefix, invocation)> <systemPromptArguments(controller-root.md, undefined, deployment-instructions.md)>` with no --resume and no --mode rpc, and the environment adds exactly LEGION_CONTROLLER=1, LEGION_ROLE=controller, LEGION_DAEMON_URL, LEGION_PROJECT=demo, LEGION_STATE_DIR, ENVOY_NATS_URL (joined), ENVOY_URL, PATH=<stateDir>/worker-bin:<stateDir>/bin:/usr/bin (inherited worker-bin entry stripped), GH_CONFIG_DIR, GH_TOKEN='', GITHUB_TOKEN='', GH_HOST='', LEGION_GRANT_FILE, DISPATCH_URL, DISPATCH_TOKEN_FILE, LEGION_CONTROLLER_SECRET_FILE, ENVOY_TOKEN_FILE — no LEGION_CONTROLLER_SECRET, ENVOY_TOKEN, or DISPATCH_TOKEN key, and the secret value appears nowhere in the launch; the command resolves to Oh My Pi's exit code (3 -> 3); --daemon-url http://127.0.0.1:13370/ overrides both the request URL and LEGION_DAEMON_URL (trailing slash stripped). Real binary: no --config prints 'Usage: legion controller start --config <controller.yaml> [--daemon-url <url>]' exit 1; a 0644 token prints 'operator_token_file <path> is readable by its group or others (mode 0644); chmod 0600 it'; an unreachable daemon prints 'could not reach the Legion daemon at http://127.0.0.1:1: …; is the port-forward running? (never falls back to another address)'.",
      "headSha": "cb8fa7a787c94a1ec3335125cc8be387cde090a8",
      "negativeControl": "A group-readable operator token file (mode 0640) -> CliError 'operator_token_file <path> is readable by its group or others (mode 0640); chmod 0600 it' with zero fetches; a missing file -> 'operator_token_file names <path>, which could not be read: ENOENT…' and a blank one -> '…, which is empty', both with zero fetches; the daemon answering 403 -> CliError '<url> answered 403: Invalid operator token — the operator token does not match the daemon's operator_token_file, or this daemon has none configured', no secrets/ written, no spawn; fetch rejecting ECONNREFUSED -> exactly one attempt, no state dir, no spawn; 'runtime: kubernetes' in the file -> 'unknown key \"runtime\" …' naming deploy/kubernetes/daemon/controller.yaml.example; missing nats_urls -> 'nats_urls is required in the controller configuration'; dispatch_url without dispatch_token_file (and the converse) refused; a /mcp dispatch_url refused. Round 2: LEGION_ROLE_PROMPTS_DIR pointing at an empty directory -> CliError '[legion] Role prompts directory <dir> is missing … controller-root.md …' with zero recorded fetches, no spawn, no state directory; a missing instructions file -> '[legion] instructions file <path> could not be read: ENOENT …' and a blank one -> '… is empty', both with zero fetches — the running controller's secret is never revoked for a failure the operator's machine could detect itself."
    },
    {
      "criterion": "Acceptance 3 (daemon side): POST /legion/v1/controller/secret mints and stores the controller secret exactly as spawnController does — good token, wrong token, missing header, unconfigured daemon.",
      "surface": "The daemon's HTTP API served by startLegionApi on a real loopback port (packages/daemon/src/daemon/__tests__/api.test.ts), real fetch round trips — the integration suite, per the standing order.",
      "command": "cd packages/daemon && bun test src/daemon/__tests__/api.test.ts -t \"controller/secret\"",
      "observed": "4 pass, 0 fail. start({operatorToken:'op-tok'}); POST /legion/v1/controller/secret with 'Authorization: Bearer op-tok' and body {} -> 200 {secret: A} (a UUID); POST /controller/ready with A -> 200; a second POST secret -> 200 {secret: B}, B != A; ready with A -> 403; ready with B -> 200 and state.roles[controllerToken] moves to the new session id.",
      "headSha": "cb8fa7a787c94a1ec3335125cc8be387cde090a8",
      "negativeControl": "Wrong bearer -> 403 {error:'Invalid operator token'}, state.controllerCapabilityHash unchanged, one log line '[legion] refused POST /legion/v1/controller/secret: wrong operator token'; no Authorization header -> 403 the same with '…: no bearer token'; a daemon started without operatorToken (tmux) -> 403 'This daemon has no operator_token_file configured; the controller secret route is disabled', hash unchanged, one log line; body {token:'op-tok'} -> 400 (strict contract). The daemon-minted controller secret keeps working after the refusals."
    },
    {
      "criterion": "Acceptance 4 (in runtime: kubernetes the daemon never launches the controller; ensureController logs once per interval; controllerAlive reads the Envoy role holder) and Acceptance 5 (tmux unchanged), plus /controller/ready recording the external record and the state page projecting it.",
      "surface": "ProcessManager over FakeRuntime({controllerLaunch:'operator'}) and KubernetesRuntime over a recording Envoy fetch fake (processes.test.ts, runtime-kubernetes.test.ts, api.test.ts, legion-state.test.ts, runtime.contract.test.ts) — the unit/integration suite, per the standing order.",
      "command": "cd packages/daemon && bun test (full: 1669 pass / 0 fail / 14 skip across 69 files); bunx tsc --noEmit; bunx biome check src/ — and in packages/contracts (120 pass) and packages/pi-envoy (187 pass; daemon-api-version.test.ts pins the manifest to LEGION_DAEMON_API_VERSION = 6)",
      "observed": "Under an operator-launched runtime three ensureController calls inside one worker_boot_timeout_seconds interval log exactly one '[legion] controller not registered; run legion controller start', a fourth after the interval logs a second; zero mints, zero spawns, no controllerLocator, no registration-deadline sleep armed. A live external record (FakeRuntime.setExternalController alive) logs nothing and arms no deadline; a gone one logs once and leaves the record in state. recordControllerReady('ses_a') then ('ses_b') leaves controllerLocator {runtime:'kubernetes', external:true, sessionId:'ses_b', registeredAt: now} and re-arms the log; it is false and writes nothing under a daemon-launched runtime. KubernetesRuntime.probe of the record GETs http://envoy.test:9020/v1/roles/legion-omp-controller with 'Authorization: Bearer <token>' (no header without a token): alive at last_seen = now-239999 ms, gone at now-240000 ms (log 'last seen 240s ago'), the window never below the boot timeout (600 s boot -> alive at 599999 ms), gone on 404 and on another holder (log names both ids), unknown on ECONNREFUSED, a 500 (log says whether a bearer was sent), and an unparseable body; stop of the record makes no API call. /controller/ready with a recordControllerReady that returns true records the external record, sets the role, ignores ompSessionFile, never stashes. GET /legion/v1/state projects {runtime:'kubernetes', external:true, sessionId:'ses_op', registeredAt:1234} unchanged and passes the strict contract. legion-state admits the record on controllerLocator only (refused on a tree and a worker claim, refused without registeredAt). TmuxRuntime: controllerLaunch 'daemon', controllerReadyLocator undefined; the existing controller pane env pin in processes.test.ts (exact -e key order) still passes byte for byte, now through controllerProcessEnvironment.",
      "headSha": "cb8fa7a787c94a1ec3335125cc8be387cde090a8",
      "negativeControl": "The tmux runtime refuses the external record in probe and stop with 'tmux runtime cannot operate a kubernetes or operator-launched controller locator'; a stale-generation/wrong-session external record never equals a pod locator in sameProcess (both directions) and locatorHandles yields nothing for it; a kubernetes legion.yaml without operator_token_file is refused 'operator_token_file is required when runtime is kubernetes: …' and a tmux one with it 'operator_token_file is only used when runtime is kubernetes: …'."
    },
    {
      "criterion": "Acceptance 3's live kind check: port-forward, legion controller start, legion state showing the external record, a triage notice reaching that terminal.",
      "surface": "BLOCKED-BY-ORDER (Sami, 2026-09-13 19:00Z: no smoke rig, scratch daemon, kind cluster, or throwaway NATS). Not run. Recorded verbatim for when the order lifts.",
      "command": "kubectl -n legion port-forward svc/legion-daemon-demo 13370:13370 & legion controller start --config deploy/kubernetes/daemon/controller.yaml.example --daemon-url http://127.0.0.1:13370; then legion state --json",
      "observed": "Not executed under the standing order. Expected: legion state shows controllerLocator {runtime:'kubernetes', external:true, sessionId, registeredAt}; creating a root issue in the overlay's dispatch_project makes the daemon publish {type:'triage'} to notifications.role.legion-demo-controller and the notice reaches the operator's terminal.",
      "headSha": "cb8fa7a787c94a1ec3335125cc8be387cde090a8",
      "negativeControl": "Planned for that run: a wrong OPERATOR_TOKEN (exit non-zero naming the URL, 403); the port-forward closed (exit non-zero naming the URL); the terminal closed (the daemon logs 'controller not registered; run legion controller start' once per interval and launches nothing)."
    },
    {
      "criterion": "Reviewer thread r4011301104: the kind overlay's legion.yaml (which replaces the base ConfigMap key wholesale) must carry the now-mandatory operator_token_file, or the kind daemon refuses to boot.",
      "surface": "The shipped deploy/kubernetes/daemon files loaded the way `legion start --check-config` loads them (loadConfigFromFile + resolveDaemonConfig({resolveSecrets:false}), pointers validated as paths and never read) — the unit suite, per the standing order; the kind check itself stays BLOCKED-BY-ORDER.",
      "command": "cd packages/daemon && bun test src/daemon/__tests__/config.test.ts -t \"shipped in-cluster\"",
      "observed": "2 pass: both the base and the kind overlay resolve to runtime kubernetes with envoyToken and operatorToken '(not executed)'. Before 25404b2e the overlay case failed with exactly the reviewer's reproduction: 'operator_token_file is required when runtime is kubernetes: the daemon cannot launch the controller there; legion controller start presents this token'.",
      "headSha": "cb8fa7a787c94a1ec3335125cc8be387cde090a8",
      "negativeControl": "The same test against the pre-fix overlay (RED run before the edit) refused the overlay while the base passed — the drift the test now locks out."
    }
  ],
  "trickyParts": [
    "#961's spawnController reads controllerLocator.ompSessionFile twice; the plan did not list those sites. Rather than teach ProcessManager the record's shape, the narrowing lives in runtime.ts as resumableTranscript(locator) (a process locator's ompSessionFile, undefined for the external record), so processes.ts still reads no runtime-specific locator field — the runtime.contract.test.ts 'ProcessManager stays runtime-agnostic' guards still pass.",
    "Bun's `typeof fetch` requires `preconnect`, which no test fake provides; KubernetesRuntimeDeps.fetch and ControllerStartDeps.fetch are typed as the one call signature each makes (RoleLookupFetch / SecretFetch).",
    "operator_token_file is required under kubernetes, so every existing kubernetes config-test fixture needed a token: a kubernetesOverrides fixture supplies it as the programmatic cliOverrides.operatorToken (the same seam envoyToken uses), and the --check-config CLI test's in-cluster legion.yaml names an operator_token_file under a missing mount to prove it is validated, never read.",
    "packages/claude-envoy-bridge commits a bundle of @legion/contracts; CI's 'Check the committed bundle is fresh' failed at the first push. Rebuilt with `bun run build` on the pinned Bun 1.3.14 and committed (chore commit). Its own suite passes 41/41 once the pane's DISPATCH_TOKEN_FILE is unset — one test reads the real bearer from this pane's environment, unrelated to this change.",
    "The controller test in processes.test.ts that occupies the controller's handle narrows the spawned locator with `first?.runtime !== 'kubernetes' || 'external' in first` because FakeRuntime.connect/occupyHandle take Locator, not ControllerLocator."
  ],
  "deviations": [
    "One extra commit beyond the plan's seven: `style(daemon): biome formatting and import order for LEGION-25 Part B` (biome --write over the touched files), and one more for the rebuilt claude-envoy-bridge bundle the contract change made stale.",
    "The plan's Task 2 named `redactLocator(state.controllerLocator, true)` for the tmux branch; kept, with the external branch via redactExternalController — an explicit field allowlist per api/state.ts's rule.",
    "Constraint (d) note: the operator token is file-only in every production path; `cliOverrides.operatorToken` exists only as the same programmatic seam every DaemonConfig field has (Partial<DaemonConfig>) and is used by tests alone — no CLI flag or environment variable reads it."
  ],
  "openQuestions": [],
  "blockedByOrder": [
    "Sami 2026-09-13 19:00Z: the kind check in proof[3] is recorded, not run."
  ],
  "round2": {
    "review": "https://github.com/sjawhar/legion/pull/1110#pullrequestreview-5204795546",
    "reviewedHead": "e82da69112b42591365b11011700c923e5adbb43",
    "fixes": [
      {
        "thread": "https://github.com/sjawhar/legion/pull/1110#discussion_r4011301104",
        "commit": "25404b2e0541",
        "change": "deploy/kubernetes/daemon/overlays/kind/legion.yaml gains operator_token_file: /var/run/legion/operator/OPERATOR_TOKEN with the base's comment; config.test.ts 'the shipped in-cluster legion.yaml files' loads base and overlay under --check-config so the two cannot drift."
      },
      {
        "thread": "https://github.com/sjawhar/legion/pull/1110#discussion_r4011301111",
        "commit": "25404b2e0541",
        "change": "docs/kubernetes.md kind shell block: `cp secrets/operator.env.example secrets/operator.env` beside the providers cp, with the openssl rand -hex 32 hint."
      },
      {
        "thread": "https://github.com/sjawhar/legion/pull/1110#discussion_r4011301117",
        "commit": "cb8fa7a787c9",
        "change": "controller-start.ts: readOperatorTokenFile, the envoy/dispatch token-file checks, resolveRolePromptsDir (stats every ROLE_PROMPT_FILES entry, controller-root.md included), and readDeploymentInstructions (new read-only half of deployment-instructions.ts; materializeDeploymentInstructions now calls it) all run before fetchControllerSecret; only writeSecretFile and what follows it run after the daemon's answer. Regression tests: empty LEGION_ROLE_PROMPTS_DIR and missing/blank instructions file -> CliError naming the path, fetches [], no state dir."
      }
    ],
    "threadsResolve": "legion threads resolve --pr 1110 --repo sjawhar/legion before the push, exit 0: three `left open … newest reply by legion-reviewer is not an acceptance` lines (the reviewer's Accepted: follows re-review).",
    "conflicting": "GitHub reports #1110 CONFLICTING at cb8fa7a7: main moved to 73a526e3 with pi-envoy releases 1.35.0/1.35.1/1.36.0 and envoy-plugin 1.25.0/1.26.0, whose release commits rewrite packages/pi-envoy/CHANGELOG.md (Unreleased), package.json (version), and packages/claude-envoy-bridge/dist/*.js — files this branch also changes for contract 6. No `Tests`/`PR Title` run exists at cb8fa7a7 (a conflicting PR has no merge ref). Not rebased: reported to the architect per instruction; the conflict-forced rebase (whole chain, fingerprint before/after, one PR comment) awaits the architect's go-ahead."
  },
  "schemaVersion": 1,
  "phase": "implement",
  "completed": "2026-09-15T02:23:55.518Z"
}