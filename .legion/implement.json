{
  "proof": [
    {
      "criterion": "Acceptance 3 (daemon half): the daemon log says when it retires a live-but-unregistered controller or root",
      "surface": "Unit suites only — Sami's 2026-09-13 19:00Z standing order forbids smoke rigs, so no production-like surface was permitted; the architect stopped the tree",
      "command": "cd packages/daemon && bun test src/daemon/__tests__/processes.test.ts -t \"registration deadline\"; bun test src/daemon/__tests__/runtime.contract.test.ts -t describeProcessLocation",
      "observed": "12 pass 0 fail: the stuck-controller and alive-but-unconfirmed-root tests each observe exactly one [legion] retiring line with pane, window, 360s deadline, and the attach command, emitted before kill-pane; the dead-pane test observes none; describeProcessLocation renders tmux and kubernetes locators with no undefined",
      "headSha": "d03cc7b6ba6e6bf652ea06d48a1703844b773c22",
      "negativeControl": "A pane that died on its own (dead-pane test) resurrects with zero retiring lines — the line means alive-but-unregistered, never a routine death"
    },
    {
      "criterion": "Acceptance 3 (extension half): a both-markers pane is told why on the pane, claims nothing, never exits, and tool calls continue",
      "surface": "Unit suite only — same standing order; live-pane visibility of ui.notify is unproven and recorded as such",
      "command": "cd packages/pi-envoy && bun test extensions/legion.test.ts -t \"both controller and tree markers\"",
      "observed": "2 pass 0 fail: session_start resolves with no /legion/ or /v1/roles/set request, no exit-hook call, one ui.notify(warning) with CONFLICTING_LAUNCH_MARKERS_NOTICE and one console.error; a following bash tool_call resolves undefined",
      "headSha": "d03cc7b6ba6e6bf652ea06d48a1703844b773c22",
      "negativeControl": "The pre-existing classify test still throws \"both controller and tree launch markers\" from classifySession itself — nothing tolerates the combination; a malformed LEGION_ROLE still escapes"
    },
    {
      "criterion": "Acceptance 1+2 (rig half): checkpoint 14 requires the controller claim and fails a pane carrying an identity the daemon did not set",
      "surface": "checkpoints.test.sh fixture harness only — same standing order; never run against a rig",
      "command": "bash scripts/smoke/checkpoints.test.sh && bash scripts/smoke/up.test.sh",
      "observed": "Full PASS including: 14 passes on the claimed fixture; fails naming the missing claim legion-exampleorg24-controller with the attach command; fails naming controller pane %1 pid and LEGION_TREE without printing CANARY-1; fails naming a root pane whose LEGION_TREE differs from its recorded tree; 14 runs ungated under recorded none and forward modes",
      "headSha": "d03cc7b6ba6e6bf652ea06d48a1703844b773c22",
      "negativeControl": "write_state (no controller claim) and the controller_canary/root_wrong fixture processes are the deliberately broken inputs; each produces the exact failure text with no observed value printed"
    }
  ],
  "filesChanged": [
    "packages/daemon/src/daemon/runtime.ts",
    "packages/daemon/src/daemon/processes.ts",
    "packages/daemon/src/daemon/__tests__/processes.test.ts",
    "packages/daemon/src/daemon/__tests__/runtime.contract.test.ts",
    "packages/daemon/src/daemon/AGENTS.md",
    "packages/pi-envoy/src/legion/classify.ts",
    "packages/pi-envoy/extensions/legion.ts",
    "packages/pi-envoy/extensions/legion.test.ts",
    "packages/pi-envoy/AGENTS.md",
    "scripts/smoke/checkpoints.sh",
    "scripts/smoke/checkpoints.test.sh",
    "scripts/smoke/README.md",
    "scripts/smoke/up.sh",
    "scripts/smoke/up.test.sh",
    "docs/solutions/daemon/config-env-keys-that-panes-also-carry.md"
  ],
  "deviations": [
    "Tree stopped mid-implementation by the architect under Sami's 2026-09-13 19:00Z standing order (no smoke rigs of any kind); LEGION-88 goes to icebox. No pull request opened, no rig proof, no dispatch_message, and legion handoff complete deliberately not run per the architect's instruction.",
    "Committed tasks: 2 (8b5bd3f2 daemon retirement log lines), 3 (6432d76e smoke checkpoint 14), 4 (7c52c337 identity-scrub solution doc), 1 (d03cc7b6 pi-envoy both-markers refusal). Plan task 5 (rig proofs, PR, plugin-version message) not performed.",
    "Task 2 is the one piece with a production user if this reopens: the daemon's retirement lines fire on the dogfood daemon whenever a controller or root is alive but never registers, independent of any rig.",
    "The one rig started before the order arrived (positive, none mode, 19:02Z) was torn down with down.sh at 19:05Z; its only observation was checkpoint 14's missing-claim failure text working as designed while the controller booted. The canary rig was never started."
  ],
  "trickyParts": [
    "Checkpoint 14 walks each pane's pid plus descendants: every pane runs worker-shim above the launch prefix, so an injected identity sits on the OMP process, never the pane pid; a pane-pid-only read would pass the exact rig the check exists to fail (plan GT5).",
    "Pane channel for CONFLICTING_LAUNCH_MARKERS_NOTICE chosen from source evidence only: ui.notify(warning) is envoy.ts's own session_start warning channel, and console.error reaches a daemon-spawned pane's terminal because worker-shim spawns OMP with stderr: inherit (worker-shim.ts:381). Live-pane visibility is UNPROVEN — the capture-pane proof was cancelled with the rig. If this reopens, prove the channel on a pane first and add the pi.sendMessage aside if notify turns out transient."
  ],
  "openQuestions": [
    "Plugin release: the extension change reaches production only through a pi-legion-envoy release built from this branch (next after 1.24.2) installed into the legion profile by the operator; not reported on the issue because the architect stopped the tree before the PR."
  ],
  "schemaVersion": 1,
  "phase": "implement",
  "completed": "2026-09-13T19:32:08.696Z"
}