{
  "issue": "LEGION-13",
  "pr": 978,
  "prUrl": "https://github.com/sjawhar/legion/pull/978",
  "headSha": "f90cf313075cfcb4c8a02b758ca1cb2466989dca",
  "headNote": "f90cf313 is the docs commit at PR open; the `implement: record handoff` commit that follows it is the branch head the tester checks out",
  "branch": "legion/LEGION-13",
  "mergeTier": "T2 (bug fix)",
  "commits": [
    "95b064ee fix(cli): legion start resolves its config from an injected env, not process.env — packages/daemon/src/cli/index.ts, packages/daemon/src/cli/__tests__/index.test.ts",
    "f90cf313 docs(solutions): retire the env -u workaround for the check-config test — docs/solutions/legion/worker-pane-shell-gotchas.md"
  ],
  "branchSurgery": {
    "abandoned": "3569742b docs(plans): LEGION-13 check-config env injection plan — abandoned at the architect's direction per LEGION-18 (Sami's rule: no plan documents in pull requests); plan.json's `planDoc` field therefore names a file that is not on the branch",
    "rebase": "jj git fetch && jj rebase -d main@origin once at phase start: base 7db2cd83 → fd4fce51 (chore: release pi-envoy v1.11.1); plan commit 7944015d → 69f26404; no conflicts",
    "neverCommitted": ".omp/config.yml (extension-provisioned, empty; still the only working-copy change)"
  },
  "divergencesFromPlan": [
    "main@origin already carried PR #967 (LEGION-22), which made the `legion start --check-config` describe scrub LEGION_*/DISPATCH_*/ENVOY_* from process.env in beforeEach/afterEach — the alternative spec v5 § Rejected forbids. Removed that scrub, its comment, and the now-unused beforeEach/afterEach imports; the two tests pass { PATH, HOME } and the third test pins injection. Architect approved by envoy_send before the commit.",
    "docs § 2 had grown three paragraphs (check-config history + env -u; the same leak in packages/pi-envoy from LEGION-29/#970; run bun test from packages/daemon from LEGION-14). Replaced only the first with the current behaviour (the env seam), retitled the section, kept the other two; dropped the check-config symptom line and the title fragment; related_issues unchanged. Architect approved.",
    "Test counts are 18 per shape, not the plan's 15: main added three `legion probe-image` tests to the same file."
  ],
  "gates": {
    "cwd": "packages/daemon, inside the live worker pane (DISPATCH_URL=http://localhost:8766, DISPATCH_TOKEN unset, DISPATCH_TOKEN_FILE set), no env prefix",
    "biome": "bunx biome check src/ — Checked 143 files, No fixes applied, exit 0",
    "tsc": "bunx tsc --noEmit — exit 0",
    "bunTest": "bun test — 872 pass, 5 skip, 0 fail, 3506 expect() calls, 56 files, exit 0"
  },
  "fourShapes": {
    "file": "src/cli/__tests__/index.test.ts",
    "paneUnprefixed": "18 pass / 0 fail, exit 0",
    "urlPlusTokenFile": "DISPATCH_URL=http://127.0.0.1:1 DISPATCH_TOKEN_FILE=/dev/null — 18 pass / 0 fail, exit 0",
    "urlPlusToken": "DISPATCH_URL=http://127.0.0.1:1 DISPATCH_TOKEN=t — 18 pass / 0 fail, exit 0",
    "unsetAll": "env -u DISPATCH_URL -u DISPATCH_TOKEN -u DISPATCH_TOKEN_FILE — 18 pass / 0 fail, exit 0"
  },
  "negativeControls": {
    "red": "tests first, old index.ts, pane unprefixed: 16 pass / 2 fail (validates private_key_command; the new injected-env test); tsc 4 × TS2554 at the cmdCheckConfig call sites",
    "revertGuard": "loadStartConfig with `env,` reverted to `env: process.env`: 17 pass / 1 fail in both the URL+token shape and the all-unset shape; the only failure is `resolves Dispatch settings from the injected env, not the process environment`",
    "operatorSurface": "bun src/cli/index.ts start acme99 --check-config --config <legion.yaml without dispatch_url>: DISPATCH_URL without DISPATCH_TOKEN → `error: dispatch_url is set but DISPATCH_TOKEN is not; the dispatch tools would not register`, exit 1; with DISPATCH_TOKEN=t → `Config OK: project=acme99`, exit 0; pane env as-is → the same error, exit 1 (the citty handler passes process.env, unchanged behaviour)"
  },
  "forTester": "verificationForTester in plan.json stands, with 18 pass per shape instead of 15. The E2E line in the PR body carries the implementer's evidence and is labelled as such; replace it with your own run at the head you check out.",
  "environmentNotes": [
    "Architect role topic had no Envoy holder at phase start; reached the architect by envoy_send to roles[...architect].sessionId from `legion state` (worker-pane-shell-gotchas § 7).",
    "Grant stacking (LEGION-12 unmerged): sourced /tmp/legion9-grant-trap.sh and ended every push/gh/handoff call with grant_release (§ 1)."
  ],
  "schemaVersion": 1,
  "phase": "implement",
  "completed": "2026-09-12T20:11:07.568Z"
}