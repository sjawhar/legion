{
  "pr": "https://github.com/sjawhar/legion/pull/1015",
  "prNumber": 1015,
  "bookmark": "legion/LEGION-78",
  "head": "1113edbefac5",
  "commits": [
    "b8659a22ef86 cli: legion gh refuses every GitHub-issue write (LEGION-78) — packages/daemon/src/cli/index.ts, packages/daemon/src/cli/__tests__/index.test.ts, AGENTS.md, skills/legion-worker/SKILL.md (refusal sentence)",
    "1113edbefac5 skills: the retro post is a dispatch_message on the Dispatch issue (LEGION-78) — skills/legion-retro/SKILL.md, skills/legion-architect/SKILL.md, skills/legion-worker/SKILL.md (D6 footer sentence)"
  ],
  "filesChanged": [
    "packages/daemon/src/cli/index.ts",
    "packages/daemon/src/cli/__tests__/index.test.ts",
    "AGENTS.md",
    "skills/legion-worker/SKILL.md",
    "skills/legion-retro/SKILL.md",
    "skills/legion-architect/SKILL.md"
  ],
  "decisionsHonoured": {
    "D1": "ghApiMethod: explicit -X/--method (exact, -XPOST, --method=POST) else POST when -f/-F (exact or attached), --raw-field/--field/--input (exact or =-attached) present, else GET; compared case-insensitively. `--method GET … -f state=open` forwards (tested).",
    "D2": "isGitHubIssueWriteInvocation drops tokens starting with '-', finds `issue`, refuses when a later positional is one of the twelve verbs; `issue --repo o/r comment 5` refused (tested).",
    "D3": "api positional + any positional matching /\\/issues(?:[/?]|$)/ + non-GET method; six raw-API forms refused, …/pulls/5/reviews and graphql forward (tested).",
    "D4": "Message `Legion issues live on Dispatch; use dispatch_message or dispatch_comment on ${env.LEGION_ISSUE || \"the Dispatch issue\"}`, CliError exit 1.",
    "D5": "One `if` in cmdGh after the merge check and before redeemGitHubToken; all helpers module-private beside isPrMergeInvocation; no new file.",
    "D6": "legion-worker footer sentence now reads: `this footer is for GitHub PR artifacts and for the retro's Dispatch message (skills/legion-retro)`.",
    "D7": "related_issues example: \"LEGION-123\" (the Dispatch issue) and \"owner/repo#456\" (the pull request).",
    "D8": "Refusal sentences name verbs as the `issue` subcommand's comment, create, …; neither `gh issue` nor `issue comment` appears; all three acceptance greps print nothing (exit 1).",
    "D9": "Smoke ran through `cd -- \"$LEGION_WORKSPACE\" && bun packages/daemon/src/cli/index.ts gh -- …` from the implementer's own bash call; refusal names LEGION-78, exit 1; `pr view 1015 --json number` and a GET on repos/sjawhar/legion/issues/1015/comments forward."
  },
  "grepOutputs": {
    "grep -rn 'gh -- issue\\|gh issue\\|issue comment <' skills/ packages/pi-envoy/roles/": "(nothing) exit=1",
    "grep -rn 'issue comment' skills/legion-*/ packages/pi-envoy/roles/": "(nothing) exit=1",
    "grep -rn 'gh issue' skills/ packages/pi-envoy/roles/": "(nothing) exit=1"
  },
  "verification": {
    "biome": "bunx biome check src/ — 159 files, clean",
    "tsc": "bunx tsc --noEmit — clean",
    "bunTest": "bun test — 1145 pass, 0 fail, 7 pre-existing skips, 63 files",
    "guardRemovedCheck": "with the cmdGh `if` disabled, the 20 refusal tests fail and the 8 forward tests pass",
    "smoke": [
      "bun packages/daemon/src/cli/index.ts gh -- issue comment 1015 --repo sjawhar/legion --body smoke → `Legion issues live on Dispatch; use dispatch_message or dispatch_comment on LEGION-78`, exit=1",
      "bun packages/daemon/src/cli/index.ts gh -- api -X POST repos/sjawhar/legion/issues/1/comments -f body=smoke → same message, exit=1",
      "bun packages/daemon/src/cli/index.ts gh -- pr view 1015 --repo sjawhar/legion --json number → {\"number\":1015}, exit=0",
      "bun packages/daemon/src/cli/index.ts gh -- api repos/sjawhar/legion/issues/1015/comments --jq length → 0, exit=0"
    ]
  },
  "trickyParts": [
    "The verb table is a `Record<string, true>` checked with `Object.hasOwn`, not the plan's literal `new Set`: the repository rule prefers Record for a static string table (state/fetch.ts precedent), and `Object.hasOwn` rather than indexing keeps an argv token such as `toString` or `constructor` from matching through the prototype.",
    "skills/legion-worker/SKILL.md carries edits from two plan steps (the refusal sentence and the D6 footer sentence); the D6 sentence was held out of the first split and applied in the second so each commit is exactly what the plan names.",
    "The pane's `legion` re-execs the daemon checkout's CLI, so `legion gh -- issue comment …` from this pane still forwards until that checkout carries the merge; every smoke ran the workspace entry with bun (D9)."
  ],
  "deviations": [
    "PR title was first opened without a conventional-commit type and the `pr-title` job failed; retitled to `fix(cli): legion gh refuses every GitHub-issue write; the retro post is a dispatch_message (LEGION-78)`, which passes.",
    "The CI line in the PR body is written as pending at 1113edbefac5 and updated once the Tests workflow settles."
  ],
  "openQuestions": [],
  "notes": [
    "An empty `.omp/config.yml` appeared in the working copy at this session's start (OMP's own project config, created at 09:36:42Z, the second this process started). It is left uncommitted in the working-copy commit above the pushed head; no commit includes it."
  ],
  "schemaVersion": 1,
  "phase": "implement",
  "completed": "2026-09-13T09:49:57.596Z"
}