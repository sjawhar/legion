{
  "summary": "All nine plan tasks are done and the Legion pull request is open: https://github.com/sjawhar/legion/pull/1081 (legion/LEGION-80, tip 7bc83404581086da2d68155256010aa8eaa00989, MERGEABLE). Fork: sjawhar/oh-my-pi feat/session-sql-storage, authored at 1fe988b7475185c0d6a6fb925ba6e1c25f3dde5c and rebased by the operator to f3278eeda197ba1e241c2800b8a0baf21ef2dc19 (resolver, seam, and test blobs identical), released as tag v18.1.21-sami.20260914-060332 (linux-x64 asset; the tag carries session-storage-config.ts at blob 3c06bf37). No upstream PR on can1357/oh-my-pi yet — the operator's, waiting on Sami's contributor sentence, and per spec v9 not on this issue's path. Legion: OMP_FORK_PIN bumped to github:sjawhar/oh-my-pi@18.1.21-sami.20260914-060332; isSubagentSession storage-independent; verifySessionStorageSetting third in legion probe-image with SESSION_STORAGE_PROBE_MARK on the OK line; docs/kubernetes.md Session store (spec v9 corrections applied: ompSessionFile unchanged, probe runs inside the image, daemon requires the token); every proof in plan.json testPlan.commands run on the tagged build and quoted verbatim in the PR body's E2E section.",
  "status": "round 2 partial: Legion-side nits and docs done and pushed (0b8b3cbbf7a3); the fork commit (A), the operator release request (B), the pin bump and round-2 proofs (D) are blocked — this pane's user cannot reach the knives-managed fork checkout",
  "fork": {
    "repo": "sjawhar/oh-my-pi (knives-managed; upstream can1357/oh-my-pi)",
    "branch": "feat/session-sql-storage",
    "tip": "1fe988b7475185c0d6a6fb925ba6e1c25f3dde5c",
    "base": "e4dd2ec3b487f216c569281e2cdb7ec476a81f2e (chore: bump version to 18.1.19; the sami release's shared base)",
    "filesChanged": [
      "packages/coding-agent/src/config/settings-schema.ts (session.storage enum file|sql default file; session.sql.dsnFile string; both hidden, env precedence comment)",
      "packages/coding-agent/src/session/session-storage-config.ts (new: resolveSessionStorage({settings, env}), SessionStorageConfigError, SESSION_STORAGE_ENV, SESSION_SQL_DSN_FILE_ENV)",
      "packages/coding-agent/src/session/session-storage.ts (defaultSessionStorage(), setDefaultSessionStorage())",
      "packages/coding-agent/src/session/session-manager.ts, session-listing.ts, session-loader.ts, cli/gc-cli.ts, cli/session-picker.ts, modes/startup-composer.ts, modes/controllers/selector-controller.ts (every `new FileSessionStorage()` default → defaultSessionStorage(); 24 sites)",
      "packages/coding-agent/src/main.ts (setDefaultSessionStorage(await resolveSessionStorage(...)) inside the session-resolution try; SessionStorageConfigError printed to stderr and exit 1 like SessionResolutionError)",
      "packages/coding-agent/test/session/session-storage-config.test.ts (new, 8 tests)",
      "packages/coding-agent/CHANGELOG.md (Unreleased/Added, one line)",
      "docs/settings.md, docs/environment-variables.md, docs/session.md (mirrors beside auth.broker.*)"
    ],
    "verification": {
      "unit": "bun test packages/coding-agent/test/session/session-storage-config.test.ts packages/coding-agent/test/session/sql-session-storage-manager.test.ts packages/coding-agent/test/session/sql-session-storage.test.ts → 33 pass, 0 fail (closed-port test included, ~11 ms)",
      "check": "bun run check:ts → exit 0 (oxlint, oxfmt, tsgo on every workspace)",
      "knivesAudit": "knives audit --no-github: feat/session-sql-storage tip 1fe988b7 = origin_tip, fork_only false, forbidden [] ",
      "fromSourceSmoke": "bun packages/coding-agent/src/cli.ts at 1fe988b7 (OMP_PROFILE=legion, LEGION_*/DISPATCH_* scrubbed), throwaway postgres:16 on 127.0.0.1:55481: (1) -p 'Remember the code word PINEAPPLE. Reply with exactly OK.' with OMP_SESSION_STORAGE=sql → exit 0, one row path=/home/ubuntu/.omp/profiles/legion/agent/sessions/-tmp-legion-80-src/2026-09-13T16-56-00-984Z_01a09bb2-7cd8-7000-9a80-86e377f99b17.jsonl bytes=7273, assistant text \"OK\", no file session dir for the cwd; (2) --resume='<that path>' -p 'What was the code word? Reply with just the word.' → exit 0, assistant text \"PINEAPPLE\", row grew to 11594 bytes; (3) unset control -p 'Reply OK.' → a .jsonl appeared under ~/.omp/profiles/legion/agent/sessions/-tmp-legion-80-src/, row count unchanged at 1; (4) dsn-closed (postgres://…@127.0.0.1:1/postgres) → exit 1 `Error: OMP_SESSION_SQL_DSN_FILE names /tmp/legion-80-src/dsn-closed, but the session database could not be opened: Connection closed (ERR_POSTGRES_CONNECTION_CLOSED)`; (5) blank → exit 1 `… names /tmp/legion-80-src/dsn-blank, which is empty`; (6) missing → exit 1 `… names /tmp/legion-80-src/does-not-exist, which could not be read: ENOENT: no such file or directory, open '/tmp/legion-80-src/does-not-exist'`; (7) OMP_SESSION_STORAGE=redis → exit 1 `Error: OMP_SESSION_STORAGE is \"redis\"; expected \"file\" or \"sql\"`. This is the source tree, not the tagged build; tasks 8-9 repeat it on the tag."
    },
    "sessionsRootUnderProfile": "/home/ubuntu/.omp/profiles/legion/agent/sessions/<encoded cwd>/<timestamp>_<session id>.jsonl (measured; resolves the plan's [INFERENCE] on the prefix)"
  },
  "filesChanged": [
    "packages/pi-envoy/extensions/legion.ts",
    "packages/pi-envoy/extensions/legion.test.ts",
    "packages/pi-envoy/AGENTS.md",
    "docs/kubernetes.md",
    "packages/daemon/src/daemon/boot-probes.ts",
    "packages/daemon/src/daemon/__tests__/boot-probes.test.ts",
    "packages/daemon/src/cli/index.ts",
    "packages/daemon/src/cli/__tests__/index.test.ts",
    "packages/daemon/docker/worker.Dockerfile",
    "packages/daemon/src/daemon/AGENTS.md",
    "packages/daemon/src/daemon/omp-pin.ts"
  ],
  "legionCommits": [
    "b16367e694cf feat(pi-envoy): recognise a task subagent without a transcript on disk (LEGION-80) — pushed to legion/LEGION-80@origin",
    "f9c7af3294d8 feat(daemon): session-storage launch probe gates the worker image (LEGION-80) — pushed to legion/LEGION-80@origin",
    "1638e7191331 (pre-rebase) docs(kubernetes): Session store matches the settled facts; probe-image marks its success line",
    "cb9b42f89cd9 (pre-rebase) chore(daemon): pin the OMP fork release carrying session.storage",
    "rebased chain on main@origin cc88ed01: bffa1ff7 pi-envoy, fbebd64f handoff, 6c6e4de3 probe, b5296f1d handoff, f56cfb65 docs+mark, bd810bd9 pin; then 7bc83404 style(daemon): biome format"
  ],
  "legionVerification": "bun test packages/pi-envoy/extensions/legion.test.ts → 61 pass, 0 fail (red-green: the new test fails with the process-local check disabled); cd packages/pi-envoy && bun run typecheck → exit 0; biome check on the two files clean",
  "trickyParts": [
    "The fork's main.ts installs the storage inside the existing session-resolution try so SessionStorageConfigError shares SessionResolutionError's stderr+exit(1) path (hint printed only for SessionResolutionError).",
    "omp gc is on-disk maintenance (fs.readdir/rename/gzip of the sessions tree, blobs, WAL); its listing goes through defaultSessionStorage() for a single seam but the gc command does not resolve session.storage — docs/session.md says so explicitly.",
    "Bun.SQL does not connect in the constructor; the CREATE TABLE in SqlSessionStorage.create is the first round trip, so an unreachable database rejects there with PostgresError ERR_POSTGRES_CONNECTION_CLOSED 'Connection closed'; the resolver ends the client best-effort and wraps naming source+path, never the DSN.",
    "The extension's process-local record lives on globalThis under Symbol.for('legion.pi-envoy.bootstrapped-session') because each task subagent's extension instance is a separate module instance; resetLegionBootstrappedSessionForTests() clears it in the suite's afterEach. The controller branch records getSessionFile() (isSubagentSession's ensureOnDisk already persisted it) so controller subagents are recognised under SQL too.",
    "The pane's bash tool dropped twice ('Unable to connect') mid-phase; non-credentialed work ran via eval subprocesses, credentialed pushes waited for bash."
  ],
  "deviations": [
    "Scope addition (spec v3, Acceptance 6) implemented in this phase: the third launch probe, its tests, probe-image wiring, and the three-probe docs; the tagged-build probe-image pass is part of the blocked task 9.",
    "The fork docs mirror went into docs/settings.md (env table + settings table), docs/environment-variables.md (new 'Session storage' subsection), and docs/session.md ('Selecting the storage'), not only where auth.broker.url appears.",
    "The two existing fs-layout subagent tests were kept: they assert consumer-observable outcomes (no route, no claim, no exit, no gate) through the on-disk fallback that remains; only a mechanism-pinning test would have been deleted and none existed.",
    "Tasks 8-9 completed in this round: the release tag existed on sjawhar/oh-my-pi with my change (verified by blob sha) although the operator had not posted it on LEGION-80; I did not wait for the report.",
    "Conflict-forced rebase at PR open (GitHub CONFLICTING): fingerprint changed by exactly the docs intro sentence; recorded in the PR comment. One follow-up format-only commit for the daemon lint job.",
    "Round 2: the fork commit is blocked by pane access (user legion cannot reach /home/ubuntu/oh-my-pi or knives); Legion-side nits done and pushed; the thread is unanswered and threads resolve not run until the corrective push can carry the pin bump."
  ],
  "openQuestions": [],
  "forTester": "Repeat plan.json testPlan.commands on the tagged build (OMP_FORK_PIN is bumped to 18.1.21-sami.20260914-060332; `mise x github:sjawhar/oh-my-pi@18.1.21-sami.20260914-060332 -- omp`). Sessions root under the profile is ~/.omp/profiles/legion/agent/sessions/<encoded cwd>/; the -p run prints a plugin's 'No pending work…' line last, so read the assistant text from the row (`select content from omp_session_files`), not from tail. Acceptance 6: repeat `legion probe-image` with LEGION_OMP_PATH at the old pin (fails on the third probe with the predates message) and at the new pin (prints `probe-image: OK`); provider keys are not needed for the third probe but the first two run through the same environment as before.",
  "forLegion81": "Pane/pod variables are OMP_SESSION_STORAGE=sql and OMP_SESSION_SQL_DSN_FILE=<0600 file, one trimmed postgres:// URL>; resume takes --resume=<row path> = ompSessionFile unchanged; same HOME and OMP_PROFILE in the resuming pod; refuse to enable session_store: postgres on a pin older than the release carrying the setting (an older build ignores the variables and stays on files). The daemon-side guard is verifySessionStorageSetting(environment.ompInvocation, config.ompLaunchPrefix, runner, options) — same shape as the two probes startDaemonLocked already chains; run it in the launch hold only under session_store: postgres.",
  "sessionStorageProbe": {
    "what": "verifySessionStorageSetting(ompInvocation, ompLaunchPrefix, runner, options) in packages/daemon/src/daemon/boot-probes.ts; called third in cmdProbeImage (packages/daemon/src/cli/index.ts) unconditionally; NOT wired into startDaemonLocked's launch hold (LEGION-81 does that under session_store: postgres). killedOutcome's negativeMarker became optional: this probe's negative answer is an exit code, not a marker, so a budget kill is always transient for it.",
    "invocation": "sh -c 'export OMP_SESSION_STORAGE=legion-launch-probe PI_TIMING=x; exec <launch prefix> <omp> --no-session --no-extensions --no-skills --no-rules --no-lsp --no-tools </dev/null >/dev/null'",
    "whyThisPath": [
      "It must exit 0 on an old build without a provider key, network, or TTY, and run the resolver first on a new one. Subcommands (omp models, omp gc) never reach runRootCommand, where the resolver lives. -p needs a model ('No models available', exit 1 without a key). --mode rpc reaches the same exit. --resume/--from-claude exit 0 only when the box has no sessions — box-state dependent. --version/--export exit before settings load.",
      "PI_TIMING=x: an interactive start-up prints its timing tree and exits 0 right before the TUI opens (main.ts `if ($env.PI_TIMING) { logger.printTimings(); if (logger.shouldExitAfterTimings()) process.exit(0) }`, shouldExitAfterTimings = timingModeIncludes('x'|'full') in packages/utils/src/logger.ts). The resolver runs at the session-resolution block, before that exit. Stdin on /dev/null: readPipedInput returns undefined for empty non-TTY stdin, so autoPrint is false and the start is interactive (print mode would exit 1 'No models available'). --no-session keeps the probe out of the sessions directory; --no-extensions/--no-skills/--no-rules/--no-lsp/--no-tools keep the profile's plugins, servers, and tools out of a run that only needs the resolver.",
      "Measured on this box with provider keys and LEGION_*/DISPATCH_* scrubbed: old pin 104423 → exit 0 in ~5 s (timing tree on stderr); feat/session-sql-storage source → exit 1 `Error: OMP_SESSION_STORAGE is \"legion-launch-probe\"; expected \"file\" or \"sql\"`.",
      "Precondition: the build honours PI_TIMING=x. Every sami pin from 18.1.18-sami.20260912-104423 on does (measured on 104423). On a build that did not, the interactive start without a TTY would exit non-zero without naming the variable → transient → the exhausted message names the launch command.",
      "No fork commit was needed: the branch as pushed (1fe988b7) already runs the resolver before this exit-0 path."
    ],
    "classification": "exit != 0 and stderr contains 'OMP_SESSION_STORAGE' → pass; exit 0 → definitive ('… started with OMP_SESSION_STORAGE=legion-launch-probe (exit 0): this build predates the session.storage setting and would silently keep sessions on files under a sql session store; pin a fork release that carries the setting'); exit != 0 without the variable → transient (stderr tail as detail), retried per policy, 'never completed within its retry budget (N attempts) for launch command …' when exhausted; runner kill → killedOutcome (aborted → ProbeAbortedError; timedOut → transient).",
    "tests": "packages/daemon/src/daemon/__tests__/boot-probes.test.ts: pass (exact command incl. a launch prefix), exit-0 definitive with no retry, non-zero without the name retried 6× then exhausted with sleeps 10/20/40/80/160 s, budget kill transient. packages/daemon/src/cli/__tests__/index.test.ts: cmdProbeImage runs three probes (third command asserted), refuses an image whose OMP exits 0 on the nonsense value. `bun test packages/daemon/src/daemon/__tests__/boot-probes.test.ts packages/daemon/src/cli/__tests__/index.test.ts -t probe` → 13 pass; packages/daemon `bun run typecheck` exit 0; biome check clean.",
    "realProof": {
      "oldPin": "env <scrubbed> OMP_PROFILE=legion LEGION_OMP_PATH=~/.mise/installs/github-sjawhar-oh-my-pi/18.1.18-sami.20260912-104423/bin/omp bun packages/daemon/src/cli/index.ts probe-image → exit 1 in 22.6 s (first two probes pass): `[legion] OMP launch command \"/home/ubuntu/.mise/installs/github-sjawhar-oh-my-pi/18.1.18-sami.20260912-104423/bin/omp\" started with OMP_SESSION_STORAGE=legion-launch-probe (exit 0): this build predates the session.storage setting and would silently keep sessions on files under a sql session store; pin a fork release that carries the setting`",
      "branchSource": "verifySessionStorageSetting alone (throwaway script, defaultRunner, IMAGE_PROBE_RETRY) against a wrapper for `bun packages/coding-agent/src/cli.ts` at 1fe988b7 → `session-storage probe: PASS`; the same script against the old pin → FAIL with the predates message. A full `legion probe-image` pass from source is not possible: the branch alone (upstream v18.1.19 base) lacks pi.agents, a sami-release member — the first probe fails `LEGION_OMP_AGENTS=missing`. The tagged release carries both; the probe-image pass is quoted once the tag exists (task 9)."
    },
    "mark": "SESSION_STORAGE_PROBE_MARK = \"session-storage=probed\" exported from boot-probes.ts; cmdProbeImage prints `probe-image: OK (<omp path>) session-storage=probed`; the cmdProbeImage test asserts the line; LEGION-81 imports the constant and requires the token in the probe pod's output"
  },
  "taggedBuildProofs": {
    "environment": "user legion on this box (the pane moved from /home/ubuntu to /home/legion between resumes; /home/ubuntu is no longer readable, the fork checkout is not under this HOME); LEGION_*/DISPATCH_* scrubbed from every omp child; OMP_PROFILE=legion (pi-legion-envoy 1.24.2); throwaway docker postgres:16 on 127.0.0.1:55480 in-process (no daemon, bus, or tmux server — the LEGION-19 root architect's 19:04Z ruling)",
    "session": "row path /home/legion/.omp/profiles/legion/agent/sessions/-tmp-legion-80/2026-09-14T06-28-30-361Z_01a09e9a-5819-7000-a11a-8ee551531d8e.jsonl, 7026 bytes, assistant text OK",
    "resume": "--resume='<row path>' → assistant text PINEAPPLE; row grew to 11363 bytes",
    "unsetControl": "a .jsonl (01a09e9a-e295) appeared under -tmp-legion-80/, row count unchanged at 1; the SQL session left no file",
    "refusals": "closed port → exit 1 `OMP_SESSION_SQL_DSN_FILE names /tmp/legion-80/dsn-closed, but the session database could not be opened: Connection closed (ERR_POSTGRES_CONNECTION_CLOSED)`; blank → `… which is empty`; missing → `… which could not be read: ENOENT …`; row count and file count unchanged",
    "probeImage": "new pin → `probe-image: OK (/home/legion/.local/share/mise/installs/github-sjawhar-oh-my-pi/18.1.21-sami.20260914-060332/bin/omp) session-storage=probed` exit 0 (7.2 s); old pin 104423 (installed for this user with mise x) → exit 1 with the predates message (10.2 s)",
    "ompSessionRef": "sessions root under the profile measured: /home/legion/.omp/profiles/legion/agent/sessions/<encoded cwd>/<timestamp>_<id>.jsonl (was /home/ubuntu/... under the previous user)"
  },
  "releaseTag": {
    "tag": "v18.1.21-sami.20260914-060332",
    "pin": "github:sjawhar/oh-my-pi@18.1.21-sami.20260914-060332",
    "verified": "legion gh -- api repos/sjawhar/oh-my-pi/contents/<file>?ref=<tag> blob shas equal my branch's for session-storage-config.ts (3c06bf37), session-storage.ts (4016ed50), session-storage-config.test.ts (0f15b5cc); settings-schema.ts differs only by the newer upstream base and other release members; `mise x github:sjawhar/oh-my-pi@<tag> -- omp --version` → omp/18.1.21; mise where → /home/legion/.local/share/mise/installs/github-sjawhar-oh-my-pi/18.1.21-sami.20260914-060332; flag-tables.ts at the tag still assigns result.appendSystemPrompt = value (last-wins)",
    "operatorReport": "the operator did not post the tag on LEGION-80; I found it via `legion gh -- release list --repo sjawhar/oh-my-pi` and verified content by blob sha before pinning"
  },
  "pullRequest": {
    "url": "https://github.com/sjawhar/legion/pull/1081",
    "tip": "0b8b3cbbf7a3 (Legion-side round-2 nits; the corrective push answering the review has not happened — it needs the pin bump to the fixed release)",
    "proofsHead": "cb9b42f89cd916cdc1001c9d39c39ba0c254992a (pre-rebase); fingerprint before b067bbe673e4e2757b783a32c37003c7e0689edfa29bc52a391a4e0da22f8cd5, after (bd810bd9) a502fbb26ad2d7bb5cb8c9e6692f90974b3a5a36dd0ad21fe5d49b5834b89e36 — differs only by the docs/kubernetes.md intro sentence main rewrote; the rebase comment on the PR records both",
    "rebase": "GitHub reported CONFLICTING at open (main had moved: pi-envoy 1.28.0, LEGION-81's daemon changes); jj rebase -s 'roots(main@origin..@)' -d main@origin; one conflict (docs/kubernetes.md intro sentence) resolved; every other file auto-merged; touched suites and both typechecks green at the new tip",
    "ci": "Tests run 34814138723 at bd810bd9: typecheck and test success, lint failed on biome formatting of boot-probes.ts and its test (my earlier root-level biome check used a different config); fixed as a new commit 7bc83404 (format only), pushed; the merger/tester reads the current run from GitHub",
    "knownSuiteInteraction": "Running packages/pi-envoy/extensions/legion.test.ts and packages/daemon/src/cli/__tests__/index.test.ts in ONE bun test invocation fails with `Export named 'AckPolicy' not found in module nats` — legion.test.ts's mock.module('nats') leaks into the daemon CLI test; pre-existing on main (reproduced at main@origin cc88ed01), each file passes alone, CI runs packages separately."
  },
  "proof": [
    {
      "criterion": "Acceptance 3 — on the new pin, one real Oh My Pi session against a throwaway local Postgres writes a row to omp_session_files and `omp --resume` on that row's path restores the conversation; omp --version and mise where on the new pin",
      "surface": "the stock tagged omp binary (`mise x github:sjawhar/oh-my-pi@18.1.21-sami.20260914-060332 -- omp`, omp/18.1.21, mise where → /home/legion/.local/share/mise/installs/github-sjawhar-oh-my-pi/18.1.21-sami.20260914-060332) run in this worker's own process with OMP_PROFILE=legion (real profile plugin tree, pi-legion-envoy 1.24.2) and LEGION_*/DISPATCH_* scrubbed, against a throwaway `docker run -d --rm --name legion-80-pg -e POSTGRES_PASSWORD=legion80 -p 127.0.0.1:55480:5432 postgres:16`; no daemon, bus, or tmux server",
      "command": "unlegion=$(printenv | sed -n 's/^\\(LEGION_[A-Z_]*\\|DISPATCH_[A-Z_]*\\)=.*/-u \\1/p'); (umask 077 && printf 'postgres://postgres:legion80@127.0.0.1:55480/postgres\\n' > /tmp/legion-80/dsn); cd /tmp/legion-80 && env $unlegion OMP_PROFILE=legion OMP_SESSION_STORAGE=sql OMP_SESSION_SQL_DSN_FILE=/tmp/legion-80/dsn mise x github:sjawhar/oh-my-pi@18.1.21-sami.20260914-060332 -- omp -p 'Remember the code word PINEAPPLE. Reply with exactly OK.'; psql postgres://postgres:legion80@127.0.0.1:55480/postgres -c \"select path, octet_length(content) as bytes, title from omp_session_files;\"; env $unlegion OMP_PROFILE=legion OMP_SESSION_STORAGE=sql OMP_SESSION_SQL_DSN_FILE=/tmp/legion-80/dsn mise x github:sjawhar/oh-my-pi@18.1.21-sami.20260914-060332 -- omp --resume='<row path>' -p 'What was the code word? Reply with just the word.'",
      "observed": "first run exit 0; one row: path /home/legion/.omp/profiles/legion/agent/sessions/-tmp-legion-80/2026-09-14T06-28-30-361Z_01a09e9a-5819-7000-a11a-8ee551531d8e.jsonl, 7026 bytes, assistant text \"OK\"; resume exit 0, assistant text \"PINEAPPLE\", the row grew to 11363 bytes; no file for the SQL session under the profile's sessions dir",
      "headSha": "cb9b42f89cd916cdc1001c9d39c39ba0c254992a",
      "negativeControl": "the same command with both OMP_SESSION_* variables unset (`env $unlegion OMP_PROFILE=legion <omp> -p 'Reply OK.'`) wrote a .jsonl (2026-09-14T06-29-05-814Z_01a09e9a-e295-7000-9b39-2a24354448dd.jsonl) under -tmp-legion-80/ and the Postgres row count stayed 1, with no storage log line — files exactly as before"
    },
    {
      "criterion": "Acceptance 4 — OMP_SESSION_STORAGE=sql with a missing, blank, or unreachable connection-string file refuses to start naming the variable and the path, never the connection string, never silent file storage",
      "surface": "the stock tagged omp binary (`mise x github:sjawhar/oh-my-pi@18.1.21-sami.20260914-060332 -- omp`) in this worker's process, LEGION_*/DISPATCH_* scrubbed, OMP_PROFILE=legion",
      "command": "printf 'postgres://postgres:x@127.0.0.1:1/postgres\\n' > /tmp/legion-80/dsn-closed; env $unlegion OMP_PROFILE=legion OMP_SESSION_STORAGE=sql OMP_SESSION_SQL_DSN_FILE=/tmp/legion-80/dsn-closed mise x github:sjawhar/oh-my-pi@18.1.21-sami.20260914-060332 -- omp -p 'hi'; echo exit=$?   # then the same with dsn-blank (`: > dsn-blank`) and /tmp/legion-80/does-not-exist",
      "observed": "closed port: `Error: OMP_SESSION_SQL_DSN_FILE names /tmp/legion-80/dsn-closed, but the session database could not be opened: Connection closed (ERR_POSTGRES_CONNECTION_CLOSED)` exit=1; blank: `Error: OMP_SESSION_SQL_DSN_FILE names /tmp/legion-80/dsn-blank, which is empty` exit=1; missing: `Error: OMP_SESSION_SQL_DSN_FILE names /tmp/legion-80/does-not-exist, which could not be read: ENOENT: no such file or directory, open '/tmp/legion-80/does-not-exist'` exit=1; the password `x` never printed; row count stayed 1 and the sessions dir stayed at one file",
      "headSha": "cb9b42f89cd916cdc1001c9d39c39ba0c254992a",
      "negativeControl": "the closed-port run is itself the deliberately broken input (a DSN to 127.0.0.1:1); its refusal names the variable, the path, and the driver error and exits 1 — against the passing run in the Acceptance 3 proof, which wrote a row with the same variables and a reachable DSN"
    },
    {
      "criterion": "Acceptance 6 — `legion probe-image` fails on the pinned build before the bump and passes, with the session-storage=probed token, on the new pin",
      "surface": "`legion probe-image` from this checkout (`bun packages/daemon/src/cli/index.ts probe-image`, the daemon's three launch probes) with LEGION_OMP_PATH at each stock build under mise, OMP_PROFILE=legion, LEGION_*/DISPATCH_* scrubbed",
      "command": "env $unlegion OMP_PROFILE=legion LEGION_OMP_PATH=/home/legion/.local/share/mise/installs/github-sjawhar-oh-my-pi/18.1.21-sami.20260914-060332/bin/omp bun packages/daemon/src/cli/index.ts probe-image; echo exit=$?   # and with LEGION_OMP_PATH=/home/legion/.local/share/mise/installs/github-sjawhar-oh-my-pi/18.1.18-sami.20260912-104423/bin/omp",
      "observed": "new pin: `probe-image: OK (/home/legion/.local/share/mise/installs/github-sjawhar-oh-my-pi/18.1.21-sami.20260914-060332/bin/omp) session-storage=probed` exit=0 in 7.2 s",
      "headSha": "cb9b42f89cd916cdc1001c9d39c39ba0c254992a",
      "negativeControl": "old pin 18.1.18-sami.20260912-104423 (omp/18.1.18): the first two probes pass and the third fails — `[legion] OMP launch command \"/home/legion/.local/share/mise/installs/github-sjawhar-oh-my-pi/18.1.18-sami.20260912-104423/bin/omp\" started with OMP_SESSION_STORAGE=legion-launch-probe (exit 0): this build predates the session.storage setting and would silently keep sessions on files under a sql session store; pin a fork release that carries the setting` exit=1 in 10.2 s"
    },
    {
      "criterion": "Acceptance 2 — a second session starting in the same process with no transcript file on disk claims no role, calls no daemon route, and does not exit",
      "surface": "the extension module itself (packages/pi-envoy/extensions/legion.ts) driven by the real OMP hook surface in bun test — the spec's own check for this line names an extension unit test; no production surface reaches a Legion process's task subagent without a daemon and a pane, which the standing order forbids starting",
      "command": "cd -- \"$LEGION_WORKSPACE\" && bun test packages/pi-envoy/extensions/legion.test.ts",
      "observed": "61 pass, 0 fail; the new test boots a root session on a transcript path nothing wrote, then a second extension instance's session_start on a different unwritten path: no /legion/ route, no /v1/roles/set, no exit, no legion tool, a plain bash tool_call passes ungated",
      "headSha": "cb9b42f89cd916cdc1001c9d39c39ba0c254992a",
      "negativeControl": "with the process-local check disabled (`if (false && bootstrapped …)`) the same test fails — the subagent bootstraps as the root (POST /legion/v1/process/started etc. recorded) — proving the on-disk fallback alone does not cover a SQL-stored transcript"
    }
  ],
  "round2": {
    "trigger": "reviewer CHANGES_REQUESTED at c4f58e44 (review 5195037174; thread PRRT_kwDORFy7ds6iBeVN / discussion_r4003047466): in the pinned fork release f3278eed, session-storage-config.ts:76 constructs `new SQL(dsn)` outside the try; a libpq-form connection string throws TypeError ERR_INVALID_URL whose message embeds the whole string, escapes main.ts's catch, and cli.ts's `fatal` prints it (password included) to stderr and OMP's log. Reproduced here with bun 1.3.14: new SQL(\"host=db user=legion password=hunter2\") → TypeError ERR_INVALID_URL, message `\"postgres://host=db user=legion password=hunter2\" cannot be parsed as a URL.`, Bun.inspect(error) contains the password.",
    "doneThisRound": {
      "legionCommit": "0b8b3cbbf7a3 docs+chore: review round-1 nits … — pushed on legion/LEGION-80 (carrying the tester's b9770a2e and reviewer's 0986645d handoff commits): docs/kubernetes.md new refusal row for a connection file whose contents are not a URL the driver accepts (names variable and path, never the contents) + the `<driver error> (<code>)` shape on the unreachable row + 'an older build never reads the variable, starts normally, and so fails the probe'; boot-probes.ts header now says the third probe runs only inside the image via probe-image, the daemon requires SESSION_STORAGE_PROBE_MARK and never probes a host OMP, and why (launch-prefix failure classified transient → the daemon's unbounded retry would loop on a secrets denial); unused export on SESSION_STORAGE_VARIABLE dropped; cli probe test uses toStartWith for the third command; legion.test.ts no longer parses request bodies; Dockerfile says 'the three launch probes (the daemon's two plus the session-storage probe)'; daemon AGENTS.md carries the retry-forever reason.",
      "verification": "bun test packages/pi-envoy/extensions/legion.test.ts → 61 pass; bun test boot-probes.test.ts + cli/__tests__/index.test.ts → 65 pass; packages/daemon `biome check --error-on-warnings src/` and `bun run typecheck` exit 0; packages/pi-envoy `biome lint` and `bun run typecheck` exit 0. Thread PRRT_kwDORFy7ds6iBeVN NOT yet answered and `legion threads resolve` NOT yet run — the corrective push must carry the pin bump to the fixed release, which does not exist yet."
    },
    "blocker": {
      "what": "The fork commit (A) cannot be made from this pane. It now runs as uid 1003 `legion` (HOME=/home/legion); the knives-managed checkout `~/oh-my-pi` and the knives registry/ledger live under /home/ubuntu (drwxr-x--- ubuntu ubuntu — no traversal for others; `ls /home/ubuntu/oh-my-pi` → Permission denied), no `knives` binary is on this user's PATH, and no knives registry exists for this user. The fork-work skill and the assignment forbid a second clone or scratch checkout, and a legion-owned clone would be invisible to knives' claims under ubuntu — exactly the collision the skill prevents — so I did not improvise one. Not tried: docker-group privilege escalation to read /home/ubuntu (available — uid legion is in `docker` — but unauthorized access to another user's home, which I refuse).",
      "whatUnblocks": "One of: (1) this worker resumed in a pane running as `ubuntu` (as it did for rounds 0-1) with ~/oh-my-pi and knives reachable; (2) the operator/architect makes the fork commit below from the ubuntu account in a knives workspace and reports the tip — I then do B/D from here (legion gh, mise, docker all work for this user); (3) the operator provisions a knives registry + colocated checkout for the legion user (not recommended: two checkouts, two ledgers).",
      "evidence": "id → uid=1003(legion) groups=legion,docker; ls -ld /home/ubuntu → drwxr-x--- ubuntu ubuntu; command -v knives → none (only /tmp/knives-main-check/target/debug/knives, owned by ubuntu); ~/.config/knives absent; ~/.local/share/mise/installs/github-sjawhar-oh-my-pi holds release tarballs only."
    },
    "forkChangeSpec": {
      "branch": "sjawhar/oh-my-pi feat/session-sql-storage at f3278eeda197ba1e241c2800b8a0baf21ef2dc19 (the operator's rebase; base the work on that tip): `cd ~/oh-my-pi && knives start feat/session-sql-storage --why \"…\"`; one described commit; push with the Legion credential helper (dry-run first: GIT_CONFIG_COUNT=2 GIT_CONFIG_KEY_0=credential.https://github.com.helper GIT_CONFIG_VALUE_0= GIT_CONFIG_KEY_1=credential.https://github.com.helper GIT_CONFIG_VALUE_1='!legion credential' jj -R <ws> git push --bookmark feat/session-sql-storage); knives preflight; knives notch; knives finish. Run only the touched tests and `bun check` (never tsc); zero Legion names in added lines.",
      "1_blocking": "packages/coding-agent/src/session/session-storage-config.ts: replace `const client = new SQL(dsn); try { return await SqlSessionStorage.create({ client }); } catch (err) { … }` with: `let client: SQL; try { client = new SQL(dsn); } catch { throw new SessionStorageConfigError(`${source} names ${dsnFile}, but its contents are not a connection URL the database driver accepts`); }` — no interpolation of the driver message and NO `cause` (the cause would carry the string into Bun.inspect/fatal), then the existing try/catch around `SqlSessionStorage.create({ client })` unchanged. Test (session-storage-config.test.ts): a dsn file containing `host=db user=legion password=hunter2\\n`, env OMP_SESSION_STORAGE=sql + OMP_SESSION_SQL_DSN_FILE → rejects SessionStorageConfigError whose message is exactly `OMP_SESSION_SQL_DSN_FILE names <path>, but its contents are not a connection URL the database driver accepts`, and `expect(Bun.inspect(error)).not.toContain(\"hunter2\")` (Bun.inspect is what cli.ts's fatal prints, so it is the consumer-observable surface). Update the module doc's precedence/refusal list to name this branch.",
      "2_minor_prepaint": "cli.ts starts the prepaint composer (beginStartupComposer) before runRootCommand for a bare TTY `omp`, and beginStartupComposer kicks refreshRecentSessions → loadRecentSessions → defaultSessionStorage() before setDefaultSessionStorage runs, so the welcome list (and its cache) read files under sql. Fix keeping one resolver and one install: (a) in main.ts move `setDefaultSessionStorage(await resolveSessionStorage({ settings: settingsInstance, env: process.env }))` to immediately after `const settingsInstance = await settingsPromise;` (before applyStartupComposerPreferences), wrapped like the auth-broker block just above it — `catch (error) { if (!(error instanceof SessionStorageConfigError)) throw error; process.stderr.write(`${chalk.red(`Error: ${error.message}`)}\\n`); process.exit(1); }` — and drop SessionStorageConfigError from the later session-resolution catch (restore that catch to SessionResolutionError only, hint logic as before); (b) in modes/startup-composer.ts, beginStartupComposer stores `options.recentSessions` on the pending record instead of calling refreshRecentSessions, and applyStartupComposerPreferences (which runRootCommand calls right after settings and the theme resolve — now after the storage install) does `pending.recentSessions ??= refreshRecentSessions(pending, pending.loadRecentSessions)`; takeStartupComposerLease is unchanged (a lease taken before preferences were applied carries no recentSessions promise, as today when the load has not started). Check test/modes/startup-composer*.test.ts for a test that expects the recentSessions override to run at begin time and move its expectation to after applyStartupComposerPreferences. Resolver stays the single resolution path; nothing else reads session.storage.",
      "3_minor_exports": "session-storage-config.ts: drop `export` on SESSION_STORAGE_ENV and SESSION_SQL_DSN_FILE_ENV (nothing imports them).",
      "4_gc_ruling": "Architect ruling: keep the two gc-cli.ts swaps (omp gc is on-disk maintenance, always file storage; one uniform seam, no hard-coded exceptions). No change.",
      "commitMessage": "fix(session): refuse a connection file whose contents are not a URL without echoing them\n\nThe database client was constructed outside the resolver's error handling, so a connection string the driver cannot parse (the libpq keyword form) surfaced as the driver's TypeError, whose message embeds the whole string — password included — and reached stderr and the log through the CLI's fatal handler. The client is now built inside the handling and a constructor failure refuses as `<source> names <path>, but its contents are not a connection URL the database driver accepts`, naming only the variable or setting and the path.\n\nAlso: the welcome screen's recent-sessions list is loaded once the configured storage is installed (the prepaint composer started it before, so under `session.storage: sql` it listed files), the storage is installed right after settings load with the same startup-refusal shape as the auth broker's, and two unused exports are dropped.",
      "changelogLine": "under ## [Unreleased] / ### Fixed: `- Fixed a `session.sql.dsnFile` whose contents are not a connection URL surfacing the driver's parse error (which embeds the connection string) instead of a refusal that names only the variable and the path; the welcome screen's recent sessions now follow `session.storage`.`"
    },
    "afterTheFix": {
      "B": "exactly one envoy_send to 'legion po' (01a08330-a0bf-7000-af19-c1db6b53ce1e) and one dispatch_message on LEGION-80 in plain English: the review found the fork could print the database password when the connection file holds a malformed connection string; the fix is one commit on feat/session-sql-storage at <tip>; Legion needs a new tagged sami release carrying it (`knives release include` + cut) and the tag reported on LEGION-80; the upstream PR should open from the fixed tip. Not sent yet: there is no fork commit to hand off.",
      "D": "bump OMP_FORK_PIN + the AGENTS.md pin sentence; on the new build: omp --version, mise where, legion probe-image (OK line with the token), closed-port / blank / missing / no-file / unknown-value refusals, and the libpq-form refusal (`printf 'host=db user=legion password=hunter2\\n' > dsn-libpq`) asserting hunter2 appears nowhere in stdout/stderr and exit 1 — quoted in an 'E2E (round 2)' section of the PR body with the head SHA. The Postgres round trip needs re-running only if the resolver's success path changes: it does not (the SQL client construction moves inside the try; on success the same create() call runs), so state that instead. Then reply on PRRT_kwDORFy7ds6iBeVN with `Accepted: …`, run `legion threads resolve --pr 1081 --repo sjawhar/legion`, push, check `legion gh -- pr view 1081 --json mergeable,mergeStateStatus` once; rebase only on CONFLICTING."
    }
  },
  "schemaVersion": 1,
  "phase": "implement",
  "completed": "2026-09-14T07:50:24.618Z"
}