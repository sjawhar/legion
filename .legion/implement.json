{
  "summary": "Client-only fix landed on legion/LEGION-73 as two commits: 1df340f0 coalesces the session-secret recovery in daemon-client.ts (per session id: newest recovered secret + in-flight recovery; newer-known → retry, in-flight → await and retry, else start the one /worker-session recovery; one retry per request; settled recovery clears the slot; onRecovered before caching), and 2332e1e8 memoizes roleDaemon() in extensions/legion.ts to one client per session plus the AGENTS.md contract sentence. PR https://github.com/sjawhar/legion/pull/1032 (Dispatch: LEGION-73). No daemon changes.",
  "pr": "https://github.com/sjawhar/legion/pull/1032",
  "head": "2332e1e82916",
  "commits": [
    "1df340f0 fix(pi-envoy): share one session-secret recovery across concurrent daemon calls (LEGION-73)",
    "2332e1e8 fix(pi-envoy): one daemon client per session so concurrent legion calls share recovery (LEGION-73)"
  ],
  "filesChanged": [
    "packages/pi-envoy/src/legion/daemon-client.ts",
    "packages/pi-envoy/src/legion/daemon-client.test.ts",
    "packages/pi-envoy/extensions/legion.ts",
    "packages/pi-envoy/extensions/legion.test.ts",
    "packages/pi-envoy/AGENTS.md"
  ],
  "tests": {
    "prefix": "daemon-client.test.ts on the parent sources: test 1 rejects with 'POST /legion/v1/waves/release failed with 403: {\"error\":\"Invalid session secret\"}' (daemon-client.ts:139, the retry); tests 2-3 fail 'Expected: 1 Received: 2' on recoveryCount(). legion.test.ts with the client fix alone: results.map(isError) received [true, undefined]. Path-scoped re-check (jj restore --from main@origin of only daemon-client.ts and legion.ts): 61 pass / 4 fail, the same four; restored: 65 pass / 0 fail.",
    "postfix": "cd packages/pi-envoy && bun test src/legion/daemon-client.test.ts extensions/legion.test.ts → 65 pass / 0 fail (10 client tests, 55 extension tests). bun run lint (biome lint extensions/ src/, CI's command) clean; biome check clean on the five changed files; bunx tsc --noEmit exit 0.",
    "ci": "Tests run 34758949795 and Legion Envoy and Contracts run 34758949882 at 2332e1e82916 (pi-envoy job: lint, typecheck, test, build); results recorded in the PR body's Verification section."
  },
  "trickyParts": [
    "The bookmark legion/LEGION-73 follows the working-copy change id through every jj split, so it sat on the empty @ after the splits; moved it to @- with --allow-backwards before the push (remote was at the plan handoff 37b36b48, an ancestor, so the push was a plain fast-forward).",
    "bunx biome from the repository root resolves the squatting biome@0.3.3 and prints 'Saved lockfile' (its own cache, not bun.lock — verified jj status unchanged). Run every check from packages/pi-envoy; docs/solutions/build-errors/bunx-from-the-repo-root-runs-the-wrong-tsc-and-biome.md on main covers it.",
    "biome check (format) fails on extensions/envoy.test.ts, extensions/envoy.ts, and src/pi-types.ts at the fork point already; CI's lint is biome lint, which passes. Named as the PR's Fast-follow, not formatted here to keep the diff to the five planned files.",
    "The extension test's fake must answer /legion/v1/process/ready with 200 while current === 'root-secret' (sessionStart calls it before the test forgets the secret); the startsWith('/legion/v1/') branch covers it."
  ],
  "deviations": [
    "Import specifier order in legion.ts: the plan's snippet listed `type LegionDaemonClient` before `LegionDaemonApiError`; biome's organizeImports requires the reverse, so the committed import is sorted.",
    "Renamed the loop variable that releases the held recovery gates from `release` to `resume` in both new tests (the plan's name shadowed the `release` promise in test 1)."
  ],
  "openQuestions": [],
  "subPlanningNeeded": false,
  "forTester": "Run the scratch-daemon rig per .legion/plan.json task 6 (head build from 2332e1e8, control from c8faafec). Re-run the two test files at the head and the path-scoped pre-fix re-check yourself. Fill the PR body's E2E line. dist/ is gitignored; bun run build in packages/pi-envoy changes nothing tracked.",
  "learningsInjected": [
    "docs/solutions/testing/race-regression-tests-that-fail-before-the-fix.md",
    "docs/solutions/testing/scratch-daemon-rig-proves-what-unit-tests-cannot.md",
    "docs/solutions/daemon/omp-pin-bump-behavioral-proof.md"
  ],
  "learningsHelpful": [
    "docs/solutions/testing/race-regression-tests-that-fail-before-the-fix.md"
  ],
  "schemaVersion": 1,
  "phase": "implement",
  "completed": "2026-09-13T13:08:53.444Z"
}