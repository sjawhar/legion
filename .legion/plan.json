{
  "spec": "dispatch://LEGION-23/spec version 4 (2026-09-12 09:28Z); parent dispatch://LEGION-19/spec §4, §5",
  "planFormat": "planMarkdown carries the full task-by-task plan (code, Dockerfile, workflow YAML, docs text, commands). Root spec §7: no plan documents in the repo — this handoff is the plan.",
  "summary": "Eight ordered tasks: (1) omp-pin.ts single-source pin imported by config.ts and derived by up.sh; (2) verbatim move of the two boot probes into daemon/boot-probes.ts; (3) hidden `legion probe-image` subcommand + 3 cli tests; (4) daemon/image-ref.ts parseImageDigestRef + 4 tests; (5) packages/daemon/docker/worker.Dockerfile (cli/tools/runtime stages, plugin linked into OMP_PROFILE=legion, probes run as the last RUN, placeholder ENTRYPOINT legion worker-shim) + .dockerignore fix; (6) .github/workflows/worker-image.yaml (workflow_call + workflow_dispatch + branch push trigger; Depot OIDC; tags; digest summary; release-body append; ECR gated on three vars; failure remedy) + release.yaml wiring; (7) docs/kubernetes.md image section, daemon AGENTS.md:128 correction, root AGENTS.md row; (8) once-only project checks, PR, and the first CI run as the last step.",
  "refinementsToSpec": [
    "R1 OMP pin: printed inside the build from omp-pin.ts (COPY --from=cli), not an ARG OMP_PIN; still one source.",
    "R2 depot/setup-action has no `project` input (action.yml: version, oidc, configure-docker); project goes on depot/build-push-action.",
    "R3 GitHub dispatches only workflows on the default branch, so a pre-merge `gh workflow run --ref <branch>` 404s; worker-image.yaml adds a push trigger for non-main branches filtered to packages/daemon/docker/** and the workflow file (sha- tag only). workflow_dispatch works post-merge.",
    "R4 `omp plugin install <tarball>` fails ENOTDIR; `omp plugin install <dir>` symlinks. The image unpacks the tarball to /opt/legion/pi-legion-envoy and links it; the profile lives at /home/legion/.omp/profiles/legion (DirResolver), not /opt/omp-profile.",
    "R5 OMP downloads native modules on first run (~/.omp/natives/<ver>/); the probe RUN runs as the runtime user so that layer ships.",
    "R6 linux/amd64 only (OMP fork release ships linux-x64 + darwin-arm64 assets).",
    "R7 jj = github:sjawhar/jj@0.45.1-sami.20260910-043938 (what the dogfood daemon runs; 0.45 line like OMP's jj-lib); gh@2.98.0 via mise registry.",
    "R8 final stage debian:bookworm-slim with Bun copied from oven/bun:1.3.14-slim (oven/bun owns uid 1000).",
    "R9 worker_image runs when cli succeeded or was skipped (envoy_plugin's condition).",
    "R10 no local builds of any kind (spec v4); this box cannot reach api.depot.dev anyway. Local validation: hadolint container (if Docker Hub answers) + host dry run of the runtime RUN logic with the pinned omp binary already installed here + compiled CLI.",
    "R11 root .dockerignore excludes skills (needed by prepack.sh) — line removed.",
    "R12 scripts/smoke/up.sh:14 pin copy now derived from omp-pin.ts.",
    "R13 release_tag stays a workflow_call input beside cli_version.",
    "R14 with ENTRYPOINT [legion, worker-shim] the spec's check commands need --entrypoint sh / --entrypoint legion."
  ],
  "tasks": [
    {
      "id": 1,
      "title": "single-source OMP pin module",
      "files": {
        "create": [
          "packages/daemon/src/daemon/omp-pin.ts"
        ],
        "modify": [
          "packages/daemon/src/daemon/config.ts:142 (import DEFAULT_OMP_INVOCATION)",
          "scripts/smoke/up.sh:14 (derive pin)"
        ],
        "tests": []
      },
      "proves": "bun packages/daemon/src/daemon/omp-pin.ts prints the pin; config.test.ts unchanged and passing; bash -n up.sh",
      "commit": "refactor(daemon): single-source the OMP fork pin in omp-pin.ts"
    },
    {
      "id": 2,
      "title": "extract boot probes into boot-probes.ts (pure move)",
      "files": {
        "create": [
          "packages/daemon/src/daemon/boot-probes.ts"
        ],
        "modify": [
          "packages/daemon/src/daemon/index.ts (lines 54-58, 139-323 move out; imports pruned)"
        ],
        "tests": [
          "packages/daemon/src/daemon/__tests__/index.test.ts must pass UNCHANGED"
        ]
      },
      "proves": "bun test src/daemon/__tests__/index.test.ts; bunx tsc --noEmit",
      "commit": "refactor(daemon): move the OMP boot probes into boot-probes.ts"
    },
    {
      "id": 3,
      "title": "hidden `legion probe-image` subcommand",
      "files": {
        "create": [],
        "modify": [
          "packages/daemon/src/cli/index.ts (cmdProbeImage, probeImageCommand hidden:true, registered)"
        ],
        "tests": [
          "packages/daemon/src/cli/__tests__/index.test.ts: 3 new tests (refuses without LEGION_OMP_PATH/--omp; runs both probes with no launch prefix; failing probe -> daemon message exit 1)"
        ]
      },
      "proves": "bun test src/cli/__tests__/index.test.ts",
      "commit": "feat(cli): hidden probe-image runs the daemon boot probes inside the worker image"
    },
    {
      "id": 4,
      "title": "parseImageDigestRef",
      "files": {
        "create": [
          "packages/daemon/src/daemon/image-ref.ts"
        ],
        "modify": [],
        "tests": [
          "packages/daemon/src/daemon/__tests__/image-ref.test.ts: 4 tests (accepts digest ref; tag beside digest; tag-only refused with exact text; malformed digests refused)"
        ]
      },
      "proves": "bun test src/daemon/__tests__/image-ref.test.ts (spec acceptance 4)",
      "commit": "feat(daemon): parseImageDigestRef refuses tag-pinned worker images"
    },
    {
      "id": 5,
      "title": "worker.Dockerfile",
      "files": {
        "create": [
          "packages/daemon/docker/worker.Dockerfile"
        ],
        "modify": [
          ".dockerignore (remove `skills`)"
        ],
        "tests": []
      },
      "proves": "static: hadolint via `docker run --rm -i hadolint/hadolint` (ignore DL3008, DL3003); host dry run (no Docker/Depot): pack plugin, link into temp HOME with OMP_PROFILE=legion using the pinned omp binary, compiled CLI `legion probe-image` -> OK; negative control LEGION_OMP_PATH=/nonexistent -> exit 1",
      "commit": "feat(docker): worker image with the pinned OMP fork, packed plugin, and boot-probe gate"
    },
    {
      "id": 6,
      "title": "worker-image.yaml + release.yaml",
      "files": {
        "create": [
          ".github/workflows/worker-image.yaml"
        ],
        "modify": [
          ".github/workflows/release.yaml (permissions packages: write; cli outputs version/release_tag; release_tag output line; worker_image job)"
        ],
        "tests": []
      },
      "proves": "yaml.parse of both files; the branch push run (Task 8 step 3) proves the failure path now and the success path once the prerequisites exist",
      "commit": "ci: build and publish the worker image on Depot from release.yaml"
    },
    {
      "id": 7,
      "title": "docs",
      "files": {
        "create": [
          "docs/kubernetes.md (image section only)"
        ],
        "modify": [
          "packages/daemon/src/daemon/AGENTS.md:128 (stale manifest/minPluginVersion paragraph replaced)",
          "AGENTS.md WHERE TO LOOK row"
        ],
        "tests": []
      },
      "proves": "review (spec acceptance 5)",
      "commit": "docs: worker image section of docs/kubernetes.md; correct the plugin-probe paragraph"
    },
    {
      "id": 8,
      "title": "final verification, PR, first CI run (last step)",
      "files": {
        "create": [],
        "modify": [],
        "tests": [
          "full: bunx biome check --error-on-warnings src/; bunx tsc --noEmit; bun test (packages/daemon)"
        ]
      },
      "proves": "bookmark legion/LEGION-23 pushed; PR with `Dispatch: LEGION-23`; branch-triggered Worker Image run observed: failure path (DEPOT_PROJECT_ID unset -> first-step refusal; no trust policy -> depot/setup-action failure + remedy summary) reported to the architect; after prerequisites: digest in summary, then `docker run --rm --entrypoint sh <digest> -c 'omp --version && legion --help && jj --version && git --version && gh --version'` and `docker run --rm --entrypoint legion <digest> probe-image` exit 0",
      "commit": "n/a"
    }
  ],
  "acceptanceProof": {
    "1": "Task 8 step 4 docker run --entrypoint sh <digest> -c '…' (tester) — blocked on DEPOT_PROJECT_ID + Depot trust policy (first successful build) and GHCR visibility for anonymous pull",
    "2": "Task 3 tests; Task 5 step 4 host dry run; Dockerfile last RUN; Task 8 step 4 docker run --entrypoint legion <digest> probe-image — same blockers for the docker run part",
    "3": "Task 6; Task 8 step 3 run: failure path provable immediately; success path after both prerequisites; release-body append observable on the first main run after merge that also releases the CLI; ECR: unset vars -> skipped-with-summary path is what runs",
    "4": "bun test src/daemon/__tests__/image-ref.test.ts — no blocker",
    "5": "docs/kubernetes.md 'Per-deployment toolchains layer on top' — no blocker"
  },
  "externalPrerequisites": [
    {
      "item": "Repository variable DEPOT_PROJECT_ID on sjawhar/legion (root decision 3 recommendation: agent-c project zd033k7gpx)",
      "checkedBy": "legion gh -- variable list --repo sjawhar/legion",
      "whenMissing": "workflow first step refuses: 'Repository variable DEPOT_PROJECT_ID is unset' (::error + summary)",
      "findingPath": "envoy_publish to notifications.role.legion-sjawharlegion-legion-23-architect with the run URL; never created by a worker",
      "blocks": "acceptance 3 success path; tester checks 1-2"
    },
    {
      "item": "Depot GitHub OIDC trust relationship admitting sjawhar/legion on that project (dashboard only)",
      "whenMissing": "depot/setup-action fails; the if: failure() step writes the remedy to the summary",
      "findingPath": "same",
      "blocks": "acceptance 3 success path; tester checks 1-2"
    },
    {
      "item": "GHCR package legion-worker public visibility (package settings, no API)",
      "whenMissing": "anonymous docker pull fails",
      "findingPath": "dispatch_ask to Sami per the spec's Errors row",
      "blocks": "tester's anonymous pull check"
    }
  ],
  "risks": [
    "Depot trust relationship + DEPOT_PROJECT_ID (external, Sami / root decision 3) — no image publishes until both exist; the workflow names the remedy on failure.",
    "GHCR package visibility (external, Sami) — precedent: ghcr.io/sjawhar/legion/envoy:latest is anonymously readable today.",
    "Spec acceptance 3's pre-merge workflow_dispatch is impossible on GitHub (R3); the push trigger is the pre-merge path — if R3 is rejected, the first real build waits for the merge.",
    "mise github backend rate limits without the github_token build secret (CI passes GITHUB_TOKEN; mount optional).",
    "OMP native-module download needs egress from the Depot builder; a failure fails loudly at legion probe-image.",
    "config.ts concurrent edit by LEGION-21 (in_progress): our diff is one import line + one deleted constant.",
    "No local image builds (R10): 1-3 CI iterations on the Dockerfile expected once prerequisites exist.",
    "oven/bun:1.3.14-slim and bookworm package availability assumed (CI surfaces a wrong tag on the first build)."
  ],
  "verifiedThisSession": [
    "OMP fork release exists as tag v18.1.15-sami.20260908-220934 (mise strips the v); assets omp-v…-linux-x64.tar.gz (124 MB) and darwin-arm64; repo public.",
    "mise where github:sjawhar/oh-my-pi@18.1.15-sami.20260908-220934 -> single 202 MB ELF at bin/omp (dynamically linked glibc); no node_modules.",
    "Clean HOME + OMP_PROFILE=legion, no provider keys: `omp plugin install <extracted dir>` -> 'Linked @sjawhar/pi-legion-envoy'; symlink at ~/.omp/profiles/legion/plugins/node_modules/@sjawhar/pi-legion-envoy; both daemon probes print LEGION_OMP_AGENTS=available and LEGION_PLUGIN_LOADED=yes, exit 0; `omp models --json` exits 0 without credentials; natives downloaded to ~/.omp/natives/18.1.15/.",
    "`omp plugin install <tarball.tgz>` fails: ENOTDIR …/package.json.",
    "bun pm pack of packages/pi-envoy (after the jq omp.extensions rewrite) produces sjawhar-pi-legion-envoy-1.5.0.tgz with dist/envoy.js, dist/legion.js, dist/skills/**; pi-envoy has no runtime dependencies.",
    "depot/setup-action action.yml inputs: version, oidc, configure-docker (no project). depot/build-push-action: project, lint, secrets, tags, labels, platforms, push; outputs digest.",
    "GitHub docs: workflow_dispatch requires the workflow on the default branch.",
    "This box: depot 2.102.7 installed, ~/.config/depot/depot.yaml present, but `depot list projects --output json` -> deadline_exceeded and curl https://api.depot.dev -> SSL connection timeout; hadolint not installed; docker 29.7.2, crane, skopeo present; ghcr.io/sjawhar/legion/envoy:latest readable anonymously.",
    "Bun 1.3.14 here; @types/node declares process.execve optional (irrelevant after spec v4 dropped the entrypoint).",
    "citty 0.2.2 CommandMeta.hidden exists. release.yaml cli job: bun build --compile --target=bun-linux-x64 packages/daemon/src/cli/index.ts; LEGION_VERSION define is not read by any source file. Pin occurrences: config.ts:142 (authoritative), AGENTS.md:65 and scripts/smoke/README.md:73 (prose), scripts/smoke/up.sh:14 (script copy).",
    "Root .dockerignore excludes skills and docs; jj on this box is 0.45.1-sami.20260910-043938 from github:sjawhar/jj (release exists with x86_64-unknown-linux-musl asset)."
  ],
  "notInThisPr": [
    "container entrypoint / *_FILE provider export (LEGION-24); ENTRYPOINT [legion, worker-shim] is a placeholder",
    "wiring parseImageDigestRef into config.ts (LEGION-24)",
    "in-cluster probe pod (LEGION-25); rest of docs/kubernetes.md (LEGION-25/26)",
    "ECR AWS side, Depot project/trust policy/DEPOT_PROJECT_ID (root decision 3) — findings never creations",
    "any token beyond secrets.GITHUB_TOKEN; no depot build outside CI"
  ],
  "planMarkdown": "# LEGION-23 Worker image + Depot pipeline — Implementation Plan\n\n> **For agentic workers:** execute task-by-task, test-first where a task names a test. Steps use\n> checkbox syntax. Commit after every task with `jj -R \"$LEGION_WORKSPACE\" describe -m \"<subject>\" && jj -R \"$LEGION_WORKSPACE\" new`.\n> Do NOT run project-wide formatters, linters, or the full test suite until Task 8.\n\n**Goal:** publish `ghcr.io/sjawhar/legion-worker` — one image carrying the pinned OMP fork build, the\n`legion` CLI built from this checkout, the `@sjawhar/pi-legion-envoy` plugin packed from this checkout\ninto the isolated `legion` OMP profile, jj, git, gh — built on Depot from `release.yaml`, digest-pinned by\nthe daemon, with the daemon's two boot probes proving the image before it publishes.\n\n**Architecture:** the two boot probes move verbatim from `daemon/index.ts` into `daemon/boot-probes.ts` so\n`startDaemon` and a hidden `legion probe-image` subcommand run identical code; the Dockerfile derives the\nOMP pin inside the build from the same module `config.ts` imports; `worker-image.yaml` is a reusable\nworkflow (`workflow_call` from `release.yaml` after `cli`, `workflow_dispatch` post-merge, and a narrow\n`push` trigger so a branch builds before merge — see refinement R3); `parseImageDigestRef` lives in a new\n`daemon/image-ref.ts` for LEGION-24 to wire.\n\n**Tech stack:** Bun 1.3.14 (`bun build --compile`), citty CLI, Dockerfile (BuildKit syntax 1.7, multi-stage),\nDepot (`depot/setup-action@v1` OIDC + `depot/build-push-action@v1`), GHCR, GitHub Actions reusable workflow,\nmise `github:` backend (OMP fork, jj fork) and registry (`gh`).\n\n**Spec:** dispatch://LEGION-23/spec (version 4, 2026-09-12 09:28Z: no entrypoint here, builds only in CI on\nDepot, `DEPOT_PROJECT_ID`/trust policy/ECR are findings never creations). Parent: dispatch://LEGION-19/spec §4, §5.\n\n**Plan location:** root spec §7 — \"no plan documents in the repo (`.legion/plan.json` only)\" — overrides the\nwriting-plans default of `docs/plans/`; this text is the `planMarkdown` field of `.legion/plan.json`.\n\n## Refinements to the spec (verified this session; architect to accept or push back)\n\nR1. **OMP pin delivery.** The spec's design row says \"pin passed as `ARG OMP_PIN`\". Instead the pin is\n    exported by a new module `packages/daemon/src/daemon/omp-pin.ts` (which `config.ts` imports for\n    `DEFAULT_OMP_INVOCATION`) and is *printed inside the build* (`bun packages/daemon/src/daemon/omp-pin.ts`)\n    in the `cli` stage; the `tools` stage reads it via `COPY --from=cli`. No build arg, no workflow-side\n    computation, nothing for `gh workflow run` to pass. Still exactly one source.\nR2. **`depot/setup-action` has no `project` input** (its `action.yml`: `version`, `oidc`,\n    `configure-docker`). The project id goes on `depot/build-push-action` as `project: ${{ vars.DEPOT_PROJECT_ID }}`;\n    `depot/setup-action@v1` gets `oidc: true` + a pinned `version`.\nR3. **A branch cannot be `workflow_dispatch`ed before merge.** GitHub docs (Manually running a workflow):\n    \"To trigger the `workflow_dispatch` event, your workflow must be in the default branch.\" Spec Acceptance 3's\n    \"one workflow_dispatch run from the PR branch\" is therefore impossible pre-merge. `worker-image.yaml`\n    gains a `push` trigger on non-`main` branches filtered to `packages/daemon/docker/**` and\n    `.github/workflows/worker-image.yaml`; a branch push builds and publishes `:sha-<short>` only (the\n    non-main rule the spec already states). `workflow_dispatch` works as specified once merged.\nR4. **Plugin install mechanics.** `omp plugin install <tarball.tgz>` fails (`ENOTDIR … /package.json`);\n    `omp plugin install <dir>` **symlinks** the directory (\"Linked @sjawhar/pi-legion-envoy from <dir>\") and\n    records `enabled: true` in `<profile>/plugins/omp-plugins.lock.json`. The image therefore unpacks the\n    `bun pm pack` tarball to `/opt/legion/pi-legion-envoy` (a permanent path) and links it. Verified on this\n    box with the pinned binary in a clean `HOME` + `OMP_PROFILE=legion`; both probes then print\n    `LEGION_OMP_AGENTS=available` and `LEGION_PLUGIN_LOADED=yes` with no provider credentials.\n    The profile itself lives where OMP's `DirResolver` puts it for `OMP_PROFILE=legion` —\n    `/home/legion/.omp/profiles/legion` — not the spec's `/opt/omp-profile`: the resolver derives the profile\n    root from `HOME`, and an `XDG_DATA_HOME` override applies only when that directory already exists.\nR5. **OMP downloads native modules on first run** into `$HOME/.omp/natives/<version>/pi_natives.linux-x64-{baseline,modern}.node`\n    (~30 s). The probe step runs as the runtime user in the runtime stage so that layer ships in the image and\n    a pod never fetches them.\nR6. **Image is `linux/amd64` only** — the OMP fork release (`v18.1.15-sami.20260908-220934`) ships\n    `omp-…-linux-x64.tar.gz` and `…-darwin-arm64.tar.gz` only. (The release exists; mise strips the `v`.)\nR7. **jj in the image is Sami's fork** `github:sjawhar/jj@0.45.1-sami.20260910-043938` — what the dogfood\n    daemon on this box runs (`jj 0.45.1-sami.20260910-043938-bb5ffc8f…`), the same 0.45 line as the jj-lib\n    the OMP fork embeds (`fix/jj-lib-0.45`). CI's live-test pin `jj-vcs/jj@0.44.0` is not used. gh is\n    `gh@2.98.0` (this box's version) via the mise registry. Both resolve through mise in the `tools` stage —\n    one install mechanism.\nR8. **Final stage is `debian:bookworm-slim`** (the in-repo convention, `packages/envoy/docker/Dockerfile`)\n    with Bun copied from the `oven/bun:1.3.14-slim` build stage: `oven/bun` already owns uid 1000 (`bun`),\n    which the spec reserves for `legion`.\nR9. **`worker_image` runs when `cli` succeeded *or was skipped*** (the `envoy_plugin` job's condition), so a\n    plugin-only change on main still publishes an image; `cli_version` is empty then.\nR10. **The image is built only by the workflow on Depot** (spec v4). Nothing here builds locally; this box\n    could not anyway (`depot list projects --output json` → `deadline_exceeded`; `curl https://api.depot.dev`\n    → SSL connection timeout). Local validation is static (hadolint via a published container, if Docker Hub\n    answers) plus a host dry run of the runtime RUN logic using the pinned `omp` binary already installed on\n    this box and the locally compiled CLI — no Docker, no Depot (Task 5 step 4).\nR11. **Root `.dockerignore` excludes `skills`**, which `packages/pi-envoy/scripts/prepack.sh` copies into\n    the tarball — the line is removed (the envoy image never COPYs skills; harmless there).\nR12. `scripts/smoke/up.sh:14` is a second hand-maintained copy of the pin; it now derives it from\n    `omp-pin.ts` (requirement: \"exactly one copy in the repo\").\nR13. `release_tag` stays a `workflow_call` input (alongside `cli_version`): the `cli-v*` tag scheme remains\n    defined only in `release.yaml`.\nR14. **Spec check commands need `--entrypoint`.** With the placeholder `ENTRYPOINT [\"legion\",\"worker-shim\"]`\n    (spec v4), `docker run --rm <image> sh -c …` runs `legion worker-shim sh -c …` (citty: missing `--socket`).\n    The tester's checks are `docker run --rm --entrypoint sh <image> -c …` and\n    `docker run --rm --entrypoint legion <image> probe-image` (Task 8 step 4).\n\n## Global constraints (from the spec — every task inherits these)\n\n- Image: `ghcr.io/sjawhar/legion-worker`, public; tags `sha-<short>` always, `<cli version>` only when\n  `cli` released in the same run and the ref is `main`.\n- Built on Depot only. Never `docker build`, `docker buildx`, or `docker compose build` on a workstation.\n  Pulling and running a published image locally is fine.\n- `runtime.kubernetes.image` is digest-only; refusal text exactly\n  `runtime.kubernetes.image must be pinned by digest (@sha256:…)`.\n- Non-root user `legion`, uid 1000; `OMP_PROFILE=legion`; placeholder `ENTRYPOINT [\"legion\", \"worker-shim\"]`\n  and nothing more — the entrypoint and the `*_FILE` provider export are LEGION-24's (spec v4).\n- The image is built only by the workflow on Depot; no `docker build`/`docker buildx`/`docker compose build`/\n  `depot build` from a workstation. `DEPOT_PROJECT_ID`, the Depot trust policy, the ECR repository and role\n  are never created by a worker — a missing one is a finding reported to the architect (spec v4).\n- Plugin packed from this checkout with the exact `release.yaml` `pi_envoy` steps (jq rewrite of\n  `omp.extensions`, `bun pm pack`); no npm pin; no `legion.minPluginVersion` (stale AGENTS.md sentence corrected).\n- OMP pin has one source; the Dockerfile derives it.\n- `parseImageDigestRef` in new `daemon/image-ref.ts` (never `config.ts` — LEGION-21 edits it concurrently;\n  `legion/LEGION-21` exists locally at `d578508a`).\n- Biome style: double quotes, semicolons, 100 cols; `node:` prefixes; `type` imports; no barrel files;\n  tests in co-located `__tests__/`.\n- Do not run project-wide lint/typecheck/test until Task 8.\n\nBind first, every shell command: `cd -- \"$LEGION_WORKSPACE\" && …`; every jj command `-R \"$LEGION_WORKSPACE\"`.\n\n---\n\n### Task 1: single-source OMP pin module\n\n**Files:**\n- Create: `packages/daemon/src/daemon/omp-pin.ts`\n- Modify: `packages/daemon/src/daemon/config.ts:142` (delete the constant; import it)\n- Modify: `scripts/smoke/up.sh:14` (derive the pin)\n\n**Produces:** `OMP_FORK_PIN` (mise tool spec string), `DEFAULT_OMP_INVOCATION`; running the module prints\nthe pin followed by `\\n` (consumed by Task 6's Dockerfile and by `up.sh`).\n\n- [ ] **Step 1: create the module**\n\n```ts\n// packages/daemon/src/daemon/omp-pin.ts\n/** The OMP fork build every Legion process runs — the one mise tool spec `DEFAULT_OMP_INVOCATION`\n * wraps and the worker image resolves (packages/daemon/docker/worker.Dockerfile runs this file with\n * `bun` and reads its one-line output). Bump the pin here and nowhere else. */\nexport const OMP_FORK_PIN = \"github:sjawhar/oh-my-pi@18.1.15-sami.20260908-220934\";\nexport const DEFAULT_OMP_INVOCATION = `mise x ${OMP_FORK_PIN} -- omp`;\n\nif (import.meta.main) process.stdout.write(`${OMP_FORK_PIN}\\n`);\n```\n\n- [ ] **Step 2: point config.ts at it** — delete line 142\n  (`const DEFAULT_OMP_INVOCATION = \"mise x github:sjawhar/oh-my-pi@18.1.15-sami.20260908-220934 -- omp\";`)\n  and add `import { DEFAULT_OMP_INVOCATION } from \"./omp-pin\";` to the import block. Nothing else in\n  `config.ts` changes (LEGION-21 conflict surface: one import line).\n\n- [ ] **Step 3: derive the smoke rig's copy** — in `scripts/smoke/up.sh`, delete line 14 and, after the\n  `fail`/`require_command` definitions (after line 32), add:\n\n```bash\nomp_pin=\"$(bun \"${repo_root}/packages/daemon/src/daemon/omp-pin.ts\")\" \\\n  || fail \"could not read the OMP pin from packages/daemon/src/daemon/omp-pin.ts (bun required)\"\nreadonly omp_pin\n```\n  (`omp_pin` is used only at line 288 inside `write_legion_yaml`'s heredoc; `require_command bun` at\n  line 479 already makes bun a rig prerequisite.)\n\n- [ ] **Step 4: verify**\n\n```\ncd -- \"$LEGION_WORKSPACE\" && bun packages/daemon/src/daemon/omp-pin.ts\n# expected: github:sjawhar/oh-my-pi@18.1.15-sami.20260908-220934\ncd -- \"$LEGION_WORKSPACE/packages/daemon\" && bun test src/daemon/__tests__/config.test.ts\n# expected: PASS (default invocation unchanged)\nbash -n \"$LEGION_WORKSPACE/scripts/smoke/up.sh\"\n```\n\n- [ ] **Step 5: commit** — `refactor(daemon): single-source the OMP fork pin in omp-pin.ts`\n\n---\n\n### Task 2: extract the boot probes into `boot-probes.ts` (pure move)\n\n**Files:**\n- Create: `packages/daemon/src/daemon/boot-probes.ts`\n- Modify: `packages/daemon/src/daemon/index.ts:1-58, 139-323, 405-419`\n- Test: existing `packages/daemon/src/daemon/__tests__/index.test.ts` must pass **unchanged**\n  (the probe tests at lines 1333–1692 exercise `startDaemon` with injected `runner`/`sleep`/`readPluginManifest`).\n\n**Produces (exact signatures, unchanged):**\n```ts\nexport async function verifyOmpAgentsCapability(\n  ompInvocation: string, ompLaunchPrefix: readonly string[], runner: CommandRunner,\n  sleep: (ms: number) => Promise<void>): Promise<void>\nexport async function verifyLegionPluginLoaded(\n  ompInvocation: string, ompLaunchPrefix: readonly string[], runner: CommandRunner,\n  readPluginManifest: (manifestPath: string) => Promise<string>,\n  sleep: (ms: number) => Promise<void>): Promise<void>\n```\n\n- [ ] **Step 1: create `boot-probes.ts`** with this header, then move these `index.ts` ranges **verbatim**\n  (comments included) below it: lines 54–58 (`OMP_AGENTS_CAPABILITY_MARKER`, `OMP_AGENTS_CAPABILITY_PROBE`),\n  139–175 (`PROBE_RETRY_DELAYS_MS`, `ProbeOutcome`, `retryBootProbe`), 177–225 (`verifyOmpAgentsCapability`),\n  227–323 (`LEGION_LOADED_MARKER`, `MAX_PROBE_STDERR_LENGTH`, `LEGION_LOAD_PROBE`, `verifyLegionPluginLoaded`).\n  Add `export` to the two `verify*` functions only.\n\n```ts\n// packages/daemon/src/daemon/boot-probes.ts\nimport { mkdtemp, rm, writeFile } from \"node:fs/promises\";\nimport os from \"node:os\";\nimport path from \"node:path\";\nimport { getPluginsNodeModules } from \"@oh-my-pi/pi-utils/dirs\";\nimport type { CommandRunner } from \"../state/fetch\";\nimport { withOmpLaunchPrefix } from \"./processes\";\n\n/** The two probes `startDaemon` runs before it owns state or serves the API, and that\n * `legion probe-image` runs inside the worker image (packages/daemon/docker/worker.Dockerfile's last\n * step): one module so the daemon and the image gate are the same code. */\n```\n\n- [ ] **Step 2: rewire `index.ts`** — delete the moved ranges; add\n  `import { verifyLegionPluginLoaded, verifyOmpAgentsCapability } from \"./boot-probes\";`; the call sites at\n  405–419 stay byte-identical. Prune imports `tsc --noUnusedLocals` now flags: `getPluginsNodeModules`\n  (line 12), `withOmpLaunchPrefix` (line 47; verify no other use), `os` (line 3), and `mkdtemp`, `rm`,\n  `writeFile` from line 2 (keep `readFile`, `stat`).\n\n- [ ] **Step 3: verify — unchanged tests pass**\n\n```\ncd -- \"$LEGION_WORKSPACE/packages/daemon\" && bun test src/daemon/__tests__/index.test.ts\ncd -- \"$LEGION_WORKSPACE/packages/daemon\" && bunx tsc --noEmit\n```\n\n- [ ] **Step 4: commit** — `refactor(daemon): move the OMP boot probes into boot-probes.ts`\n\n---\n\n### Task 3: hidden `legion probe-image` subcommand\n\n**Files:**\n- Modify: `packages/daemon/src/cli/index.ts` (imports; `cmdProbeImage`; `probeImageCommand`; register in `mainCommand.subCommands`)\n- Test: `packages/daemon/src/cli/__tests__/index.test.ts` (new `describe(\"legion probe-image\")`)\n\n**Consumes:** Task 2's two functions. **Produces:** `cmdProbeImage(omp: string | undefined, deps: ProbeImageDeps): Promise<void>`;\nCLI `legion probe-image [--omp <path>]` — exit 0 and `probe-image: OK (<path>)` on success; exit 1 with the\ndaemon's own probe message on failure; refuses to run without `LEGION_OMP_PATH`/`--omp` (no PATH fallback).\n\n- [ ] **Step 1: write the failing tests** (append to `cli/__tests__/index.test.ts`; add\n  `cmdProbeImage` to the import from `../index` and `import type { CommandRunner } from \"../../state/fetch\";`)\n\n```ts\ndescribe(\"legion probe-image\", () => {\n  const deps = (runner: CommandRunner, env: NodeJS.ProcessEnv) => ({\n    env,\n    runner,\n    sleep: async () => {},\n    readPluginManifest: async () => \"{}\",\n  });\n  const passing = async () => ({\n    stdout: \"\",\n    stderr: \"LEGION_OMP_AGENTS=available\\nLEGION_PLUGIN_LOADED=yes\\n\",\n    exitCode: 0,\n  });\n\n  it(\"refuses to probe without an explicit OMP executable (no PATH fallback)\", async () => {\n    let ran = false;\n    await expect(\n      cmdProbeImage(undefined, deps(async () => { ran = true; return passing(); }, {}))\n    ).rejects.toEqual(\n      expect.objectContaining({ message: expect.stringContaining(\"LEGION_OMP_PATH\"), code: 1 })\n    );\n    expect(ran).toBe(false);\n  });\n\n  it(\"runs the daemon's two boot probes against LEGION_OMP_PATH with no launch prefix\", async () => {\n    const commands: string[][] = [];\n    await cmdProbeImage(\n      undefined,\n      deps(async (command) => { commands.push(command); return passing(); }, { LEGION_OMP_PATH: \"/opt/omp/bin/omp\" })\n    );\n    expect(commands).toHaveLength(2);\n    expect(commands[0]?.[2]).toStartWith('exec /opt/omp/bin/omp models --no-extensions --extension \"$1\" --json');\n    expect(commands[1]?.[2]).toStartWith('exec /opt/omp/bin/omp models --extension \"$1\" --json');\n  });\n\n  it(\"surfaces a failing probe as the daemon's own message with exit 1\", async () => {\n    await expect(\n      cmdProbeImage(\n        \"/opt/omp/bin/omp\",\n        deps(async () => ({ stdout: \"\", stderr: \"LEGION_OMP_AGENTS=missing\\n\", exitCode: 0 }), {})\n      )\n    ).rejects.toEqual(\n      expect.objectContaining({ message: expect.stringContaining(\"does not expose pi.agents\"), code: 1 })\n    );\n  });\n});\n```\n\n- [ ] **Step 2: run, expect failure** — `cd -- \"$LEGION_WORKSPACE/packages/daemon\" && bun test src/cli/__tests__/index.test.ts`\n  → FAIL: `cmdProbeImage` is not exported.\n\n- [ ] **Step 3: implement in `cli/index.ts`**\n\n```ts\nimport { verifyLegionPluginLoaded, verifyOmpAgentsCapability } from \"../daemon/boot-probes\";\nimport { type CommandRunner, defaultRunner } from \"../state/fetch\";\n\ninterface ProbeImageDeps {\n  env: NodeJS.ProcessEnv;\n  runner: CommandRunner;\n  sleep(ms: number): Promise<void>;\n  readPluginManifest(manifestPath: string): Promise<string>;\n}\n\n/** The daemon's two boot probes (boot-probes.ts) against one OMP executable, with no launch prefix\n * — an image carries no `secrets` wrapper. The worker image build runs this as its last step; a\n * failure is the daemon's own probe message, exit 1, so a broken image never publishes. */\nexport async function cmdProbeImage(omp: string | undefined, deps: ProbeImageDeps): Promise<void> {\n  const ompPath = omp ?? deps.env.LEGION_OMP_PATH;\n  if (!ompPath) {\n    throw new CliError(\"probe-image: set LEGION_OMP_PATH (or pass --omp) to the OMP executable to probe\");\n  }\n  try {\n    await verifyOmpAgentsCapability(ompPath, [], deps.runner, deps.sleep);\n    await verifyLegionPluginLoaded(ompPath, [], deps.runner, deps.readPluginManifest, deps.sleep);\n  } catch (error) {\n    throw new CliError(error instanceof Error ? error.message : String(error));\n  }\n  console.log(`probe-image: OK (${ompPath})`);\n}\n\nconst probeImageCommand = defineCommand({\n  meta: {\n    name: \"probe-image\",\n    description: \"Run the daemon's OMP boot probes against this image's OMP executable\",\n    hidden: true,\n  },\n  args: { omp: { type: \"string\", description: \"OMP executable (default: $LEGION_OMP_PATH)\" } },\n  run: ({ args }) =>\n    runCli(() =>\n      cmdProbeImage(args.omp as string | undefined, {\n        env: process.env,\n        runner: defaultRunner,\n        sleep: (ms) => Bun.sleep(ms),\n        readPluginManifest: (manifestPath) => fs.promises.readFile(manifestPath, \"utf8\"),\n      })\n    ),\n});\n```\n  Register `\"probe-image\": probeImageCommand` in `mainCommand.subCommands`. (citty 0.2.2 `CommandMeta.hidden`\n  exists.) Note `retryBootProbe`'s real delays (5 s…180 s) apply on transient failures inside a build — bounded.\n\n- [ ] **Step 4: run, expect pass** — same command as Step 2 → PASS (3 new tests; existing ones untouched).\n\n- [ ] **Step 5: commit** — `feat(cli): hidden probe-image runs the daemon boot probes inside the worker image`\n\n---\n\n### Task 4: `parseImageDigestRef`\n\n**Files:**\n- Create: `packages/daemon/src/daemon/image-ref.ts`\n- Test: `packages/daemon/src/daemon/__tests__/image-ref.test.ts`\n\n**Produces (LEGION-24 imports this into `config.ts` for `runtime.kubernetes.image`):**\n```ts\nexport interface ImageDigestRef { readonly reference: string; readonly name: string; readonly digest: `sha256:${string}` }\nexport const IMAGE_DIGEST_REQUIRED = \"runtime.kubernetes.image must be pinned by digest (@sha256:…)\";\nexport function parseImageDigestRef(reference: string): ImageDigestRef  // throws Error(IMAGE_DIGEST_REQUIRED)\n```\n\n- [ ] **Step 1: write the failing tests**\n\n```ts\n// packages/daemon/src/daemon/__tests__/image-ref.test.ts\nimport { describe, expect, it } from \"bun:test\";\nimport { IMAGE_DIGEST_REQUIRED, parseImageDigestRef } from \"../image-ref\";\n\nconst DIGEST = `sha256:${\"a\".repeat(64)}`;\n\ndescribe(\"parseImageDigestRef\", () => {\n  it(\"accepts a digest-pinned reference and splits name from digest\", () => {\n    expect(parseImageDigestRef(`ghcr.io/sjawhar/legion-worker@${DIGEST}`)).toEqual({\n      reference: `ghcr.io/sjawhar/legion-worker@${DIGEST}`,\n      name: \"ghcr.io/sjawhar/legion-worker\",\n      digest: DIGEST,\n    });\n  });\n\n  it(\"keeps an informational tag beside the digest (the digest still pins)\", () => {\n    expect(parseImageDigestRef(`ghcr.io/sjawhar/legion-worker:1.2.3@${DIGEST}`).name).toBe(\"ghcr.io/sjawhar/legion-worker:1.2.3\");\n  });\n\n  it(\"refuses a tag-only reference with the configured-field message\", () => {\n    expect(() => parseImageDigestRef(\"ghcr.io/sjawhar/legion-worker:1.2.3\")).toThrow(IMAGE_DIGEST_REQUIRED);\n    expect(IMAGE_DIGEST_REQUIRED).toBe(\"runtime.kubernetes.image must be pinned by digest (@sha256:…)\");\n  });\n\n  it(\"refuses malformed digests: short, wrong algorithm, uppercase hex, empty\", () => {\n    for (const bad of [\n      \"ghcr.io/sjawhar/legion-worker@sha256:abc\",\n      `ghcr.io/sjawhar/legion-worker@md5:${\"a\".repeat(32)}`,\n      `ghcr.io/sjawhar/legion-worker@sha256:${\"A\".repeat(64)}`,\n      \"\",\n    ]) {\n      expect(() => parseImageDigestRef(bad)).toThrow(IMAGE_DIGEST_REQUIRED);\n    }\n  });\n});\n```\n\n- [ ] **Step 2: run, expect failure** — `cd -- \"$LEGION_WORKSPACE/packages/daemon\" && bun test src/daemon/__tests__/image-ref.test.ts`.\n\n- [ ] **Step 3: implement**\n\n```ts\n// packages/daemon/src/daemon/image-ref.ts\nexport interface ImageDigestRef {\n  /** The reference exactly as configured, e.g. `ghcr.io/sjawhar/legion-worker@sha256:…`. */\n  readonly reference: string;\n  /** Everything before `@`: registry/repository, with an optional informational tag. */\n  readonly name: string;\n  readonly digest: `sha256:${string}`;\n}\n\nexport const IMAGE_DIGEST_REQUIRED = \"runtime.kubernetes.image must be pinned by digest (@sha256:…)\";\nconst DIGEST_REF = /^([^@\\s]+)@(sha256:[0-9a-f]{64})$/;\n\n/** Accepts only `<name>[:tag]@sha256:<64 lowercase hex>`. A tag is mutable and the daemon must know\n * exactly what it probed, so a tag-only or malformed reference is refused with `IMAGE_DIGEST_REQUIRED`.\n * Consumed by LEGION-24 when it wires `runtime.kubernetes.image` into config.ts. */\nexport function parseImageDigestRef(reference: string): ImageDigestRef {\n  const match = DIGEST_REF.exec(reference);\n  if (!match) throw new Error(IMAGE_DIGEST_REQUIRED);\n  return { reference, name: match[1] as string, digest: match[2] as `sha256:${string}` };\n}\n```\n\n- [ ] **Step 4: run, expect pass.**\n- [ ] **Step 5: commit** — `feat(daemon): parseImageDigestRef refuses tag-pinned worker images`\n\n---\n\n### Task 5: `worker.Dockerfile`\n\n**Files:**\n- Create: `packages/daemon/docker/worker.Dockerfile`\n- Modify: `.dockerignore` (remove line 15 `skills`; add the comment below)\n\n**Consumes:** Task 1 (`omp-pin.ts` printed in-build), Task 3 (`legion probe-image`). **Produces:** the image\ncontract — `OMP_PROFILE=legion`, `LEGION_OMP_PATH=/opt/omp/bin/omp`, `PATH=/opt/legion/bin:/opt/omp/bin:…`,\nuser `legion` uid 1000, `HOME=/home/legion` (writable at runtime: OMP writes sessions/logs/models.db under\n`~/.omp/profiles/legion`), plugin linked at\n`/home/legion/.omp/profiles/legion/plugins/node_modules/@sjawhar/pi-legion-envoy -> /opt/legion/pi-legion-envoy`,\nnatives baked at `/home/legion/.omp/natives/18.1.15/`, placeholder `ENTRYPOINT [\"legion\",\"worker-shim\"]`, `linux/amd64`.\n\n- [ ] **Step 1: `.dockerignore`** — delete the `skills` line; put this comment where it was:\n  `# skills/ ships inside the worker image's plugin tarball (packages/pi-envoy/scripts/prepack.sh).`\n\n- [ ] **Step 2: write the Dockerfile**\n\n```dockerfile\n# syntax=docker/dockerfile:1.7\n# Legion worker image: every Legion agent process under `runtime: kubernetes` runs from this image.\n# Build context: the repo root. Built on Depot by .github/workflows/worker-image.yaml (called from\n# release.yaml after `cli`, on a branch push touching this file, or dispatched post-merge). Never build\n# it on a workstation — no `docker build`, `docker buildx`, or `docker compose build` (Sami, 2026-09-12).\n#\n# Contents: pinned Bun; the `legion` CLI compiled from this checkout (one binary: legion, worker-shim,\n# credential, gh, handoff, probe-image); the pinned OMP fork build the daemon's default\n# `omp_invocation` names, resolved with mise's github backend exactly as the daemon resolves it;\n# @sjawhar/pi-legion-envoy packed from this checkout's packages/pi-envoy and linked into the isolated OMP\n# profile `legion`; jj; git; gh. The last RUN executes the daemon's two boot probes (`legion probe-image`)\n# as the runtime user, so a broken image never publishes.\n\n# Pins not derived from daemon code. The OMP fork pin is deliberately NOT an ARG: it is printed from\n# packages/daemon/src/daemon/omp-pin.ts (the single source config.ts's DEFAULT_OMP_INVOCATION uses).\nARG BUN_VERSION=1.3.14\nARG MISE_VERSION=v2026.8.12\n# Sami's jj fork: what the dogfood daemon runs on sami-agents; same 0.45 line as the jj-lib inside OMP.\nARG JJ_TOOL=github:sjawhar/jj@0.45.1-sami.20260910-043938\nARG GH_TOOL=gh@2.98.0\n\n# ------------------------------------------------------------------------------------------------\n# cli: workspace install, the compiled legion CLI, the OMP pin, and the packed plugin.\nFROM oven/bun:${BUN_VERSION}-slim AS cli\nWORKDIR /repo\n# jq: the same omp.extensions rewrite release.yaml's pi_envoy job runs. python3/make/g++: native\n# devDependencies in the workspace lockfile (mirrors packages/envoy/docker/Dockerfile).\nRUN apt-get update && apt-get install -y --no-install-recommends jq python3 make g++ \\\n    && rm -rf /var/lib/apt/lists/*\nCOPY package.json bun.lock ./\nCOPY packages/contracts/package.json packages/contracts/package.json\nCOPY packages/envoy-client/package.json packages/envoy-client/package.json\nCOPY packages/pi-envoy/package.json packages/pi-envoy/package.json\nCOPY packages/daemon/package.json packages/daemon/package.json\nCOPY packages/envoy-plugin/package.json packages/envoy-plugin/package.json\nCOPY packages/claude-envoy-bridge/package.json packages/claude-envoy-bridge/package.json\nCOPY packages/dispatch/package.json packages/dispatch/package.json\nCOPY packages/workspace/package.json packages/workspace/package.json\nRUN bun install --frozen-lockfile\nCOPY packages/contracts packages/contracts\nCOPY packages/envoy-client packages/envoy-client\nCOPY packages/workspace packages/workspace\nCOPY packages/daemon packages/daemon\nCOPY packages/pi-envoy packages/pi-envoy\nCOPY skills skills\nRUN mkdir -p /out \\\n    && bun build --compile --target=bun-linux-x64 packages/daemon/src/cli/index.ts --outfile /out/legion \\\n    && bun packages/daemon/src/daemon/omp-pin.ts > /out/omp-pin \\\n    && test -s /out/omp-pin\n# The plugin ships from this checkout with the steps release.yaml's pi_envoy job runs before\n# `bun pm pack` (prepack.sh refuses to pack with the source manifest). The tarball is unpacked into a\n# directory: `omp plugin install` links a directory and rejects a tarball path (ENOTDIR).\nRUN cd packages/pi-envoy \\\n    && jq '.omp.extensions = [\"dist/envoy.js\",\"dist/legion.js\"]' package.json > tmp.json \\\n    && mv tmp.json package.json \\\n    && rm -f ./*.tgz && bun pm pack \\\n    && mkdir -p /out/pi-legion-envoy \\\n    && tar xzf ./*.tgz -C /out/pi-legion-envoy --strip-components=1\n\n# ------------------------------------------------------------------------------------------------\n# tools: mise resolves the pinned OMP fork build, jj, and gh — the same backend and pin string the\n# daemon hands to `mise where` on a tmux host.\nFROM debian:bookworm-slim AS tools\nARG MISE_VERSION\nARG JJ_TOOL\nARG GH_TOOL\nRUN apt-get update && apt-get install -y --no-install-recommends ca-certificates curl \\\n    && rm -rf /var/lib/apt/lists/*\nRUN curl -fsSL https://mise.run | MISE_VERSION=\"${MISE_VERSION}\" MISE_INSTALL_PATH=/usr/local/bin/mise sh\nCOPY --from=cli /out/omp-pin /omp-pin\n# github_token (optional BuildKit secret): mise's github backend reads MISE_GITHUB_TOKEN; a shared\n# builder IP without it can hit GitHub's unauthenticated API limit (403). CI passes secrets.GITHUB_TOKEN;\n# a build without the secret still runs, unauthenticated.\nRUN --mount=type=secret,id=github_token \\\n    set -eu; \\\n    if [ -s /run/secrets/github_token ]; then export MISE_GITHUB_TOKEN=\"$(cat /run/secrets/github_token)\"; fi; \\\n    pin=\"$(cat /omp-pin)\"; \\\n    mise x \"$pin\" -- omp --version; \\\n    cp -r \"$(mise where \"$pin\")\" /opt/omp; \\\n    mkdir -p /opt/tools; \\\n    install -m 0755 \"$(mise x \"$JJ_TOOL\" -- sh -c 'command -v jj')\" /opt/tools/jj; \\\n    install -m 0755 \"$(mise x \"$GH_TOOL\" -- sh -c 'command -v gh')\" /opt/tools/gh; \\\n    /opt/omp/bin/omp --version && /opt/tools/jj --version && /opt/tools/gh --version\n\n# ------------------------------------------------------------------------------------------------\n# runtime\nFROM debian:bookworm-slim\nLABEL org.opencontainers.image.source=https://github.com/sjawhar/legion\n# git: jj's git backend and the workers' own git use. ca-certificates: GitHub, Dispatch, model APIs.\nRUN apt-get update && apt-get install -y --no-install-recommends ca-certificates git \\\n    && rm -rf /var/lib/apt/lists/* \\\n    && groupadd --gid 1000 legion \\\n    && useradd --uid 1000 --gid 1000 --create-home --shell /bin/bash legion\n# Pinned Bun: the binary the cli stage built with (oven/bun:${BUN_VERSION}-slim).\nCOPY --from=cli /usr/local/bin/bun /usr/local/bin/bun\nRUN ln -s bun /usr/local/bin/bunx\nCOPY --from=tools /opt/omp /opt/omp\nCOPY --from=tools /opt/tools/jj /usr/local/bin/jj\nCOPY --from=tools /opt/tools/gh /usr/local/bin/gh\nCOPY --from=cli /out/legion /opt/legion/bin/legion\nCOPY --from=cli --chown=legion:legion /out/pi-legion-envoy /opt/legion/pi-legion-envoy\n# OMP_PROFILE=legion: the isolated profile the plugin is linked into (plugins resolve to\n# /home/legion/.omp/profiles/legion/plugins/node_modules). LEGION_OMP_PATH: how `legion probe-image`\n# — and a daemon pointed at this image — names the OMP executable without mise.\nENV OMP_PROFILE=legion \\\n    LEGION_OMP_PATH=/opt/omp/bin/omp \\\n    PATH=/opt/legion/bin:/opt/omp/bin:/usr/local/bin:/usr/bin:/bin\nUSER legion\nWORKDIR /home/legion\n# 1. Link the packed plugin into the legion profile (omp-plugins.lock.json records it enabled).\n# 2. Run the daemon's two boot probes; OMP's first run also downloads its native modules into\n#    /home/legion/.omp/natives/<version>/, so this layer ships them and a pod never fetches them.\n# Any failure fails the build: a broken image never publishes.\nRUN omp plugin install /opt/legion/pi-legion-envoy \\\n    && legion probe-image \\\n    && rm -rf /home/legion/.omp/profiles/legion/logs\n# Placeholder only: the real entrypoint (provider *_FILE export into the OMP child, shim dial-out) is the\n# Kubernetes runtime's (LEGION-24), which sets the pod command. Override with --entrypoint to run anything else.\nENTRYPOINT [\"legion\", \"worker-shim\"]\n```\n\n- [ ] **Step 3: static lint (no build)** — `hadolint` is not installed; running a published image is allowed:\n  `docker run --rm -i hadolint/hadolint hadolint --ignore DL3008 --ignore DL3003 - < \"$LEGION_WORKSPACE/packages/daemon/docker/worker.Dockerfile\"`\n  (DL3008 = unpinned apt versions, the envoy Dockerfile's accepted convention; DL3003 = `cd` in RUN, used\n  once for the pack step). Fix every error and warning it reports beyond those two; anything else you\n  decide to keep gets a `# hadolint ignore=<rule>` line with the reason. If Docker Hub is unreachable from\n  this box (a `crane ls oven/bun` hung during planning), record that in the handoff and rely on the CI\n  build's `lint: true` (Task 7).\n\n- [ ] **Step 4: host dry run of the runtime RUN logic** (this is exactly what the image's last RUN does;\n  it needs no Docker — it was run this session and passed). From a temp dir:\n\n```bash\nT=$(mktemp -d /tmp/legion23-dryrun-XXXX); OMP=$(mise where github:sjawhar/oh-my-pi@18.1.15-sami.20260908-220934)/bin/omp\ncd -- \"$LEGION_WORKSPACE\" && rsync -a --exclude .jj --exclude .git --exclude node_modules ./ \"$T/repo/\"\ncd \"$T/repo\" && bun install --frozen-lockfile\ncd packages/pi-envoy && jq '.omp.extensions = [\"dist/envoy.js\",\"dist/legion.js\"]' package.json > tmp.json && mv tmp.json package.json && rm -f ./*.tgz && bun pm pack\nmkdir -p \"$T/plugin\" && tar xzf ./*.tgz -C \"$T/plugin\" --strip-components=1\ncd \"$T/repo\" && bun build --compile --target=bun-linux-x64 packages/daemon/src/cli/index.ts --outfile \"$T/legion\"\nmkdir -p \"$T/home\"; export HOME=\"$T/home\" OMP_PROFILE=legion LEGION_OMP_PATH=\"$OMP\"\nunset ANTHROPIC_API_KEY GEMINI_API_KEY OPENAI_API_KEY XDG_DATA_HOME XDG_CONFIG_HOME XDG_STATE_HOME XDG_CACHE_HOME\n\"$OMP\" plugin install \"$T/plugin\"          # expect: ✔ Linked @sjawhar/pi-legion-envoy from …\n\"$T/legion\" probe-image                     # expect: probe-image: OK (…/bin/omp), exit 0\nreadlink \"$HOME/.omp/profiles/legion/plugins/node_modules/@sjawhar/pi-legion-envoy\"   # expect: $T/plugin\nls \"$HOME/.omp/natives\"                     # expect: 18.1.15\n```\n  Negative control: `LEGION_OMP_PATH=/nonexistent \"$T/legion\" probe-image` → exit 1, message contains\n  `does not expose pi.agents`/launch failure text (definitive, no retries because no marker).\n\n- [ ] **Step 5: commit** — `feat(docker): worker image with the pinned OMP fork, packed plugin, and boot-probe gate`\n\n---\n\n### Task 6: `worker-image.yaml` + `release.yaml`\n\n**Files:**\n- Create: `.github/workflows/worker-image.yaml`\n- Modify: `.github/workflows/release.yaml:16-18` (permissions), `:51-56` (cli outputs), `:84-86` (release_tag output), new `worker_image` job after `cli`\n\n**Produces:** on `release.yaml` main runs and on branch pushes touching the image files, a Depot build that\npushes `ghcr.io/sjawhar/legion-worker:sha-<12>` (+ `:<version>` on main when `cli` released), writes the\ndigest to the job summary, appends it to the `cli-v*` release body, mirrors to ECR only when the three\nrepository variables are set, and writes the Depot trust-policy remedy on failure.\n\n- [ ] **Step 1: write `worker-image.yaml`**\n\n```yaml\nname: Worker Image\n\non:\n  workflow_call:\n    inputs:\n      cli_version:\n        description: Version the `cli` job released in this run (empty when it released nothing)\n        type: string\n        required: false\n        default: \"\"\n      release_tag:\n        description: The cli-v* release to append the image digest to (empty when cli_version is empty)\n        type: string\n        required: false\n        default: \"\"\n  # Post-merge only: GitHub dispatches a workflow only once it exists on the default branch.\n  workflow_dispatch:\n    inputs:\n      cli_version:\n        description: Optional CLI version to also tag the image with (main only)\n        type: string\n        required: false\n        default: \"\"\n      release_tag:\n        description: Optional cli-v* release to append the digest to (main only)\n        type: string\n        required: false\n        default: \"\"\n  # Pre-merge iteration: a branch push touching the image files builds and publishes :sha-<short> only.\n  push:\n    branches-ignore: [main]\n    paths:\n      - packages/daemon/docker/**\n      - .github/workflows/worker-image.yaml\n\npermissions:\n  contents: write # gh release edit\n  packages: write # ghcr.io push\n  id-token: write # depot/setup-action OIDC; aws-actions/configure-aws-credentials OIDC\n\njobs:\n  build:\n    runs-on: ubuntu-24.04\n    # Job-level (not workflow-level): this file is also a called workflow, and job-level concurrency/env\n    # behave identically for `workflow_call`, `workflow_dispatch`, and `push`.\n    concurrency:\n      group: worker-image-${{ github.ref }}\n      cancel-in-progress: false\n    env:\n      IMAGE: ghcr.io/sjawhar/legion-worker\n    steps:\n      - uses: actions/checkout@v5\n\n      - name: Require the Depot project variable\n        run: |\n          if [ -z \"${{ vars.DEPOT_PROJECT_ID }}\" ]; then\n            echo \"::error::Repository variable DEPOT_PROJECT_ID is unset; the worker image builds on Depot and needs a project (root decision 3 on LEGION-19).\"\n            echo \"**Worker image not built** — repository variable \\`DEPOT_PROJECT_ID\\` is unset.\" >> \"$GITHUB_STEP_SUMMARY\"\n            exit 1\n          fi\n\n      - name: Compute tags\n        id: meta\n        env:\n          CLI_VERSION: ${{ inputs.cli_version }}\n          RELEASE_TAG: ${{ inputs.release_tag }}\n        run: |\n          short=\"${GITHUB_SHA::12}\"\n          tags=\"${IMAGE}:sha-${short}\"\n          tag_release=\"false\"\n          if [ \"$GITHUB_REF\" = \"refs/heads/main\" ] && [ -n \"$CLI_VERSION\" ]; then\n            tags=\"${tags}\"$'\\n'\"${IMAGE}:${CLI_VERSION}\"\n            [ -n \"$RELEASE_TAG\" ] && tag_release=\"true\"\n          fi\n          {\n            echo \"short=$short\"\n            echo \"tag_release=$tag_release\"\n            echo \"version_label=${CLI_VERSION:-sha-$short}\"\n            echo \"tags<<EOF\"\n            echo \"$tags\"\n            echo \"EOF\"\n          } >> \"$GITHUB_OUTPUT\"\n\n      - uses: depot/setup-action@v1\n        id: depot\n        with:\n          oidc: true\n          # Pinned, not latest: a new depot CLI can briefly lag on one distribution channel and break\n          # the build (agent-c's experience with dl.depot.dev vs public.ecr.aws/depot/cli); bump deliberately.\n          version: \"2.102.7\"\n\n      - uses: docker/login-action@v3\n        with:\n          registry: ghcr.io\n          username: ${{ github.actor }}\n          password: ${{ secrets.GITHUB_TOKEN }}\n\n      - name: Build and push\n        id: build\n        uses: depot/build-push-action@v1\n        with:\n          project: ${{ vars.DEPOT_PROJECT_ID }}\n          context: .\n          file: packages/daemon/docker/worker.Dockerfile\n          # The OMP fork release ships linux-x64 (and darwin-arm64) only.\n          platforms: linux/amd64\n          push: true\n          lint: true\n          tags: ${{ steps.meta.outputs.tags }}\n          labels: |\n            org.opencontainers.image.source=https://github.com/${{ github.repository }}\n            org.opencontainers.image.revision=${{ github.sha }}\n            org.opencontainers.image.version=${{ steps.meta.outputs.version_label }}\n          secrets: |\n            github_token=${{ secrets.GITHUB_TOKEN }}\n\n      - name: Depot failure remedy\n        if: failure() && (steps.depot.outcome == 'failure' || steps.build.outcome == 'failure')\n        run: |\n          {\n            echo \"### Worker image build failed\"\n            echo\n            echo \"If the log shows a Depot authentication / OIDC error: add a GitHub trust relationship for \\`${{ github.repository }}\\` on Depot project \\`${{ vars.DEPOT_PROJECT_ID }}\\` (Depot dashboard → project → Settings → Trust Relationships; https://depot.dev/docs/cli/authentication#oidc-trust-relationships), then re-run: \\`gh workflow run worker-image.yaml --ref ${{ github.ref_name }}\\`.\"\n          } >> \"$GITHUB_STEP_SUMMARY\"\n\n      - name: Record digest\n        env:\n          DIGEST: ${{ steps.build.outputs.digest }}\n          TAGS: ${{ steps.meta.outputs.tags }}\n        run: |\n          test -n \"$DIGEST\"\n          {\n            echo \"### Worker image\"\n            echo\n            echo \"\\`${IMAGE}@${DIGEST}\\`\"\n            echo\n            echo \"Tags:\"\n            printf '%s\\n' \"$TAGS\" | sed 's/^/- `/; s/$/`/'\n            echo\n            echo \"Pin \\`runtime.kubernetes.image\\` to the digest reference above; the daemon refuses a tag.\"\n          } >> \"$GITHUB_STEP_SUMMARY\"\n\n      - name: Append digest to the CLI release\n        if: steps.meta.outputs.tag_release == 'true'\n        env:\n          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n          DIGEST: ${{ steps.build.outputs.digest }}\n          RELEASE_TAG: ${{ inputs.release_tag }}\n        run: |\n          gh release view \"$RELEASE_TAG\" --repo \"$GITHUB_REPOSITORY\" --json body -q .body > body.md\n          if grep -q \"$DIGEST\" body.md; then\n            echo \"Release $RELEASE_TAG already lists $DIGEST\"\n            exit 0\n          fi\n          printf '\\n\\n## Worker image\\n\\n`%s@%s`\\n' \"$IMAGE\" \"$DIGEST\" >> body.md\n          gh release edit \"$RELEASE_TAG\" --repo \"$GITHUB_REPOSITORY\" --notes-file body.md\n\n      - name: Decide ECR mirror\n        id: ecr\n        run: |\n          missing=\"\"\n          [ -n \"${{ vars.AWS_ECR_PUBLISH_ROLE_ARN }}\" ] || missing=\"$missing AWS_ECR_PUBLISH_ROLE_ARN\"\n          [ -n \"${{ vars.AWS_ECR_REGISTRY }}\" ] || missing=\"$missing AWS_ECR_REGISTRY\"\n          [ -n \"${{ vars.AWS_REGION }}\" ] || missing=\"$missing AWS_REGION\"\n          if [ -n \"$missing\" ]; then\n            echo \"enabled=false\" >> \"$GITHUB_OUTPUT\"\n            for v in $missing; do echo \"ECR mirror skipped: $v unset\" >> \"$GITHUB_STEP_SUMMARY\"; done\n          else\n            echo \"enabled=true\" >> \"$GITHUB_OUTPUT\"\n          fi\n\n      - uses: aws-actions/configure-aws-credentials@v4\n        if: steps.ecr.outputs.enabled == 'true'\n        with:\n          role-to-assume: ${{ vars.AWS_ECR_PUBLISH_ROLE_ARN }}\n          aws-region: ${{ vars.AWS_REGION }}\n\n      - name: Mirror to ECR\n        if: steps.ecr.outputs.enabled == 'true'\n        env:\n          ECR_REGISTRY: ${{ vars.AWS_ECR_REGISTRY }}\n          AWS_REGION: ${{ vars.AWS_REGION }}\n          DIGEST: ${{ steps.build.outputs.digest }}\n          TAGS: ${{ steps.meta.outputs.tags }}\n        run: |\n          aws ecr get-login-password --region \"$AWS_REGION\" | docker login --username AWS --password-stdin \"$ECR_REGISTRY\"\n          # Manifest copy by digest (agent-c's mechanism); the GHCR digest stays authoritative.\n          printf '%s\\n' \"$TAGS\" | while read -r tag; do\n            [ -n \"$tag\" ] || continue\n            docker buildx imagetools create --tag \"${ECR_REGISTRY}/legion-worker:${tag##*:}\" \"${IMAGE}@${DIGEST}\"\n          done\n          echo \"Mirrored to \\`${ECR_REGISTRY}/legion-worker\\` with the same tags; the GHCR digest \\`${DIGEST}\\` remains authoritative.\" >> \"$GITHUB_STEP_SUMMARY\"\n\n      - name: ECR mirror failure note\n        if: failure() && steps.ecr.outputs.enabled == 'true' && steps.build.outcome == 'success'\n        run: |\n          echo \"ECR mirror failed after the GHCR push succeeded. The GHCR digest above is authoritative; retry the mirror with \\`gh workflow run worker-image.yaml --ref ${{ github.ref_name }}\\`.\" >> \"$GITHUB_STEP_SUMMARY\"\n```\n\n- [ ] **Step 2: `release.yaml` edits**\n\n  1. `permissions:` (lines 16–18) becomes:\n     ```yaml\n     permissions:\n       contents: write\n       packages: write\n       id-token: write\n     ```\n  2. `cli` job (line 51) gains outputs:\n     ```yaml\n       cli:\n         needs: changes\n         if: needs.changes.outputs.cli == 'true' || github.event_name == 'workflow_dispatch'\n         runs-on: ubuntu-24.04\n         outputs:\n           version: ${{ steps.version.outputs.version }}\n           release_tag: ${{ steps.version.outputs.release_tag }}\n     ```\n  3. In the \"Determine next version\" step, after `echo \"version=$VERSION\" >> \"$GITHUB_OUTPUT\"` (line 84) add\n     `echo \"release_tag=cli-v${VERSION}\" >> \"$GITHUB_OUTPUT\"` (the `skip=true` branches emit neither, so both\n     outputs are empty when nothing is released).\n  4. New job after `cli` (before `envoy_plugin`), mirroring `envoy_plugin`'s condition:\n     ```yaml\n       worker_image:\n         needs: [changes, cli]\n         if: >-\n           always() && (needs.cli.result == 'success' || needs.cli.result == 'skipped') &&\n           (needs.changes.outputs.cli == 'true' || needs.changes.outputs.pi_envoy == 'true' || github.event_name == 'workflow_dispatch')\n         uses: ./.github/workflows/worker-image.yaml\n         secrets: inherit\n         with:\n           cli_version: ${{ needs.cli.outputs.version }}\n           release_tag: ${{ needs.cli.outputs.release_tag }}\n     ```\n  Note: the image is built from `github.sha` (the triggering commit), the same tree `cli` compiled its\n  release binary from; the `[skip ci]` version-bump commit `cli` pushes never re-triggers.\n\n- [ ] **Step 3: static validation** — both files parse:\n  `cd -- \"$LEGION_WORKSPACE\" && bun -e 'import yaml from \"yaml\"; for (const f of [\".github/workflows/worker-image.yaml\", \".github/workflows/release.yaml\"]) { yaml.parse(await Bun.file(f).text()); console.log(\"ok\", f); }'`\n  (run from `packages/daemon` if `yaml` does not resolve at the root). Then the real check is the branch\n  push (Task 8 step 3): the `push` trigger fires; without `DEPOT_PROJECT_ID` it fails at the first step,\n  without the trust policy at `depot/setup-action`, and the summary must show the matching text — that run\n  is the proof of the failure path.\n\n- [ ] **Step 4: commit** — `ci: build and publish the worker image on Depot from release.yaml`\n\n---\n\n### Task 7: docs\n\n**Files:**\n- Create: `docs/kubernetes.md` (image section only — LEGION-25/26 add the rest)\n- Modify: `packages/daemon/src/daemon/AGENTS.md:128` (replace the stale paragraph)\n- Modify: `AGENTS.md:67-78` (WHERE TO LOOK row)\n\n- [ ] **Step 1: `docs/kubernetes.md`**\n\n```markdown\n# Legion on Kubernetes\n\nThis runbook grows with the LEGION-19 tree. This page currently covers the worker image (LEGION-23);\nthe in-cluster daemon (LEGION-25) and the kind smoke (LEGION-26) add their sections beside it.\n\n## Worker image\n\nEvery Legion agent process under `runtime: kubernetes` — architect, planner, implementer, tester, reviewer,\nmerger — runs from one image, `ghcr.io/sjawhar/legion-worker` (public). It carries:\n\n- the pinned OMP fork build the daemon's default `omp_invocation` names — resolved at build time with the\n  same `mise x github:sjawhar/oh-my-pi@<pin>` mechanism a tmux host uses, from the single pin source\n  `packages/daemon/src/daemon/omp-pin.ts`; installed at `/opt/omp/bin/omp` (`LEGION_OMP_PATH`);\n- the `legion` CLI compiled from the same commit (`legion`, `worker-shim`, `credential`, `gh`, `handoff`, and\n  the hidden `probe-image`), at `/opt/legion/bin/legion`;\n- `@sjawhar/pi-legion-envoy` packed from that commit's `packages/pi-envoy` (the exact `bun pm pack` steps\n  `release.yaml`'s `pi_envoy` job runs) and linked into the isolated OMP profile `legion`\n  (`OMP_PROFILE=legion`; plugins resolve to `/home/legion/.omp/profiles/legion/plugins/node_modules`);\n- OMP's native modules, pre-downloaded into `/home/legion/.omp/natives/<version>/` so a pod never fetches them;\n- pinned Bun, `jj` (Sami's fork, the version the dogfood daemon runs), `git`, `gh`.\n\nIt runs as user `legion` (uid 1000) with `HOME=/home/legion`, which must be writable (OMP writes sessions,\nlogs, and `models.db` under `~/.omp/profiles/legion`). It is `linux/amd64` only: the OMP fork release ships\na linux-x64 build only. One commit ⇒ one image: nothing in it is pinned to an npm version.\n\n### The image is probed before it publishes\n\nThe daemon refuses to serve unless its OMP exposes `pi.agents` and actually loads `pi-legion-envoy`\n(`packages/daemon/src/daemon/boot-probes.ts`). The image build's last step runs the same two probes through\n`legion probe-image`, so a build whose OMP or plugin is broken fails instead of publishing. The in-cluster\ndaemon (LEGION-25) runs `legion probe-image` in a one-shot pod against the configured digest. To run it\nyourself: `docker run --rm ghcr.io/sjawhar/legion-worker@sha256:… legion probe-image`.\n\n### Pin by digest, never by tag\n\n`legion.yaml` `runtime.kubernetes.image` accepts only `ghcr.io/sjawhar/legion-worker@sha256:…`. A tag is\nmutable; the daemon must know exactly what it probed, so a tag reference is refused at startup with\n`runtime.kubernetes.image must be pinned by digest (@sha256:…)` (`packages/daemon/src/daemon/image-ref.ts`).\n\nWhere the digest is published:\n\n- the job summary of every `Worker Image` run (Actions → Worker Image → the run → Summary);\n- the body of the `cli-v<version>` GitHub release, under \"Worker image\", when `release.yaml` released the\n  CLI in the same run (`gh release view cli-v<version> --json body -q .body`);\n- `docker buildx imagetools inspect ghcr.io/sjawhar/legion-worker:<tag>` for any published tag.\n\nTags: `sha-<12 hex of the commit>` on every run; `<cli version>` only on `main` when `cli` released that\nversion in the same run. Runs from any other ref publish the `sha-` tag only and never touch a release.\n\n### How it is built — and the iteration rule\n\n`.github/workflows/worker-image.yaml` builds on Depot (`depot/setup-action` with GitHub OIDC — no static\nDepot token — and `depot/build-push-action`, project `vars.DEPOT_PROJECT_ID`). It runs (1) from\n`release.yaml` after the `cli` job on every `main` push that touches the daemon or plugin, (2) on a push to\nany non-`main` branch that touches `packages/daemon/docker/**` or the workflow itself (publishes `sha-` only),\nand (3) by `gh workflow run worker-image.yaml --ref <ref>` once the workflow exists on `main`.\n\n**The image is built only by this workflow, on Depot.** Never build it on a workstation — no `docker build`,\n`docker buildx`, `docker compose build`, or `depot build`: an unrelated buildx job took the sami-agents host\nto load 646 on 2026-09-12 and the Legion daemon with it. Iterate by pushing the branch (trigger 2) or, once\nmerged, dispatching (trigger 3); check the Dockerfile and workflow statically (`hadolint`, `actionlint`\nwhere installed) and run `bun test` for the TypeScript. Pulling and running the published image locally is\nfine.\n\nTwo prerequisites only a human can create, both one-time and both outside this repository (root decision 3\non LEGION-19); a missing one is a finding for the architect, never something a worker creates:\n\n- **`DEPOT_PROJECT_ID` repository variable** on `sjawhar/legion`. Unset ⇒ the workflow refuses at its first\n  step and says so in the summary.\n- **Depot trust relationship.** The first build from `sjawhar/legion` fails at `depot/setup-action` until the\n  Depot project's settings list a GitHub trust relationship for `sjawhar/legion`. The failed run's summary\n  carries this remedy.\n- **GHCR visibility.** If the `legion-worker` package is private after its first push, an anonymous\n  `docker pull` fails; visibility is a package-settings action on GitHub with no API.\n\n### ECR mirror\n\nNode instance roles pull from the account's ECR without pull secrets, so the workflow mirrors the image\n(by digest, same tags) when — and only when — repository variables `AWS_ECR_PUBLISH_ROLE_ARN`,\n`AWS_ECR_REGISTRY`, and `AWS_REGION` are all set. With any unset, the step is skipped and the summary says\n`ECR mirror skipped: <variable> unset`. The GHCR digest is authoritative; a failed mirror never changes it.\nThe AWS side (publish role trusting `repo:sjawhar/legion:ref:refs/heads/main`, ECR repository `legion-worker`)\nlives in agent-c's `meta/infra` Pulumi, not here.\n\n### Per-deployment toolchains layer on top\n\nThe base image carries Legion's own tools only. A deployment whose repositories need more (agent-c: `uv`,\nPython, Node) builds its own image in **its** repo:\n\n```dockerfile\nFROM ghcr.io/sjawhar/legion-worker@sha256:…\n# deployment toolchain here\n```\n\nand pins `runtime.kubernetes.image` to *that* image's digest. The toolchain churns on the deployment's\nschedule, not Legion's release schedule, and the deployment's repo owns its own reproducibility — one Legion\nrelease therefore never has to know about anyone's Python version.\n\n### Entrypoint\n\nThe image's `ENTRYPOINT` is `[\"legion\", \"worker-shim\"]` — a placeholder so the image has a sensible\ndefault command. The real entrypoint (how provider keys reach the OMP child from `*_FILE` secrets, and how\nthe shim dials the daemon) is the Kubernetes runtime's (LEGION-24), which sets the pod's `command`. To run\nanything else in the image, override it: `docker run --rm --entrypoint sh <image> -c '…'`,\n`docker run --rm --entrypoint legion <image> probe-image`.\n```\n\n- [ ] **Step 2: `packages/daemon/src/daemon/AGENTS.md`** — replace line 128 (the \"also reads the installed\n  … manifest … `legion.minPluginVersion`\" paragraph) with:\n\n```markdown\nBefore loading state, opening core NATS, or serving the API, the daemon also proves that `@sjawhar/pi-legion-envoy` is what OMP actually loads: `verifyLegionPluginLoaded` (`boot-probes.ts`) runs `omp models --extension <probe.mjs> --json` through the same launch prefix and passes only when the probe prints `LEGION_PLUGIN_LOADED=yes` — the marker `legion.ts` sets on `globalThis[Symbol.for(\"legion.pi-envoy.legion-loaded\")]` when OMP's ambient discovery loads it. A manifest on disk is never the gate: a disabled (`omp plugin disable`) or unregistered plugin has a manifest and still fails this probe. The installed manifest (`getPluginsNodeModules()/@sjawhar/pi-legion-envoy/package.json`, resolved through OMP's own `DirResolver` precedence: `OMP_PROFILE`/`PI_PROFILE` profile root, else `~/.omp/plugins`) is read only to name the version in the failure message. There is no minimum plugin version field; the plugin ships from this repo and the daemon spawns sessions without `--extension`, so they load `envoy.ts`/`legion.ts` solely from that installed copy. Both boot probes live in `boot-probes.ts` so `startDaemon` and the hidden `legion probe-image` subcommand — which the worker image build runs as its last step (`packages/daemon/docker/worker.Dockerfile`; see `docs/kubernetes.md`) — execute identical code.\n```\n\n- [ ] **Step 3: root `AGENTS.md` WHERE TO LOOK** — add a row after \"Envoy OMP adapter\":\n  `| Worker image (Kubernetes) | \\`packages/daemon/docker/worker.Dockerfile\\`, \\`.github/workflows/worker-image.yaml\\` | See \\`docs/kubernetes.md\\` |`\n\n- [ ] **Step 4: commit** — `docs: worker image section of docs/kubernetes.md; correct the plugin-probe paragraph`\n\n---\n\n### Task 8: final verification, PR, first CI run\n\n- [ ] **Step 1: the once-only project-wide checks**\n  ```\n  cd -- \"$LEGION_WORKSPACE/packages/daemon\" && bunx biome check --error-on-warnings src/ && bunx tsc --noEmit && bun test\n  ```\n  Expect: clean; every pre-existing `index.test.ts` probe test passes unchanged.\n\n- [ ] **Step 2: ancestry, bookmark, push, PR** (per the legion-worker skill; PR body in the READY format\n  with `Dispatch: LEGION-23`):\n  ```\n  jj -R \"$LEGION_WORKSPACE\" log -r 'ancestors(@, 12)'\n  jj -R \"$LEGION_WORKSPACE\" bookmark set legion/LEGION-23 && jj -R \"$LEGION_WORKSPACE\" git push --bookmark legion/LEGION-23\n  legion gh -- pr create --repo sjawhar/legion --base main --head legion/LEGION-23 --title \"feat(docker): worker image + Depot pipeline (LEGION-23)\" --body-file /tmp/legion23-pr-body.md\n  ```\n  where `/tmp/legion23-pr-body.md` is the legion-worker skill's READY-format body (CI / Threads / Thermo /\n  E2E / Fast-follow / Chain sections) and contains the line `Dispatch: LEGION-23`.\n\n- [ ] **Step 3: observe the branch-triggered `Worker Image` run** (trigger R3 fires on the push because the\n  Dockerfile and workflow changed): `legion gh -- run list --workflow worker-image.yaml --branch legion/LEGION-23`,\n  then `legion gh -- run view <id>`. (A pre-merge `gh workflow run worker-image.yaml --ref legion/LEGION-23`\n  will 404: GitHub dispatches only workflows present on the default branch — R3.)\n  - If `DEPOT_PROJECT_ID` is unset: expected failure at \"Require the Depot project variable\" with the\n    summary line `Worker image not built — repository variable DEPOT_PROJECT_ID is unset`.\n  - If it is set but no trust relationship exists: expected **failure at `depot/setup-action`** with the\n    \"Worker image build failed … add a GitHub trust relationship\" remedy in the summary\n    (`gh api repos/sjawhar/legion/actions/runs/<id>/jobs` shows step outcomes; the summary is on the run page).\n  - Either failure is the proof of that failure path. Report it to the LEGION-23 architect with the run URL\n    (`envoy_publish` to `notifications.role.legion-sjawharlegion-legion-23-architect`) — do not create the\n    variable, project, or trust policy — and finish everything else in this task.\n  - Once both prerequisites exist: re-push (or, post-merge, `legion gh -- workflow run worker-image.yaml --ref …`);\n    expected success with the digest in the summary. Then the checks below (the tester repeats them).\n\n- [ ] **Step 4: image checks against the published digest** (pulling/running a published image is allowed):\n  ```\n  D=ghcr.io/sjawhar/legion-worker@sha256:<from the summary>\n  docker run --rm --entrypoint sh \"$D\" -c 'omp --version && legion --help && jj --version && git --version && gh --version && bun --version && id -u'\n  docker run --rm --entrypoint legion \"$D\" probe-image        # expect: probe-image: OK (/opt/omp/bin/omp), exit 0\n  docker run --rm --entrypoint sh \"$D\" -c 'readlink ~/.omp/profiles/legion/plugins/node_modules/@sjawhar/pi-legion-envoy && ls ~/.omp/natives'\n  docker run --rm \"$D\" --help                                  # the placeholder ENTRYPOINT: worker-shim usage, exit 0\n  ```\n  (`--entrypoint` because the image's ENTRYPOINT is `legion worker-shim`; the spec's check commands\n  `docker run --rm <image> sh -c …` / `docker run --rm <image> legion probe-image` are these with the\n  entrypoint overridden.) Negative control: `docker run --rm --entrypoint legion -e LEGION_OMP_PATH=/nonexistent \"$D\" probe-image` → exit 1.\n  Anonymous pull check: `docker logout ghcr.io && docker pull \"$D\"` — a failure here is the GHCR-visibility\n  risk (Sami makes the package public).\n\n## Acceptance → proof map\n\n| spec acceptance | proof | blocked on |\n| --- | --- | --- |\n| 1 image contents | Task 8 step 4 `docker run --rm --entrypoint sh <digest> -c 'omp --version && legion --help && jj --version && git --version && gh --version'` exit 0 (tester, against the published digest) | the two external prerequisites below (first successful build); GHCR visibility for the anonymous pull |\n| 2 probes pass inside; `legion probe-image`; build's final stage runs it | Task 3 tests (`cli/__tests__/index.test.ts`), Task 5 step 4 host dry run (`legion probe-image` → OK with the compiled CLI against the pinned omp already on this box), Dockerfile last RUN, Task 8 step 4 `docker run … legion probe-image` | same |\n| 3 workflow: Depot, tags, summary digest, release body, ECR gating | Task 6; Task 8 step 3 run — failure path provable now (remedy summary, or the `DEPOT_PROJECT_ID` refusal); success path after both prerequisites; release-body append observable only on the first `main` run after merge that also releases the CLI | both external prerequisites; ECR variables (unset ⇒ the skipped-with-summary path is what runs) |\n| 4 digest-only `parseImageDigestRef` + error text | `bun test src/daemon/__tests__/image-ref.test.ts` | — |\n| 5 per-deployment layers documented with the reason | `docs/kubernetes.md` \"Per-deployment toolchains layer on top\" | — |\n\n### The two external prerequisites (not the implementer's to create)\n\nBoth gate acceptance 3's success path and therefore the tester's checks 1–2. Neither is created by this\nchild — a missing one is a **finding reported to the LEGION-23 architect** (`envoy_publish` to\n`notifications.role.legion-sjawharlegion-legion-23-architect` with the run URL and the failing step), who\ncarries it to root decision 3; the implementer keeps working on everything else and never creates an\noutside-platform object.\n\n1. Repository variable `DEPOT_PROJECT_ID` on `sjawhar/legion` (root decision 3's recommendation: agent-c's\n   project `zd033k7gpx`). Check: `legion gh -- variable list --repo sjawhar/legion`. Unset ⇒ the workflow's\n   first step refuses with `Repository variable DEPOT_PROJECT_ID is unset` (summary + `::error`).\n2. A Depot GitHub OIDC trust relationship admitting `sjawhar/legion` on that project (Depot dashboard;\n   no API, no IaC). Missing ⇒ `depot/setup-action` fails; the `if: failure()` step writes the remedy.\n\nEverything else — Tasks 1–7, the failure path of acceptance 3, the static and host validations — is\ncompletable and provable before either exists. The first CI build is the last step (Task 8).\n\n## Risks\n\n1. **Depot trust relationship + `DEPOT_PROJECT_ID` (external, Sami / root decision 3).** See above. Until\n   both exist no image publishes; the workflow names the remedy on failure.\n2. **GHCR package visibility (external, Sami).** A first push may create `legion-worker` private; anonymous\n   pulls (the tester's check; node pulls without pull secrets) fail until Sami flips it to public in package\n   settings (no API). Precedent: `ghcr.io/sjawhar/legion/envoy:latest` is anonymously readable today.\n3. Spec Acceptance 3 assumes a pre-merge `workflow_dispatch` from the PR branch; GitHub only dispatches\n   workflows present on the default branch (R3). The `push` trigger is the pre-merge path; if the architect\n   rejects R3, the first real build waits for the merge to `main`.\n4. mise `github:` backend in the build hits GitHub's API: without the `github_token` build secret a shared\n   builder IP can 403. CI passes `secrets.GITHUB_TOKEN`; the mount is optional.\n5. `omp` native-module download during the probe step needs egress from the Depot builder to wherever OMP\n   fetches `pi_natives.*.node` (worked from this box; URL not captured). If it fails, the build fails loudly at\n   `legion probe-image`.\n6. `config.ts` concurrent edit by LEGION-21 (`runtime:`/`daemon_url`/`bind`; LEGION-21 is `in_progress`):\n   our change is one import line plus deleting line 142 — trivial to rebase.\n7. The image is never built locally (R10): every Dockerfile edit is validated statically and by the host dry\n   run, then by CI. Expect 1–3 CI iterations on the Dockerfile once the prerequisites exist.\n8. `oven/bun:1.3.14-slim` and Debian bookworm package availability are assumed (CI surfaces a wrong tag on\n   the first build).\n\n## Not in this PR (explicitly)\n\n- The container entrypoint and the `*_FILE` provider export (LEGION-24 owns both; the image's\n  `ENTRYPOINT [\"legion\", \"worker-shim\"]` is a placeholder). No `entrypoint.sh`, no `legion worker-entrypoint`.\n- Wiring `parseImageDigestRef` into `config.ts` (LEGION-24). The Kubernetes runtime and pod spec (LEGION-24).\n- The in-cluster daemon's probe pod (LEGION-25). The rest of `docs/kubernetes.md` (LEGION-25/26).\n- The ECR AWS side (root architect, agent-c `meta/infra`); the Depot project, trust policy, and\n  `DEPOT_PROJECT_ID` variable (root decision 3) — findings, never creations.\n- Any secret or token beyond `secrets.GITHUB_TOKEN`; no `DEPOT_TOKEN`; no `depot build` anywhere but CI.\n",
  "schemaVersion": 1,
  "phase": "plan",
  "completed": "2026-09-12T09:35:57.781Z"
}