{
  "issue": "LEGION-18",
  "title": "Skill and role-prompt audit fixes",
  "spec": "dispatch://LEGION-18 (read with dispatch_doc_read({ issue: \"LEGION-18\" })). The spec is the record; none of the nine fixes is re-decided here. The architect accepted eight planner findings on 2026-09-12 (11 negative-control hits, fix 5 body shape, fix 9 root-AGENTS scope, fence fix, scout+oracle for fix 1, no-rebase rule, knowledge-injection untouched, skills/AGENTS.md stale sentences folded into fix 9) and recorded them in the spec.",
  "planLocation": "This handoff plus the Dispatch issue are the plan. Nothing under docs/plans/ or anywhere else in the repository; the PR must contain no docs/plans/* file (acceptance check 4).",
  "scope": {
    "kind": "text-only pull request: Markdown skill files, role prompts, two AGENTS.md files",
    "noCodeChanges": "No daemon, extension, or test change. Verified: packages/pi-envoy/package.json omp.skills is the directory [\"dist/skills\"] (not an enumeration); packages/pi-envoy/scripts/prepack.test.ts asserts only that skills/ is non-empty and contains legion-controller; no other test names skills/github or skills/linear (grep over packages/**/*.test.ts and __tests__/).",
    "exactlyNineFixes": "A tenth fix (architect skill's spec upload replacing the human's document) is NOT in scope; PR sjawhar/legion#948 carried it and merged 2026-09-12T04:16:35Z.",
    "branch": "legion/LEGION-18, base 7db2cd83 (chore: release cli v1.1.2). origin/main is 8 commits ahead (#946, #947, #948, #949 + release commits); every phrase we remove is still present on main; #947 rewrote root AGENTS.md lines 109-117, four lines above our Plans-row edit — clean merge expected. Do not rebase to catch up."
  },
  "workspaceFacts": {
    "predecessorHandoffs": "None. .legion/ did not exist when the planner started; the spec was the only predecessor record.",
    "ompConfigHazard": "The working copy carries `A .omp/config.yml` — an empty file the daemon writes at workspace provisioning (packages/daemon/src/daemon/processes.ts writeOmpConfig). It must never enter a commit. Commit ONLY with `jj -R \"$LEGION_WORKSPACE\" split -m \"<msg>\" <paths…>` (never `jj describe` + `jj new` on the whole working copy), and set the bookmark with `-r @-` so it never points at the working-copy commit that still holds .omp/config.yml.",
    "lineNumbers": "Line numbers below are from the workspace at base 7db2cd83 and drift as edits land; the quoted text is authoritative. Search for the quote, not the number.",
    "agentsAvailableInAPane": "Panes inherit OMP_PROFILE=legion (the daemon never sets --profile; packages/daemon/src/daemon/index.ts:193-197). ~/.omp/profiles/legion/agent/agents/ holds 19 files: astra, bug-finder, code-architect, code-reviewer, code-simplifier, comment-analyzer, cybersecurity-expert, deep, oracle, performance-engineer, planner, prompt-engineer, red-teamer, sami-proxy, test-analyzer, thermonuclear-code-quality, thermonuclear-deep-review, type-checker, ux-designer. OMP built-ins scout, task, reviewer, security-reviewer, sonic also appear in a pane's task tool (verified in the planner's own tool inventory). Architect decision: fix 1 names only `scout` and `oracle`."
  },
  "coordination": [
    "LEGION-16 (Controller runs as an attachable TUI session and owns the merge queue) rewrites where the merger sends READY. Its architect confirmed its edits: skills/legion-worker/SKILL.md lines 228-231 only (the merger bullet), packages/pi-envoy/roles/merger.md lines 12-13 and 41-47, skills/legion-architect/SKILL.md line 184, skills/legion-controller/SKILL.md (a new Merge queue section), and the AGENTS.md files. Leave every one of those lines untouched; keep the current `pr-queue` wording wherever it already appears (worker:230, merger:13/42/44, architect:184). Fix 8's two bullets go between the fast-follow bullet ending `line instead of iterating per push.` (line 214) and the tester bullet starting `- The tester fills in the `E2E` section` (line 215) — never adjacent to line 228. As of 2026-09-12T05:00Z no legion/LEGION-16 branch or PR exists on GitHub.",
    "sjawhar/legion#948 (docs(skills): write specs and asks for the human who reads them) MERGED 2026-09-12T04:16:35Z. It edited skills/dispatch/SKILL.md, the architect skill's design-gate steps (lines 78-99), packages/pi-envoy/roles/architect-root.md and architect.md, and one controller-skill bullet (lines 44-50 on main). Our only shared files are the architect skill (we delete lines 13-16, far above) and the controller skill (we delete lines 12-14, far above). No conflict expected.",
    "Rebase only if GitHub reports the PR `CONFLICTING` (`legion gh -- pr view <n> --json mergeable`). Never rebase to pick up main or to refresh CI. A single failed CI job is re-run with `legion gh -- run rerun <run-id> --failed`, never by pushing a new commit.",
    "Every phase communicates with the architect by envoy_publish to notifications.role.legion-sjawharlegion-legion-18-architect. A human question goes through dispatch_ask on LEGION-18, never the transcript."
  ],
  "fixes": [
    {
      "n": 1,
      "file": "skills/legion-oracle/SKILL.md",
      "why": "Names `Task learnings-researcher`, `Task Explore`, `Task best-practices-researcher`, `Context7 MCP` — none exist for a Legion pane.",
      "edits": [
        {
          "replace": "## Research Strategy\n\nRun steps 1-2 first (parallel OK), then 3-4 if needed:\n\n| Step | Tool | Query |\n|------|------|-------|\n| 1. Institutional learnings | `Task learnings-researcher` | Search docs/solutions/ for [question] |\n| 2. Codebase patterns | `Task Explore` | Find how src/ handles [topic] |\n| 3. Framework docs | Context7 MCP | resolve-library-id → query-docs |\n| 4. External practices | `Task best-practices-researcher` or `WebSearch` | Current best practices for [topic] |",
          "with": "## Research Strategy\n\nIf the deployment instructions name a librarian role, ask it first (publish to its\n`notifications.role.<name>` topic with `expects_reply: required`). Then run steps 1-2 (parallel\nOK), and 3-4 if needed, with tools a Legion pane actually has: `read`, `grep`, `web_search`, and\n`task(agent=\"scout\")` (fast read-only codebase search) or `task(agent=\"oracle\")` (deeper\nread-only analysis when the answer needs judgment across many files). Do not name any other agent.\n\n| Step | Tool | Query |\n|------|------|-------|\n| 1. Institutional learnings | `grep` then `read` over `docs/solutions/` (front-matter `tags`, then the body) | [question]'s keywords |\n| 2. Codebase patterns | `task(agent=\"scout\")`; `task(agent=\"oracle\")` when judgment across many files is needed | Find how [module] handles [topic] |\n| 3. Framework docs | `read` the library's documentation URL | [library] [topic] |\n| 4. External practices | `web_search`, then `read` the primary source | Current best practices for [topic] |"
        },
        {
          "replace": "## Example\n\n```\n/legion-oracle How should I handle GraphQL pagination?\n\n[learnings-researcher] → No matches\n[Explore] → Found src/legion/state/fetch.py uses cursor-based pagination\n\nAnswer: Use cursor-based pagination per src/legion/state/fetch.py:42\n```",
          "with": "## Example\n\n```\nQuestion: How should I paginate a GraphQL connection?\n\ngrep pagination docs/solutions/   → no matches\ntask(agent=\"scout\")               → packages/daemon/src/state/fetch.ts loops on\n                                    pageInfo.hasNextPage / endCursor (contextsPage)\n\nAnswer: cursor-based, per the `while (page.hasNextPage)` loop in packages/daemon/src/state/fetch.ts\n```"
        }
      ],
      "verified": "packages/daemon/src/state/fetch.ts really contains contextsPage and a `while (page.hasNextPage)` loop paging on endCursor (lines ~435-462 at base), so the example cites real code. The librarian sentence is the spec's wording verbatim."
    },
    {
      "n": 2,
      "file": "skills/github/SKILL.md, skills/linear/SKILL.md (delete); skills/legion-worker/SKILL.md (add three facts); skills/AGENTS.md and AGENTS.md (references)",
      "why": "Dispatch is the sole issue backend; LEGION_ISSUE_BACKEND exists nowhere in the codebase; the Projects-V2 workflow is dead. Each directory holds only SKILL.md (verified by glob).",
      "edits": [
        {
          "delete": "skills/github/SKILL.md and skills/linear/SKILL.md — `cd -- \"$LEGION_WORKSPACE\" && rm -r skills/github skills/linear`; jj snapshots the deletion."
        },
        {
          "file": "skills/legion-worker/SKILL.md",
          "insertAfter": "The end of the `Phase work` section's code block:\n```bash\nlegion gh -- <gh args…>\n```\n(line 139; directly before the blank line and `## GitHub PR comment attribution`)",
          "with": "\nThree facts about `gh` in a worker pane. The `gh` on your `PATH` is a shim\n(`packages/pi-envoy/src/legion/gh-shim.ts`) that execs `legion gh -- \"$@\"`, so `gh …` and\n`legion gh -- …` are the same call, and each call redeems a fresh token from your session's\ngrant — identity is supplied per call, never stored. Never run `gh auth login` or\n`gh auth setup-git`; there is no login state to create. The shim refuses `pr merge` (and a raw\n`gh api …/merge`): no worker role merges a pull request — the merge queue does, under its own\nauthority."
        },
        {
          "file": "AGENTS.md (repository root)",
          "replace": "- **jj (Jujutsu)** for version control, **Linear** or **GitHub Issues** for issue tracking",
          "with": "- **jj (Jujutsu)** for version control, native **Dispatch** for issue tracking"
        },
        {
          "file": "skills/AGENTS.md",
          "note": "The `├── github/` and `├── linear/` tree lines are removed as part of fix 9's redraw below."
        },
        {
          "file": "skills/legion-worker/references/knowledge-injection.md",
          "note": "Line 43 `- Feature areas (e.g., \"skills\", \"linear\", \"github\", \"review\", \"retro\")` uses the words as example keywords, not skill names. Leave it untouched (architect confirmed)."
        },
        {
          "file": "packages/pi-envoy/package.json",
          "note": "omp.skills is [\"dist/skills\"] — a directory, not a per-skill list. No change."
        }
      ],
      "verified": "gh-shim.ts writes `<state_dir>/worker-bin/gh` = `exec legion gh -- \"$@\"` and exports LEGION_GRANT per pane; packages/daemon/src/cli/index.ts:86-96 rejects `pr … merge` and `api …/merge`; AGENTS.md:48 already documents the refusal."
    },
    {
      "n": 3,
      "file": "skills/legion-worker/SKILL.md, skills/legion-architect/SKILL.md, skills/legion-controller/SKILL.md",
      "why": "The Dispatch-native contract has been live on main since #873/#923 and ran LEGION-6 end to end; the caveats are stale.",
      "edits": [
        {
          "file": "skills/legion-worker/SKILL.md",
          "delete": "This skill documents the target Dispatch-native contract. `LEGION_ISSUE` as a Dispatch key,\nthe `dispatch_ask` tool, and the `Dispatch: <KEY>` PR-body linkage land with PR B (#TBD);\nuntil that PR merges, this skill's contract is not yet runnable on `main`.\n",
          "note": "Lines 14-17 (paragraph plus its trailing blank line), so `## Identity, scope, and role` follows the intro paragraph after one blank line."
        },
        {
          "file": "skills/legion-architect/SKILL.md",
          "delete": "This skill documents the target Dispatch-native contract. The `legion` tool's\n`register_gate`/`release_wave`/`set_status` ops, the Dispatch key format, and the\n`dispatch_*` tool family land with PR B (#TBD); until that PR merges, this skill's contract\nis not yet runnable on `main`.\n",
          "note": "Lines 13-17. #948 did not touch this paragraph (verified on main@origin)."
        },
        {
          "file": "skills/legion-controller/SKILL.md",
          "delete": "This skill documents the target Dispatch-native contract. The `legion` tool's `set_status`\nop, `dispatch_read`/`dispatch_issue`, and the Dispatch key format land with PR B (#TBD);\nuntil that PR merges, this skill's contract is not yet runnable on `main`.\n",
          "note": "Lines 12-15. #948's controller edit is at lines 44-50 on main; no overlap."
        }
      ]
    },
    {
      "n": 4,
      "file": "skills/legion-worker/SKILL.md (Workspace and handoff precedence section)",
      "why": "Claims no schema validation runs anywhere. False: validatePhaseHandoff (packages/contracts/src/handoff-schema.ts:205-208) gates every ledger read (packages/daemon/src/handoff/ledger.ts:87-91); a failing file is returned as null (missing).",
      "edits": [
        {
          "replace": "Read only files that precede the assigned phase. There is no handoff schema (rejected\ndesign — no schema validation runs anywhere in this pipeline): write the phase-specific\nfields the next phase and the architect need, consistent with what predecessor phases\nalready wrote. The durable copy lives in `$LEGION_WORKSPACE/.legion/<phase>.json`. If a\ncommitted handoff conflicts with memory or a prior transcript, the committed file wins: it\nis the copy that survived.",
          "with": "Read only files that precede the assigned phase. Every handoff is validated when it is read:\n`validatePhaseHandoff` (`packages/contracts/src/handoff-schema.ts`) checks the base fields\n`legion handoff write` supplies (`schemaVersion: 1`, `phase`, `completed`) plus each phase's\ndeclared optional fields — for the plan, `requiredSkills.implement`/`.test`/`.review` as string\narrays, `taskCount`, `concerns`, `workflowRecommendation` — and the ledger\n(`packages/daemon/src/handoff/ledger.ts`) treats a file that fails it as missing. A declared\nfield of the wrong type therefore erases the whole handoff for the daemon; fields outside the\ndeclared shape stay on disk for the next worker but the daemon ignores them. Write the\nphase-specific fields the next phase and the architect need, consistent with what predecessor\nphases already wrote. The durable copy lives in `$LEGION_WORKSPACE/.legion/<phase>.json`. If a\ncommitted handoff conflicts with memory or a prior transcript, the committed file wins: it is\nthe copy that survived."
        }
      ],
      "verified": "handoff-schema.ts: baseHandoffSchema {schemaVersion literal 1, phase enum, completed ISO regex, learningsInjected?, learningsHelpful?}; planSchema adds taskCount?, independentTasks?, routingHints?, concerns?, workflowRecommendation?, requiredSkills? {implement?/test?/review?: string[]} .passthrough(); zod's default object mode strips unknown keys rather than rejecting them; safeParse failure → null → readPhaseHandoff returns null."
    },
    {
      "n": 5,
      "file": "packages/pi-envoy/roles/reviewer.md AND skills/legion-worker/SKILL.md (reviewer bullet)",
      "why": "On #923 the reviewer submitted 13 separate COMMENTED reviews in 20 seconds, each firing a pr-review wake. Architect-confirmed mechanics: `event` must be inside the JSON body — gh's own help states that with --input, field flags (-f) are appended to the URL query string, so `-f event=… --input` would create a PENDING draft.",
      "edits": [
        {
          "file": "packages/pi-envoy/roles/reviewer.md",
          "replace": "run `task(agent=\"thermonuclear-deep-review\")` and `task(agent=\"thermonuclear-code-quality\")` once\nat that head. Post every correctness finding as a PR review comment and return the issue to the\narchitect with `changes_requested`; cleanup findings go in one comment as a named fast-follow.",
          "with": "run `task(agent=\"thermonuclear-deep-review\")` and `task(agent=\"thermonuclear-code-quality\")` once\nat that head. Submit **one review per round** — `CHANGES_REQUESTED` when any correctness finding\nstands, `APPROVED` when none does — carrying every inline comment in that single submission:\n`legion gh -- api --method POST repos/{owner}/{repo}/pulls/{number}/reviews --input body.json`,\nwhere `body.json` holds `commit_id` (the head you reviewed), `event` (`REQUEST_CHANGES` or\n`APPROVE`), `body` (your summary, cleanup findings as one named fast-follow, and the Legion\nfooter), and a `comments[]` array of `{path, line, side: \"RIGHT\", body}` — one entry per finding.\nNever one `pr review` call per finding: each submission fires a `pr-review` wake. Then return the\nissue to the architect with `changes_requested`, or proceed to the clean-review steps below.\n\n```json\n{\"commit_id\": \"<head-sha>\", \"event\": \"REQUEST_CHANGES\",\n \"body\": \"<summary>\\n\\nFast-follow: <one named cleanup item>\\n\\n<!-- legion: {\\\"session\\\":\\\"<session-id>\\\",\\\"phase\\\":\\\"review\\\"} -->\",\n \"comments\": [{\"path\": \"<file>\", \"line\": <n>, \"side\": \"RIGHT\", \"body\": \"<finding>\"}]}\n```"
        },
        {
          "file": "skills/legion-worker/SKILL.md",
          "replace": "- The reviewer verifies the `CI`, `Threads`, and `E2E` facts against GitHub directly —\n  never from a handoff — then runs `task(agent=\"thermonuclear-deep-review\")` and\n  `task(agent=\"thermonuclear-code-quality\")` once at that head and records the verdict.\n  Skip the `Thermo` line entirely on a docs-only PR. Post every correctness finding as a\n  PR review comment and return the issue to the architect; when clean, have the architect send\n  the implementer back to push the `.legion/` deletion (the review App cannot push), then\n  review **that** head and approve it by name.",
          "with": "- The reviewer verifies the `CI`, `Threads`, and `E2E` facts against GitHub directly —\n  never from a handoff — then runs `task(agent=\"thermonuclear-deep-review\")` and\n  `task(agent=\"thermonuclear-code-quality\")` once at that head and records the verdict.\n  Skip the `Thermo` line entirely on a docs-only PR. Submit **one review per round**,\n  `CHANGES_REQUESTED` or `APPROVED`, carrying every inline comment in that single call —\n  `legion gh -- api --method POST repos/{owner}/{repo}/pulls/{number}/reviews --input body.json`\n  with `commit_id`, `event` (`REQUEST_CHANGES` or `APPROVE`), `body` (with the Legion footer),\n  and a `comments[]` array of `{path, line, side, body}`, one entry per finding — never one\n  `pr review` call per finding (each submission fires a `pr-review` wake). Then return the\n  issue to the architect; when clean, have the architect send the implementer back to push\n  the `.legion/` deletion (the review App cannot push), then review **that** head and approve\n  it by name."
        }
      ],
      "leaveAlone": "reviewer.md's `## Final review gate` (`legion gh -- pr review --approve` naming the deletion head) is already a single submission; unchanged. The worker-skill merger bullet two lines below (lines 228-231) is LEGION-16's; do not touch it."
    },
    {
      "n": 6,
      "file": "packages/pi-envoy/roles/planner.md, skills/legion-worker/SKILL.md (new planner section), AGENTS.md (Plans row + fence)",
      "why": "#923 carried a 1,706-line docs/plans document because the worker read the root AGENTS.md Plans row as a mandate. Sami's standing rule: plan and spec content goes into the issue, never a PR.",
      "edits": [
        {
          "file": "packages/pi-envoy/roles/planner.md",
          "insertAfter": "The end of the first instruction paragraph, which ends:\n`when they improve the plan; never spawn a Legion role yourself.` (line 20)",
          "with": "\nThe plan lives in `.legion/plan.json` and the Dispatch issue document; never commit a plan or\nspec file to the repository. No `docs/plans/*`, `docs/superpowers/plans/*`, or spec markdown goes\ninto the pull request — plan and spec content goes into the issue, never into a PR. The root\n`AGENTS.md`'s `docs/plans/` row describes human-authored design history, not a Legion artifact;\na skill step that says \"save the plan to a file\" is satisfied by the handoff write below."
        },
        {
          "file": "skills/legion-worker/SKILL.md",
          "insertBefore": "## Implementer push and pull request",
          "with": "## Planner artifact\n\nThe plan lives in `.legion/plan.json` and the Dispatch issue document; never commit a plan or\nspec file to the repository. No `docs/plans/*`, `docs/superpowers/plans/*`, or spec markdown goes\ninto the pull request: plan and spec content goes into the issue, never into a PR (the root\n`AGENTS.md`'s `docs/plans/` row is human-authored design history, not a Legion artifact). A skill\nstep that says \"save the plan to a file\" is satisfied by the handoff write in the completion gate\nbelow; the planner's only commit is `plan: record handoff`.\n",
          "note": "A new H2 section placed between `## GitHub PR comment attribution` and `## Implementer push and pull request`, keeping the lifecycle order planner → implementer."
        },
        {
          "file": "AGENTS.md (repository root)",
          "replace": "- Plans: `docs/plans/YYYY-MM-DD-<slug>.md`",
          "with": "- Plans: `docs/plans/YYYY-MM-DD-<slug>.md` — human-authored design history, not a Legion artifact. A Legion planner's plan lives in `.legion/plan.json` and the Dispatch issue document; no Legion role commits a plan or spec file here."
        },
        {
          "file": "AGENTS.md (repository root) — fence fix, architect-approved",
          "note": "The ``` at line 92 (opening the Issue Lifecycle diagram) is only closed by the file's last line (126), so `## Documentation` and the Plans row render inside a code block. Insert a line containing exactly ``` directly after the diagram's last line `                    (urgent + clear)` (line 99), and delete the final line 126 (```) so the file ends after the `> Many docs in …` blockquote. Afterwards `grep -c '^```' AGENTS.md` is even and `## Documentation` is a real heading."
        }
      ]
    },
    {
      "n": 7,
      "file": "packages/pi-envoy/roles/tester.md AND skills/legion-worker/SKILL.md (tester bullet)",
      "why": "#923's PR body carried the LEGION_*/DISPATCH_*/ENVOY_* scrub evidence three times.",
      "edits": [
        {
          "file": "packages/pi-envoy/roles/tester.md",
          "replace": "describe. Record the exact command or run id, what you observed, the head SHA, and one negative\ncontrol (a deliberately broken input and the refusal it produced) in the PR body's `E2E` section.",
          "with": "describe. Record the exact command or run id, what you observed, the head SHA, and one negative\ncontrol (a deliberately broken input and the refusal it produced) in the PR body's `E2E` section.\nEnvironment or secret-scrub evidence (for example \"`LEGION_*`/`DISPATCH_*`/`ENVOY_*` unset\") is\nrecorded once, in your `.legion/test.json` handoff, and only when the issue's acceptance criteria\ncall for it — never re-pasted into the PR body on every round."
        },
        {
          "file": "skills/legion-worker/SKILL.md",
          "replace": "- The tester fills in the `E2E` section: the real surface a user reaches the criterion\n  through, the exact command or run id, what was observed, the head SHA, and one negative\n  control — a deliberately broken input and the refusal or failure it produced. A unit or\n  integration test is a regression lock, never proof of a criterion.",
          "with": "- The tester fills in the `E2E` section: the real surface a user reaches the criterion\n  through, the exact command or run id, what was observed, the head SHA, and one negative\n  control — a deliberately broken input and the refusal or failure it produced. A unit or\n  integration test is a regression lock, never proof of a criterion. Environment or\n  secret-scrub evidence (e.g. \"`LEGION_*`/`DISPATCH_*`/`ENVOY_*` unset\") is recorded once, in\n  `.legion/test.json`, and only when the issue's acceptance criteria call for it — never\n  re-pasted into the PR body each round."
        }
      ]
    },
    {
      "n": 8,
      "file": "skills/legion-worker/SKILL.md (PR body and merge-queue discipline bullets)",
      "why": "Merge-queue rules workers must know; both Sami quotes must appear word for word, each on a single line so a grep for the whole quote matches.",
      "edits": [
        {
          "insertBetween": "the fast-follow bullet ending `  line instead of iterating per push.` (line 214) and the tester bullet starting `- The tester fills in the `E2E` section:` (line 215)",
          "with": "- **Rebase only on a real conflict.** Sami, 2026-09-11, verbatim:\n  \"Please don't do unnecessary rebases (i.e. unless there are merge conflicts). The CI queue is too long and slow.\"\n  The implementer rebases the issue branch only when GitHub reports it `CONFLICTING` or the\n  controller asks because of a conflict — never to pick up `main` or to refresh CI. A single\n  failed CI job is re-run on its own with `legion gh -- run rerun <run-id> --failed`, never by\n  pushing a new commit.\n- **No deferrals.** Sami, 2026-09-11, verbatim: \"My rule is no deferrals.\" The `Fast-follow:`\n  field names naming, duplication, or wording cleanup only; anything that changes behaviour,\n  hides an error, or breaks a gate lands in this PR."
        }
      ],
      "partC": "READY target: leave the merger bullet (lines 228-231, `notifications.role.pr-queue`) exactly as it is; LEGION-16 owns that sentence. Do not reintroduce `pr-queue` anywhere new.",
      "quotes": [
        "Please don't do unnecessary rebases (i.e. unless there are merge conflicts). The CI queue is too long and slow.",
        "My rule is no deferrals."
      ]
    },
    {
      "n": 9,
      "file": "skills/AGENTS.md (tree + the two stale coordination sentences, architect-approved) and AGENTS.md (root)",
      "why": "Structure tree lists the deleted skills; the dispatch line describes the retired GitHub-issue-thread mechanism; lines 29-32 describe hub and an MCP tool that are not how workers coordinate.",
      "edits": [
        {
          "file": "skills/AGENTS.md",
          "replace": "skills/\n├── dispatch/            # Raising a durable human question as a GitHub-issue thread\n├── github/              # GitHub issue and pull-request operations\n├── linear/              # Linear adapter instructions\n├── legion-architect/    # Tree ownership, decomposition, gates, and scheduling",
          "with": "skills/\n├── dispatch/            # Writing specs, asks, comments, and artifacts on native Dispatch\n├── legion-architect/    # Tree ownership, decomposition, gates, and scheduling"
        },
        {
          "file": "skills/AGENTS.md",
          "replace": "Workers do not run a controller loop or mutate lifecycle labels. Workers coordinate\nlifecycle, scope, and cross-phase decisions with the owning architect through hub, sending\nthe verified observation and decision needed. A worker may use the `dispatch` MCP tool\ndirectly for a durable human question; replies come back to the worker's own session.",
          "with": "Workers do not run a controller loop or mutate lifecycle labels. Workers coordinate\nlifecycle, scope, and cross-phase decisions with the owning architect by `envoy_publish` to its\nrole topic, sending the verified observation and decision needed (`hub` reaches only subagents\ninside the worker's own process). A worker may call the native `dispatch_*` tools directly for a\ndurable human question; replies come back to the worker's own session."
        },
        {
          "file": "AGENTS.md (repository root)",
          "note": "The WHERE-TO-LOOK table (lines 67-78) has no github or linear row — nothing to update there. The root file's only changes are fix 2's tech-stack line (30) and fix 6's Plans row plus the fence fix."
        }
      ]
    }
  ],
  "implementerSteps": [
    "1. Bind: `cd -- \"$LEGION_WORKSPACE\" && jj -R \"$LEGION_WORKSPACE\" status` — expect parent `plan: record handoff`, working copy holding only `A .omp/config.yml`. Read dispatch://LEGION-18 and this file. Do not run `jj new`, `jj describe`, or any rebase.",
    "2. Apply every edit in `fixes` above. Edit with the anchored quotes, not line numbers. Delete skills/github and skills/linear with `rm -r`. Leave skills/legion-worker/SKILL.md lines 228-231 (merger bullet), roles/merger.md, and the architect skill's line 184 untouched.",
    "3. Self-check before committing (all from `$LEGION_WORKSPACE`): (a) `grep -rn 'learnings-researcher\\|best-practices-researcher\\|Task Explore\\|LEGION_ISSUE_BACKEND\\|not yet runnable\\|PR B (#TBD)' skills/ packages/pi-envoy/roles/ AGENTS.md` prints nothing; (b) `grep -rn 'Post every correctness finding\\|no schema validation' skills/ packages/pi-envoy/roles/` prints nothing; (c) `grep -c \"Please don't do unnecessary rebases (i.e. unless there are merge conflicts). The CI queue is too long and slow.\" skills/legion-worker/SKILL.md` → 1 and `grep -c \"My rule is no deferrals.\" skills/legion-worker/SKILL.md` → 1; (d) `grep -n pr-queue skills/legion-worker/SKILL.md packages/pi-envoy/roles/merger.md skills/legion-architect/SKILL.md` still shows the same five lines as main; (e) `grep -c '^```' AGENTS.md` is even; (f) `jj -R \"$LEGION_WORKSPACE\" diff --summary` lists exactly: M AGENTS.md, M packages/pi-envoy/roles/planner.md, M packages/pi-envoy/roles/reviewer.md, M packages/pi-envoy/roles/tester.md, M skills/AGENTS.md, D skills/github/SKILL.md, D skills/linear/SKILL.md, M skills/legion-architect/SKILL.md, M skills/legion-controller/SKILL.md, M skills/legion-oracle/SKILL.md, M skills/legion-worker/SKILL.md, plus A .omp/config.yml which must stay uncommitted; (g) `bun test packages/pi-envoy` from the repo root is green.",
    "4. Commit by path with jj split, in this order, each from `$LEGION_WORKSPACE` (the remainder — .omp/config.yml — stays in the working copy): `jj -R \"$LEGION_WORKSPACE\" split -m 'docs(skills): legion-oracle names only agents and tools a Legion pane has' skills/legion-oracle/SKILL.md`; then `jj -R \"$LEGION_WORKSPACE\" split -m 'docs: delete the github and linear skills; root AGENTS.md names Dispatch and marks docs/plans as human-authored' skills/github skills/linear skills/AGENTS.md AGENTS.md`; then `jj -R \"$LEGION_WORKSPACE\" split -m 'docs(skills): drop the stale not-yet-runnable caveats from the architect and controller skills' skills/legion-architect/SKILL.md skills/legion-controller/SKILL.md`; then `jj -R \"$LEGION_WORKSPACE\" split -m 'docs(skills): worker contract fixes — handoff validation, one review per round, plan location, tester evidence, rebase and no-deferral rules' skills/legion-worker/SKILL.md packages/pi-envoy/roles/planner.md packages/pi-envoy/roles/reviewer.md packages/pi-envoy/roles/tester.md`. Then `jj -R \"$LEGION_WORKSPACE\" log -r 'ancestors(@, 8)'` must show those four commits on top of `plan: record handoff` on top of 7db2cd83, and `jj -R \"$LEGION_WORKSPACE\" diff -r @ --summary` must show only .omp/config.yml.",
    "5. Push: `jj -R \"$LEGION_WORKSPACE\" bookmark set legion/LEGION-18 -r @- && jj -R \"$LEGION_WORKSPACE\" git push --bookmark legion/LEGION-18` (`-r @-` so the bookmark never points at the working-copy commit holding .omp/config.yml).",
    "6. Open the PR: `legion gh -- pr create --repo sjawhar/legion --base main --head legion/LEGION-18 --title 'docs(skills): skill and role-prompt audit fixes (LEGION-18)' --body-file <file>`. The body must contain the line `Dispatch: LEGION-18` and the READY-format `## Verification` block from the worker skill: CI (fill after the first run), Threads `0 resolved, 0 unresolved`, no Thermo line (docs-only PR), E2E left for the tester, `Fast-follow: none`, `Chain: not stacked`. Text-only PR: do not add the Thermo line.",
    "7. Watch CI: `legion gh -- pr checks <n> --watch`. One failed job → `legion gh -- run rerun <run-id> --failed`. Check `legion gh -- pr view <n> --json mergeable`; rebase only on `CONFLICTING`.",
    "8. Handoff: `legion handoff write --phase implement` (stdin JSON: prNumber, prUrl, headSha, filesChanged, commits, selfChecks with outputs, deviationsFromPlan, testerNotes), `test -f .legion/implement.json`, `jj -R \"$LEGION_WORKSPACE\" split -m 'implement: record handoff' .legion/implement.json`, `jj -R \"$LEGION_WORKSPACE\" bookmark set legion/LEGION-18 -r @- && jj -R \"$LEGION_WORKSPACE\" git push --bookmark legion/LEGION-18`, then `legion handoff complete --summary '<two sentences>'`."
  ],
  "testerChecks": [
    {
      "acceptance": "grep for removed phrases → nothing",
      "check": "At the PR head (record `legion gh -- pr view <n> --json headRefOid --jq .headRefOid` and confirm it equals `jj -R \"$LEGION_WORKSPACE\" log -r @- --no-graph -T commit_id`): `cd -- \"$LEGION_WORKSPACE\" && grep -rn 'learnings-researcher\\|best-practices-researcher\\|Task Explore\\|LEGION_ISSUE_BACKEND\\|not yet runnable\\|PR B (#TBD)' skills/ packages/pi-envoy/roles/ AGENTS.md; echo exit=$?` → no lines, exit=1. Record output and head SHA in the PR body's E2E section.",
      "negativeControl": "Same patterns against main before the fix: `git -C \"$LEGION_WORKSPACE\" grep -n -e 'learnings-researcher' -e 'best-practices-researcher' -e 'Task Explore' -e 'LEGION_ISSUE_BACKEND' -e 'not yet runnable' -e 'PR B (#TBD)' origin/main -- skills packages/pi-envoy/roles AGENTS.md` → exactly 11 lines (planner-verified 2026-09-12 on main@origin e9b5f70a): skills/github/SKILL.md:3; skills/legion-architect/SKILL.md:15, 16; skills/legion-controller/SKILL.md:13, 14; skills/legion-oracle/SKILL.md:43, 44, 46, 59; skills/legion-worker/SKILL.md:15, 16. The spec originally said seven; the architect accepted 11."
    },
    {
      "acceptance": "skills/github/ and skills/linear/ gone; `bun test packages/pi-envoy` green",
      "check": "`cd -- \"$LEGION_WORKSPACE\" && test ! -e skills/github && test ! -e skills/linear && echo gone` → gone; `jj -R \"$LEGION_WORKSPACE\" file list skills/github skills/linear` → empty; `cd -- \"$LEGION_WORKSPACE\" && bun test packages/pi-envoy` → all pass (packages/pi-envoy/scripts/prepack.test.ts still finds legion-controller under skills/). Record the pass/fail counts."
    },
    {
      "acceptance": "Reviewer, planner, tester role prompts contain the new rules verbatim; legion-worker carries the two Sami quotes verbatim",
      "check": "From `$LEGION_WORKSPACE`, each command's expected count in brackets: `grep -c \"Please don't do unnecessary rebases (i.e. unless there are merge conflicts). The CI queue is too long and slow.\" skills/legion-worker/SKILL.md` [1]; `grep -c \"My rule is no deferrals.\" skills/legion-worker/SKILL.md` [1]; `grep -c 'one review per round' packages/pi-envoy/roles/reviewer.md skills/legion-worker/SKILL.md` [1 each]; `grep -c 'comments\\[\\]' packages/pi-envoy/roles/reviewer.md skills/legion-worker/SKILL.md` [≥1 each]; `grep -c 'never commit a plan or spec file to the repository' packages/pi-envoy/roles/planner.md skills/legion-worker/SKILL.md` [1 each]; `grep -c 'recorded once' packages/pi-envoy/roles/tester.md skills/legion-worker/SKILL.md` [1 each]; `grep -c 'If the deployment instructions name a librarian role, ask it first' skills/legion-oracle/SKILL.md` [1]; `grep -rn 'Post every correctness finding\\|no schema validation' skills/ packages/pi-envoy/roles/` [nothing]; `grep -c 'human-authored design history' AGENTS.md` [1]; `grep -c '^```' AGENTS.md` [even]."
    },
    {
      "acceptance": "The PR commits no docs/plans/* file",
      "check": "`legion gh -- pr view <n> --repo sjawhar/legion --json files --jq '.files[].path'` → no path starting `docs/`; the list is exactly AGENTS.md, packages/pi-envoy/roles/planner.md, packages/pi-envoy/roles/reviewer.md, packages/pi-envoy/roles/tester.md, skills/AGENTS.md, skills/github/SKILL.md, skills/linear/SKILL.md, skills/legion-architect/SKILL.md, skills/legion-controller/SKILL.md, skills/legion-oracle/SKILL.md, skills/legion-worker/SKILL.md, plus .legion/plan.json and .legion/implement.json (handoffs, deleted at the end of a clean review). Also `.omp/config.yml` must NOT be in that list."
    },
    {
      "acceptance": "Coordination rules held (not an Acceptance line, but a reviewer would check it)",
      "check": "`jj -R \"$LEGION_WORKSPACE\" diff --from 'main@origin' --to @- --git packages/pi-envoy/roles/merger.md` → empty; `jj -R \"$LEGION_WORKSPACE\" diff --from 'main@origin' --to @- --git skills/legion-worker/SKILL.md | grep -n 'pr-queue'` → no `-`/`+` line touching `pr-queue`; `grep -n 'pr-queue' skills/legion-architect/SKILL.md` → still line with `notifications.role.pr-queue`. `legion gh -- pr view <n> --json mergeable --jq .mergeable` → MERGEABLE (if CONFLICTING, that is the implementer's one permitted rebase reason — report to the architect)."
    }
  ],
  "testerNotes": "This is a docs-only PR: the E2E surface is the text itself as the acceptance greps define it, plus the pi-envoy test suite. Record environment evidence, if any, once in .legion/test.json per fix 7 — not in the PR body. Do not rebase or push text changes; a failed check is a finding for the architect.",
  "reviewerNotes": "Docs-only PR: skip the Thermo line entirely (worker skill). Apply the PR's own fix 5 to this review: one review submission per round via `legion gh -- api --method POST repos/sjawhar/legion/pulls/<n>/reviews --input body.json` with `event` in the body and all inline comments in one comments[] array. Verify the coordination rules above (merger bullet, merger.md, architect:184 untouched). On a clean review, the implementer pushes the .legion/ deletion and you approve that head by name.",
  "requiredSkills": {
    "implement": [
      "legion-worker — the phase contract this PR edits: workspace binding, path-scoped `jj split` commits, `legion/LEGION-18` bookmark set with `-r @-` and push, PR body in READY format carrying `Dispatch: LEGION-18`, handoff write/complete",
      "dispatch — the spec is the Dispatch issue document (dispatch_doc_read({ issue: \"LEGION-18\" })); any human question is a dispatch_ask on LEGION-18, never a transcript question"
    ],
    "test": [
      "legion-worker — fills the PR body's E2E section (command, observation, head SHA, negative control) and writes .legion/test.json; fix 7's rule (environment evidence once, in the handoff) applies to this very PR"
    ],
    "review": [
      "legion-worker — verifies CI/Threads/E2E against GitHub, skips Thermo on a docs-only PR, and submits one review per round with all inline comments in a single comments[] array (fix 5, which this PR introduces), then approves the .legion/-deletion head by name"
    ]
  },
  "taskCount": 9,
  "independentTasks": 9,
  "routingHints": {
    "complexity": "small",
    "estimatedImplementers": 1
  },
  "workflowRecommendation": "One implementer applies all nine fixes in one pass, commits by path in four jj split commits, opens one PR; tester runs the five checks above; reviewer applies fix 5 to its own review. No rebase unless GitHub reports CONFLICTING.",
  "concerns": [
    "Fix 5's reviewer bullet ends two lines above the merger bullet LEGION-16 rewrites (worker skill 226-227 unchanged between them); a 3-way merge should stay clean, but if GitHub reports CONFLICTING after LEGION-16 lands, that is the one permitted rebase — keep LEGION-16's READY wording.",
    "The acceptance grep also matches this branch's own .legion/*.json if they ever quote the removed phrases; the handoffs quote them (this file does) but sit outside the grep's paths (skills/, packages/pi-envoy/roles/, AGENTS.md), so they cannot cause a false hit.",
    "`.omp/config.yml` sits in the working copy as an added file; any whole-working-copy commit would leak it into the PR. Path-scoped `jj split` only."
  ],
  "schemaVersion": 1,
  "phase": "plan",
  "completed": "2026-09-12T05:23:52.642Z"
}