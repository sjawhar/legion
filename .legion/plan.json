{
  "taskCount": 7,
  "independentTasks": 2,
  "routingHints": {
    "complexity": "medium",
    "estimatedImplementers": 1
  },
  "workflowRecommendation": "One implementer, one PR (`legion/LEGION-34`), steps in the order below. Step 1 (code + unit test) and step 2 (skills, role prompts, docs) are independent and become two reviewable commits; step 3 (live proof on this PR) needs both. Skip formatters/linters/project-wide suites inside a step; at the end of each step run `cd packages/daemon && bunx biome check --error-on-warnings src/ && bunx tsc --noEmit` plus the named test file. Steps 4-6 are the tester's, reviewer's, and merger's duties on this PR and are written here because acceptance 2 is proven only in the review round.",
  "requiredSkills": {
    "implement": [
      "legion-worker",
      "test-driven-development",
      "verification-before-completion"
    ],
    "test": [
      "legion-worker",
      "verification-before-completion"
    ],
    "review": [
      "legion-worker",
      "requesting-code-review"
    ]
  },
  "spec": "dispatch://LEGION-34 primary document (Summary, Decisions needed: None, New since we talked, Acceptance 1-4, Requirements, Design, Errors, Testing, Rejected) as read on 2026-09-13 with dispatch_doc_read; the Design, Errors and Testing sections are the contract.",
  "goal": "A new CLI subcommand `legion threads resolve --pr <number> --repo <owner>/<name>` (beside `legion gh` in packages/daemon/src/cli/index.ts) redeems the caller's grant through the same grantFrom + /legion/v1/gh-token path `legion gh` uses, reads every review thread of the PR over GitHub GraphQL with an injected fetch, resolves (one resolveReviewThread per thread) every unresolved thread whose newest comment was written by the account that opened it and begins `Accepted:`, prints `resolved <url>` / `left open <url> — newest reply by <login> is not an acceptance`, exits 0 when every accepted thread resolved and 1 naming the thread URL and GitHub's message when a call fails. Skills, role prompts, the daemon AGENTS.md and the solutions doc say who resolves threads, when, and why the review App cannot.",
  "globalConstraints": [
    "Repository conventions (AGENTS.md): strict TS, Biome (double quotes, semicolons, 100 cols, `bunx biome check --error-on-warnings src/` in packages/daemon), `node:` prefixes, `type` imports, interfaces for object shapes, no barrel files, dependency injection for testability, co-located `__tests__/`, Bun test.",
    "jj not git. Commit with `cd -- \"$LEGION_WORKSPACE\" && jj -R \"$LEGION_WORKSPACE\" split -m '<msg>' <paths…>` (never a whole-working-copy commit: the daemon-provisioned, empty `.omp/config.yml` sits in the working copy and must never enter a commit). Before every push: `jj -R \"$LEGION_WORKSPACE\" bookmark set legion/LEGION-34 -r @- --allow-backwards && jj -R \"$LEGION_WORKSPACE\" git push --bookmark legion/LEGION-34` (after a split the bookmark sits on the empty working copy — docs/solutions/legion/worker-pane-shell-gotchas.md §3). Never `jj abandon`, `jj edit @-`, `jj op restore`, `jj undo`.",
    "PR body must contain `Dispatch: LEGION-34` and the READY-format Verification block from skills/legion-worker/SKILL.md. PR title (conventional, the squash subject the release workflow versions from): `feat(cli): legion threads resolve resolves the review threads the reviewer accepted (LEGION-34)`.",
    "Rebase only when GitHub reports CONFLICTING (Sami, 2026-09-11). CI (`.github/workflows/pr-and-main.yaml`) runs on pull_request opened/edited/reopened/synchronize and push to main — a push to `legion/LEGION-34` before the PR exists runs nothing, and every PR body edit re-runs the whole `Tests` workflow: edit the body once per round, after the push, never per fact.",
    "The grant is never typed, copied, or exported by hand: the extension prepends the real one to every bash command; a copied `export LEGION_GRANT=` line shadows it and every `legion …` call answers `Unable to redeem LEGION_GRANT (403)` (this planner reproduced it six times). If a command must be long, put it in a script file and run the file. Never handle a GitHub token: `legion gh -- …` and the credential helper supply identity per call.",
    "The pane's `legion` on PATH is the deployed daemon's build (main) and has no `threads` subcommand until this PR merges and the operator restarts the daemon. Every live run of the new command on this PR uses the branch CLI: `cd -- \"$LEGION_WORKSPACE\" && bun packages/daemon/src/cli/index.ts threads resolve --pr <n> --repo sjawhar/legion` (needs `bun install --frozen-lockfile` once in the workspace; done by this planner on 2026-09-13, `bun packages/daemon/src/cli/index.ts --help` prints the command list).",
    "Design gate is off on this deployment; do not open or wait on a gate ask. Only the operator edits ~/.omp/profiles/legion/plugins and the daemon's checkout.",
    "GitHub GraphQL facts verified live by this planner on 2026-09-13 (read-only, PRs #966 and #992): Bot authors' `login` is the App slug without `[bot]` (`legion-reviewer`, `legion-implementer`); a review thread has no URL of its own — the opening comment's `url` (`…/pull/<n>#discussion_r<id>`) is the anchor; `comments(first: 1)` and `comments(last: 1)` aliases on one thread work; `reviewThreads(first: 100, after: $after)` pages with `pageInfo { hasNextPage endCursor }`; the implement App sees `viewerCanResolve: true` on every unresolved thread of PR #992 (its own PR) and PR #992's 10 unresolved threads all have newest reply by `legion-implementer` (`Fixed in …`/`Done in …`) — exactly the `left open` shape."
  ],
  "verifiedStartingPoints": {
    "packages/daemon/src/cli/index.ts (682 lines at main 94146572)": "`type Fetch` 52; `GhCommandDeps` 53-58 (env, fetch, spawnGh, daemonUrl?); `daemonUrl(env, explicit?)` 81-85; `grantFrom(env)` 87-95 (reads LEGION_GRANT only; PR #992 rewrites it to read LEGION_GRANT_FILE first — keep calling `grantFrom(deps.env)`, never read the grant any other way); `cmdGh` 117-135 (redemption 121-132: POST `${daemonUrl}/legion/v1/gh-token` with `{grantId}`, `Unable to redeem LEGION_GRANT (<status>)` on !ok, `LegionDaemonApi.GitHubToken.response.safeParse`, `Daemon returned an invalid GitHub credential response`; spawn 133); `runCli` 369-380 (CliError → message on stderr, `process.exit(error.code)`); `ghCommand` 567-576; `mainCommand.subCommands` 665-680. `CliError(message, code = 1)` in cli/errors.ts.",
    "packages/daemon/src/cli/__tests__/": "index.test.ts (532 lines; 22 tests pass from this pane: `cd packages/daemon && bun test src/cli/__tests__/index.test.ts`) — `legion gh` tests 15-162 show the fake-fetch style (`new Request(String(input), init)`, `Response.json({ token: \"scoped-token\", appLogin: \"legion-implementer[bot]\" })`, `rejects.toEqual(expect.objectContaining({ message, code: 1 }))`). One test file per module is the convention (worker-shim.test.ts, legion-resolver.test.ts) — the new test goes in a new file, which also keeps this PR off PR #992's rewrite of index.test.ts's import block.",
    "packages/daemon/src/daemon/api/github.ts": "`appRoleForLegionRole(role)` 16-18: `reviewer` → `review`, everything else → `implement`; used by api/routes/credentials.ts 51 (git-credential) and 64 (gh-token) and api/routes/workers.ts 171 (worker git identity lease). config.ts 9: `GitHubAppRole = \"implement\" | \"review\"`; legion.yaml keys `github_apps.implement` / `github_apps.review`.",
    "packages/contracts/src/legion-daemon-api.ts 256-259": "`GitHubToken.response = z.object({ token: nonEmptyString, appLogin: z.string().endsWith(\"[bot]\") })`. No LegionDaemonApi shape changes in this PR: LEGION_DAEMON_API_VERSION and pi-envoy `legion.daemonApiVersion` stay as they are.",
    "PR #992 (LEGION-54, OPEN 2026-09-13, review round 2, CI green at fa521041e866)": "Touches cli/index.ts (`grantFrom` 87-95 → reads LEGION_GRANT_FILE via new `readSecretPointer`; cmdGh's spawn line 133 → three lines with `pathWithoutWorkerBin`), cli/__tests__/index.test.ts (import block + new `grant resolution` describe), skills/legion-worker/SKILL.md 139-151 (the `gh` facts paragraph), packages/daemon/src/daemon/AGENTS.md (Files-table rows ~40-46 and the pane-secret paragraph ~96-115), root AGENTS.md line 52, packages/pi-envoy/CHANGELOG.md line 11. This plan's edits are placed away from every one of those hunks except cmdGh's tail (see step 1 note).",
    "skills/legion-worker/SKILL.md (workspace copy, 364 lines)": "`## PR body and merge-queue discipline` 208-301: Verification template 213-231 (Threads lines 218-220); `Threads are dispositioned individually` bullet 233-235; reviewer bullet 272-285; merger bullet 288-291. The installed skill the panes load is older than this copy (it lacks the production-testing paragraphs) — edit the workspace copy only.",
    "skills/legion-architect/SKILL.md 199-217": "`## 6. Architect sign-off and merge` order list; line 216-217 `If anything else changes the head, return to review; …` — the new paragraph goes after it, before `## 7. Close` (219).",
    "packages/pi-envoy/roles/": "reviewer.md (94 lines; review paragraph 11-25, final gate 53-61), implementer.md (60 lines; paragraph 11-19), merger.md (67 lines; Verification list 30-48, step 3 is the READY publish). The daemon appends these from its own checkout (`EXTENSION_PACKAGE/roles/<role>.md`, processes.ts 2516), so a daemon restart on the merged main deploys them; skills ship in the plugin's `dist/skills` (packages/pi-envoy/package.json `omp.skills`), so the skill change reaches panes only after the operator installs the released plugin.",
    "packages/daemon/src/daemon/AGENTS.md (159 lines)": "`## Operational invariants` 47-68, blank 69, `## OMP invocation and daemon tools` 70. The new `## GitHub Apps` section is inserted between 68 and 70.",
    "docs/solutions/daemon/external-red-and-phase-ownership.md": "Section 3 is lines 74-90 (`## 3. The review App can neither push nor resolve threads; the implementer App does both` through the `Thread ids are the PRRT_…` paragraph). Replace the whole section.",
    "AGENTS.md (root) 41-55": "Commands block; `legion gh -- <args>` is line 48. Insert the new line after it (lines 49-51 stay between it and PR #992's edit of line 52).",
    "packages/pi-envoy/CHANGELOG.md 3-7": "`## [Unreleased]` / `### Added` with one bullet at line 7; add the new bullet after it.",
    "Branch protection of sjawhar/legion main (acceptance 3)": "`legion gh -- api repos/sjawhar/legion/branches/main/protection` → 403 `Resource not accessible by integration` (the implement App cannot read legacy protection). `legion gh -- api repos/sjawhar/legion/rules/branches/main` → ruleset 12331919 (Repository): `deletion`, `non_fast_forward`, `required_linear_history`, `pull_request` {required_approving_review_count: 1, dismiss_stale_reviews_on_push: false, require_code_owner_review: false, require_last_push_approval: false, **required_review_thread_resolution: false**, require_extra_approval_for_unattributed_changes: true, allowed_merge_methods: [squash]}, `required_status_checks` {lint, typecheck, test — integration 15368; strict: false}. Conversation resolution is NOT required before merge on this repository today; reported to the architect for the Requirements table.",
    "Planner's throwaway check (2026-09-13)": "The exact files in `code` were dropped into this workspace, `bun test src/cli/__tests__/review-threads.test.ts src/cli/__tests__/index.test.ts` → 25 pass / 0 fail, `bunx tsc --noEmit` clean, `bunx biome check --error-on-warnings src/cli/` clean after `--write` (import order + test formatting), then removed again before the handoff commit. Nothing of it is committed; the implementer re-creates it in step 1 (TDD order)."
  },
  "deployment": {
    "afterMerge": [
      "The release workflow publishes a new `cli-v*` (packages/daemon changed) and a new `pi-legion-envoy` (roles/skills changed) from the squash title.",
      "Operator: install the released plugin into `~/.omp/profiles/legion/plugins` (skills ship in `dist/skills`) and restart the daemon on the merged main (`legion restart sjawhar-legion`: the CLI launcher `<state_dir>/bin/legion` re-execs the daemon's own entry, and role prompts are read from the daemon checkout's packages/pi-envoy/roles). Live panes keep the old CLI/skill until relaunched; nothing breaks in the window — the new command is additive.",
      "Sign-off (implementer, per the worker skill's production-verification rule): the first review round on any later issue — the implementer runs `legion threads resolve` from the pane's own `legion` (no `bun …` prefix) and quotes the output on that PR."
    ]
  },
  "steps": [
    {
      "n": 0,
      "title": "Bind, install, and decide the base once (no CI cost)",
      "change": [
        "0a. `cd -- \"$LEGION_WORKSPACE\" && jj -R \"$LEGION_WORKSPACE\" status` — expect `A .omp/config.yml` in the working copy on top of the planner's `plan: record handoff` commit, bookmark `legion/LEGION-34` (local). `bun install --frozen-lockfile` (fast; the planner already populated the cache).",
        "0b. `jj -R \"$LEGION_WORKSPACE\" git fetch` then check whether PR #992 (LEGION-54) has merged: `jj -R \"$LEGION_WORKSPACE\" file show -r main@origin packages/daemon/src/cli/index.ts | grep -c readSecretPointer`. If ≥1 (merged) and nothing of this issue has been pushed yet, move the unpushed chain onto it once before any code: `jj -R \"$LEGION_WORKSPACE\" rebase -s <plan handoff change id> -d main@origin` — this is not a CI-costing rebase (no PR exists yet) and removes every conflict below. If 0 (still open), proceed on the current base and expect one `CONFLICTING` report if #992 lands first; the recorded resolution is in step 1's notes.",
        "0c. Read `.legion/plan.json` (this file) and the spec (`dispatch_doc_read({issue: \"LEGION-34\"})`) before touching code."
      ],
      "tests": [
        "`bun packages/daemon/src/cli/index.ts --help` lists the existing commands (no `threads` yet)."
      ],
      "acceptance": []
    },
    {
      "n": 1,
      "title": "CLI: `legion threads resolve` with its unit test (TDD)",
      "files": {
        "create": [
          "packages/daemon/src/cli/review-threads.ts",
          "packages/daemon/src/cli/__tests__/review-threads.test.ts"
        ],
        "modify": [
          "packages/daemon/src/cli/index.ts"
        ]
      },
      "interfaces": {
        "produces": [
          "review-threads.ts: `export type Fetch`; `export const GITHUB_GRAPHQL_URL = \"https://api.github.com/graphql\"`; `export type GraphqlCall = <T>(query: string, variables: Record<string, unknown>) => Promise<T>`; `export interface GitHubRepo { owner; name }`; `export interface UnresolvedThread { id; url; openerLogin: string | null; newestLogin: string | null; newestBody }`; `export function githubGraphql(fetch: Fetch, token: string): GraphqlCall` (POST with `authorization: Bearer <token>`; !ok → CliError `GitHub GraphQL request failed (<status>): <body>`; `errors[]` → CliError of the joined messages); `export function isAcceptance(body): boolean` (`body.trimStart().startsWith(\"Accepted:\")`); `export function acceptedByOpener(thread): boolean`; `export async function listUnresolvedThreads(graphql, repo, number): Promise<UnresolvedThread[]>` (paginates `reviewThreads(first: 100, after)`, skips `isResolved`); `export async function resolveThread(graphql, threadId): Promise<void>`.",
          "index.ts: private `redeemGitHubToken(deps: GrantRedemptionDeps): Promise<string>` (the body lifted verbatim from cmdGh 121-132; cmdGh now calls it); `export async function cmdThreadsResolve(options: { repo: string; pr: string }, deps: ThreadsResolveCommandDeps): Promise<void>` where `ThreadsResolveCommandDeps = { env; fetch; daemonUrl?; log(line: string): void }`; citty `threads` command with `resolve` subcommand (`--pr <string, required>`, `--repo <string, required>`), registered as `threads: threadsCommand` in `mainCommand.subCommands` after `gh`.",
          "Output contract (verbatim, one line per unresolved thread found, in GitHub's order): `resolved <url>`; `left open <url> — newest reply by <login> is not an acceptance`; when no unresolved thread exists exactly `no unresolved threads`. Failure: CliError `resolveReviewThread failed for <url>: <GitHub message>` (exit 1, nothing after it attempted); grant failure `Unable to redeem LEGION_GRANT (<status>)`; a `left open` thread never fails the run."
        ]
      },
      "change": [
        "1a. Write the failing test first: create `packages/daemon/src/cli/__tests__/review-threads.test.ts` with the content in `code.reviewThreadsTest` below. Run `cd packages/daemon && bun test src/cli/__tests__/review-threads.test.ts` — expect a failure because `cmdThreadsResolve` is not exported from `../index`.",
        "1b. Create `packages/daemon/src/cli/review-threads.ts` with the content in `code.reviewThreadsTs` (verbatim; the GraphQL query and mutation strings were exercised live by the planner — see globalConstraints).",
        "1c. Edit `packages/daemon/src/cli/index.ts` per `code.indexTsAdditions`: (i) delete the local `type Fetch = …` (line 52) and import `type Fetch` with the helpers from `./review-threads`; (ii) add `GrantRedemptionDeps` and `ThreadsResolveCommandDeps`; (iii) add `redeemGitHubToken` above `cmdGh` and make `cmdGh` call it (its own lines 121-132 go away; the spawn line becomes `deps.spawnGh(args, buildGitHubTokenEnv(token, deps.env))`); (iv) add `parseRepo`, `parsePullNumber`, `cmdThreadsResolve`; (v) add `threadsResolveCommand` + `threadsCommand` beside `ghCommand` and register `threads: threadsCommand` in `mainCommand`. `GhCommandDeps` already satisfies `GrantRedemptionDeps` structurally — no change to it.",
        "1d. Run the new test file — 3 pass. Run `bun test src/cli/__tests__/index.test.ts` — the 22 existing `legion gh`/handoff/probe tests still pass (the extraction must not change cmdGh's observable behaviour: same request body, same error strings, same child env).",
        "1d-bis. Formatting: the blocks under `code.indexTsAsFormatted_*` and `code.reviewThreadsTest` are exactly what `bunx biome check --write src/cli/` produced on this planner's throwaway drop-in (25 tests green, tsc clean, biome clean on 2026-09-13); paste them and biome has nothing to fix.",
        "1e. `cd packages/daemon && bunx biome check --error-on-warnings src/ && bunx tsc --noEmit` clean. `bun src/cli/index.ts threads resolve --help` prints the two required flags; `bun src/cli/index.ts threads resolve --pr 1 --repo nope` fails `--repo must be <owner>/<name>`.",
        "1f. Commit: `cd -- \"$LEGION_WORKSPACE\" && jj -R \"$LEGION_WORKSPACE\" split -m 'feat(cli): legion threads resolve resolves the review threads the reviewer accepted' packages/daemon/src/cli/review-threads.ts packages/daemon/src/cli/index.ts packages/daemon/src/cli/__tests__/review-threads.test.ts`."
      ],
      "tests": [
        "packages/daemon/src/cli/__tests__/review-threads.test.ts (new, 3 tests; fails on main because the export does not exist): (1) six threads over two pages — accepted by opener (resolved), `Still open:` by opener (left open), `Accepted:` by another login (left open), an already-resolved thread (no line, no mutation), `Accepted:` after leading whitespace (resolved), `Accepted (round 2):` — the PR #966 wording — (left open): exactly `[\"T1\",\"T5\"]` mutated, the five output lines verbatim, the grant redeemed once at `/legion/v1/gh-token` with `{grantId}`, every GraphQL request to `https://api.github.com/graphql` with `authorization: Bearer scoped-token`, the second page requested with `after: \"CURSOR\"`; (2) a FORBIDDEN `errors[]` on the first mutation → rejects `resolveReviewThread failed for <url1>: Resource not accessible by integration` with code 1, only one mutation attempted, no `resolved` line printed; (3) `/gh-token` 403 → rejects `Unable to redeem LEGION_GRANT (403)` with no GraphQL request. This is acceptance 1's check.",
        "packages/daemon/src/cli/__tests__/index.test.ts unchanged and still green (regression lock on the `redeemGitHubToken` extraction)."
      ],
      "acceptance": [
        "1"
      ],
      "notes": [
        "Why the acceptance rule compares logins from the same query and not `[bot]` names: GraphQL reports Bot actors as the bare App slug (`legion-reviewer`), while `/gh-token`'s `appLogin` is `legion-implementer[bot]`. Never compare a GraphQL login against `appLogin`.",
        "Why `comments(first: 1)` + `comments(last: 1)` instead of paginating comments: the rule reads only the opener (author + URL) and the newest comment (author + body); two one-element connections read exactly that with no second pagination loop.",
        "PR #992 conflict, if it lands first and GitHub reports CONFLICTING: the only code overlap is cmdGh's tail. Resolve to: `const token = await redeemGitHubToken(deps); const childEnv = buildGitHubTokenEnv(token, deps.env); // Never the pane's own gh shim … if (childEnv.PATH !== undefined) childEnv.PATH = pathWithoutWorkerBin(childEnv.PATH); const exitCode = await deps.spawnGh(args, childEnv);` keeping #992's import of `pathWithoutWorkerBin` and its `grantFrom`/`readSecretPointer` untouched. `redeemGitHubToken` keeps calling `grantFrom(deps.env)`, so the LEGION_GRANT_FILE path lands in this command for free.",
        "`--repo` is required: nothing in a pane's environment names the repository (the daemon exports LEGION_TREE/ISSUE/ROLE/PROJECT/WORKSPACE, not the repo), the daemon's `/state` response carries no repo and any new field there is a daemon-API bump, and `gh` itself takes `--repo` in every skill example. Deviation from the architect's `--pr <number>`-only sketch; reported in the planner's completion summary.",
        "Bun's fetch sends a `User-Agent` by default; GitHub rejects UA-less requests, so do not strip headers when constructing the request."
      ]
    },
    {
      "n": 2,
      "title": "Skills, role prompts, and documentation: who resolves threads, when, and why the review App cannot",
      "files": {
        "modify": [
          "skills/legion-worker/SKILL.md",
          "skills/legion-architect/SKILL.md",
          "packages/pi-envoy/roles/reviewer.md",
          "packages/pi-envoy/roles/implementer.md",
          "packages/pi-envoy/roles/merger.md",
          "packages/daemon/src/daemon/AGENTS.md",
          "AGENTS.md",
          "docs/solutions/daemon/external-red-and-phase-ownership.md",
          "packages/pi-envoy/CHANGELOG.md"
        ]
      },
      "change": [
        "2a. skills/legion-worker/SKILL.md: (i) replace the three `**Threads:**` template lines 218-220 with `docs.workerSkillTemplateThreads`; (ii) replace the `Threads are dispositioned individually` bullet (233-235) with `docs.workerSkillThreadsBullet`; (iii) append `docs.workerSkillReviewerAddition` to the reviewer bullet (after `and approve it by name.` at 285, same indentation); (iv) replace the merger bullet (288-291) with `docs.workerSkillMergerBullet`. Nothing else in the file changes (PR #992 edits lines 139-151).",
        "2b. skills/legion-architect/SKILL.md: insert `docs.architectSkillSentence` as a new paragraph after line 217 (`for an obsolete approval.`), before `## 7. Close`.",
        "2c. packages/pi-envoy/roles/reviewer.md: insert `docs.reviewerRoleAddition` as a new paragraph after line 31 (the closing fence of the JSON example), before `Bot Minors are not a gate.`; in the final gate paragraph (lines 57-59) replace the words 'confirm it differs from the reviewed head only by that deletion, and approve it by name' with 'confirm it differs from the reviewed head only by that deletion and that every thread you accepted shows isResolved: true in gh api graphql (the implementer's legion threads resolve output sits in the PR body's Threads section; verify against GitHub, not the body), and approve it by name' (format the command names as inline code).",
        "2d. packages/pi-envoy/roles/implementer.md: insert `docs.implementerRoleAddition` as a new paragraph after line 19 (`before reporting it.`), before `## Shared workspace and credentials`.",
        "2e. packages/pi-envoy/roles/merger.md: insert `docs.mergerRoleStep` as new list item 3 after item 2 (ends line 40, 'not by you.') and renumber the READY publish item to 4; in the first paragraph (lines 11-15) replace 'Confirm the approved head equals the current head; publish' with 'Run legion threads resolve --pr <n> --repo <owner>/<repo> (step 3 below), confirm the approved head equals the current head; publish' (command as inline code).",
        "2f. packages/daemon/src/daemon/AGENTS.md: insert `docs.daemonAgentsSection` between line 68 (last Operational-invariants bullet) and `## OMP invocation and daemon tools` (70), keeping one blank line on each side.",
        "2g. AGENTS.md (root): insert `docs.rootAgentsLine` after line 48 (`legion gh -- <args> …`).",
        "2h. docs/solutions/daemon/external-red-and-phase-ownership.md: replace lines 74-90 (all of section 3) with `docs.solutionsSection3`; in the frontmatter `related_issues` add `- \"LEGION-34\"` under `- \"sjawhar/legion#966\"`.",
        "2i. packages/pi-envoy/CHANGELOG.md: add `docs.changelogLine` after line 7 under `## [Unreleased]` / `### Added`.",
        "2j. Read every edited file once end to end for a person on a phone: no line numbers, no jargon the reader has not met. Commit: `jj -R \"$LEGION_WORKSPACE\" split -m 'docs(legion): who resolves review threads and why the review App cannot' skills/legion-worker/SKILL.md skills/legion-architect/SKILL.md packages/pi-envoy/roles/reviewer.md packages/pi-envoy/roles/implementer.md packages/pi-envoy/roles/merger.md packages/daemon/src/daemon/AGENTS.md AGENTS.md docs/solutions/daemon/external-red-and-phase-ownership.md packages/pi-envoy/CHANGELOG.md`."
      ],
      "tests": [
        "No unit tests: the reviewer reads the diff against acceptance 4 (worker skill implementer/reviewer/merger sections and the daemon AGENTS.md App section say who resolves threads and why the reviewer cannot, and that the reviewer App cannot push the `.legion/` cleanup commit). `cd packages/pi-envoy && bun run build` must still succeed (skills are copied into `dist/skills`)."
      ],
      "acceptance": [
        "4"
      ],
      "notes": [
        "The architect-skill paragraph is one file beyond the spec's Design list; it closes the spec's Errors row (`the architect opens an action ask for a human to resolve that thread`) at the place the architect reads. Reported in the planner's completion summary.",
        "Deployment-instruction rule for this box: Sami does not answer asks — but a thread GitHub refuses the App is exactly the case only a human account can act on, and the spec names an action ask (`kind: \"action\"`, fixed Done/Can't) for it."
      ]
    },
    {
      "n": 3,
      "title": "Implementer live proof on this PR, then the PR body",
      "files": {
        "modify": [
          "(PR body only)"
        ]
      },
      "change": [
        "3a. Push and open the PR: `jj -R \"$LEGION_WORKSPACE\" log -r 'ancestors(@, 5)'` (only this issue's commits above main), `jj -R \"$LEGION_WORKSPACE\" bookmark set legion/LEGION-34 -r @- --allow-backwards && jj -R \"$LEGION_WORKSPACE\" git push --bookmark legion/LEGION-34`, then `legion gh -- pr create --repo sjawhar/legion --base main --head legion/LEGION-34 --title '<title from globalConstraints>' --body-file <file>` with the READY-format body containing `Dispatch: LEGION-34`. Record the PR number `<n>` and head sha.",
        "3b. First live run, empty case: `cd -- \"$LEGION_WORKSPACE\" && bun packages/daemon/src/cli/index.ts threads resolve --pr <n> --repo sjawhar/legion` → exactly `no unresolved threads`, exit 0. This proves the real grant → daemon `/gh-token` → implement-App token → GitHub GraphQL read path from a real pane.",
        "3c. Live positive and negative control on real threads you open yourself (you are the implement App; the PR is yours): create two review threads with `legion gh -- api --method POST repos/sjawhar/legion/pulls/<n>/comments -f commit_id=<head-sha> -f path=packages/daemon/src/cli/review-threads.ts -F line=1 -f side=RIGHT -f body=$'Implementer live check A: this thread is accepted by its opener and must be resolved by legion threads resolve.\\n\\n<!-- legion: {\"session\":\"<session-id>\",\"phase\":\"implement\"} -->'` and the same with `line=2` and body `Implementer live check B: this thread stays open (negative control).`; note each response's `id`. Reply on A: `legion gh -- api --method POST repos/sjawhar/legion/pulls/<n>/comments/<A id>/replies -f body=$'Accepted: not a defect — live check of legion threads resolve.\\n\\n<!-- legion: … -->'`; reply on B with `Still open: negative control for legion threads resolve.` plus the footer.",
        "3d. Run the command again → exactly two lines: `resolved https://github.com/sjawhar/legion/pull/<n>#discussion_r<A id>` and `left open https://github.com/sjawhar/legion/pull/<n>#discussion_r<B id> — newest reply by legion-implementer is not an acceptance`; exit 0. Verify against GitHub, not the output: `legion gh -- api graphql -F owner=sjawhar -F name=legion -F number=<n> -f query='query($owner:String!,$name:String!,$number:Int!){repository(owner:$owner,name:$name){pullRequest(number:$number){reviewThreads(first:100){nodes{id isResolved comments(first:1){nodes{url}}}}}}}'` shows A `isResolved: true`, B `false`.",
        "3e. Close the control: reply on B `Accepted: not a defect — negative control complete.` (with footer), run the command a third time → `resolved …r<B id>`; the graphql query now shows both true; a fourth run prints `no unresolved threads`.",
        "3f. PR body (one edit, after all pushes of this round — every edit re-runs CI): `Threads` section lists A and B as `not a defect — implementer live check` with the pasted output of runs 3d and 3e; `E2E` section (the tester overwrites it later): surface = the branch CLI from a real worker pane against the real daemon and GitHub, the four commands and their outputs, head sha, negative control = thread B's `left open` line plus GitHub's `isResolved: false`; `Thermo` line left for the reviewer; `Fast-follow: none`; `Chain: not stacked` (or the #992 note from step 0b).",
        "3g. Handoff: `legion handoff write --phase implement --data '<JSON>'` naming the PR, the head, the four live outputs, the files changed, and any deviation (the `--repo` flag; the architect-skill paragraph); `test -f \"$LEGION_WORKSPACE/.legion/implement.json\"`; `jj -R \"$LEGION_WORKSPACE\" split -m 'implement: record handoff' .legion/implement.json`; bookmark set `-r @- --allow-backwards`; push; `legion handoff complete --summary '…'`."
      ],
      "tests": [
        "The three live runs (empty → A resolved/B left open → B resolved) and the two graphql confirmations are the implementer's production-like proof, recorded in the PR body and `.legion/implement.json`."
      ],
      "acceptance": [
        "1",
        "2 (partially: implement-App-opened threads; the review-App-opened case is step 5)"
      ],
      "notes": [
        "The threads you open are real review comments on your own PR; the reviewer must see them dispositioned in the Threads section and resolved on GitHub, which 3e guarantees.",
        "Never run the command against another issue's PR (#992 today has 10 open threads): if its reviewer has replied `Accepted:` by then, your run would resolve threads on a PR that is not yours."
      ]
    },
    {
      "n": 4,
      "title": "Tester: acceptance 1 at the head, plus the live negative control before any reviewer acceptance",
      "files": {
        "modify": [
          "(PR body E2E section; .legion/test.json)"
        ]
      },
      "change": [
        "4a. At the PR head: `cd -- \"$LEGION_WORKSPACE/packages/daemon\" && bun test src/cli/__tests__/review-threads.test.ts src/cli/__tests__/index.test.ts` — 25 pass; `bunx biome check --error-on-warnings src/ && bunx tsc --noEmit` clean. Quote the pass counts and the head sha. This is the regression lock for acceptance 1, not its proof.",
        "4b. Live negative control (you also run as the implement App): open one review thread on the PR as in step 3c (`Tester negative control: this thread must be reported left open.` with the test footer), reply on it `Still open: tester negative control.`; run `cd -- \"$LEGION_WORKSPACE\" && bun packages/daemon/src/cli/index.ts threads resolve --pr <n> --repo sjawhar/legion` → exactly one line `left open https://github.com/sjawhar/legion/pull/<n>#discussion_r<id> — newest reply by legion-implementer is not an acceptance`, exit 0; the step-3d graphql query shows that thread `isResolved: false` and no other thread changed. Every unresolved thread on the PR at that moment is reported `left open` — that is the architect's negative control.",
        "4c. Live positive (same thread): reply `Accepted: not a defect — tester negative control complete.`; run again → `resolved …r<id>`; graphql shows `isResolved: true`. A third run → `no unresolved threads`.",
        "4d. Malformed input: `bun packages/daemon/src/cli/index.ts threads resolve --pr <n> --repo sjawhar` → `--repo must be <owner>/<name> (got \"sjawhar\")`, exit 1, and no grant redeemed (nothing in the daemon log for that second).",
        "4e. Write the PR body's `E2E` section (one edit): surface = branch CLI from the tester pane against the real daemon and GitHub; the commands of 4b-4d with outputs; head sha; negative control = 4b's `left open` line + `isResolved: false`. `.legion/test.json` records the same plus the unit-test counts; commit `test: record handoff`, bookmark set `-r @- --allow-backwards`, push, `legion handoff complete`.",
        "4f. If a corrective round follows the review (step 5), test again at the new head: at that point the reviewer's threads exist with the implementer's replies and no acceptance yet, so 4b's run needs no thread of your own — every reviewer thread is reported `left open`; record that output as the negative control instead."
      ],
      "tests": [
        "Unit file green at the head; live `left open` on a real thread with GitHub confirming `isResolved: false`; live `resolved` with GitHub confirming `true`; malformed `--repo` refused before any credential is redeemed."
      ],
      "acceptance": [
        "1"
      ],
      "notes": [
        "The tester's thread is one more real thread on the PR; 4c leaves it resolved and the Threads section must list it (`not a defect — tester negative control`)."
      ]
    },
    {
      "n": 5,
      "title": "Review round proves acceptance 2 (review-App-opened thread, accepted, resolved by the implementer App)",
      "files": {
        "modify": [
          "(GitHub review, PR body Threads section, .legion/review.json)"
        ]
      },
      "change": [
        "5a. Reviewer, first review: verify CI, Threads, and E2E facts against GitHub; run the two thermo agents once at the head; submit ONE review (`COMMENT`, or `REQUEST_CHANGES` if a correctness finding stands) through `legion gh -- api --method POST repos/sjawhar/legion/pulls/<n>/reviews --input body.json` carrying **at least one inline `comments[]` entry** — a real finding if there is one, otherwise the acceptance-2 probe on `packages/daemon/src/cli/review-threads.ts` (`Reviewer thread for acceptance 2: confirm isAcceptance rejects `Accepted (round 2):` — see the unit test.`) with the review footer. Return the issue to the architect.",
        "5b. Implementer, corrective push (if any) — before the push run `bun packages/daemon/src/cli/index.ts threads resolve --pr <n> --repo sjawhar/legion`: the reviewer's thread(s) print `left open … — newest reply by legion-implementer is not an acceptance` (you answered them, the reviewer has not accepted yet); paste into Threads; push. Tester re-tests (step 4f).",
        "5c. Reviewer, re-review: reply on each thread you opened with `Accepted: fixed in <commit> — <one line>` / `Accepted: not a defect — <reason>` / `Still open: <what remains>` (for the acceptance-2 probe: `Accepted: not a defect — isAcceptance is exact; see the unit test.`); reply nothing further. Report to the architect that the review is clean and the `.legion/` deletion is the only work left.",
        "5d. Implementer, the `.legion/` deletion push — before it: run the command; it prints `resolved <reviewer thread url>` for every thread the reviewer accepted (this is the cross-App case: the review App opened it, the implement App resolves it); paste into Threads; then `jj -R \"$LEGION_WORKSPACE\" file untrack`/delete `.legion/` per the skill, commit, push.",
        "5e. Reviewer, approval of that head by sha: `gh api graphql` (the step-3d query) shows `isResolved: true` for every thread you opened — quote that JSON fragment in the approval body. That quote is acceptance 2's recorded check."
      ],
      "tests": [
        "The reviewer's approval body quotes `isResolved: true` for a thread the review App opened and the implementer App resolved."
      ],
      "acceptance": [
        "2"
      ],
      "notes": [
        "Exactly one review submission per round; the acceptance-2 probe thread is a `COMMENT`-class finding and must not turn a clean review into `REQUEST_CHANGES` on its own."
      ]
    },
    {
      "n": 6,
      "title": "Merger: one more run, then READY",
      "files": {
        "modify": [
          "(none)"
        ]
      },
      "change": [
        "6a. `bun packages/daemon/src/cli/index.ts threads resolve --pr <n> --repo sjawhar/legion` from the merger pane → `no unresolved threads` (every thread was resolved in step 5). Any `left open` line or exit 1: do not publish; report to the architect.",
        "6b. Confirm the current head is the reviewer-approved head plus only `docs/solutions/` retro commits (`jj diff --from <approved head> --to <tip> --summary`), then publish `READY #<n> at <sha>` with the PR body's gate facts, the tier (T3 follow-up: a new CLI command plus documentation; nothing broken in production), and the link to the E2E evidence, to `notifications.role.pr-queue`."
      ],
      "tests": [
        "The READY packet quotes the command's `no unresolved threads` output at the published head."
      ],
      "acceptance": [
        "2"
      ]
    }
  ],
  "code": {
    "reviewThreadsTs": "import { CliError } from \"./errors\";\n\nexport type Fetch = (input: string | URL | Request, init?: RequestInit) => Promise<Response>;\n\nexport const GITHUB_GRAPHQL_URL = \"https://api.github.com/graphql\";\n\n/** One GraphQL call against GitHub as the App whose token was redeemed. Rejects with a CliError\n * carrying GitHub's own message (HTTP status + body for a transport failure, the `errors[]`\n * messages for a GraphQL-level refusal such as `Resource not accessible by integration`). */\nexport type GraphqlCall = <T>(query: string, variables: Record<string, unknown>) => Promise<T>;\n\nexport interface GitHubRepo {\n  owner: string;\n  name: string;\n}\n\n/** An unresolved review thread reduced to what the acceptance rule reads. A thread has no URL of\n * its own on GitHub; `url` is its opening comment's, the anchor the PR page scrolls to. */\nexport interface UnresolvedThread {\n  id: string;\n  url: string;\n  openerLogin: string | null;\n  newestLogin: string | null;\n  newestBody: string;\n}\n\ninterface Actor {\n  login: string;\n}\n\ninterface ThreadsPage {\n  repository: {\n    pullRequest: {\n      reviewThreads: {\n        pageInfo: { hasNextPage: boolean; endCursor: string | null };\n        nodes: Array<{\n          id: string;\n          isResolved: boolean;\n          opener: { nodes: Array<{ url: string; author: Actor | null }> };\n          newest: { nodes: Array<{ author: Actor | null; body: string }> };\n        }>;\n      };\n    } | null;\n  } | null;\n}\n\nconst THREADS_QUERY = `query($owner: String!, $name: String!, $number: Int!, $after: String) {\n  repository(owner: $owner, name: $name) {\n    pullRequest(number: $number) {\n      reviewThreads(first: 100, after: $after) {\n        pageInfo { hasNextPage endCursor }\n        nodes {\n          id\n          isResolved\n          opener: comments(first: 1) { nodes { url author { login } } }\n          newest: comments(last: 1) { nodes { author { login } body } }\n        }\n      }\n    }\n  }\n}`;\n\nconst RESOLVE_MUTATION = `mutation($threadId: ID!) {\n  resolveReviewThread(input: { threadId: $threadId }) { thread { id isResolved } }\n}`;\n\nexport function githubGraphql(fetch: Fetch, token: string): GraphqlCall {\n  return async <T>(query: string, variables: Record<string, unknown>): Promise<T> => {\n    const response = await fetch(GITHUB_GRAPHQL_URL, {\n      method: \"POST\",\n      headers: { authorization: `Bearer ${token}`, \"content-type\": \"application/json\" },\n      body: JSON.stringify({ query, variables }),\n    });\n    if (!response.ok) {\n      throw new CliError(\n        `GitHub GraphQL request failed (${response.status}): ${await response.text()}`\n      );\n    }\n    const payload = (await response.json()) as { data?: T; errors?: Array<{ message: string }> };\n    if (payload.errors && payload.errors.length > 0) {\n      throw new CliError(payload.errors.map((error) => error.message).join(\"; \"));\n    }\n    if (payload.data === undefined) throw new CliError(\"GitHub GraphQL response carried no data\");\n    return payload.data;\n  };\n}\n\n/** The reviewer's acceptance reply form: the comment's first non-blank line begins `Accepted:`\n * (`Accepted: fixed in <commit> — <one line>` or `Accepted: not a defect — <reason>`). Anything\n * else — `Still open: …`, `Accepted (round 2): …`, a bare \"fixed\" — is not an acceptance. */\nexport function isAcceptance(body: string): boolean {\n  return body.trimStart().startsWith(\"Accepted:\");\n}\n\n/** True when the thread's newest comment is its opener's own `Accepted:` reply: the account that\n * raised the point is the one closing it, and nobody has replied since. */\nexport function acceptedByOpener(thread: UnresolvedThread): boolean {\n  return (\n    thread.openerLogin !== null &&\n    thread.openerLogin === thread.newestLogin &&\n    isAcceptance(thread.newestBody)\n  );\n}\n\n/** Every unresolved review thread on the pull request, across every page of `reviewThreads`. */\nexport async function listUnresolvedThreads(\n  graphql: GraphqlCall,\n  repo: GitHubRepo,\n  number: number\n): Promise<UnresolvedThread[]> {\n  const threads: UnresolvedThread[] = [];\n  let after: string | null = null;\n  do {\n    const page: ThreadsPage = await graphql<ThreadsPage>(THREADS_QUERY, {\n      owner: repo.owner,\n      name: repo.name,\n      number,\n      after,\n    });\n    const pullRequest = page.repository?.pullRequest;\n    if (!pullRequest) {\n      throw new CliError(`${repo.owner}/${repo.name}#${number} was not found by GitHub`);\n    }\n    for (const node of pullRequest.reviewThreads.nodes) {\n      if (node.isResolved) continue;\n      const opener = node.opener.nodes[0];\n      const newest = node.newest.nodes[0];\n      if (!opener || !newest) throw new CliError(`review thread ${node.id} has no comments`);\n      threads.push({\n        id: node.id,\n        url: opener.url,\n        openerLogin: opener.author?.login ?? null,\n        newestLogin: newest.author?.login ?? null,\n        newestBody: newest.body,\n      });\n    }\n    const { pageInfo } = pullRequest.reviewThreads;\n    after = pageInfo.hasNextPage ? pageInfo.endCursor : null;\n  } while (after !== null);\n  return threads;\n}\n\n/** One `resolveReviewThread` mutation; a refusal surfaces as the CliError `githubGraphql` throws. */\nexport async function resolveThread(graphql: GraphqlCall, threadId: string): Promise<void> {\n  await graphql(RESOLVE_MUTATION, { threadId });\n}\n",
    "indexTsAdditions": "// --- imports: replace the local `type Fetch = …` alias (line 52 today) with an import, and\n// import the GitHub helpers. Biome orders \"./review-threads\" between \"./errors\" and \"./worker-shim\".\nimport {\n  acceptedByOpener,\n  type Fetch,\n  type GitHubRepo,\n  githubGraphql,\n  listUnresolvedThreads,\n  resolveThread,\n} from \"./review-threads\";\n\n// --- beside the other *CommandDeps interfaces\ninterface GrantRedemptionDeps {\n  env: NodeJS.ProcessEnv;\n  fetch: Fetch;\n  daemonUrl?: string;\n}\n\ninterface ThreadsResolveCommandDeps extends GrantRedemptionDeps {\n  log(line: string): void;\n}\n\n// --- directly above cmdGh; cmdGh's own redemption lines (the fetch of /legion/v1/gh-token, the\n// `response.ok` check, and the safeParse) move here verbatim, so both commands act as the App of\n// the role that runs them through one path.\n/** Redeems the pane's grant for a GitHub App token: the App of the role running the command\n * (`appRoleForLegionRole` in the daemon — the review App for the reviewer, the implement App for\n * every other role). */\nasync function redeemGitHubToken(deps: GrantRedemptionDeps): Promise<string> {\n  const response = await deps.fetch(`${daemonUrl(deps.env, deps.daemonUrl)}/legion/v1/gh-token`, {\n    method: \"POST\",\n    headers: { \"content-type\": \"application/json\" },\n    body: JSON.stringify({ grantId: grantFrom(deps.env) }),\n  });\n  if (!response.ok) {\n    throw new CliError(`Unable to redeem LEGION_GRANT (${response.status})`);\n  }\n  const payload = LegionDaemonApi.GitHubToken.response.safeParse(await response.json());\n  if (!payload.success) {\n    throw new CliError(\"Daemon returned an invalid GitHub credential response\");\n  }\n  return payload.data.token;\n}\n\nexport async function cmdGh(args: string[], deps: GhCommandDeps): Promise<void> {\n  if (isPrMergeInvocation(args)) {\n    throw new CliError(\"Legion workers never merge; publish READY to the merge queue\");\n  }\n  const token = await redeemGitHubToken(deps);\n  const exitCode = await deps.spawnGh(args, buildGitHubTokenEnv(token, deps.env));\n  if (exitCode !== 0) throw new CliError(`gh exited with status ${exitCode}`, exitCode);\n}\n\nfunction parseRepo(value: string): GitHubRepo {\n  const match = /^([^/\\s]+)\\/([^/\\s]+)$/.exec(value);\n  if (!match) throw new CliError(`--repo must be <owner>/<name> (got ${JSON.stringify(value)})`);\n  return { owner: match[1] as string, name: match[2] as string };\n}\n\nfunction parsePullNumber(value: string): number {\n  if (!/^\\d+$/.test(value)) {\n    throw new CliError(`--pr must be a pull request number (got ${JSON.stringify(value)})`);\n  }\n  return Number(value);\n}\n\n/** `legion threads resolve --pr <n> --repo <owner>/<name>`: as the App of the role running it,\n * resolves every unresolved review thread whose newest comment is its opener's own `Accepted:`\n * reply and names every other unresolved thread as left open. GitHub lets only a pull request's\n * author, a comment's author, or an account with push access resolve a thread, so the review App\n * that opens threads cannot close them (nor push); the implementer runs this before every push\n * that answers a review and the merger once more before READY. A thread GitHub refuses ends the\n * run with exit 1 naming the thread's URL and GitHub's message; nothing after it is attempted. */\nexport async function cmdThreadsResolve(\n  options: { repo: string; pr: string },\n  deps: ThreadsResolveCommandDeps\n): Promise<void> {\n  const repo = parseRepo(options.repo);\n  const number = parsePullNumber(options.pr);\n  const graphql = githubGraphql(deps.fetch, await redeemGitHubToken(deps));\n  const threads = await listUnresolvedThreads(graphql, repo, number);\n  if (threads.length === 0) {\n    deps.log(\"no unresolved threads\");\n    return;\n  }\n  for (const thread of threads) {\n    if (!acceptedByOpener(thread)) {\n      const by = thread.newestLogin ?? \"an unknown account\";\n      deps.log(`left open ${thread.url} — newest reply by ${by} is not an acceptance`);\n      continue;\n    }\n    try {\n      await resolveThread(graphql, thread.id);\n    } catch (error) {\n      const message = error instanceof Error ? error.message : String(error);\n      throw new CliError(`resolveReviewThread failed for ${thread.url}: ${message}`);\n    }\n    deps.log(`resolved ${thread.url}`);\n  }\n}\n\n// --- citty wiring, beside ghCommand; register as `threads: threadsCommand` in mainCommand.subCommands\nconst threadsResolveCommand = defineCommand({\n  meta: {\n    name: \"resolve\",\n    description:\n      \"Resolve every review thread whose newest comment is its opener's `Accepted:` reply, as the GitHub App of the role running it\",\n  },\n  args: {\n    pr: { type: \"string\", required: true, description: \"Pull request number\" },\n    repo: { type: \"string\", required: true, description: \"Repository as <owner>/<name>\" },\n  },\n  run: ({ args }) =>\n    runCli(() =>\n      cmdThreadsResolve(\n        { repo: args.repo as string, pr: args.pr as string },\n        { env: process.env, fetch, log: (line) => console.log(line) }\n      )\n    ),\n});\n\nconst threadsCommand = defineCommand({\n  meta: { name: \"threads\", description: \"Pull request review threads\" },\n  subCommands: { resolve: threadsResolveCommand },\n});\n",
    "reviewThreadsTest": "import { describe, expect, it } from \"bun:test\";\nimport { cmdThreadsResolve } from \"../index\";\n\nconst PR = \"https://github.com/sjawhar/legion/pull/993#discussion_r\";\n\ninterface Comment {\n  login: string;\n  body: string;\n}\n\nfunction thread(id: string, n: number, opener: string, newest: Comment, isResolved = false) {\n  return {\n    id,\n    isResolved,\n    opener: { nodes: [{ url: `${PR}${n}`, author: { login: opener } }] },\n    newest: { nodes: [{ author: { login: newest.login }, body: newest.body }] },\n  };\n}\n\nfunction page(nodes: unknown[], endCursor: string | null) {\n  return {\n    data: {\n      repository: {\n        pullRequest: {\n          reviewThreads: { pageInfo: { hasNextPage: endCursor !== null, endCursor }, nodes },\n        },\n      },\n    },\n  };\n}\n\nconst resolvedOk = (threadId: string) => ({\n  data: { resolveReviewThread: { thread: { id: threadId, isResolved: true } } },\n});\n\n/** A fake daemon + GitHub: `/legion/v1/gh-token` redeems the grant; `api.github.com/graphql`\n * serves `reviewThreads` pages keyed by the `after` cursor (\"null\" for the first page) and\n * records every `resolveReviewThread` mutation. */\nfunction fakeGitHub(\n  pages: Record<string, unknown>,\n  mutation: (threadId: string) => unknown,\n  tokenStatus = 200\n) {\n  const requests: Request[] = [];\n  const graphqlBodies: Array<{ query: string; variables: Record<string, unknown> }> = [];\n  const resolved: string[] = [];\n  const fetch = async (input: string | URL | Request, init?: RequestInit) => {\n    const request = new Request(String(input), init);\n    requests.push(request);\n    if (new URL(request.url).pathname === \"/legion/v1/gh-token\") {\n      if (tokenStatus !== 200) return new Response(\"nope\", { status: tokenStatus });\n      return Response.json({ token: \"scoped-token\", appLogin: \"legion-implementer[bot]\" });\n    }\n    const body = (await request.json()) as { query: string; variables: Record<string, unknown> };\n    graphqlBodies.push(body);\n    if (body.query.includes(\"resolveReviewThread\")) {\n      const threadId = body.variables.threadId as string;\n      resolved.push(threadId);\n      return Response.json(mutation(threadId));\n    }\n    return Response.json(pages[String(body.variables.after)]);\n  };\n  return { fetch, requests, graphqlBodies, resolved };\n}\n\ndescribe(\"legion threads resolve\", () => {\n  it(\"resolves exactly the unresolved threads whose newest comment is the opener's Accepted: reply, across pages\", async () => {\n    const reviewer = \"legion-reviewer\";\n    const github = fakeGitHub(\n      {\n        null: page(\n          [\n            thread(\"T1\", 1, reviewer, {\n              login: reviewer,\n              body: \"Accepted: fixed in abc1234 — guard added.\",\n            }),\n            thread(\"T2\", 2, reviewer, {\n              login: reviewer,\n              body: \"Still open: the docstring still says env.\",\n            }),\n          ],\n          \"CURSOR\"\n        ),\n        CURSOR: page(\n          [\n            thread(\"T3\", 3, reviewer, {\n              login: \"legion-implementer\",\n              body: \"Accepted: fixed in abc1234\",\n            }),\n            thread(\n              \"T4\",\n              4,\n              reviewer,\n              { login: reviewer, body: \"Accepted: not a defect — by design.\" },\n              true\n            ),\n            thread(\"T5\", 5, reviewer, {\n              login: reviewer,\n              body: \"\\n  Accepted: not a defect — by design.\",\n            }),\n            thread(\"T6\", 6, reviewer, {\n              login: reviewer,\n              body: \"Accepted (round 2): fixed in abc1234\",\n            }),\n          ],\n          null\n        ),\n      },\n      resolvedOk\n    );\n    const lines: string[] = [];\n\n    await cmdThreadsResolve(\n      { repo: \"sjawhar/legion\", pr: \"993\" },\n      { env: { LEGION_GRANT: \"grant-123\" }, fetch: github.fetch, log: (line) => lines.push(line) }\n    );\n\n    expect(github.resolved).toEqual([\"T1\", \"T5\"]);\n    expect(lines).toEqual([\n      `resolved ${PR}1`,\n      `left open ${PR}2 — newest reply by legion-reviewer is not an acceptance`,\n      `left open ${PR}3 — newest reply by legion-implementer is not an acceptance`,\n      `resolved ${PR}5`,\n      `left open ${PR}6 — newest reply by legion-reviewer is not an acceptance`,\n    ]);\n    const grant = github.requests[0] as Request;\n    expect(new URL(grant.url).pathname).toBe(\"/legion/v1/gh-token\");\n    expect(await grant.json()).toEqual({ grantId: \"grant-123\" });\n    for (const request of github.requests.slice(1)) {\n      expect(request.url).toBe(\"https://api.github.com/graphql\");\n      expect(request.headers.get(\"authorization\")).toBe(\"Bearer scoped-token\");\n    }\n    const listCalls = github.graphqlBodies.filter((body) => body.query.includes(\"reviewThreads\"));\n    expect(listCalls.map((body) => body.variables.after)).toEqual([null, \"CURSOR\"]);\n  });\n\n  it(\"exits 1 naming the thread and GitHub's message when a resolve is refused, attempting nothing further\", async () => {\n    const reviewer = \"legion-reviewer\";\n    const github = fakeGitHub(\n      {\n        null: page(\n          [\n            thread(\"T1\", 1, reviewer, { login: reviewer, body: \"Accepted: fixed in abc1234\" }),\n            thread(\"T2\", 2, reviewer, { login: reviewer, body: \"Accepted: fixed in abc1234\" }),\n          ],\n          null\n        ),\n      },\n      () => ({\n        data: { resolveReviewThread: null },\n        errors: [{ type: \"FORBIDDEN\", message: \"Resource not accessible by integration\" }],\n      })\n    );\n    const lines: string[] = [];\n\n    await expect(\n      cmdThreadsResolve(\n        { repo: \"sjawhar/legion\", pr: \"993\" },\n        { env: { LEGION_GRANT: \"grant-123\" }, fetch: github.fetch, log: (line) => lines.push(line) }\n      )\n    ).rejects.toEqual(\n      expect.objectContaining({\n        message: `resolveReviewThread failed for ${PR}1: Resource not accessible by integration`,\n        code: 1,\n      })\n    );\n    expect(github.resolved).toEqual([\"T1\"]);\n    expect(lines).toEqual([]);\n  });\n\n  it(\"fails before reaching GitHub when the grant cannot be redeemed\", async () => {\n    const github = fakeGitHub({}, resolvedOk, 403);\n\n    await expect(\n      cmdThreadsResolve(\n        { repo: \"sjawhar/legion\", pr: \"993\" },\n        { env: { LEGION_GRANT: \"grant-123\" }, fetch: github.fetch, log: () => undefined }\n      )\n    ).rejects.toEqual(\n      expect.objectContaining({ message: \"Unable to redeem LEGION_GRANT (403)\", code: 1 })\n    );\n    expect(github.graphqlBodies).toEqual([]);\n  });\n});\n",
    "indexTsAsFormatted_redeemThroughCmdThreadsResolve": "/** Redeems the pane's grant for a GitHub App token: the App of the role running the command\n * (`appRoleForLegionRole` in the daemon — the review App for the reviewer, the implement App for\n * every other role). */\nasync function redeemGitHubToken(deps: GrantRedemptionDeps): Promise<string> {\n  const response = await deps.fetch(`${daemonUrl(deps.env, deps.daemonUrl)}/legion/v1/gh-token`, {\n    method: \"POST\",\n    headers: { \"content-type\": \"application/json\" },\n    body: JSON.stringify({ grantId: grantFrom(deps.env) }),\n  });\n  if (!response.ok) {\n    throw new CliError(`Unable to redeem LEGION_GRANT (${response.status})`);\n  }\n  const payload = LegionDaemonApi.GitHubToken.response.safeParse(await response.json());\n  if (!payload.success) {\n    throw new CliError(\"Daemon returned an invalid GitHub credential response\");\n  }\n  return payload.data.token;\n}\n\nexport async function cmdGh(args: string[], deps: GhCommandDeps): Promise<void> {\n  if (isPrMergeInvocation(args)) {\n    throw new CliError(\"Legion workers never merge; publish READY to the merge queue\");\n  }\n  const token = await redeemGitHubToken(deps);\n  const exitCode = await deps.spawnGh(args, buildGitHubTokenEnv(token, deps.env));\n  if (exitCode !== 0) throw new CliError(`gh exited with status ${exitCode}`, exitCode);\n}\n\nfunction parseRepo(value: string): GitHubRepo {\n  const match = /^([^/\\s]+)\\/([^/\\s]+)$/.exec(value);\n  if (!match) throw new CliError(`--repo must be <owner>/<name> (got ${JSON.stringify(value)})`);\n  return { owner: match[1] as string, name: match[2] as string };\n}\n\nfunction parsePullNumber(value: string): number {\n  if (!/^\\d+$/.test(value)) {\n    throw new CliError(`--pr must be a pull request number (got ${JSON.stringify(value)})`);\n  }\n  return Number(value);\n}\n\n/** `legion threads resolve --pr <n> --repo <owner>/<name>`: as the App of the role running it,\n * resolves every unresolved review thread whose newest comment is its opener's own `Accepted:`\n * reply and names every other unresolved thread as left open. GitHub lets only a pull request's\n * author, a comment's author, or an account with push access resolve a thread, so the review App\n * that opens threads cannot close them (nor push); the implementer runs this before every push\n * that answers a review and the merger once more before READY. A thread GitHub refuses ends the\n * run with exit 1 naming the thread's URL and GitHub's message; nothing after it is attempted. */\nexport async function cmdThreadsResolve(\n  options: { repo: string; pr: string },\n  deps: ThreadsResolveCommandDeps\n): Promise<void> {\n  const repo = parseRepo(options.repo);\n  const number = parsePullNumber(options.pr);\n  const graphql = githubGraphql(deps.fetch, await redeemGitHubToken(deps));\n  const threads = await listUnresolvedThreads(graphql, repo, number);\n  if (threads.length === 0) {\n    deps.log(\"no unresolved threads\");\n    return;\n  }\n  for (const thread of threads) {\n    if (!acceptedByOpener(thread)) {\n      const by = thread.newestLogin ?? \"an unknown account\";\n      deps.log(`left open ${thread.url} — newest reply by ${by} is not an acceptance`);\n      continue;\n    }\n    try {\n      await resolveThread(graphql, thread.id);\n    } catch (error) {\n      const message = error instanceof Error ? error.message : String(error);\n      throw new CliError(`resolveReviewThread failed for ${thread.url}: ${message}`);\n    }\n    deps.log(`resolved ${thread.url}`);\n  }\n}\n",
    "indexTsAsFormatted_commands": "const threadsResolveCommand = defineCommand({\n  meta: {\n    name: \"resolve\",\n    description:\n      \"Resolve every review thread whose newest comment is its opener's `Accepted:` reply, as the GitHub App of the role running it\",\n  },\n  args: {\n    pr: { type: \"string\", required: true, description: \"Pull request number\" },\n    repo: { type: \"string\", required: true, description: \"Repository as <owner>/<name>\" },\n  },\n  run: ({ args }) =>\n    runCli(() =>\n      cmdThreadsResolve(\n        { repo: args.repo as string, pr: args.pr as string },\n        { env: process.env, fetch, log: (line) => console.log(line) }\n      )\n    ),\n});\n\nconst threadsCommand = defineCommand({\n  meta: { name: \"threads\", description: \"Pull request review threads\" },\n  subCommands: { resolve: threadsResolveCommand },\n});\n"
  },
  "docs": {
    "workerSkillTemplateThreads": "**Threads:** <n> resolved, 0 unresolved. Each disposed individually, never in bulk:\n- Thread <id>: fixed in <commit-sha> — <one line>.\n- Thread <id>: not a defect — <reason>.\n`legion threads resolve --pr <n> --repo <owner>/<repo>` at <head-sha>:\nresolved <thread URL>\nleft open <thread URL> — newest reply by <login> is not an acceptance",
    "workerSkillThreadsBullet": "- **Threads are dispositioned individually, never resolved in bulk.** Every open review\n  thread gets its own line naming the fixing commit or the reason it isn't a defect. The\n  reviewer answers each thread it opened with exactly one of `Accepted: fixed in <commit> — <one line>`,\n  `Accepted: not a defect — <reason>`, or `Still open: <what remains>`; nothing else is an\n  acceptance, and nobody replies after an `Accepted:` (a later reply by anyone else puts the\n  thread back to left open). The review App can reply on a thread but can neither resolve it\n  nor push — GitHub grants both only to the pull request's author, the comment's author, or an\n  account with push access (`packages/daemon/src/daemon/AGENTS.md`, GitHub Apps) — so the\n  **implementer** runs `legion threads resolve --pr <number> --repo <owner>/<repo>` before every\n  push that answers a review (the corrective push and the final `.legion/` deletion push) and\n  pastes its output into the `Threads` section. The command resolves each unresolved thread\n  whose newest comment is the opener's own `Accepted:` reply, one `resolveReviewThread` per\n  thread, prints `resolved <url>` or `left open <url> — newest reply by <login> is not an acceptance`,\n  and exits 1 naming the thread's URL and GitHub's message when GitHub refuses one; report that\n  exit to the architect, which opens an action ask for a human to resolve the thread by hand —\n  never skip it silently. The merger runs the same command once more before publishing READY\n  and does not publish while any `left open` line remains.",
    "workerSkillReviewerAddition": "  When you re-review after a corrective push, answer every thread you opened in one of the\n  three forms above — `Accepted:` is the only reply the implementer's `legion threads resolve`\n  acts on — and approve only once every thread you opened carries your `Accepted:` reply and the\n  implementer's run has resolved it (verify `isResolved: true` with `gh api graphql`, never from\n  the PR body).",
    "workerSkillMergerBullet": "- The merger runs `legion threads resolve --pr <n> --repo <owner>/<repo>` (it acts as the same\n  code-writing App as the implementer; resolving a thread changes no commit, so this run never\n  invalidates the approval), does not publish while any `left open` line remains or the command\n  exits 1 (report the thread to the architect instead), confirms the approved head still equals\n  the current head, then publishes `READY #<n> at <sha>` plus the PR body's gate facts to the\n  merge queue's role (`notifications.role.pr-queue`) with `envoy_publish`. The merger never\n  merges; the queue merges under its own authority.",
    "architectSkillSentence": "If a worker reports that `legion threads resolve` exited 1 naming a review thread GitHub refused\nto resolve, open an action ask (`dispatch_ask` with `kind: \"action\"`) that names the thread's URL\nand GitHub's message for a human to resolve it by hand; the merger does not publish while it is\nopen. That is the one review-thread step a human takes: the review App cannot resolve threads,\nand the implementer's and merger's runs of the command close every accepted one.",
    "reviewerRoleAddition": "When you re-review after a corrective push, answer every thread you opened with exactly one of\n`Accepted: fixed in <commit> — <one line>`, `Accepted: not a defect — <reason>`, or\n`Still open: <what remains>`, and reply nothing further after an `Accepted:`. You cannot resolve a\nthread yourself: the review App may reply, but GitHub refuses it `resolveReviewThread` exactly as\nit refuses its push (`packages/daemon/src/daemon/AGENTS.md`, GitHub Apps). The implementer runs\n`legion threads resolve --pr <number> --repo <owner>/<repo>` before its next push; it resolves\neach unresolved thread whose newest comment is the opener's `Accepted:` reply and nothing else.\nApprove only once every thread you opened carries your `Accepted:` reply and shows\n`isResolved: true` in `gh api graphql` — quote that in the approval.",
    "implementerRoleAddition": "Before every push that answers a review — the corrective push and the `.legion/` deletion push —\nrun `legion threads resolve --pr <number> --repo <owner>/<repo>` from the bash tool and paste its\noutput into the PR body's `Threads` section. It resolves, as the implementer App, every unresolved\nthread whose newest comment is the reviewer's own `Accepted:` reply (the review App cannot resolve\nthreads or push — `packages/daemon/src/daemon/AGENTS.md`, GitHub Apps) and names every other\nunresolved thread `left open`. A non-zero exit names the thread GitHub refused and GitHub's\nmessage: report it to the architect with `envoy_publish`; never skip it.",
    "mergerRoleStep": "3. Run `legion threads resolve --pr <n> --repo <owner>/<repo>`. You act as the same code-writing\n   App as the implementer, so it resolves any thread the reviewer accepted that the implementer's\n   runs missed; resolving a thread changes no commit, so the approval stands. If any line reads\n   `left open`, or the command exits 1 naming a thread GitHub refused, do not publish: report the\n   thread URLs (and GitHub's message) to the architect with `envoy_publish` and stay idle.",
    "daemonAgentsSection": "## GitHub Apps\n\nLegion acts on GitHub through two GitHub Apps, chosen per Legion role by `appRoleForLegionRole`\n(`api/github.ts`): the `reviewer` role runs as the **review** App (`github_apps.review` in\n`legion.yaml`; `legion-reviewer[bot]`), and every other role — root and sub-architects, planner,\nimplementer, tester, merger — runs as the **implement** App (`github_apps.implement`;\n`legion-implementer[bot]`). Every credential a pane redeems — `/legion/v1/gh-token` behind\n`legion gh` and `legion threads resolve`, `/legion/v1/git-credential` behind `jj git push`, and the\ngit identity lease minted at `/worker/started` — goes through that mapping, so the App a command\nacts as is the App of the role that runs it, never a choice the command makes.\n\nThe review App holds `pull_requests: write` and no `contents` permission, and GitHub lets only a\npull request's author, a comment's author, or an account with push access resolve a review thread\nor push to its branch; the review App is none of those by design, so it can post reviews and reply\non threads but can neither push the `.legion/` deletion commit nor resolve the threads it opened —\nboth answer `Resource not accessible by integration`, and widening the App does not change that\n(LEGION-34). The implementer therefore pushes the `.legion/` deletion at the reviewer's direction,\nand the implementer — before every push that answers a review — and the merger — once more before\nREADY — resolve every thread the reviewer has accepted with\n`legion threads resolve --pr <number> --repo <owner>/<repo>` (`cli/review-threads.ts`,\n`cmdThreadsResolve` in `cli/index.ts`). The command redeems the caller's grant through the same\n`/gh-token` path `legion gh` uses, reads every review thread over GitHub GraphQL with an injected\n`fetch`, resolves each unresolved thread whose newest comment was written by the account that\nopened it and begins `Accepted:` (one `resolveReviewThread` per thread), prints `resolved <url>` /\n`left open <url> — newest reply by <login> is not an acceptance`, and exits 1 naming the first\nthread GitHub refuses; a `Still open:` reply, or any reply by another account, leaves the thread\nopen with exit 0.\n",
    "rootAgentsLine": "legion threads resolve --pr <n> --repo <owner>/<repo>  # Implementer before every push that answers a review, merger before READY: resolves each unresolved review thread whose newest comment is its opener's `Accepted:` reply (the review App cannot); exits 1 naming a thread GitHub refuses",
    "solutionsSection3": "## 3. The review App can neither push nor resolve threads; the implementer App does both\n\nThe `legion-reviewer` GitHub App holds `pull_requests: write` and no `contents` permission, and\nGitHub lets only the pull request's author, the comment's author, or an account with push access\nresolve a review thread: `resolveReviewThread` from the review App returns\n`Resource not accessible by integration`, and so does its push. Widening the App does not change\nthat. So in every round:\n\n- the reviewer's `.legion/review.json` commit exists only in the shared workspace until the\n  implementer's next push carries it (check `jj log` that it is an ancestor before building on it);\n- the reviewer answers each thread it opened with one of `Accepted: fixed in <commit> — <one line>`,\n  `Accepted: not a defect — <reason>`, or `Still open: <what remains>`, and the **implementer**\n  runs `legion threads resolve --pr <number> --repo <owner>/<repo>` (LEGION-34) before its next\n  push: it resolves every unresolved thread whose newest comment is the opener's own `Accepted:`\n  reply, one `resolveReviewThread` per thread, prints `resolved <url>` /\n  `left open <url> — newest reply by <login> is not an acceptance`, and exits 1 naming the thread\n  and GitHub's message when GitHub refuses one (report it to the architect; a human resolves that\n  thread). The merger runs it once more before READY. Never a hand-written GraphQL mutation and\n  never a bulk resolve;\n- the reviewer's approval is the review itself; the head it approves by name is the\n  implementer-pushed `.legion/` deletion.\n\nThread ids are the `PRRT_…` node ids the reviewer records in `review.json` (`findings[*].threadId`);\nthey are stable across pushes even when the thread goes `isOutdated`. On `sjawhar/legion`, `main`'s\nruleset (id 12331919, read with `legion gh -- api repos/sjawhar/legion/rules/branches/main`) sets\n`required_review_thread_resolution: false`, so an unresolved thread does not stop the merge queue\nthere today; the command keeps the Threads record truthful whatever that setting is.",
    "changelogLine": "- Reviewer, implementer, and merger role prompts (and the `legion-worker` skill) name the three thread reply forms — `Accepted: fixed in <commit> — <one line>`, `Accepted: not a defect — <reason>`, `Still open: <what remains>` — and `legion threads resolve --pr <n> --repo <owner>/<repo>`, which the implementer runs before every push that answers a review and the merger before READY to resolve the threads the reviewer accepted (the review App cannot; LEGION-34)."
  },
  "prBody": {
    "title": "feat(cli): legion threads resolve resolves the review threads the reviewer accepted (LEGION-34)",
    "mustContain": [
      "Dispatch: LEGION-34",
      "the READY-format Verification block from skills/legion-worker/SKILL.md, with the Threads section carrying the pasted `legion threads resolve` output for every push that answered a review",
      "E2E: the live runs of step 3 (implementer) and step 4 (tester) with commands, outputs, head sha, and the `isResolved` graphql confirmations",
      "Acceptance 3 record: main ruleset 12331919 `required_review_thread_resolution: false` (read with `legion gh -- api repos/sjawhar/legion/rules/branches/main`; the protection endpoint answers 403 to the App)",
      "Deployment note: operator installs the released plugin (skills) and restarts the daemon on the merged main (CLI + role prompts)",
      "Chain: not stacked; if PR #992 landed first, one sentence naming the conflict-forced rebase and the cmdGh resolution"
    ]
  },
  "concerns": [
    "`--repo <owner>/<name>` is required (deviation from the `--pr <number>`-only sketch): nothing in a pane names the repository and adding it to the daemon's state response would be a daemon-API version bump. Architect to confirm or overrule before implementation; the plan is written for the flag.",
    "PR #992 (LEGION-54) rewrites `grantFrom` and cmdGh's tail; this PR extracts cmdGh's redemption into `redeemGitHubToken`. Whichever lands second sees one trivial conflict in cmdGh (resolution recorded in step 1 notes) unless step 0b's pre-push rebase applies.",
    "`sjawhar/legion` main does not require conversation resolution (`required_review_thread_resolution: false`), so today an unresolved thread does not stall the merge queue on this repository; the command's value here is a truthful Threads record and no human clicks, and it is the safeguard if that setting is ever turned on.",
    "Every PR body edit re-runs the `Tests` workflow (`pull_request: types: [… edited …]`) — pasting command output into the body per fact multiplies CI runs on a queue Sami says is already slow. The plan says one body edit per round; a workflow change is outside this issue.",
    "Acceptance 2 depends on the reviewer opening at least one inline thread in its first review; step 5a instructs it to, using a real finding or a named acceptance-2 probe.",
    "The acceptance rule is exact (`Accepted:` prefix, same login as the opener, newest comment). PR #966's own replies (`Accepted (round 2): …`) would be left open by it; the skill and role prompts now fix the wording, and the unit test pins that case."
  ],
  "architectFacts": {
    "acceptance3": {
      "setting": "sjawhar/legion main, ruleset 12331919 (Repository): pull_request rule with required_review_thread_resolution: false; required_approving_review_count: 1; require_extra_approval_for_unattributed_changes: true; allowed_merge_methods: [squash]; required_status_checks lint/typecheck/test (integration 15368, strict: false); plus deletion, non_fast_forward, required_linear_history.",
      "command": "legion gh -- api repos/sjawhar/legion/rules/branches/main (run 2026-09-13 as the planner, implement App). `legion gh -- api repos/sjawhar/legion/branches/main/protection` answered 403 `Resource not accessible by integration`.",
      "consequence": "Conversation resolution is not required before merge on this repository today; the merge queue does not stall on an open thread here."
    }
  },
  "learningsInjected": [
    "docs/solutions/daemon/external-red-and-phase-ownership.md",
    "docs/solutions/legion/worker-pane-shell-gotchas.md"
  ],
  "learningsHelpful": [
    "docs/solutions/daemon/external-red-and-phase-ownership.md",
    "docs/solutions/legion/worker-pane-shell-gotchas.md"
  ],
  "schemaVersion": 1,
  "phase": "plan",
  "completed": "2026-09-13T04:52:16.680Z"
}